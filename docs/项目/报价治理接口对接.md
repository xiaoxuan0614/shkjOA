# 报价治理接口对接（2026-09-17）

2026-09-21 报价通过后详情定价列修复：原canPrice仅mode=price且simpleLoad固定price=false导致普通查看详情无指导比例/指导价。现已通过详情按需读取permissions，以后端canManage和plan:quotation:price按钮资格显示两列及确认定价，不要求专门price入口；非编辑、非技术审批模式才可定价，成本只读并隐藏成本修改解锁按钮。提交前复查授权，原price载荷和版本校验保留。草稿/待审批及非管理账号不扩大权限。普通非通过详情仍不查询permissions。模式/权限矩阵及既有报价回归、ESLint通过，未真实管理员/主管账号联调。菜单码/角色未改。

2026-09-21 列表项目名称统一（部分完成）：项目管理、计划方案、报价管理、实施管理均用projectDisplayName显示主项目名称-分期名称，缺一侧只显示已有名称，全空—；搜索标签项目名称、仅传projectName，移除分期名搜索，项目列表不再由该搜索框传keyword。采购订单展示也改组合名称，复用projectPeriodList完整分页补齐主项目/分期名称，不改表单保存字段。后端阻塞：purchaseOrder/list仅支持periodName搜索；stock/apply/list的projectName实际是保存的分期名称，缺主项目返回及查询协议。因此采购搜索和出入库记录暂保持原行为，不用前端当前页过滤或伪造查询参数；全OA统一尚未完成，需后端补主项目名字段与分页前查询。菜单/权限无变化，两份指南已检查。

2026-09-21 报价驳回原因：详情顶部在status=0时显示驳回提示，按periodId/candidateId补查materialCandidate/history，分页寻找最新明确审批驳回的reason，不显示价格审计快照、不误用编辑/作废原因。失败独立提示重试，不阻塞原修改功能；空/未匹配明确提示核实。原修改解锁、保存只读及再次提交流程保留。主单schema无原因字段，因此驳回详情额外读取审计；action/result未定义枚举，前端支持明确REJECT/APPROVE+0等值，真实后端审计值与账号展示待联调。定向选择/分页/错误模拟及静态检查通过。菜单已检查，无新增码/权限变化。

2026-09-20 报价草稿保存后留在只读详情：列表移除修改操作，详情按原edit权限及业务状态显示修改按钮，点击才解锁；已有edit URL也默认只读。保存明细成功后锁定并回查主单最新version及明细，URL更新为view，不返回列表。只读且待提交/驳回、具submit权限时可提交已保存内容；编辑未保存时隐藏并拦截提交。提交后按待审批状态锁定，原审批/市场定价权限保留。写入成功但回查失败时关闭写入口，刷新恢复最新版本，避免重复写入；不自动套新版本重试失败写入。沿用editBatch/submit及主单回查接口，无新菜单码/角色变更。状态模拟（新增/详情/旧edit链接、解锁保存、回查失败恢复、版本同步、提交锁定）、既有报价回归和ESLint通过，未真实账号浏览器保存验收。

2026-09-20 新增报价弹窗：首次展开项目分期时加载projectPeriodList，每页统一带contractStatus=-1,0,2,3（英文逗号字符串，后端分页前筛选），排除合同审核通过分期。下拉加载中显示spinner和文字，下一步禁用并有函数级拦截；失败可重新展开重试，成功缓存至本次弹窗关闭，重开清空旧选项并忽略旧请求响应。空态仅成功加载后显示。仅影响报价新增选项，不改其他项目列表筛选；菜单/权限无变化。

2026-09-18 报价职责门禁：待审批仅原提交人可凭withdraw按钮撤回；technicalApprove为技术部主管只读技术审批，只显示通过/驳回且隐藏markupRate/finalPrice；通过后仅canManage+price的市场部主管从市场定价入口维护比例和指导价；报价授权仅向canManage+grant的市场部主管开放。普通详情不展示两项市场价格。仍调用withdraw/approve/price/grants/grant原接口和原载荷。Apifox当前approve及submit待办仍限定管理员/市场部负责人，技术部主管服务端审批资格、待办接收人及鉴权尚未对齐；本次未改正式角色授权。

2026-09-18 报价新流程：新增先add主单，新增/修改明细统一editBatch(candidateId/version/完整records)，已有名称只读（该接口不支持改名）。提交前隐藏指导比例/指导价。技术审批用technicalApprove菜单码和approve接口，市场定价要求canManage+price菜单+审批通过，成本只读。指导价=成本×(1+百分数比例/100)，四舍五入2位；修改比例重算，可手改指导价，已有指导价不覆盖。默认比例兼容明细guideMarkupRate，缺失不补0。grant仅canExport可选，canViewPrice/canEditPrice固定false，提示清除目标旧价格授权。submit/withdraw及版本校验保留。后端待对齐：Apifox approve仍限定市场负责人，需支持技术负责人及待办；明细schema未含guideMarkupRate，需补默认比例及百分比口径；成本写入仍需后端放开新增编辑者权限。未改真实角色或后端，未完成真实流程验收。

2026-09-18 报价成本价输入：成本价与数量输入框统一列宽并共用最多2位小数精度；成本价0有效，仍以basePrice原字段保存，不新增或改变接口参数。

2026-09-18 报价新选物料成本默认值：stock/material/list 返回的 costPrice 为 null/undefined 时回显0，正常成本原值保留，仍可修改并以basePrice保存。只作用于新选物料，不将已有报价接口因权限隐藏的null价格补零。菜单/接口路径不变。

2026-09-18 报价修改从列表传version：点击行通过URL携带version、名称、状态、采用/定价标记；修改页首载仅projectPeriodDetail和materialCandidateItem/list，不补主单、permissions、物料主数据。菜单和状态控制成本编辑，URL不作为后端授权依据，保存revise携带原version，由后端校验冲突；缺失/非法version禁止进入。保存后回列表获取最新版本，提交审批在列表操作，避免猜测保存后的版本。点击选择物料才请求stock/material/list，直接使用选中行的物料信息、unitList与costPrice，成本回显可修改且必填，0有效，不覆盖已有报价，不回写物料库。菜单路由和权限码不变。

2026-09-18 查看报价详情精简请求：mode=view且非审批时，只读取projectPeriodDetail和materialCandidateItem/list（明细分页可能多次）；不查询permissions、候选主单、物料主数据或历史。只读表格使用明细快照，不补物料价格；价格以后端返回为准，null显示—。缺少主单来源时不伪造审批/采用/定价状态，不显示操作记录；编辑、审批、定价仍保持原加载与写入校验。菜单码与路由未变，菜单指南已检查。

2026-09-18 报价列表移除 permissions 分期请求：加载、筛选、翻页直接调用 materialCandidate/list，不逐分期补权限；列表操作入口按菜单按钮、状态、采用/定价及version判断，不代表已获分期授权。提交/撤回/作废直接走专用接口并由后端校验，保留原版本和失败提示；详情/编辑器权限校验及导出exportCheck/exportData保留。未修改角色授权或菜单码，真实账号操作未验收。

2026-09-18 成本权限拆分（覆盖旧成本需价格授权的说明）：PC 新增/编辑报价时，成本 basePrice 随报价新增/编辑资格开放，不再要求 canEditPrice/canViewPrice；仍保留本人/管理资格和待审批、作废、已采用锁定规则。成本列与提价比例、终价分别判断，后两者权限不扩大。选料带入 costPrice、成本必填和提交 basePrice 保留。Apifox 当前 revise/addBatch 契约仍要求价格授权，后端需同步成本读写权限，否则可能隐藏成本或拒绝保存；本次不代表真实账号保存已通过。没有新增菜单码或修改角色授权。

2026-09-18 报价选料成本价：有价格查看及编辑权限时，新选物料将 costPrice 带入报价 basePrice；页面原“底价”更名“成本价”，可编辑时必填（0有效）。缺失需手填，已有报价不回填覆盖，采用锁定及无价格权限规则不变。后端契约仍为独立 basePrice，未新增 costPrice 提交字段、不回写物料成本。菜单与路由无变化。

本说明以本次刷新的 Apifox 8679942 契约为准，覆盖旧的“edit 改审批状态、待审批时定价、只凭菜单即可导出、status=3 已采用、价格公式联算”说明。代码仅改本地 PC，不涉及测试环境部署或移动端新入口。

## 页面与权限

- 保留平铺报价列表及主项目名称、审批状态、采用状态组合筛选；`0` 有效，名称直接传关键词。增加独立定价状态。
- 列表不请求 `permissions`，按钮入口按菜单权限和业务状态显示，最终操作资格由后端验证；编辑器打开和写入前仍获取分期授权。普通查看只读。
- 管理员、有效市场部负责人由后端 `canManage` 判定；其他用户按分期获得查看价格、修改价格、导出授权。修改价格要求同时允许查看；导出独立。
- 普通申请人只可维护本人非待审批、非作废报价的非价格内容。无价格查看权时隐藏价格列，不把接口的 null 补成零、不从物料库补回报价价格，保存时不发送无权修改的价格字段。
- 待审批只读，申请人或管理者可撤回。已采用报价锁定物料、单位、数量、增删和底价，管理员同样受业务限制。
- `mode=view&audit=1` 为报价审批；`mode=price` 为独立定价；通过后仍需确认定价才能导出。驳回原因必填，最多500字。`PROJECT_CANDIDATE_APPROVAL` 待办按分期及报价 ID 打开审批详情。

## 调用顺序

| 操作 | 当前调用及载荷 |
| --- | --- |
| 新增保存 | `POST materialCandidate/add`（periodId、candidateName）→ 返回 id/version → 有明细时 `materialCandidateItem/addBatch`（candidateId、version、records）→ 回查主单及明细。主单成功即保留 id，明细失败不重新新增主单。 |
| 修改保存 | `POST materialCandidate/revise`：candidateId、candidateName、version、完整 records、可选 changeReason。空数组表示清空；有实际变化时后端复制修改前内容为独立草稿 B 单。 |
| 提交 | 编辑器先保存 → 回查版本 → `POST materialCandidate/submit`（candidateId、version）；列表提交直接提交当前行版本。后端校验非空明细及每行底价。 |
| 审批 | `POST materialCandidate/approve`：candidateId、version、result字符串1/0、驳回必填reason。不再先修改明细或用 edit 改状态。 |
| 撤回/作废 | 专用 `POST materialCandidate/withdraw|void`，传 candidateId、version。待审批或已采用单不能作废。 |
| 定价 | `POST materialCandidate/price`：candidateId、version、全部明细 records（id、markupRate、finalPrice）。审批通过且有修改价格权限才允许。 |
| 授权 | `GET grants?periodId` 获取用户授权与独立版本；`POST grant` 传 periodId、userId、version、三项布尔权限。新用户版本0，已有用户按原版本提交，三项全false撤销。保存前核对最新授权版本，不覆盖他人更改。 |
| 导出 | `GET exportCheck?candidateId&version` → allowed=true 后 `GET exportData?candidateId&version` → 仅据受控JSON生成Excel。后端再次验证导出资格。 |
| 操作记录 | `GET history?periodId&candidateId?&pageNo&pageSize=20`，返回数组；不足20条禁用下一页。可切换本分期全部记录查看授权审计。当前仅展示动作、操作人、时间、结果、原因，不渲染价格快照。 |

表内未写完整前缀的路径均为 `/project/`。状态为 -1草稿、0驳回、1通过、2待审批、4作废；采用使用 adopted=0/1，定价使用 priced=0/1。

所有报价写入使用整单 version。读取缺失版本不允许操作；页面打开版本与最新版本不一致时要求刷新核对，不自动获取新版本重放旧表单。主单/明细成功后回查版本，再执行后续提交。版本冲突不得通过改状态或换接口绕过。

价格按输入原值提交：底价/终价两位小数，比例四位小数，不自动计算或互相换算。输入采用保守上限9999999999，避免JSON数字精度丢失。底价不是物料成本价。导出Excel仅有编码、类别、名称、品牌、型号、数量、单位、终价，不含成本、底价、比例和内部备注。

修改已通过未采用单的计价基础会由后端退回草稿并取消定价；更改比例或终价取消原定价确认。合同调整入口同步使用报价授权、原版本及 revise，调整后回查状态，不能提示直接提交已退回草稿的报价。

合同 addWithPaymentRecords/editWithPaymentRecords 当前契约仍只提供 materialCandidateId，无候选版本参数；不擅自新增未定义字段。合同事务内采用资格与并发校验由后端负责，前端独立解除采用使用候选 edit/version。

## 菜单配置

2026-09-18 更新：沿用报价管理和隐藏编辑器路由；菜单按钮权限是必要条件，不替代申请人、后端canManage和业务状态资格。授权弹窗仅由市场部主管以canManage+grant打开；市场定价由canManage+price打开；导出要求export按钮码、canExport、审批通过且已定价。合同内报价调整同样要求edit按钮码。比例和指导价只在市场定价入口展示，普通详情与技术审批隐藏。

以下按钮在 `/plan/material-draft` 下配置为PC、有效、可见/可访问：

| 名称 | 权限编码 |
| --- | --- |
| 新增报价单 | `plan:quotation:add` |
| 修改报价 | `plan:quotation:edit` |
| 提交审批 | `plan:quotation:submit` |
| 撤回审批 | `plan:quotation:withdraw` |
| 旧市场审批（当前流程不用） | `plan:quotation:lock` |
| 技术审批 | `plan:quotation:technicalApprove` |
| 市场定价 | `plan:quotation:price` |
| 报价授权 | `plan:quotation:grant` |
| 导出报价 | `plan:quotation:export` |
| 作废报价 | `plan:quotation:delete` |

旧lock/unlock保留不删除，但不用于当前审批或定价入口。提交岗位按需授submit/withdraw，撤回仍校验原提交人；技术部主管仅授technicalApprove；市场部主管授price和grant，报价授权由市场部主管办理。当前localhost:3101已存在这些按钮节点，admin保留原权限并集，技术部主管和市场部主管的正式角色勾选本次未修改。Apifox尚未支持技术部主管审批及待办，完成后端改造前不能把按钮存在视为审批闭环可用。

本次仅前端按钮与操作入口加门禁，后端仍须独立校验相应菜单码及分期/状态权限，不能以隐藏按钮替代接口鉴权。

## 验证记录

- 2026-09-18：10项正式按钮及admin角色授权回读通过；刷新当前管理员权限后确认全部编码下发、报价列表按钮正常显示。权限组合回归和定向ESLint通过。未重新登录或跨账号执行真实提交/审批/定价/导出，不证明后端接口已实施菜单码鉴权。

- 本地生产构建、定向 ESLint、git diff --check 通过。
- 浏览器模拟并发版本变化：保存被阻止，未发出写入请求；模拟撤销价格查看权：三个价格列与授权按钮均隐藏。模拟结果不替代真实跨账号验收。
- 权限/状态矩阵、版本缺失及冲突、专用动作载荷、筛选零值、价格精度与非公式价格、Excel列白名单、Vue组件编译回归通过。
- 本地 localhost:3101 管理员列表及报价详情打开正常，permissions返回可用，未定价报价不显示导出按钮；授权及操作记录弹窗可打开。
- 本轮未修改真实报价、价格或用户授权；跨账号提交→审批→定价→导出，以及真实保存重开仍需联调。后端V103迁移执行情况不由本轮前端验证证明。
