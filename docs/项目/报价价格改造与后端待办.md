# 报价价格改造与后端待办（2026-09-15）

2026-09-17 报价治理更新（覆盖下文旧报价权限、价格联算和审批接口说明）：提交/审批/撤回/作废/定价接入专用接口；整单 version 校验；价格及导出按分期授权；审批通过且已定价才可受控导出；合同调整和报价审批待办同步。平铺列表与三个筛选保留。菜单及隐藏路由不变，未修改真实角色或用户授权。详见[报价治理接口对接](报价治理接口对接.md)。本地页面和静态回归已验证，真实跨账号完整流程未验收。


## 本次前端范围

- 物料维护增加 `costPrice` 成本价，可新增、编辑、回显；以基准单位计价，与 `unitPrice` 和库存金额独立。历史 null 显示 —；编辑留空按后端规则保留原值，0 为有效值。
- 报价编辑页增加当前成本价参考、底价、提价比例和终价列。当前成本价来自物料库，不是报价快照，不允许在报价页修改，绝不使用 `basePrice` 代存成本。
- 创建/编辑仅可改底价和原有数量、单位、备注。保存草稿可暂缺底价，提交审批每项必须有非负底价；底价独立填写、不从成本自动复制。底价及终价按报价行所选单位展示，成本参考列始终明确为基准单位，两者不自动换算。
- 审批时仅比例、终价可编辑，名称、物料、数量、单位、底价、备注等只读。提价比例界面显示百分数，API 存小数比率：20% 存 0.2；终价=底价×(1+markupRate)。修改终价反算比例，再将比例四舍五入至4位小数并重新计算2位终价，以保证保存及回显关系一致。界面明确提示这一精度规则。
- 底价为0只允许终价为0，反算比例置0；不允许终价低于底价。前端价格输入上限暂限9999999999，避免超大金额转 JSON number 丢失精度。后端若需要支持完整 DECIMAL 范围，应约定十进制字符串契约。
- 报价页分页展示，跨页数据保留；审批前拉取完整明细，比对打开时的业务/价格快照，发现变更要求刷新。仅将当前完整明细的比例/终价调整写入 editBatch，其他字段沿用最新记录；回读价格一致后才调用主表 edit 将状态设为1。价格保存或回读失败不通过；价格成功但状态失败保留重试基线。
- 已通过1或已采用3才显示导出，点击重新校验状态，分批读取全部明细；导出字段白名单仅含物料信息、数量、单位、终价、备注，不含成本、底价、比例或隐藏成本工作表。
- 合同双单生成仍未实施；移动端未发现独立物料维护/报价创建审批页面，本轮不增加新入口，未完成移动端全链路验收。

## 可直接发给后端

现有 costPrice/basePrice/markupRate/finalPrice 前端已接，但请补以下能力：

1. 候选报价明细增加独立 `costPrice` 成本快照，新增时可从物料成本带入且允许创建人在草稿/驳回状态修改，支持保存、列表、详情和修改历史；不回写物料库，不能用 basePrice 替代。请明确所选单位与基准单位换算口径。
2. 如继续要求物料维护默认提价比例，请在物料基础表增加独立比例字段并更新 Apifox；目前只有 costPrice，不存在默认比例字段。
3. 增加事务化报价审批：一次提交明细定价和通过状态，带版本校验；待审批仅审批人能修改比例/终价，其他字段不可改，通过后锁定。当前 editBatch+edit 两次请求存在并发及部分成功窗口，前端回读不能代替事务。
4. 统一公式与存储：markupRate=0.2代表提价20%，finalPrice=basePrice×(1+markupRate)，比例4位、金额2位；反算按比例精度重新规范终价。后端验证非负及公式，不仅校验数据库精度。
5. 从响应层隔离成本信息：销售等无成本权限的账号不能从物料/报价接口获取成本，禁止仅依靠前端隐藏列；同时落实提交、审批、锁定和导出权限。
6. 确认 V3.9.3_95、V3.9.3_96 迁移及服务已部署；编辑 null 目前代表保留原值，如需清空请提供明确契约。

## 接口与验证边界

已刷新 Apifox 8679942 并核对 StockMaterial、候选明细字段和 editBatch/main edit 契约。现有后端仅存储价格字段，无独立审批事务/角色字段隔离，不能声称完整业务锁定已完成。

回归脚本：`node scripts/quotation-pricing-regression.cjs`，覆盖双向计算、0值、精度、无效输入、成本不混入价格payload、XLSX导出白名单/状态限制及Vue编译。未调用真实新增、修改或审批接口，也未导出真实业务数据；正式联调须覆盖保存→重开→审批→导出与两人并发审批。

本轮上述回归脚本、git diff --check 和最终 PC 生产构建均通过；构建写入临时目录，未更新既有部署压缩包。依据 ui-ux-pro-max 表单反馈指导，价格错误在输入框附近展示，不在输入每个数字时连续弹提示。未完成真实登录浏览器操作验收。
