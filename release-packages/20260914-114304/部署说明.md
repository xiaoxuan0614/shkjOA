# 测试部署包

- `oa-pc-test.zip`：解压内容部署到 PC 站点根目录 `/`，已包含 `_app.config.js`。
- `oa-h5-test.zip`：解压内容部署到站点 `/h5/` 目录，不要放到 `/shouhuiApi/h5/`。
- 两端 API 均为 `https://oa-test.shouhuikeji.com/shouhuiApi`。
- 服务端分别配置 PC 与 H5 的 SPA 路由回退，API `/shouhuiApi/` 不得回退到前端 HTML。
- 仅包含前端静态产物，不含后端程序或数据库；本次未上传、未部署。
- 已验证两端构建、PC 运行时配置、入口静态引用及压缩包完整性；不含源映射、环境文件或私钥文件。未完成部署后登录及真机验收。
- 仍有 7 项高危依赖告警及后端安全待办，仅用于内网或限制访问的测试环境。详情参见仓库 `docs/测试部署安全检查.md`。
- 构建前的旧产物已备份：PC `/private/tmp/shkj-pc-dist-backup.8k0rHx/dist`；H5 `/private/tmp/shkj-h5-dist-backup.GTyhQ3/h5-test`。
