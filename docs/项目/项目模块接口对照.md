# 项目模块接口对照文档

> 依据 apifox 接口文档：https://s.apifox.cn/ffb89e1a-f7ea-4dd5-99ae-e8365679dc0c（「首汇OA」项目模块）
>
> 后端统一前缀 `/jeecgboot`（axios apiUrl）；所有实体均为**项目分期(periodId)**维度子表。
>
> 前端 API 定义：`src/views/project/Project.api.ts`、`src/views/project/detail/ProjectDetail.api.ts`、`src/views/payment/Payment.api.ts`。

## 一、接口清单（Apifox 127 个 + 当前页面批量接口）

### `/project/project`（12）

| 方法   | 路径                                   | 说明                                                                                      |
| ------ | -------------------------------------- | ----------------------------------------------------------------------------------------- |
| POST   | `/project/project/add`                 | 项目管理-添加                                                                             |
| POST   | `/project/project/addProjectPeriod`    | 新增主项目/已有主项目新增分期；multipart：必填 `data` JSON 字符串、可选 `attachment` 文件 |
| DELETE | `/project/project/delete`              | 项目管理-删除                                                                             |
| DELETE | `/project/project/deleteBatch`         | 项目管理-批量删除                                                                         |
| DELETE | `/project/project/deleteProjectPeriod` | 项目管理新增页面-删除分期                                                                 |
| POST   | `/project/project/edit`                | 项目管理-编辑                                                                             |
| POST   | `/project/project/editProjectPeriod`   | 修改主项目及分期；multipart：必填 `data` JSON 字符串、可选新 `attachment` 文件            |
| GET    | `/project/project/list`                | 项目管理-分页列表查询                                                                     |
| GET    | `/project/project/periods`             | 项目-查询项目分期列表                                                                     |
| GET    | `/project/project/projectPeriodDetail` | 项目管理新增页面-详情                                                                     |
| GET    | `/project/project/projectPeriodList`   | 项目管理新增页面-分页列表                                                                 |
| GET    | `/project/project/queryById`           | 项目管理-详情                                                                             |

### `/project/period`（17）

| 方法   | 路径                                     | 说明                  |
| ------ | ---------------------------------------- | --------------------- |
| POST   | `/project/period/accept`                 | 项目分期-进入客户验收 |
| POST   | `/project/period/acceptComplete`         | 项目分期-客户验收完成 |
| POST   | `/project/period/add`                    | 项目分期-添加         |
| POST   | `/project/period/close`                  | 项目分期-项目关闭     |
| POST   | `/project/period/complete`               | 项目分期-项目完结     |
| POST   | `/project/period/debugComplete`          | 项目分期-调试完成     |
| DELETE | `/project/period/delete`                 | 项目分期-删除         |
| DELETE | `/project/period/deleteBatch`            | 项目分期-批量删除     |
| POST   | `/project/period/edit`                   | 项目分期-编辑         |
| POST   | `/project/period/implementComplete`      | 项目分期-实施完成     |
| POST   | `/project/period/internalAccept`         | 项目分期-进入内部验收 |
| POST   | `/project/period/internalAcceptComplete` | 项目分期-内部验收完成 |
| GET    | `/project/period/list`                   | 项目分期-分页列表查询 |
| GET    | `/project/period/queryById`              | 项目分期-详情         |
| GET    | `/project/period/searchByName`           | 项目分期名称模糊搜索  |
| POST   | `/project/period/start`                  | 项目分期-开始实施     |
| POST   | `/project/period/warranty`               | 项目分期-进入质保     |

### `/project/plan`（6）

| 方法   | 路径                        | 说明                  |
| ------ | --------------------------- | --------------------- |
| POST   | `/project/plan/add`         | 计划方案-添加         |
| DELETE | `/project/plan/delete`      | 计划方案-删除         |
| DELETE | `/project/plan/deleteBatch` | 计划方案-批量删除     |
| POST   | `/project/plan/edit`        | 计划方案-编辑         |
| GET    | `/project/plan/list`        | 计划方案-分页列表查询 |
| GET    | `/project/plan/queryById`   | 计划方案-详情         |

### `/project/materialPlan`（8）

| 方法   | 路径                                | 说明                      |
| ------ | ----------------------------------- | ------------------------- |
| POST   | `/project/materialPlan/add`         | 项目用料计划-添加         |
| POST   | `/project/materialPlan/addBatch`    | 项目用料计划-批量新增     |
| DELETE | `/project/materialPlan/delete`      | 项目用料计划-删除         |
| DELETE | `/project/materialPlan/deleteBatch` | 项目用料计划-批量删除     |
| POST   | `/project/materialPlan/edit`        | 项目用料计划-编辑         |
| POST   | `/project/materialPlan/editBatch`   | 按分期全量同步用料计划    |
| GET    | `/project/materialPlan/list`        | 项目用料计划-分页列表查询 |
| GET    | `/project/materialPlan/queryById`   | 项目用料计划-详情         |

### `/project/member`（当前批量接口）

| 方法   | 路径                          | 说明                    |
| ------ | ----------------------------- | ----------------------- |
| POST   | `/project/member/add`         | 项目成员-单条添加       |
| POST   | `/project/member/addBatch`    | 项目成员-批量新增       |
| DELETE | `/project/member/delete`      | 项目成员-删除（已废弃） |
| DELETE | `/project/member/deleteBatch` | 项目成员-批量删除       |
| POST   | `/project/member/edit`        | 项目成员-编辑（已废弃） |
| POST   | `/project/member/editBatch`   | 项目成员-全量同步       |
| GET    | `/project/member/list`        | 项目成员-分页列表查询   |
| GET    | `/project/member/queryById`   | 项目成员-详情           |

### `/project/process`（实施计划）

| 方法   | 路径                           | 说明                                                                                            |
| ------ | ------------------------------ | ----------------------------------------------------------------------------------------------- |
| POST   | `/project/process/add`         | 项目工序-添加（已废弃）                                                                         |
| POST   | `/project/process/addBatch`    | 首次批量新增实施计划；第一层传 `periodId/plannedAcceptanceDate/plannedAcceptanceRemark/records` |
| DELETE | `/project/process/delete`      | 项目工序-删除                                                                                   |
| DELETE | `/project/process/deleteBatch` | 项目工序-批量删除                                                                               |
| POST   | `/project/process/edit`        | 项目工序-编辑                                                                                   |
| POST   | `/project/process/editBatch`   | 已有实施计划按分期全量同步；请求结构同 `addBatch`，明细有 `id` 更新、无 `id` 新增               |
| GET    | `/project/process/list`        | 项目工序-分页列表查询                                                                           |
| GET    | `/project/process/queryById`   | 项目工序-详情                                                                                   |
| GET    | `/project/process/detail`      | 整份实施计划详情（Apifox 标注未开发，前端暂不调用）                                             |

### `/project/location`（当前批量接口）

| 方法   | 路径                            | 说明                  |
| ------ | ------------------------------- | --------------------- |
| POST   | `/project/location/add`         | 实施位置-添加         |
| DELETE | `/project/location/delete`      | 实施位置-删除         |
| DELETE | `/project/location/deleteBatch` | 实施位置-批量删除     |
| POST   | `/project/location/edit`        | 实施位置-编辑         |
| POST   | `/project/location/editBatch`   | 按分期全量同步位置    |
| GET    | `/project/location/list`        | 实施位置-分页列表查询 |
| GET    | `/project/location/queryById`   | 实施位置-详情         |

### `/project/memberOutsource`（计划外协配置）

| 方法 | 路径                                 | 说明                       |
| ---- | ------------------------------------ | -------------------------- |
| GET  | `/project/memberOutsource/list`      | 按项目分期查询外协配置     |
| POST | `/project/memberOutsource/editBatch` | 按项目分期全量同步外协配置 |

人员配置的外协单位选择器另调用 `GET /project/outsourcingUnit/list?status=0` 加载启用单位；选择后写入 `unitId/unitName/unitType/contactPerson/contactPhone`。

### `/project/implementLog`（6）

| 方法   | 路径                                | 说明                  |
| ------ | ----------------------------------- | --------------------- |
| POST   | `/project/implementLog/add`         | 实施记录-添加         |
| DELETE | `/project/implementLog/delete`      | 实施记录-删除         |
| DELETE | `/project/implementLog/deleteBatch` | 实施记录-批量删除     |
| POST   | `/project/implementLog/edit`        | 实施记录-编辑         |
| GET    | `/project/implementLog/list`        | 实施记录-分页列表查询 |
| GET    | `/project/implementLog/queryById`   | 实施记录-详情         |

### `/project/implementLogPerson`（6）

| 方法   | 路径                                      | 说明                      |
| ------ | ----------------------------------------- | ------------------------- |
| POST   | `/project/implementLogPerson/add`         | 实施参与人员-添加         |
| DELETE | `/project/implementLogPerson/delete`      | 实施参与人员-删除         |
| DELETE | `/project/implementLogPerson/deleteBatch` | 实施参与人员-批量删除     |
| POST   | `/project/implementLogPerson/edit`        | 实施参与人员-编辑         |
| GET    | `/project/implementLogPerson/list`        | 实施参与人员-分页列表查询 |
| GET    | `/project/implementLogPerson/queryById`   | 实施参与人员-详情         |

### `/project/internalAcceptance`（6）

| 方法   | 路径                                      | 说明                      |
| ------ | ----------------------------------------- | ------------------------- |
| POST   | `/project/internalAcceptance/add`         | 项目内部验收-添加         |
| DELETE | `/project/internalAcceptance/delete`      | 项目内部验收-删除         |
| DELETE | `/project/internalAcceptance/deleteBatch` | 项目内部验收-批量删除     |
| POST   | `/project/internalAcceptance/edit`        | 项目内部验收-编辑         |
| GET    | `/project/internalAcceptance/list`        | 项目内部验收-分页列表查询 |
| GET    | `/project/internalAcceptance/queryById`   | 项目内部验收-详情         |

### `/project/acceptance`（6）

| 方法   | 路径                              | 说明                  |
| ------ | --------------------------------- | --------------------- |
| POST   | `/project/acceptance/add`         | 项目验收-添加         |
| DELETE | `/project/acceptance/delete`      | 项目验收-删除         |
| DELETE | `/project/acceptance/deleteBatch` | 项目验收-批量删除     |
| POST   | `/project/acceptance/edit`        | 项目验收-编辑         |
| GET    | `/project/acceptance/list`        | 项目验收-分页列表查询 |
| GET    | `/project/acceptance/queryById`   | 项目验收-详情         |

### `/project/file`（当前批量接口）

| 方法   | 路径                        | 说明                    |
| ------ | --------------------------- | ----------------------- |
| POST   | `/project/file/add`         | 项目文件-添加           |
| POST   | `/project/file/addBatch`    | 项目文件-批量新增       |
| DELETE | `/project/file/delete`      | 项目文件-删除（已废弃） |
| DELETE | `/project/file/deleteBatch` | 项目文件-批量删除       |
| POST   | `/project/file/edit`        | 项目文件-编辑           |
| POST   | `/project/file/editBatch`   | 项目文件-全量同步       |
| GET    | `/project/file/list`        | 项目文件-分页列表查询   |
| GET    | `/project/file/queryById`   | 项目文件-详情           |

### `/project/dynamic`（6）

| 方法   | 路径                           | 说明                  |
| ------ | ------------------------------ | --------------------- |
| POST   | `/project/dynamic/add`         | 项目动态-添加         |
| DELETE | `/project/dynamic/delete`      | 项目动态-删除         |
| DELETE | `/project/dynamic/deleteBatch` | 项目动态-批量删除     |
| POST   | `/project/dynamic/edit`        | 项目动态-编辑         |
| GET    | `/project/dynamic/list`        | 项目动态-分页列表查询 |
| GET    | `/project/dynamic/queryById`   | 项目动态-详情         |

### `/project/attendance`（6）

| 方法   | 路径                              | 说明                  |
| ------ | --------------------------------- | --------------------- |
| POST   | `/project/attendance/add`         | 施工打卡-添加         |
| DELETE | `/project/attendance/delete`      | 施工打卡-删除         |
| DELETE | `/project/attendance/deleteBatch` | 施工打卡-批量删除     |
| POST   | `/project/attendance/edit`        | 施工打卡-编辑         |
| GET    | `/project/attendance/list`        | 施工打卡-分页列表查询 |
| GET    | `/project/attendance/queryById`   | 施工打卡-详情         |

### `/project/delay`（8）

| 方法   | 路径                         | 说明                  |
| ------ | ---------------------------- | --------------------- |
| POST   | `/project/delay/add`         | 工期延期-添加         |
| POST   | `/project/delay/approve`     | 工期延期-审批通过     |
| DELETE | `/project/delay/delete`      | 工期延期-删除         |
| DELETE | `/project/delay/deleteBatch` | 工期延期-批量删除     |
| POST   | `/project/delay/edit`        | 工期延期-编辑         |
| GET    | `/project/delay/list`        | 工期延期-分页列表查询 |
| GET    | `/project/delay/queryById`   | 工期延期-详情         |
| POST   | `/project/delay/reject`      | 工期延期-审批驳回     |

### `/project/delayApproval`（6）

| 方法   | 路径                                 | 说明                      |
| ------ | ------------------------------------ | ------------------------- |
| POST   | `/project/delayApproval/add`         | 工期延期审批-添加         |
| DELETE | `/project/delayApproval/delete`      | 工期延期审批-删除         |
| DELETE | `/project/delayApproval/deleteBatch` | 工期延期审批-批量删除     |
| POST   | `/project/delayApproval/edit`        | 工期延期审批-编辑         |
| GET    | `/project/delayApproval/list`        | 工期延期审批-分页列表查询 |
| GET    | `/project/delayApproval/queryById`   | 工期延期审批-详情         |

### `/project/contract`（当前合同页使用组合新增、详情、编辑与审批）

| 方法   | 路径                                       | 说明                                                       |
| ------ | ------------------------------------------ | ---------------------------------------------------------- |
| POST   | `/project/contract/add`                    | 合同管理-添加                                              |
| POST   | `/project/contract/addWithPaymentRecords`  | 合同、合同文件元数据与回款计划事务新增                     |
| POST   | `/project/contract/editWithPaymentRecords` | 按 `periodId` 差量修改合同、替换可选文件并全量同步回款计划 |
| DELETE | `/project/contract/delete`                 | 合同管理-删除                                              |
| DELETE | `/project/contract/deleteBatch`            | 合同管理-批量删除                                          |
| POST   | `/project/contract/edit`                   | 合同管理-编辑（已废弃，前端无调用）                        |
| GET    | `/project/contract/list`                   | 合同管理-分页列表查询                                      |
| GET    | `/project/contract/queryById`              | 合同管理-详情                                              |
| POST   | `/project/contract/status`                 | 合同审批状态变更                                           |

### `/project/paymentRecord`（合同页当前使用 4 个）

| 方法   | 路径                                 | 说明                           |
| ------ | ------------------------------------ | ------------------------------ |
| GET    | `/project/paymentRecord/list`        | 按 `periodId` 分页查询回款计划 |
| POST   | `/project/paymentRecord/addBatch`    | 项目回款计划-批量新增          |
| POST   | `/project/paymentRecord/editBatch`   | 项目回款计划-全量同步          |
| DELETE | `/project/paymentRecord/deleteBatch` | 项目回款计划-批量删除          |

**合同信息页调用约定**：

- 新增 `mode=create`：按 `periodId` 调用项目详情用于顶部基本信息回显；不调用合同列表、合同详情或合同明细列表。
- 查看/编辑：合同详情接口仅接受必填 `periodId`，按该分期一次返回 `contract/contractFile/materialFile/records`；合同信息页不再额外调用回款计划列表，也不得传合同 `id`。
- 合同只读详情使用 `contract_type` 映射 `contractType`、使用 `payback_node` 映射回款计划的 `paymentNode`；`projectManagerUserName` 为空时按 `projectManagerUserId` 从 `/sys/user/list` 补齐。附件字段存在路径时通过静态文件访问地址提供预览。
- 合同附件与合同物料文件选择时只在本地文件列表回显，不预调用 `/sys/common/upload`；提交时分别作为可选 `attachment/materialAttachment` part 随合同组合请求上传，`data` 为 JSON 字符串。未传新文件时保留原文件，传入时由后端事务替换。
- 组合新增的 `records` 元素使用 `paymentNode/ratio/plannedAmount`，不带 `periodId/id/contractId`，至少提交一条；后端任一步骤失败时整体回滚。
- 已有合同重提调用 `/project/contract/editWithPaymentRecords`：`data.contract.periodId` 必传，其他合同字段与详情原值比较后只传变化项；`records` 为全量列表，带 `id` 更新、不带 `id` 新增，未提交的旧记录逻辑删除。接口成功后合同状态固定为 `2`。
- 新增合同在组合请求的 `contract.status` 中直接传 `2`（待审核）。统一合同信息页内审批：驳回传 `{ periodId, status: '0', approvalReason }`；通过时当前仍需调用合同编辑接口保存项目经理，再传 `{ periodId, status: '1' }` 并推进分期到 `PREPARING`；原提交人撤回传 `{ periodId, status: '3' }`。
- 项目列表通过 `contractStatus` 控制统一入口：合同已存在且未通过时显示“合同信息”，通过后隐藏；合同信息页仅在状态 `2` 且拥有 `project:contract:audit` 时显示审批区域。
- 项目列表对接人列优先读取 `projectLiaisonUserName`；姓名快照为空时使用 `projectLiaisonUserId` 从 `/sys/user/list` 补齐，仍无法识别的行按 `periodId` 调 `GET /project/project/projectPeriodDetail` 补查对接人字段。
- 新增项目与已有主项目新增分期统一调用 `POST /project/project/addProjectPeriod`，编辑调用 `POST /project/project/editProjectPeriod`；均传 `multipart/form-data`，其中 `data` 是合法 JSON 字符串，`attachment` 是单个可选文件。前端不预调用 `/sys/common/upload`，不在 `data` 中传 `attachmentFileId`；编辑未传新附件时保留旧文件。
- 当前 `defHttp` 对非 GET 请求会将 `params` 移入 body，项目 multipart 请求需从 `params` 传入完整 FormData；若直接设置 `data=FormData`，其 `Object.keys(data)` 判断会误判为空并丢失 multipart parts。
- 合同审批规则与最新 Apifox 一致：`0=驳回`、`1=通过`、`2=待审核`、`3=已撤回`、其他`=待提交`。
- 原提交人可撤回待审核合同，并可修改待提交、驳回或已撤回合同；驳回只读页与编辑页展示 `approvalReason`，重新提交后状态改为 `2` 并清空旧驳回原因。
- `mock/demo/project.ts` 与 `mock/demo/contract.ts` 已删除，这些接口均需真实后端。

## 二、实体字段（后端返回）

### `acceptance`

| 字段                     | 说明(依据接口返回) |
| ------------------------ | ------------------ |
| `id`                     |                    |
| `periodId`               |                    |
| `acceptDate`             |                    |
| `implementCompleteDate`  |                    |
| `acceptUnitLeader`       |                    |
| `acceptUnitPhone`        |                    |
| `acceptLeaderId`         |                    |
| `acceptLeaderName`       |                    |
| `completionReportFileId` |                    |
| `acceptanceFormFileId`   |                    |
| `result`                 |                    |
| `remark`                 |                    |
| `createBy`               |                    |
| `createTime`             |                    |
| `updateBy`               |                    |
| `updateTime`             |                    |
| `delFlag`                |                    |

### `attendance`

| 字段             | 说明(依据接口返回) |
| ---------------- | ------------------ |
| `id`             |                    |
| `periodId`       |                    |
| `processId`      |                    |
| `userId`         |                    |
| `userName`       |                    |
| `sourceType`     |                    |
| `relatedNo`      |                    |
| `attendanceDate` |                    |
| `signInTime`     |                    |
| `signOutTime`    |                    |
| `workHours`      |                    |
| `workContent`    |                    |
| `locationName`   |                    |
| `longitude`      |                    |
| `latitude`       |                    |
| `photoFileId`    |                    |
| `status`         |                    |
| `remark`         |                    |
| `createBy`       |                    |
| `createTime`     |                    |
| `updateBy`       |                    |
| `updateTime`     |                    |
| `delFlag`        |                    |

### `contract`

| 字段                  | 说明(依据接口返回) |
| --------------------- | ------------------ |
| `id`                  |                    |
| `contractNo`          |                    |
| `projectId`           |                    |
| `periodId`            |                    |
| `contractName`        |                    |
| `contractType`        |                    |
| `contractSignedDate`  |                    |
| `contractAmount`      |                    |
| `plannedDeliveryDate` |                    |
| `actualDeliveryDate`  |                    |
| `warrantyPeriod`      |                    |
| `warrantyEndDate`     |                    |
| `receivedAmount`      |                    |
| `contractFileId`      |                    |
| `status`              |                    |
| `remark`              |                    |
| `createBy`            |                    |
| `createTime`          |                    |
| `updateBy`            |                    |
| `updateTime`          |                    |
| `delFlag`             |                    |

### `contractItem`

| 字段            | 说明(依据接口返回) |
| --------------- | ------------------ |
| `id`            |                    |
| `contractId`    |                    |
| `itemName`      |                    |
| `specification` |                    |
| `quantity`      |                    |
| `unit`          |                    |
| `unitPrice`     |                    |
| `amount`        |                    |
| `remark`        |                    |
| `createBy`      |                    |
| `createTime`    |                    |
| `updateBy`      |                    |
| `updateTime`    |                    |
| `delFlag`       |                    |

### `paymentRecord`

| 字段            | 说明                              |
| --------------- | --------------------------------- |
| `id`            | 回款计划 ID（编辑全量同步时使用） |
| `periodId`      | 项目分期 ID（批量请求仅放在外层） |
| `paymentNode`   | 回款节点                          |
| `ratio`         | 回款比例                          |
| `plannedAmount` | 计划回款金额                      |
| `actualAmount`  | 实际回款金额                      |
| `plannedDate`   | 计划回款日期，`yyyy-MM-dd`        |
| `paymentDate`   | 实际回款日期，`yyyy-MM-dd`        |
| `invoiceStatus` | 开票状态                          |
| `payStatus`     | 回款状态                          |
| `payMethod`     | 回款方式                          |
| `rollbackTime`  | 回款周期（整数天；新增默认 7）    |
| `remark`        | 备注                              |

### `delay`

| 字段              | 说明(依据接口返回) |
| ----------------- | ------------------ |
| `id`              |                    |
| `projectId`       |                    |
| `periodId`        |                    |
| `processId`       |                    |
| `applyNo`         |                    |
| `delayReason`     |                    |
| `originalEndTime` |                    |
| `expectedEndTime` |                    |
| `delayDays`       |                    |
| `status`          |                    |
| `applyUserId`     |                    |
| `applyUserName`   |                    |
| `remark`          |                    |
| `createBy`        |                    |
| `createTime`      |                    |
| `updateBy`        |                    |
| `updateTime`      |                    |
| `delFlag`         |                    |

### `delayApproval`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `delayId`          |                    |
| `nodeName`         |                    |
| `approvalUserId`   |                    |
| `approvalUserName` |                    |
| `approvalResult`   |                    |
| `approvalComment`  |                    |
| `approvalTime`     |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `dynamic`

| 字段            | 说明(依据接口返回) |
| --------------- | ------------------ |
| `id`            |                    |
| `projectId`     |                    |
| `periodId`      |                    |
| `actionType`    |                    |
| `actionContent` |                    |
| `operatorId`    |                    |
| `operatorName`  |                    |
| `operateTime`   |                    |
| `createBy`      |                    |
| `createTime`    |                    |
| `updateBy`      |                    |
| `updateTime`    |                    |
| `delFlag`       |                    |

### `file`

| 字段         | 说明(依据接口返回) |
| ------------ | ------------------ |
| `id`         |                    |
| `periodId`   |                    |
| `fileType`   |                    |
| `fileId`     |                    |
| `fileName`   |                    |
| `remark`     |                    |
| `createBy`   |                    |
| `createTime` |                    |
| `updateBy`   |                    |
| `updateTime` |                    |
| `delFlag`    |                    |

### `implementLog`

| 字段                | 说明(依据接口返回) |
| ------------------- | ------------------ |
| `id`                |                    |
| `periodId`          |                    |
| `processId`         |                    |
| `logDate`           |                    |
| `submitterId`       |                    |
| `submitterName`     |                    |
| `workContent`       |                    |
| `locationId`        |                    |
| `hours`             |                    |
| `vehicleNo`         |                    |
| `mileage`           |                    |
| `signInTime`        |                    |
| `signOutTime`       |                    |
| `outsourcingUnit`   |                    |
| `outsourcingPeople` |                    |
| `outsourcingHours`  |                    |
| `photos`            |                    |
| `remark`            |                    |
| `createBy`          |                    |
| `createTime`        |                    |
| `updateBy`          |                    |
| `updateTime`        |                    |
| `delFlag`           |                    |

### `implementLogPerson`

| 字段          | 说明(依据接口返回) |
| ------------- | ------------------ |
| `id`          |                    |
| `logId`       |                    |
| `userId`      |                    |
| `userName`    |                    |
| `signInTime`  |                    |
| `signOutTime` |                    |
| `workContent` |                    |
| `score`       |                    |
| `createBy`    |                    |
| `createTime`  |                    |
| `updateBy`    |                    |
| `updateTime`  |                    |
| `delFlag`     |                    |

### `internalAcceptance`

| 字段                    | 说明(依据接口返回) |
| ----------------------- | ------------------ |
| `id`                    |                    |
| `periodId`              |                    |
| `acceptDate`            |                    |
| `implementCompleteDate` |                    |
| `acceptLeaderId`        |                    |
| `acceptLeaderName`      |                    |
| `result`                |                    |
| `remark`                |                    |
| `createBy`              |                    |
| `createTime`            |                    |
| `updateBy`              |                    |
| `updateTime`            |                    |
| `delFlag`               |                    |
| `reportFileId`          |                    |

### `location`

| 字段           | 说明(依据接口返回) |
| -------------- | ------------------ |
| `id`           |                    |
| `periodId`     |                    |
| `locationName` |                    |
| `longitude`    |                    |
| `latitude`     |                    |
| `description`  |                    |
| `createBy`     |                    |
| `createTime`   |                    |
| `updateBy`     |                    |
| `updateTime`   |                    |
| `delFlag`      |                    |

### `materialPlan`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `periodId`         |                    |
| `materialId`       |                    |
| `materialCategory` |                    |
| `materialName`     |                    |
| `brand`            |                    |
| `model`            |                    |
| `unit`             |                    |
| `supplierId`       |                    |
| `supplierName`     |                    |
| `purchaseQty`      |                    |
| `plannedQty`       |                    |
| `actualQty`        |                    |
| `purchaseStatus`   |                    |
| `expressNo`        |                    |
| `remark`           |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `member`

| 字段              | 说明(依据接口返回) |
| ----------------- | ------------------ |
| `id`              |                    |
| `periodId`        |                    |
| `userId`          |                    |
| `userName`        |                    |
| `memberRole`      |                    |
| `outsourcingFlag` |                    |
| `outsourcingUnit` |                    |
| `remark`          |                    |
| `createBy`        |                    |
| `createTime`      |                    |
| `updateBy`        |                    |
| `updateTime`      |                    |
| `delFlag`         |                    |

### `period`

> 注：下表为分期实体的旧有字段清单；项目管理合并新增/编辑/详情接口已使用 `projectLiaisonUserId/projectLiaisonUserName` 表示“项目对接人”，前端不再将 `projectLeaderId/projectLeaderName` 当作对接人。

| 字段                      | 说明(依据接口返回) |
| ------------------------- | ------------------ |
| `id`                      |                    |
| `projectId`               |                    |
| `periodNo`                |                    |
| `periodName`              |                    |
| `periodSort`              |                    |
| `projectType`             |                    |
| `projectLeaderId`         |                    |
| `projectLeaderName`       |                    |
| `projectAddress`          |                    |
| `projectRequirement`      |                    |
| `involvedProducts`        |                    |
| `businessAttribute`       |                    |
| `startDate`               |                    |
| `status`                  |                    |
| `totalProgress`           |                    |
| `attachmentFileId`        |                    |
| `remark`                  |                    |
| `createBy`                |                    |
| `createTime`              |                    |
| `updateBy`                |                    |
| `updateTime`              |                    |
| `delFlag`                 |                    |
| `internalAcceptanceList`  |                    |
| `projectLeaderDepartId`   |                    |
| `projectLeaderDepartName` |                    |

### `plan`

| 字段                | 说明(依据接口返回) |
| ------------------- | ------------------ |
| `id`                |                    |
| `periodId`          |                    |
| `planName`          |                    |
| `planType`          |                    |
| `planFileId`        |                    |
| `planStartTime`     |                    |
| `planEndTime`       |                    |
| `plannedTotalHours` |                    |
| `status`            |                    |
| `remark`            |                    |
| `createBy`          |                    |
| `createTime`        |                    |
| `updateBy`          |                    |
| `updateTime`        |                    |
| `delFlag`           |                    |

### `process`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `periodId`         |                    |
| `processName`      |                    |
| `siteLeaderId`     |                    |
| `siteLeaderName`   |                    |
| `plannedStartTime` |                    |
| `plannedEndTime`   |                    |
| `plannedHours`     |                    |
| `actualStartTime`  |                    |
| `actualEndTime`    |                    |
| `status`           |                    |
| `sortNo`           |                    |
| `remark`           |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `project`

| 字段          | 说明(依据接口返回) |
| ------------- | ------------------ |
| `id`          |                    |
| `projectNo`   |                    |
| `projectName` |                    |
| `customerId`  |                    |
| `remark`      |                    |
| `createBy`    |                    |
| `createTime`  |                    |
| `updateBy`    |                    |
| `updateTime`  |                    |
| `delFlag`     |                    |

## 三、前端页面 ↔ 后端接口 对照

| 前端页面 | 后端接口 | 说明 |
| --- | --- | --- |
| 项目管理列表 [project/index.vue](src/views/project/index.vue) | `GET /project/project/projectPeriodList` | 主项目+分期合并行；搜索 projectName/periodName/customerName/projectType/status |
| 新增/编辑项目 [ProjectApply.vue](src/views/project/apply/ProjectApply.vue) | 新建主项目+首期或已有主项目新增期均调 `POST /project/project/addProjectPeriod`；编辑调 `POST /project/project/editProjectPeriod`；两者 multipart 传 `data` 与可选 `attachment`；回显 `GET /project/project/projectPeriodDetail`(periodId) | `parentProjectId` 为空时新建主项目；项目对接人字段 `projectLiaisonUserId/projectLiaisonUserName`，新增默认当前用户；创建后仅该对接人可修改 |
| 删除分期 | `DELETE /project/project/deleteProjectPeriod`(periodId) | 后端仅允许删「期」 |
| 状态流转（列表/详情/合同审批） | `POST /project/period/status`（`{ periodId, status }`） | 合同审批通过并指定项目经理后进入 `PREPARING`；合同仅提交不推进项目 |
| 详情-基本信息 [DetailBasic.vue](src/views/project/detail/components/DetailBasic.vue) | `GET /project/project/projectPeriodDetail` | 主项目+分期合并字段 |
| 详情-计划方案 [DetailPlan.vue](src/views/project/detail/components/DetailPlan.vue) | `GET /project/plan/list`(periodId) | project_plan 实体 |
| 详情-项目成员 [DetailMember.vue](src/views/project/detail/components/DetailMember.vue) | `GET /project/member/list`(periodId) | project_member 实体 |
| 详情-实施位置 [DetailPosition.vue](src/views/project/detail/components/DetailPosition.vue) | `GET /project/location/list`；增 `POST /project/location/add`；改 `POST /project/location/edit`；删 `DELETE /project/location/delete` | project_location 实体 |
| 详情-实施记录 [DetailImplement.vue](src/views/project/detail/components/DetailImplement.vue) | `GET /project/implementLog/list`(periodId)；详情 `GET /project/implementLog/queryById` | project_implement_log 实体 |
| 详情-验收记录 [DetailAcceptance.vue](src/views/project/detail/components/DetailAcceptance.vue) | 内部验收 `GET /project/internalAcceptance/list` + `POST add/edit`；客户验收 `GET /project/acceptance/list` + `POST add/edit` | 并行两卡片；均需上传验收报告 |
| 详情-项目文件 [DetailFile.vue](src/views/project/detail/components/DetailFile.vue) | 文件先调 `POST /sys/common/upload`，multipart 传 `file` 与 `biz=periodId`；列表 `GET /project/file/list`；新增 `POST /project/file/add`；删除 `DELETE /project/file/deleteBatch?ids=...` | 上传响应 `message` 写入 `fileId`；上传入口仅保留一个文件，删除后才显示上传按钮；详情删除仅项目对接人或项目经理可见 |
| 编辑计划-方案文件 [PlanFileMgmt.vue](src/views/project/plan/PlanFileMgmt.vue) | 列表 `GET /project/plan/list`（`periodId`）；新增行保存 `POST /project/plan/add`；已有行保存 `POST /project/plan/edit`；删除 `DELETE /project/plan/deleteBatch` | 标签名称为“方案文件”；列表来自 `project_plan` 并排除 `CONTRACT_MATERIAL_DRAFT`，不显示合同文件；新增/修改均为 multipart，`data` 是当前行 JSON，`attachment` 是可选新文件，不再预调用统一上传；保存成功锁定本行，点击“修改”解锁，选择阶段文件列表直接显示完成态图标 |
| 详情-用料清单 [DetailMaterial.vue](src/views/project/detail/components/DetailMaterial.vue) | `GET /project/materialPlan/list`(periodId) | project_material_plan 实体 |
| 详情-项目动态(右栏) | `GET /project/dynamic/list`（`periodId`） | 按接口倒序展示 `actionContent/operatorName/operateTime`，右栏限制高度并独立滚动 |
| 计划页基本信息抽屉 [ProjectBasicDrawer.vue](src/views/project/components/ProjectBasicDrawer.vue) | `GET /project/project/projectPeriodDetail`（`periodId`） | “编辑/查看计划方案”页面标题旁的详情按钮打开；展示主项目、分期与对接、实施基本信息，不展示附件内部路径，不改变路由 |
| 编辑计划方案 [ProjectPlan.vue](src/views/project/plan/ProjectPlan.vue) | 固定信息：`GET /project/project/projectPeriodDetail` + `GET /project/contract/queryById`；方案文件逐条 `POST /project/plan/add | edit`；用料、外协、工序、位置分别 `POST /project/materialPlan | memberOutsource | process | location/editBatch`；成员 `GET /project/member/list`、系统用户 `GET /sys/user/list`、多角色邀请 `POST /project/member/addBatch`、删除 `DELETE /project/member/deleteBatch`；提交审批 `POST /project/period/status`；物料单位 `GET /stock/material/queryById` | 不提供“保存全部”；用料、外协和实施计划使用“保存本页/修改”。人员邀请在弹窗提交时立即发送，人员页顶部保存只提交外协配置；位置信息保持行内保存；提交审批只传 `{ periodId, status: 'PENDING_APPROVAL' }`，不再隐式保存任何计划数据 |
| 邀请接收弹窗、首页与站内待办 [ProjectInvitationModal.vue](src/views/project/plan/ProjectInvitationModal.vue) / [todo/index.vue](src/views/todo/index.vue) | 待处理邀请 `GET /project/member/myInvitations`；办理 `POST /project/member/respond`；项目名称补齐 `GET /project/project/projectPeriodDetail`；项目经理姓名补齐 `GET /project/contract/queryById` | 当前用户登录后弹窗；首页“我的待办”和站内待办同步展示。办理进入项目基本信息抽屉，支持同意/拒绝；响应参数为 `memberId + inviteStatus`，`1=同意`、`0=拒绝`，处理成功后共享列表同步移除 |
| 编辑计划方案-回款计划 [PlanPayment.vue](src/views/project/plan/PlanPayment.vue) | 合同 `GET /project/contract/queryById`（`periodId`）；回款 `GET /project/paymentRecord/list`（`periodId`）；分期兜底 `GET /project/project/projectPeriodDetail`；用户补齐 `GET /sys/user/list`；成员兜底 `GET /project/member/list`（`periodId`） | 只读展示当前分期合同；销售负责人按快照、ID、`createBy` 用户账号和销售角色成员多级补齐；合同文件使用 `contractFile.fileId` 或合同文件 ID 打开站内预览；明细只显示回款项、比例和金额 |
| 计划审批详情 [PlanAuditModal.vue](src/views/project/components/PlanAuditModal.vue) | 查看详情跳转 `/project/plan?mode=view&periodId=...`，六类详情复用计划页现有查询；审批流转 `POST /project/period/status` | 弹窗底部为“查看详情/驳回/通过”；不创建额外抽屉，计划页强制只读；通过传 `IMPLEMENTING`，驳回传 `PREPARING` |
| 合同信息/审批 [project/contract/index.vue](src/views/project/contract/index.vue) | 详情 `contract/queryById`；新增/修改 `contract/addWithPaymentRecords | editWithPaymentRecords`；审批/撤回 `POST /project/contract/status` | 详情一次返回合同、文件、回款；回款周期使用 `rollbackTime`，默认 7 天；修改时 `periodId` 必传、合同字段差量提交、回款全量同步，两个文件可选替换 |
| 外协维护 [views/resource/outsourcing](src/views/resource/outsourcing/) | `GET /project/outsourcing/list` + `POST add/edit` + `DELETE delete/deleteBatch` | ⚠️ 接口为推断(仿 /project/supplier)，后端待建/确认；人员配置页「外协单位」下拉数据源 |
| 计划方案管理 [views/plan](src/views/plan/) | 入口 `GET /project/project/projectPeriodList`；方案 `GET /project/plan/list`；用料 `GET /project/materialPlan/list`；位置 `GET /project/location/list` | 独立菜单 |
| 报价管理 [material-draft](src/views/plan/material-draft/) | 分期 `GET /project/project/projectPeriodList`；候选主表 `GET /project/materialCandidate/list`、`POST /project/materialCandidate/add\|edit`；候选明细 `GET /project/materialCandidateItem/list`、`POST /project/materialCandidateItem/addBatch\|editBatch`；物料单位 `GET /stock/material/queryById`；计划保存 `POST /project/materialPlan/editBatch` | 候选主表列表必须传 `periodId`，页面按分期聚合；新增主表只传 `periodId/candidateName`，兼容返回对象、直接 ID 和按唯一名称反查三种取 ID 方式，再以 `candidateId` 新增明细；修改按 `candidateId` 全量同步。明细传 `materialId/quantity/unitId/remark`。状态直接读取 `project_material_candidate_status` 的 `0/1/2/3`；删除转作废状态 `3`。隐藏页支持新增、修改、只读查看。导出和 `.xlsx` 解析均在前端完成；导入已锁定报价只读取当前 `periodId` 下状态 `1` 的主表及对应明细，去掉候选明细 `id` 后合并为计划草稿，最终仍由 `/project/materialPlan/editBatch` 保存；Apifox 无专用导入/导出接口 |
| 实施管理 [views/implement](src/views/implement/) | 工序 `GET /project/process/list`；工序详情 `GET /project/process/queryById`；实施日志 `GET /project/implementLog/list`(processId) | 独立菜单 |
| 回款管理 [views/payment](src/views/payment/) | 合同 `GET /project/contract/list` + `POST add` + `GET queryById`（`POST edit` 已废弃）；回款计划 `GET /project/paymentRecord/list` + `POST addBatch/editBatch` | 回款计划按 `periodId` 维护，批量请求使用 `{ periodId, records }` |

## 四、对接要点 / 待确认

1. **状态码**：字典编码 `project_period_status`；当前前端通过 `POST /project/period/status` 传 `{ periodId, status }`。2026-08-27 刷新的 Apifox 请求 schema 已包含 `PENDING_APPROVAL`，计划提交、审批通过和驳回分别传 `PENDING_APPROVAL`、`IMPLEMENTING`、`PREPARING`。
2. **项目文件路径**：项目基本信息附件由 `addProjectPeriod/editProjectPeriod` 组合提交；合同双文件由 `addWithPaymentRecords/editWithPaymentRecords` 组合提交；方案、详情和验收等仍使用统一上传的页面，仅在成功响应有效时读取路径。
3. **删除主项目**：后端仅提供删除分期，主项目删除/批量删除未接入。
4. **回款计划**：合同信息页从合同组合详情的 `records` 读取；新增/修改均随合同组合请求提交全量列表，页面节点、金额、回款周期分别映射为 `paymentNode/plannedAmount/rollbackTime`。
5. **standalone 计划/实施/回款模块**：列表已对齐后端，详情页(方案详情/日志详情/合同回款)为设计稿结构，后续按后端实体重构。
