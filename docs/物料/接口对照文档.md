# 前端模块 ↔ 后端接口 对照表

> 后端接口以 **apifox 接口文档** 为准：https://s.apifox.cn/624803a6-fdb4-4609-bb70-3ebd21050a81
> 前端 API 定义在 `src/views/<模块>/*.api.ts`。正式后端已实现：物料/出入库/客户 + **项目模块（2026-08-16，见 `docs/项目/`）**；车辆仍待后端。

---

## 一、后端接口文档全集（4 类）

### 1. 物料库存 `/stock/material`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/stock/material/list` | 物料-分页列表查询 |
| GET | `/stock/material/queryById` | 物料-详情 |
| POST | `/stock/material/add` | 物料-添加 |
| POST | `/stock/material/edit` | 物料-编辑 |
| DELETE | `/stock/material/delete` | 物料-删除 |
| DELETE | `/stock/material/deleteBatch` | 物料-批量删除 |

### 2. 出入库台账 `/stock/ioRecord`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/stock/ioRecord/list` | 台账-分页列表 |
| GET | `/stock/ioRecord/queryById` | 台账-详情 |
| POST | `/stock/ioRecord/manualIn` | 手动入库 |
| POST | `/stock/ioRecord/manualOut` | 手动出库 |

### 3. 出入库申请 `/stock/apply`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/stock/apply/list` | 申请-分页列表 |
| GET | `/stock/apply/queryById` | 申请-详情 |
| POST | `/stock/apply/add` | 申请-添加 |
| POST | `/stock/apply/edit` | 申请-编辑 |
| DELETE | `/stock/apply/delete` | 申请-删除 |
| DELETE | `/stock/apply/deleteBatch` | 申请-批量删除 |
| POST | `/stock/apply/approve` | 审批通过 |
| POST | `/stock/apply/reject` | 驳回 |
| POST | `/stock/apply/execute` | 申请-执行出入库 |
| POST | `/stock/apply/cancel` | 取消 |
| GET | `/stock/apply/items` | 申请-明细分页列表 |
| GET | `/stock/apply/approvals` | 申请-审批记录分页列表 |

### 4. 客户管理 `/project/customer`
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/project/customer/list` | 客户-分页列表 |
| GET | `/project/customer/queryById` | 客户-详情 |
| POST | `/project/customer/add` | 客户-添加 |
| POST | `/project/customer/edit` | 客户-编辑 |
| DELETE | `/project/customer/delete` | 客户-删除 |
| DELETE | `/project/customer/deleteBatch` | 客户-批量删除 |

---

## 二、前端模块 ↔ 接口 对照与状态

| 前端模块 | 前端 API 路径 | 文档/后端 | 状态 |
| --- | --- | --- | --- |
| 物料管理 `goods` | `/stock/material/*` | 同 | ✅ 已对齐 |
| 库存管理 `stock` | `/stock/material/list` + `/stock/ioRecord/manualIn|manualOut` + `/stock/ioRecord/takeStock` | 同 | ✅ 已对齐（2026-08-12，盘存弹窗 08-15） |
| 盘存记录 `stocktake` | `/stock/ioRecord/list`(sourceType=stocktake) + `/stock/ioRecord/takeStock` | 同 | ✅ 已对齐（2026-08-15，新增页面） |
| 出入库(手动) | `/stock/ioRecord/manualIn|manualOut` | 同 | ✅ 已对齐 |
| 出入库管理 `record` | `/stock/apply/*` + `/stock/ioRecord/list` | 同 | ✅ 已对齐（2026-08-13，补齐明细/挂起/批量出入库） |
| 领料申请 `pick` | `/stock/apply/add`(OUT+PICK) + `/project/period/searchByName` | 项目接口文档无，按约定字段 | ✅ 已对齐（2026-08-13） |
| 还料申请 `return` | `/stock/apply/add`(IN+RETURN) + `/stock/apply/returnList` + `/project/period/searchByName` | returnList 待后端 | ✅ 已对齐（2026-08-13，接口异常回退本地示例） |
| 采购入库 `purchase` | `/project/purchaseOrder/*` + `/project/supplier/*` + `/stock/ioRecord/manualIn` | 同 | ✅ 已对齐（2026-08-13，改独立采购订单模块） |
| 物料申请(出入库申请) | `/stock/apply/add` | 同 | ✅ 已对齐（2026-08-12） |
| 往来客户 | `/project/customer/*` | 同 | ✅ 已对齐（2026-08-12） |
| 车辆管理 | `/resource/vehicle/*` | **文档无** | ❌ 后端未实现 |
| 项目管理 | `/project/project/*` + `/project/period/*` | 同 | ✅ 已对齐（2026-08-16，见 `docs/项目/项目模块接口对照.md`） |
| 计划方案管理 | `/project/plan/*` | 同 | ✅ 已对齐（2026-08-16，计划方案实体） |
| 实施管理 | `/project/process/*` + `/project/implementLog/*` | 同 | ✅ 已对齐（2026-08-16，工序/实施记录） |
| 回款管理 | `/project/contract/*` | 同 | ✅ 已对齐（2026-08-16，合同管理；回款计划后端暂无） |

---

## 三、已对齐 / 修复记录（2026-08-12）

1. **物料编码**：mock 专用 `generateCode` 接口正式后端没有 → 已改**前端不在表单预填**，编码由后端生成（新增时留空、保存后自动带出）；mock 在 add 接口模拟生成（`WL+日期+序号`）。
2. **客户**：路径 `/resource/customer/*` → `/project/customer/*`；字段 `contact/phone` → `contactPerson/contactPhone`，补 `customerNo/address/customerInfo`。
3. **物料申请(出入库申请)**：接口 `/material/apply/*` → `/stock/apply/*`；表单字段对齐 `StockApply`（`applyType` IN/OUT、`useDate`、`applyUserName`、`deptName`）；明细字段对齐 `StockApplyItem`，提交结构 `detailList` → `itemList`；选物料抽屉改调 `/stock/material/list`，类别树用前端静态。
4. **出入库记录页重构**：原旧 mock 页改为「申请出入库 + 库管操作」——出入库申请列表(`/stock/apply/list`) + 库管审批(通过 `approve`/驳回 `reject`) + 出入库台账(`/stock/ioRecord/list`)。
5. **库存管理页新增**：所有在库物料库存数量列表 + 行操作「出入库」(手动入库/出库 `manualIn`/`manualOut`)。

## 四、本轮（2026-08-13）物料模块功能补齐

1. **领料 / 还料 / 采购 三页面接入**：统一走 `/stock/apply/add`（领料 OUT+PICK、还料 IN+RETURN、采购 IN+PURCHASE）；项目联动 `/project/period/searchByName`；还料列表 `/stock/apply/returnList`（接口异常回退本地示例 `Return.data.ts` 的 `returnListMock`）；采购供应商选 `/project/customer/list`。
2. **出入库管理页补齐**：
   - 明细查看：共用组件 `material/components/ApplyDetailDrawer.vue`，调 `/stock/apply/queryById`；
   - **挂起**：apifox 无挂起接口 → 走 `/stock/apply/reject`，`approvalComment` 前缀「挂起：原因」，前端按前缀把状态展示为「已挂起」（`record/index.vue` `getStatusText`）；
   - **批量出库/批量入库**：选中申请逐条 `queryById` 后，对每条明细循环 `/stock/ioRecord/manualOut|manualIn`（`execIo`）。
3. **采购入库页接入查看明细**：`getDropDownAction` 的「查看明细」接 `/stock/apply/queryById` + `ApplyDetailDrawer`（原为占位提示）。
4. **计划页用料抽屉修复**：`project/plan/components/MaterialSelectDrawer` 旧依赖 `/material/*`（`getCategoryTree` 等已被重构删除）→ 改为前端静态类别树(`Goods.data.ts` 的 `categoryOptions`) + 对齐 `/stock/material/list` 字段（`materialName/materialCategory/stockQty/unit`），修复了构建 `[MISSING_EXPORT]`。
5. **mock 补齐**（`pnpm mock` 全流程可跑）：
   - 新增 `mock/demo/stockApply.ts`：`/stock/apply` 的 list / queryById / add / approve / reject / cancel / returnList；
   - `mock/demo/project.ts` 增 `/project/period/searchByName`。
6. **页面运行期 bug 修复**（2026-08-13，修复后领料/还料/采购/出入库/库存 5 页均实测可开）：
   - **领料/还料/采购弹窗崩溃根因**：`userStore.getLoginInfo` 是 store **getter(返回对象)**，原代码误写成 `getLoginInfo()` 当函数调用 → `TypeError: ... is not a function`，页面打不开。已改属性访问，并局部 `any` 处理 `LoginInfo.departs?: []` 的空元组类型。
   - **`updateSchema`/`setFieldsValue` 原在 setup 顶层调用**（表单未挂载）→ 报「form instance has not been obtained」。已移入 `onMounted`（pick/return）与 `useModalInner`（PurchaseModal 打开时）。
   - **采购弹窗 `loadCustomers`** 原在 setup 顶层立即请求 `/project/customer/list`（未 mock → 502），且未兼容分页返回结构 `{records,total}`。已移入打开时调用 + 兼容处理。
   - **mock 补齐**：`customer.ts` 路径 `/resource/customer/*` → `/project/customer/*`（对齐前端，兼作采购供应商下拉）；新增 `/sys/user/verifyIzDefaultPwd` mock（返回 `success:true, message:'no'`，避免 mock 模式下默认密码轮询 502）。

## 五、本轮（2026-08-13）功能补齐（接口文档对齐）

1. **部门带出**：登录接口 `/sys/login` 返回 `result.userInfo`（含 `id/departName/departIds`）+ `result.departs[]`。前端新增 `src/views/material/material.util.ts` 的 `getCurrentUser()`，统一取当前登录人「申请人ID/姓名/部门ID/部门名称」；领料/还料提交带 `applyUserId/deptId/deptName`。
2. **采购改走独立「采购订单」模块**：接口文档有 `/project/purchaseOrder/*`（orderNo/projectId/periodId/supplierId/supplierName/orderDate/expectedArrivalDate/totalAmount/status）+ `/project/purchaseOrderItem/*`，不再复用出入库申请。采购页改为对接 purchaseOrder；**供应商下拉改 `/project/supplier/list`**（此前误用 `/project/customer/list`）；状态流转 `待采购→采购中→已到货→已入库` 通过 `edit` 更新；入库逐条调 `manualIn`（sourceType=apply、sourceId=订单id）。新增 `mock/demo/purchase.ts`。
3. **申请撤回/删除/重新编辑**：出入库管理页对**自己的申请**显示：撤回（`cancel?id=`，状态置「已撤回」）、删除（`delete?id=`）、修改（已撤回/已驳回 → 跳 `pick|return?applyId=` → `queryById` 预填 → `edit` 重提，状态回待审批）。仅库管未出库前（待审批）可撤回。
4. **字典配置**：物料模块字典**走后端「系统管理 → 数据字典」**（sys_dict），前端自动读取（`JDictSelectTag` 表单 + `loadDictMap` 标签，读 `sysAllDictItems` 缓存、再实时调 `/sys/dict/getDictItems/{code}`）；**已删除前端写死常量 `material.dict.ts`**。字典编码与傻瓜配置见 `docs/物料模块字典配置.md`。
5. **领料/还料提交成功后自动返回** `/material/record`；项目搜索加 `useDebounceFn` 300ms 防抖；提交按钮 `:loading` 防连点。
6. **后端需补字段清单**：见 `docs/后端需补字段-出入库申请与采购.md`（`stock_apply` 需补 `bizType/projectNo/projectName/returnUser/executeStatus` 等；取消/删除按 query `id`；采购 add/edit 接收 `itemList`）。

## 七、与真实后端的对齐（2026-08-13，重要）

实测正式后端 `StockApply` 实际字段：`applyType`(OUT/IN)、`status`(**英文码** PENDING/APPROVED/REJECTED/CANCELED)、`applyUserId/applyUserName/deptId/deptName/useDate/remark/itemList/approvalList`。**暂无** `bizType/executeStatus/projectNo/projectName/returnUser`。

前端已据此对齐：
1. **出入库管理页**：
   - 审批状态显示走后端字典 `stock_apply_status`（**字典值=英文码**：PENDING→待审批、APPROVED→已通过、REJECTED→已驳回、CANCELED→已撤回），正式后端字典已更新+刷缓存。
   - **移除「执行状态」列**（后端暂无 executeStatus）。
   - **业务类型列**由 `applyType` 推导（OUT=领料/出库、IN=还料/入库；采购走独立采购订单模块，不在申请列表）。
   - **库管执行**：出入库管理页 审批(PENDING→APPROVED) → 出库/入库(APPROVED 行) / 驳回。
2. **dict 状态值已改英文码**：`docs/物料模块字典配置.md` 同步更新。
3. mock 同步：申请状态英文码、移除 mock 中采购类申请（采购已独立）。

> 待后端补 `bizType/executeStatus/projectNo` 后：业务类型可精确区分领料/还料/采购、执行状态列恢复、项目关联生效（见后端字段清单文档）。

## 六、本轮（2026-08-13）出入库操作明细维度改造（已实现）

1. **出入库执行弹窗** `record/components/StockExecuteModal.vue`：单条出库/入库改为**明细维度**——勾选部分明细（部分出库）、本次执行数默认=剩余可执行数（申请−已执行，按台账 `sourceId+materialId` 聚合）、可改数量、逐行差异说明（留痕到台账 remark）。
2. **还料三层数量**：还料申请明细列显示 **应还数量**(后端算)/**实际还库数量**(申请人填)，提交带 `shouldReturn/actualReturn`；库管入库弹窗显示 应还/实际还/**最终入库数**（可改），不一致必填说明。
3. **采购入库差异留痕** `purchase/components/StockInModal.vue`：实际入库数量/单价/**规格型号**可改，与采购不一致必填差异说明，逐条写台账 remark（含「数量由X改Y/型号由A改B」）。
4. **批量出库/入库**：选中多张申请，未执行明细按剩余可执行数默认执行，跳过已执行完的。
5. **mock**：`/stock/ioRecord/list` 支持 `sourceId` 过滤；manualIn/out 透传 `sourceType/sourceId/sourceNo/model/remark`。
6. **字典已在正式后端配好**（8 个，含 `material_category`），前端标签/下拉走 `sysAllDictItems`/字典接口，已刷缓存（重新登录生效）。

> 待后端补：`StockApplyItem.outQty/inQty`（已执行数量，免前端聚合台账）、`shouldReturn/actualReturn` 落库。

### 追加（2026-08-14）
- **出入库管理页批量删除**：选中行 →「批量删除」，调 `/stock/apply/deleteBatch`（`?ids=a,b`，joinParamsToUrl），仅删除自己的 待审批/已撤回/已驳回 申请。
- **供应商管理页**（新）`/resource/supplier`：走 `/project/supplier/*`（list/add/edit/delete/deleteBatch/queryById），采购入库页供应商下拉同源。
- **客户/供应商 详情补全**（接口文档已更新）：客户加 销售/维保/客户经理（用户选择器，全用户模糊搜索 `loadUserOptions`）、开票抬头/税号/开户银行/账号/开票地址/电话；供应商加 **对接人**（用户选择器）；**编号（客户/供应商/物料编码）由后端生成**，新增时留空、禁手填，保存后自动带出。
- **批量删除按钮**：所有页面（出入库/物料/客户/供应商/车辆）**常显**，点击时校验"未勾选则提示"。

### 追加（2026-08-14 第二轮，后端字段已补齐）
- **后端接口文档已更新，字段已补**：`StockApply` 现含 `bizType(PICK/RETURN/PURCHASE)/projectNo/projectName/returnUser/supplierName/expressNo/executeStatus/approvalList`；明细含 `executeStatus/executeUserId/executeUserName/executeTime`。
- **前端恢复使用**：业务类型列走 `bizType` 字典；**恢复「执行状态」列**（走 `stock_execute_status` 字典）。
- **状态枚举更新**：`PENDING` 待审批 / `PARTIAL_APPROVED` 部分通过 / `APPROVED` 已通过 / `REJECTED` 已驳回 / `WITHDRAWN` 已撤回 / `CANCELED` 已取消。前端动作：审批(PENDING)、出库/入库(APPROVED)、**修改(WITHDRAWN/REJECTED)**、**删除(PENDING/REJECTED/WITHDRAWN)**、撤回(PENDING→WITHDRAWN)。
- **删除「挂起」功能**（原走 reject+注释，现移除；`SuspendModal.vue` 已删除）。
- **明细抽屉展示审批动态**：`approvalList`（审批人/节点/结果 AGREE-REJECT/备注/时间）。
- **字典**：`stock_apply_status` 已配 6 项（含 WITHDRAWN/CANCELED），`stock_execute_status`/`stock_apply_biz_type` 同步；mock+文档已更新，**正式后端字典已按此在库配置**。

### 追加（2026-08-14 第三轮）
- **出入库申请页**：搜索表单新增 申请单号/业务类型(字典)/审批状态(字典)/关联单号(=项目分期编号 projectNo)/项目名称；列表新增 关联单号/项目名称 列。
- **审批动态**：审批人按 `approvalUserId` 从用户列表解析姓名，缺失节点默认"审批"。
- **执行弹窗**：明细数量优先取 `applyQty`（正式后端字段）；加载失败/空明细/整单已执行完均有提示。
- **物料基本维护**：新增**导入**（`POST /stock/material/importExcel`，multipart）与**下载模板**（`GET /stock/material/importTemplate`）。
- **品牌改字典 `material_brand`**：物料新增/编辑/搜索的品牌字段走字典（mock+SQL 已配 7 个品牌）。

### 追加（2026-08-14 第四轮）
- **字典配置机制**：所有字典（业务类型/状态/类别/**品牌 material_brand** 等）均配在【系统管理 → 数据字典】（前端管理页，走后端 sys_dict），前端 `JDictSelectTag`/`loadDictMap` 读取；**正式后端已在库配置**，缺某字典时后台数据字典页录入即可。
- **采购入库供应商下拉修复**：根因是 `openModal(true)` 不传 data 时 vben `useModalInner` 回调不触发 → 供应商/项目下拉为空。修复：新增采购单打开改为 `openPurchaseModal(true, {})` 传空对象。供应商下拉(3)与分期项目搜索实测正常。

### 追加（2026-08-14 第六轮，执行出入库传参修复）
- **修复「执行出库/入库传的数据不对」**：根因是 `StockExecuteModal.vue` 与 `record/index.vue`（批量出入库）调 `manualInOut` 时**漏传 `unitId`**（对比已验证的库存管理页 `IOModal.vue`、采购入库 `StockInModal.vue` 都传了 `unitId`）。后端 `StockIoRecord` 靠 `unitId` 做「当前单位→基准单位」换算，漏传导致基准数量/金额/库存变动算错。已补传 `unitId`（明细带出）+ 简化 `unitPrice` 取值，同时补齐 **`model`（规格型号）**——与采购入库 `StockInModal` 一致，执行出库/批量出入库的台账也留规格型号。
- **物料批次/库位/SN 改造设计稿**：见 `docs/物料批次-库位-SN管理改造方案.md`（批次=入库单×物料×单价；仓库-库区-层；SN 确认到货录入/系统生成/打标签；入库推荐库位；出库 FIFO 推荐批次）。待评审后按 P0→P4 实施，后端需新增 batch/warehouse/zone/level/sn 接口。
- **后端需求（供与后端团队交流逻辑）**：见 `docs/后端开发需求-批次-库位-SN.md`——含**仓库管理**（仓库-库区-层三级维护，前端新增「仓库管理」页面）、后端要新建的 5 张表、9 个新接口、`manualIn/manualOut` 扩展入参、存量批次初始化，以及 11 个待确认逻辑点（D1–D11）。

### 追加（2026-08-14 第五轮）
- **采购入库补齐**：
  - **periodName 列表解析**：后端列表/详情可能只回 `periodId`，前端新增 `listPeriod`(`/project/period/list`，`Purchase.api`)拉全量分期建 `periodId→periodName` 映射，列表 `api` 包装 `listWithPeriod` 查询后按映射补 `periodName`（详情抽屉同样用列表行兜底）。
  - **待采购可修改**：`PurchaseModal` 支持编辑模式（`openPurchaseModal(true,{record,isUpdate:true})`），`queryById` 回填订单头+明细，提交走 `edit`（仍为待采购）。
  - **未入库可关闭填原因**：新增 `CloseModal`（`status=已关闭`+`closeReason` 必填），待采购/采购中/已到货 三个未入库状态行操作均可「关闭」。
- **新增物料：型号(规格)必填**：物料基本维护 `Goods.data.ts` 与选物料抽屉内新增弹窗 `MaterialAddModal.vue` 的 `model` 字段加必填校验。
- **选物料抽屉 `MaterialSelectDrawer`**：分类树顶部加「全部」节点，默认选中并展示全部物料（选中全部/重置时清空 `materialCategory` 过滤）。
- **下拉统一**：品牌（选物料抽屉）补「初次点击不展示、输入后模糊」`filterOption` 模式——与供应商(`Purchase.data.ts`)、用户（供应商对接人/客户三个负责人）、分期（`searchByName` 远程模糊）一致。
- **字典**：`purchase_order_status` 增 **已关闭** 项（`docs/物料模块字典配置.md` 已同步；**正式后端字典已配置**，若缺该项后台补录后清缓存/重启生效）。
- **删除物料域 mock**：`materialGoods.ts`/`stockApply.ts`/`purchase.ts`/`customer.ts`/`materialApply.ts` 已删除，物料/库存/出入库/采购/客户/供应商均改走**真实后端**（`pnpm dev`，VITE_PROXY 到 `http://192.168.18.2:8080/shouhuiApi`）。`pnpm mock` 下这些页面需后端可达。

### 追加（2026-08-14 第七轮，审批拆解改造 + 执行出入库参数修正）

**1. 审批拆成两种方式**（`record/index.vue` + 两个弹窗组件）：
- **一条一条审批（明细维度）** `ApproveModal.vue`：打开时调 `queryById` 拿 `itemList + approvalList`，逐条三态 **通过 / 驳回 / 暂不处理**（默认通过）；已审批过的明细行**只读**显示「已通过/已驳回」+ 最近备注，不可再改；整单备注 textarea。提交：
  - 全通过 → `approve`（整单，items 空）；全驳回 → `reject`（整单）。
  - 混合 / 部分审批 / PARTIAL_APPROVED 继续审批剩余 → 通过项走 `approve({...AGREE, items: 通过项(approve:true)})`，驳回项走 `reject({...REJECT, items: 驳回项(approve:false)})`；**暂不处理项不提交** → 保持待审批，提示「N 条暂不处理，下次再批」。
  - 明细审批结果统一经 `items`（字段名以 apifox 实读为准，非 `itemApprovals`）。
- **批量审批（整单批量）** `BatchApproveModal.vue`（新）：列表勾选多条申请（`批量审批` 按钮，`selectedKeys.length` 时显示），每条整单 通过/驳回 + 备注，并行查明细数/总量展示；提交逐条调 `approve/reject`（整单不带 items）；**只处理 `PENDING`**，其余自动跳过并提示。

**2. 明细审批状态推算（核心规则）** `material.util.ts` 新增 `calcItemApproval(itemId, approvalList, fallbackStatus)`：
- 按 `itemId` 从 `approvalList` **倒序取最新一条**：优先顶层 `approval.itemId` 匹配，否则在 `approval.items[]` 里按 `itemId` 找。
- 该记录/元素的 `approve === true` → 已通过；`false` → 已驳回；无任何记录 → 待审批（`fallbackStatus`：APPROVED→true、REJECTED→false、其余→null）。
- **不靠 `itemList[].status`**。`approve` 用 `!== undefined` 判断（保留 null 的「暂不处理」语义，`??` 会把 null 当缺失）。
- 复用点：ApproveModal（已审批行只读）、StockExecuteModal / handleBatchIo（只执行已通过明细）、ApplyDetailDrawer（明细「审批状态」列）。

**3. `PARTIAL_APPROVED` 状态接通**（此前全 src 零逻辑引用）：部分通过后行操作「审批」（继续批剩余待审批明细）+「出库/入库」（执行已通过明细，沿用执行弹窗）；申请人**不提供** 撤回/修改/删除（部分已生效）；`handleBatchIo` 过滤条件扩为 `APPROVED/PARTIAL_APPROVED`，明细循环按 `calcItemApproval(...).approve === true` 过滤（待审批跳过）。

**4. 执行出入库参数修正**（修正第六轮）：
- **删掉 `model` 字段**：apifox 实读 `/stock/ioRecord/manualIn|manualOut` 入参**无 `model` 字段**（第六轮补传是错的），已从 StockExecuteModal / handleBatchIo 移除。
- **`unitId` 从字典 `inv_unit` 取**：申请页提交明细只带 `unitName` 不带 `unitId`（单位来自物料 `unitList` 子表，后端明细回传的 unitId 不可靠）→ `material.util.ts` 新增 `resolveDictUnitId(unitName, fallbackId)`：`loadDictMap('inv_unit')` 按 `text === unitName` 反查 value，匹配不到兜底明细 `unitId`。执行出库/批量出入库的 `unitId` 均由此解析。
- **传当前执行用户 id**：执行出入库（执行弹窗 + 批量出入库）给 `manualIn/out` 传 `approvalUserId: getCurrentUser().applyUserId`（**执行人字段名 = `approvalUserId`**，经用户确认），后端据此记录执行人再查。
- 库存管理页手动出入库 `IOModal.vue` 不受影响（本身 `unitId` 正确）。

**5. 明细抽屉审批状态**：明细列「审批状态」读分页接口 `/stock/apply/items` 返回的 `status` 字段走字典 `stock_item_status`（待审批/已通过/已驳回）。（注：第九轮改分页接口后不再用 `calcItemApproval` 推算，见上文第九轮。）

### 追加（2026-08-14 第八轮，执行出入库统一走 /stock/apply/execute）

**出入库执行（单条 + 批量）统一改走 `POST /stock/apply/execute`**，不再逐条调 `manualIn/manualOut`：
- **接口契约**（apifox 实读确认）：Body `StockApplyExecuteRequest = { applyId, itemIds?, items? }`，`items:[{ itemId, executeQty }]`；`executeQty` 空=执行剩余数量，`items` 空=按 `itemIds` 或全部待执行明细；**方向由申请自身决定**（IN/OUT），无需传 `ioType`。
- **单条执行** `StockExecuteModal.vue`：`handleOk` 从「逐条 manualInOut」改为**整单一次 executeApply**——选中行 `{ itemId, executeQty(本次执行数，默认剩余可执行) }` + `applyId` + `itemIds`；不再传 `unitId/unitPrice/remark/approvalUserId`（执行人由后端从登录 token 取）。
- **批量执行** `record/index.vue` `handleBatchIo`：每张申请一次 executeApply——只处理 `APPROVED/PARTIAL_APPROVED` 且 applyType 匹配的申请，已通过明细按「剩余可执行数(申请−已执行)」作为 `executeQty`，跳过已执行完明细。
- **执行弹窗勾选列改选择框（表头可全选/取消全选）**：明细表加 `#headerCell` 插槽——`column.key === 'check'` 渲染 `a-checkbox`（`:checked=allChecked`、`:indeterminate=someChecked`、`:disabled=无可执行行`），`onHeaderCheck` 一键全选/取消全选「剩余>0」的可执行行；行内 checkbox 对 `remain<=0` 行禁用。
- **说明(留痕)列保留但暂不提交**：`execute` 的 `items` 目前只有 `{itemId, executeQty}`，**无 `remark` 字段** → 弹窗「说明」列保留（本地记录），待后端给 `StockApplyExecuteItem` 补 `remark` 后再随 execute 提交。
- 移除 `manualInOut`（`../goods/Goods.api`）在申请执行路径的调用；**采购入库（`purchase/components/StockInModal.vue`）与库存管理页手动出入库（`IOModal.vue`）仍走 `manualIn/manualOut`，不受影响**。
- **实测通过（2026-08-14 连真实后端）**：主列表与执行弹窗表头勾选框均可全选/取消全选；单条执行「执行出库」触发 `POST /stock/apply/execute`，请求体 `{applyId, itemIds, items:[{itemId, executeQty}]}`（无 remark/approvalUserId/unitId）符合契约。后端会拒绝已执行完成的明细（返回业务错误「所选明细已执行完成：XXX」）；前端「剩余可执行」目前按台账 `sourceId` 聚合兜底，与后端明细实际执行状态可能不一致（见 `docs/后端需补字段-出入库申请与采购.md` 实测注），待后端 `queryById` 返回 `outQty/inQty` 后精确化。

- **项目/车辆/计划/实施/回款 5 个模块**：接口文档里没有，**后端未开发**。前端已按 mock 完成页面，待后端开发后按本文档路径接入。
- **库存管理菜单**：后端已新增 `stock` 子菜单并授权，但 `getUserPermissionByToken` 有缓存，**需后端清缓存/重启**后侧边栏才会出现「库存管理」。

### 追加（2026-08-14 第九轮，明细分页/审批分页接口 + 独立详情页 + 字典全面对齐）

**1. 明细 / 审批记录改用分页接口**（接口文档已更新，`StockApply.api.ts` 新增 `queryItems`/`queryApprovals`）：
- `GET /stock/apply/items`（出入库申请-明细分页列表）：`params { applyId, pageNo?, pageSize? }`，返回 `{ records, total, size, current, pages }`；明细字段含 `materialName/materialCategory/brand/model/unitName/applyQty/status/executedQty/executeStatus/executeUserName/executeTime/remark`。
- `GET /stock/apply/approvals`（出入库申请-审批记录分页列表）：`params { applyId, pageNo?, pageSize? }`；审批字段含 `nodeName/approvalUserName/materialName/approvalResult(AGREE/REJECT)/approvalComment/approvalTime`。

**2. 申请明细回归「抽屉」（原独立详情页/详情路由已删除）**：
- **独立详情页取消**：`src/views/material/record/detail/index.vue` 与 `/material/record/detail/:id` 静态路由（`staticRouter.ts`）已删除，列表「明细」不再 `router.push`。
- 共用抽屉 `material/components/ApplyDetailDrawer.vue`（**加宽 `width:1000`**，字典驱动、零硬编码）：
  - **申请信息**（`queryById`，`a-descriptions :column=3`）：申请单号/业务类型(`stock_apply_biz_type`)/出入库方向(`stock_apply_type`)/审批状态(`stock_apply_status`)/执行状态(`stock_execute_status`)/申请人/部门/项目/供应商(有则显)/还料人/快递单号/采购总价/使用日期/创建人/创建时间/备注。列表行数据兜底（`{...data.record, ...res}`）。
  - **物料明细 tab**：`queryItems` 分页（`applyId` 入参），`immediate:false` 打开后才拉取；申请数量 `applyQty ?? unitQty`；审批状态(`stock_item_status`)/执行状态(`stock_execute_status`) 走字典 tag。
  - **审批记录 tab**：`queryApprovals` 分页，审批结果(`approval_result` AGREE/REJECT) 走字典 tag。
  - 打开时 `Promise.all([reloadItems(), reloadApprovals()])`，tab 切换按需 reload。

**3. 页面状态字段全面走系统数据字典（消灭前端硬编码）**——按用户要求「状态字段都应取系统字典，不要前端自己定义」：
- 已改字典驱动：`BatchApproveModal`（状态 tag + 业务类型列）、`StockExecuteModal`（业务类型/方向）、`ApproveModal`（类型 + 明细审批状态 tag）、`ApplyDetailDrawer`（业务类型/方向/审批状态/执行状态/明细审批状态/审批结果 6 类 tag）。
- `MaterialApply.data.ts`「出入库类型」下拉 `Select 硬编码[入库/出库]` → `JDictSelectTag`（dictCode `stock_apply_type`，与 SQL 字典值完全一致 IN/OUT）。
- 修正 `pick`/`return` 重新提交 `status` 传中文「待审批」→ **`PENDING`**（`stock_apply_status` 字典值=英文码）。
- 已核对无需改：`purchase` 模块（`purchase_order_status` 字典值本就是中文，列表已 `statusMap` 字典显示）、`stock_execute_status`（值=中文，作为值+显示均正确）、单位下拉（SQL 无 `inv_unit` 字典，物料 `unitList` 子表驱动，保留）。
- 剩余「硬编码」均为**交互控件**（通过/驳回/暂不处理 radio = 提交 AGREE/REJECT 语义；返回/成功提示文案），非状态展示，保留。

> 对接新模块时，前端 API 路径/字段一律以 apifox 文档为准；文档没有的接口视为后端未实现，需后端开发。

### 追加（2026-08-15 第十轮，申请明细/执行出入库物料接口拆分）

**接口文档已更新：`queryById` 的 `itemList`/`approvalList` 已废弃**（文档标注「申请明细；请使用 /stock/apply/items 分页查询」「审批记录；请使用 /stock/apply/approvals 分页查询」）。三个仍在用 `queryById({id}).itemList/.approvalList` 的组件全部改为拆出来的分页接口：

1. **逐条审批 `ApproveModal.vue`**：打开时并行调 `queryItems`（明细分页）+ `queryApprovals`（审批记录分页），不再走 `queryById`。明细审批状态以**明细 `status` 字段**为准（`stock_item_status`：PENDING/APPROVED/REJECTED）；`status !== 'PENDING'` 即已审批过 → 该行只读，备注取审批动态按 `itemId` 最新一条（`calcItemApproval`）。
2. **执行出库/入库 `StockExecuteModal.vue`**：打开时调 `queryItems` 拉明细分页，**只保留已通过明细（`status === 'APPROVED'`）**；**已执行数直接取明细 `executedQty`**（`/stock/apply/items` 直接返回，**不再聚台账 `listRecord`/`getExecMap`**），剩余可执行数 = `applyQty(回退 unitQty) − executedQty`。
3. **批量出库/入库 `record/index.vue` `handleBatchIo`**：遍历选中行，调 `queryItems` 拉明细分页 → 已通过明细（`status === 'APPROVED'`）按 `executedQty` 算剩余，逐张申请一次 `executeApply`；**删除 `getExecMap`**（不再用台账聚合），`calcItemApproval` 仅留在 `ApproveModal` 用于取审批备注。
4. **批量审批 `BatchApproveModal.vue`**：物料数/申请总数量展示由 `queryById.itemList` 改为 `queryItems`（明细分页）。

**要点**：
- `queryItems` 返回分页结构 `{ records, total, size, current, pages }`（MyBatis-Plus），前端取 `records`。
- 已执行数/执行状态/执行人 均为明细级字段（`executedQty/executeStatus/executeUserName/executeTime`），拆接口后执行弹窗与批量出入库无需再依赖台账 `sourceId` 聚合。
- 执行筛选从「`calcItemApproval(...).approve === true`」改为「`status === 'APPROVED'`」——分页接口的明细 `status` 是权威字段，`PARTIAL_APPROVED` 单只有已通过明细带 `APPROVED`。

**明细抽屉首次打开列表为空修复**（`ApplyDetailDrawer.vue`）：
- **根因**：抽屉正文用 `v-if="detail.applyNo || detail.id"` 门控，打开时 `detail.value = {}` 先清空、`queryById` 后再赋值——同一 tick 内立即调 `reloadItems()/reloadApprovals()`，此时两个 `<BasicTable>` 尚未挂载注册，vben `getTableInstance()` 未取到实例 → reload 静默 no-op；且 Ant Design Vue 的 `a-tabs` 默认**惰性渲染非激活 tab**，审批记录表首次打开也未挂载。
- **修复**：① 两个 `a-tab-pane` 加 `forceRender`，抽屉打开即全部挂载；② `detail.value` 赋值后 `await nextTick()` 再刷新（等 v-if 子树挂载、`@register` 注册完成）。`handleTabChange` 按需 reload 保留。


### 追加（2026-08-15 全局规定：当前操作人身份）

**规定（此后所有模块照此执行）**：
1. **当前用户身份在登录时已保存**：登录流程已把用户信息写入 auth 缓存（`USER_INFO_KEY` + `LOGIN_INFO_KEY`，localStorage/sessionStorage 由 `permissionCacheType` 决定）。物料模块统一经 `getCurrentUser()`（`src/views/material/material.util.ts`）读取，返回 `{ applyUserId, applyUserName, deptId, deptName }`。
2. **凡涉及审批/操作类提交，必须传当前操作人 id**：字段名统一 **`approvalUserId`** = `getCurrentUser().applyUserId`。已接入：`ApproveModal`（逐条审批，含 items 明细）、`BatchApproveModal`（整单批量）、`StockApply.api.ts` 的 `approveApply`/`rejectApply` 入参注释。
3. **列表/详情展示用户时显示其姓名**：后端回传姓名优先；后端只回传 id 时，用 `loadUserMap()`（`/sys/user/list` 全量 + 模块级缓存）按 id 解析姓名。已接入：`ApplyDetailDrawer`（物料明细 tab「执行人」、审批记录 tab「审批人」按 `executeUserId`/`approvalUserId` 解析，后端姓名兜底）。

**实现点**：
- `material.util.ts`：新增 `loadUserMap()`（返回 `Record<userId, realname>`）与 `resolveUserName(id, fallback)`（同步从缓存解析，组件内常挂在 ref 上，模板直接取 `ref[id]`）。
- `ApplyDetailDrawer.vue`：打开时 `loadUserMap().then(m => userMap.value = m)`；`executeUserName`/`approvalUserName` 列 `bodyCell` 渲染 `userMap[record.executeUserId] || record.executeUserName || '—'`（审批人同理用 `approvalUserId`）。
- 审批提交：`{ applyId, approvalUserId: getCurrentUser().applyUserId, approvalResult, approvalComment?, items? }`。

> 注：执行出库/入库 `executeApply` 不传 `approvalUserId`——执行人由后端从登录 token 取，见 `StockApply.api.ts` 注释。

**驳回校验（2026-08-15 追加）**：凡提交含「驳回」（逐条审批 `ApproveModal` 任一条明细选驳回、或整单驳回；批量审批 `BatchApproveModal` 任一单选驳回）时，**审批备注必填**（驳回原因），未填拦截提交并提示：
- `ApproveModal.vue`：`rejectItems.length && !form.approvalComment?.trim()` → 拦截「驳回时必须填写审批备注（驳回原因）」。
- `BatchApproveModal.vue`：提交前遍历校验「申请单 X 驳回时必须填写备注（驳回原因）」，全部通过再逐单提交。

### 追加（2026-08-15 UI 约定：下载模板统一放导入弹窗内）

**约定（此后所有导入场景照此执行）**：导入页的「下载模板」按钮**不放在列表工具栏**，而是放进导入上传弹窗内。物料基本维护页已改为此方式。

**实现**：共享组件 `JImportModal.vue` 新增可选 `template` prop `{ name, url }` —— 传入时弹窗内（上传区上方）显示「下载模板」按钮，点击走 `useMethods().handleExportXls(name, url)` 下载。页面只需：
```html
<JImportModal @register="registerImportModal" :url="importExcel" :template="{ name: '物料导入模板', url: importTemplate }" />
```
- `goods/index.vue`：已移除表格外的「下载模板」按钮与 `onDownloadTemplate`/`useMethods` 引入，模板配置传 `JImportModal` 的 `template` prop。
- 不传 `template` 时弹窗不显示下载按钮，原有用例不受影响。

**物料单位子表基准单位锁定（2026-08-15 追加，`GoodsModal.vue`）**：
- 新增/编辑物料弹窗的单位子表，**第一行即基准单位**，固定不可移动、不可删除（上移/下移/删除按钮均禁用）。
- **第 2 行起的副单位可上移/下移调整顺序**：换位区间限定在 `1..length-1`（`moveUnit` 增加 `index < 1 || target < 1` 守卫），副单位永远不能上移到第 1 行抢占基准单位位置；删除也限 `index !== 0`。
- 保存逻辑不变：第一行 `isBaseUnit=1`、`conversionQty=1`（基准单位换算系数固定 1），副单位按当前行序生成 `sortNo`。

### 追加（2026-08-15 第十一轮，库存管理：盘存对接 / 台账详情 / 采购状态字典码）

**① 采购订单状态改为字典数字码 + editOrder 补带 orderNo（数据传输问题修复）**

实测后端 `purchase_order_status` 字典的**值是数字码 0~4**（`0关闭/1待采购/2采购中/3已到货/4已入库`），而前端此前提交/流转直接写中文（待采购/采购中/已到货/已入库/已关闭）→ 后端按码存、前端按中文传，状态码错乱。已把 5 处调用点全部对齐为数字码：

| 文件 | 改动 |
| --- | --- |
| `PurchaseModal.vue` | 新增/编辑提交 `status:'1'`（注释标注字典码含义）；编辑分支 `editOrder({ id, orderNo, ...payload })` 补带 orderNo |
| `purchase/index.vue` | `handleConfirm` → `status:'2'`、`handleComplete` → `status:'3'`；行操作/关闭按钮的判断由中文 `'待采购'/'采购中'/'已到货'` 改数字码 `'1'/'2'/'3'` |
| `StockInModal.vue` | 入库成功后 `editOrder({ id, orderNo, status:'4', periodId })` |
| `CloseModal.vue` | 关闭传 `status:'0'` + `closeReason` + 补带 orderNo/periodId |

标签显示不变：`statusMap` 读字典 `purchase_order_status` 渲染，字典 text 显示中文。

**发现的 3 个后端不一致（前端已规避 / 无法改）**：
1. **`POST /project/purchaseOrder/inbound` 自动把订单状态写 `COMPLETED`**（英文码，不在字典）——前端入库成功后立即 `editOrder` 覆盖为 `'4'`（已入库）。
2. **`POST /project/purchaseOrder/edit` 缺 `orderNo` 会重生成采购单号**（实测 `CG…201 → CG…489`）——前端所有 edit 调用统一补带 `orderNo` 原样带回（记录编辑/确认采购/采购完成/入库/关闭）。
3. **后端禁止删除采购单**：`DELETE /project/purchaseOrder/delete?id=…` 与 `deleteBatch` 均返回「采购订单不允许删除」——前端无删除按钮；curl 联调产生的测试采购单无法清理，只能留作样例数据或由后台处理。

**② 盘存（Stocktake）接口对接（2026-08-15 实测通过）**

- 新增接口 `POST /stock/ioRecord/takeStock`（apifox「库存盘存」）。Body `{materialId, actualQty, takeNo?, remark?}`；`takeNo` 为空自动生成（实测 `PD` 前缀单号）。
- 行为：按「实际库存 vs 系统实时库存」差异生成**盘盈/盘亏台账**——盘盈 `ioType=IN`(差异>0)、盘亏 `ioType=OUT`(差异<0)；`beforeQty`=盘点前系统库存、`afterQty`=盘点后实际库存；台账 `sourceType=stocktake`。返回 `{actualQty, diff, ioType, sourceNo, systemQty}`。
- 前端：
  - 库存管理行操作「盘存」弹窗 `StocktakeModal.vue`：展示物料/当前库存，填盘点后数量+备注 → 提交 takeStock。
  - **盘存记录页面** `material/stocktake/index.vue`（新增）：固定按 `sourceType=stocktake` 过滤 `/stock/ioRecord/list`，`diff=afterQty−beforeQty`（正盈绿/负亏红），物料编码/名称按 `materialId` 富化。
- 已实测：takeStock 成功生成盘盈台账；`/stock/ioRecord/list?sourceType=stocktake` 正确过滤出盘存记录。

**③ 出入库台账「详情」弹窗（2026-08-15）**

- `record/index.vue` 台账 tab 行操作新增「详情」→ `IoRecordDetailModal.vue`：本次台账变动（类型/来源/来源单号/数量/基准数量/单价/金额/变动前后库存/备注）+ **物料详情**（按 `materialId` 查物料主表 `queryById`：编码/名称/类别/品牌/型号/基准单位/当前库存/安全库存/备注）+ 单位换算子表；物料查询失败降级用 `resolveMaterial` 列表缓存。

**④ 台账来源/类型字典需补配（2026-08-15）**

- `stock_io_source_type`（台账来源）：后端库**当前为空**，需配 **4 项**：`apply=申请`、`manual=手动`、`stocktake=盘存`、`purchase=采购入库`。
- `stock_io_type`（台账类型）：后端库**当前为空**，需配 **2 项**：`IN=入库`、`OUT=出库`。
- 前端台账列表/详情已用 `loadDictMap` 读取这两个字典，配置后重新登录生效；未配置时「来源/类型」标签显示原始值。

**⑤ 物料新增/编辑接口数据传输修正（2026-08-15）**

- **快速新增（选物料抽屉内「新增物料」`MaterialAddModal.vue` → `POST /stock/material/add`）**：
  - 字段名对齐 `StockMaterial` 契约：`goodsName→materialName`、`categoryName→materialCategory`（改用字典 `material_category`）、`stock→stockQty`、`goodsCode→materialCode`（按编号显示规则：新增隐藏、后端生成、只读展示）。
  - **新增 `unitPrice`（基准单价）/ `stockQty`（初始库存）字段**——原来整个弹窗没有这两个字段，导致新增数据里基准单价/初始库存从不传（后端默认 0）；现可填，未填默认 0（`unitPrice/stockQty: values.xx ?? 0`），保证两者**始终随新增请求上送**。
  - 去掉提交里伪造的 `id: Date.now()`（主键/编码由后端生成，不再往 add 传 id）。
- **物料基本维护新增/编辑 `GoodsModal.vue`**：
  - **编辑不再回传 `version`**（后端 add/edit 契约不接收乐观锁 version）：提交时剥掉；`Goods.data.ts` 隐藏的 `version` 表单字段已删除。
  - 新增/编辑统一 `unitPrice/stockQty: rest.xx ?? 0`（未填默认 0，对齐后端「没写默认就是 0」）。
  - **单位子表查重校验**：同一单位只能出现一次——下拉里其他行已选中的单位在当前行禁用（`unitSelectOptions`），提交时 `validateUnits` 兜底查重。

**⑥ 物料单位统一走数据字典 `inv_unit`（2026-08-15）**

- **背景**：物料单位下拉之前前端写死（`Goods.data.ts` 的 `unitOptions` 静态数组、申请页 `MaterialApply.vue` 本地静态列表），后台字典加单位不生效。
- **改造**：新增 `material.util.ts::loadUnitOptions()`，统一从数据字典 `inv_unit` 加载单位下拉选项（label/value 均取字典 **text**，与后端契约一致——物料 `unitName/unit` 存单位名，执行出入库由 `resolveDictUnitId` 按 text 反查字典 value 上送）。
- **替换点**：
  - `Goods.data.ts` 静态 `unitOptions` **已删除**；`GoodsModal.vue` 单位子表下拉、`MaterialApply.vue` 申请页单位下拉改由 `loadUnitOptions()` 加载（改后台 `inv_unit` 字典 → 重新登录生效）。
  - `PurchaseModal` / `pick` / `IOModal` 的单位下拉来自所选物料自身的 `unitList`（已是字典来源），无需改动；台账/详情里单位展示直接显示单位名（字典 text）。
- **mock 同步**：`mock/sys/user.ts` 的 `createDictItems` 已补 `inv_unit`（mock 模式单位下拉可用）。
- **⚠️ 正式后端需确认 `inv_unit` 字典已配置**（值=文本），否则单位下拉为空（见 物料模块字典配置.md）。

**⑦ 输入框全局禁自动填充 + 弹窗/抽屉下拉首页预加载（2026-08-15）**

- **① 全局禁用浏览器自动填充下拉**（`autocomplete="off"`）：
  - 需求：所有输入框（物料名称/型号/品牌/备注/搜索框等）不再弹「之前填过的值」下拉，直接输入。
  - 实现：新增 `src/utils/autocompleteOff.ts`（MutationObserver 监听动态渲染的弹窗/抽屉/表格输入框），`main.ts` 在 `app.mount` 后调用一次；跳过密码框与已显式设置 `autocomplete` 的元素（如密码 `new-password`）。
- **② 弹窗/抽屉下拉数据在「主页面渲染时」预载第 1 页 10 条**：
  - 规则：下拉走分页+模糊查询的列表（供应商/项目分期等），主页面 `onMounted` 时先请求第 1 页(10 条)并缓存（`src/views/material/material.options.ts` 的 `ensureSupplierOptions` / `ensurePeriodOptions`），弹窗打开直接读缓存展示，不再「打开才请求、每次重复请求」；输入模糊查询/翻页由组件服务端调用。
  - 采购入库：`purchase/index.vue` onMounted 预载供应商+项目分期；`PurchaseModal` 打开读缓存——供应商下拉打开即展示预载首页、输入后走 `/project/supplier/list` **服务端模糊查询**（原为一次拉 100 条前端过滤，已改为服务端分页+模糊）；分期下拉同样预载。
  - 说明：选物料抽屉 `MaterialSelectDrawer` 本就在 setup 顶层 `loadData()`（页面渲染即请求），符合该规则无需改；字典类下拉（单位/状态等）走 `loadDictMap` 模块级缓存，首次后不再重复请求。

**⑧ 领料/还料页修复（2026-08-15）**

- **领料 `pick` / 还料 `return` 页「分期项目」下拉没数据（修复）**：
  - 原因：`onProjectSearch` 空关键词清空选项，打开下拉看不到任何分期，需手输才出结果。
  - 修复：两页 `onMounted` 时预载项目分期第 1 页(10 条)（`material.options.ts::ensurePeriodRecords`），打开下拉即有数据；输入模糊查询仍走 `/project/period/searchByName`；空关键词回到预载首页。映射统一 `mapPeriodNoOptions`（value=分期编号 periodNo）。
- **还料页「使用人」改为「还料人」= 当前用户**：
  - `return/Return.data.ts`：字段 `applyUserName` 标签「使用人」→「还料人」（禁用、自动填充）；删除原需手填的 `returnUser` 字段。
  - `return/index.vue`：`initUserInfo` 自动注入当前用户为还料人；提交载荷补 `returnUser: cur.applyUserName`（还料人=当前用户）；编辑回填去掉已删除的 `returnUser`。

**⑨ 领料/还料页交互 + 申请明细展示（2026-08-15）**

- **① 面包屑 + 返回（领料/还料页）**：全局面包屑 `showBreadCrumb=false`（布局不显示），两页顶部新增页面级 `a-breadcrumb`「出入库管理 → 领料申请/还料申请」，点「出入库管理」返回 `/material/record`。
- **② 领料/还料「项目名称」= 主项目名称**：`mapPeriodNoOptions` 里 `projectName` 原来取的是分期名 `r.periodName`，改为取主项目名 **`r.projectName`**（选择分期后自动带出）。
- **③ 申请明细-物料明细 tab 新增「审批人」列**：审批提交本已传 `approvalUserId`（ApproveModal/BatchApproveModal 均带）；明细 tab 原只有「审批状态」无「审批人」，现新增「审批人」列，**直接用接口回传的 `approvalUserName` 字段展示**（审批记录 tab 同）。
- **④ 申请明细「执行人」= 执行出入库的人**：明细 tab「执行人」列展示 `executeUserName`（后端回传，缺省按 `executeUserId` 从用户列表解析），即执行出入库的经办人——已实现，本次确认不改。

**⑩ 采购入库状态机 + 审批改造 + mock 清理（2026-08-16）**

- **采购入库状态机（新接口 `POST /project/purchaseOrder/status`，传 `{id, status}`）**：
  - 状态流转：`1待采购 →(确认采购)→ 2采购中 →(采购完成)→ 3已到货 →(开始入库)→ 4入库中 →(入库+入库完成)→ 5已完成`；`1/2/3` 可 `(关闭)→ 0`。
  - `purchase/index.vue`：状态切换全部改走 `changeStatus`（不再用 editOrder 改状态）；新增「开始入库」(3→4)、「入库完成」(4→5)；采购完成不再登记到货。
  - `StockInModal`：入库中(4) 打开，逐物料填实际入库数量，调 `POST /project/purchaseOrder/inbound`（`{orderId, items:[{itemId, inboundQty}]}`），入库后状态保持 4（全部完成再点「入库完成」置 5，后端校验）。
  - `CloseModal`：关闭改走 `changeStatus({id, status:'0', closeReason})`。
  - ⚠️ **后端字典 `purchase_order_status` 需更新**：`4=入库中`、新增 `5=已完成`（原 4=已入库）。
- **出入库审批（`ApproveModal`，整单审批）**：
  - 去掉整单「审批备注」输入框；去掉「暂不处理」；**每行物料默认「通过」**，驳回手动选「驳回」并填**行级审批备注**（驳回行必填）。
  - 提交=**只调 `/stock/apply/approve` 一个接口**，整单所有物料审批结果一次经 `items` 传（`{applyId, approvalUserId?, items:[{itemId, approve, remark}]}`，`approve:true` 通过 / `false` 驳回）；**任一条驳回 → 整单状态变更为「已驳回」**。
  - 动作逻辑：已撤回(WITHDRAWN)/已驳回(REJECTED) → 申请人可「**修改**」后重新提交；已驳回不可删除；删除仅限 待审批/已撤回。
- **mock 清理**：`mock/sys/user.ts` 删除全部 mock 字典项（`createDictItems` 返回空），状态/字典统一取正式后端 `sys_dict`（如 `purchase_order_status`）；顺手清理 mock handler 未用参数与孤儿 `fakeCodeList`。

**⑪ 物料管理代码审查清理 + 审批/状态细节（2026-08-16）**

- **代码/mock 清理**：
  - 删除 `mock/demo/material.ts`（旧 `/material/*` mock，前端已全改 `/stock/material/*`，无引用）。
  - 删除未用导出：`Goods.api.ts::generateCode`、`Purchase.api.ts::purchaseArrival`（均无调用）。
  - `mock/demo/system.ts` 通用演示菜单里指向已删页面的 `/material/index` 改为 `/dashboard/welcome/index`。
- **采购状态接口入参 `id → orderId`**：`changeStatus({ orderId, status })`（采购订单ID）。
- **出入库申请动作细化**：
  - 审批状态标签走字典 **`stock_item_status`**（明细级）；**只有「待审批(PENDING)」显示「审批」按钮**（PARTIAL_APPROVED 不再审批）。
  - 申请人：**待审批/已撤回/已驳回** 均可「修改」申请单重新提交；删除仅限 待审批/已撤回（已驳回不可删）。
- **新增文档**：`docs/物料模块逻辑总结.md`（物料管理模块全量逻辑总结）。

**⑫ 审批改整单（移除逐条审批概念）（2026-08-16）**

- **背景**：审批不再有逐条（明细级）审批，全部为整单审批。
- **`ApproveModal.vue` 重写为整单审批**：申请明细改**只读展示**（物料/品牌/型号/数量/单位），删除每行 通过/驳回 单选与行级备注；审批结果单选 **整单通过 / 整单驳回** + 审批备注（**驳回必填**）。提交：整单通过 `approveApply({applyId, approvalUserId, approvalResult:'AGREE', approvalComment})`、整单驳回 `rejectApply({...REJECT, approvalComment})`，**不带明细 `items`**。
- **删除无用代码**：`material.util.ts::calcItemApproval`（明细审批状态推算，整单后无引用）；`ApplyDetailDrawer` 移除「明细审批状态」列与 `stock_item_status` 字典（明细 tab 仅保留执行维度 已执行数量/执行状态/执行人/执行时间）。
- **接口契约注释更新**：`StockApply.api.ts` 的 `approveApply`/`rejectApply` 入参注释改为整单契约（`approvalResult`+`approvalComment`，无 items）。
