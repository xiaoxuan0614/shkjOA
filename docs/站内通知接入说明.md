# 站内通知

2026-09-17 暂停：`notify/index.vue` 的 notificationsEnabled=false，隐藏铃铛且不挂载通知抽屉，停止通知计数/列表/详情/已读请求和通知 WebSocket、焦点自动刷新。接口代码保留供后续恢复。默认密码安全检查及首页待办不受影响。本轮 Apifox 工具不可用，未修改接口契约。

2026-09-16：PC 顶部铃铛打开站内通知抽屉，提供未读/全部、标题搜索、分页、详情与全部已读。与首页待办独立，无新增菜单和权限码。

## 接口与状态

- 列表与未读数：GET /sys/sysAnnouncementSend/getMyAnnouncementSend。列表传 pageNo/pageSize、可选 readFlag=0、titile；角标单独传 readFlag=0/pageSize=1，使用 total，避免旧计数接口的月份及分类限制。
- 详情：GET /sys/sysAnnouncementSend/getOne，sendId 使用本人列表 records.id。result=null 显示不可查看，不标记已读。
- 单条已读：详情加载后 PUT /sys/sysAnnouncementSend/editByAnntIdAndUserId，JSON body 为 { anntId }；使用通知主表 ID，不混用 sendId。失败保留正文并可重试；成功回查列表/角标，不直接假设减一。
- 全部已读：确认后 PUT /sys/sysAnnouncementSend/readAll，无请求体，作用于账号全部通知而非当前筛选。
- 通知 API 单独按 success 解包，兼容 code=0/200；不修改全局响应规则。
- WebSocket topic/user、打开抽屉、页面重新获得焦点触发刷新；卸载清除事件监听和计时器。并发列表与详情请求只接受最新响应。
- 正文使用 sanitizeHtml，摘要作为普通文字。未知业务及任意组件路径不执行。

## 业务入口与边界

已支持带 periodId 的项目类驳回通知及合法 project/detail 路由进入对应项目，点击前检查路由存在。页面及后端仍负责业务访问权限。当前为“查看相关业务”，不表示直接定位每类驳回单据：入出库等缺少已核对详情入口的通知暂只显示正文；附件字段格式未明确，暂不增加附件解析。

Apifox 当前明确 getOne 尚未附加当前用户归属条件，需后端补齐，前端使用本人列表 sendId 不能替代后端鉴权。

## 验证

已通过生产构建、定向 ESLint 与 diff 检查；已在 localhost:3101 管理员登录环境验证铃铛打开抽屉、未读/全部切换及空状态，当前未读数为0，两类列表均为空。无可用通知样本，单条详情/已读、全部已读及新通知 WebSocket 推送仍待真实数据联调，未创建通知或执行业务审批来制造样本。
