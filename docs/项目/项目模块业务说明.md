# 项目模块业务说明

> 更新：2026-08-16（接口对齐首汇OA apifox 项目模块）
> 接口文档：https://s.apifox.cn/ffb89e1a-f7ea-4dd5-99ae-e8365679dc0c

## 一、模块定位

项目模块覆盖：**项目管理**（主项目+分期）、**计划方案**、**实施管理**（工序/实施记录/参与人员/施工打卡/工期延期）、**项目成员/实施位置/项目文件/项目动态/用料计划**、**验收管理**（内部验收 + 客户验收并行）、**合同管理/回款**。

后端以「**项目(project) — 分期(period)**」两级结构组织：主项目下挂多个分期，**所有子表（计划/成员/位置/实施/验收/文件/用料/动态）都挂在分期(periodId)上**。

## 二、角色 × 页面

| 角色 | 页面 | 职责 |
| --- | --- | --- |
| 项目负责人/管理员 | 项目管理 | 新建主项目及分期、编辑、删除分期、状态流转推进 |
| 实施人员 | 项目详情-实施记录/实施位置/成员/文件 | 查看/维护实施维度数据 |
| 项目经理 | 项目详情-验收记录 | 录入内部验收、客户验收（上传验收报告） |
| 财务 | 回款管理(合同) | 合同信息、已回款金额 |

## 三、项目生命周期（状态流转）

```
未开始 ──开始实施──> 实施中 ──调试完成──> 调试完成 ──完成实施──> 实施完成
实施完成 ──进入验收阶段──> 内部验收 ◄──顺序随意──► 客户验收
   内部验收完成 + 客户验收完成 ──进入质保──> 质保中 ──项目完结──> 完结
任意状态 ──项目关闭──> 关闭(例外终态)
```

- 流转由后端 **独立接口** 驱动（`/project/period/{start|debugComplete|implementComplete|internalAccept|internalAcceptComplete|accept|acceptComplete|warranty|complete|close}`），不直接改 status 字段。
- **验收阶段 = 内部验收 + 客户验收并行**：谁先谁后无所谓；两者都需上传验收报告；**客户验收完成后即可进入质保期**（前端在客户验收记录存在且有结果时，额外显示「进入质保」按钮）。
- ⚠️ 状态码（`NOT_STARTED/IMPLEMENTING/...`）为前端推断值，需与后端确认 `period.status` 字典。

## 四、页面清单

| 页面 | 路由 | 说明 |
| --- | --- | --- |
| 项目管理列表 | `/project/list` | 主项目+分期合并列表，行操作=状态流转+编辑 |
| 新增/编辑项目 | `/project/apply` | 新建主项目+首期 / 已有主项目下新增期 / 编辑 |
| 项目详情(8-tab) | `/project/detail/:periodId` | 基本信息/计划方案/成员/位置/实施记录/验收记录/文件/用料 |
| 编辑计划方案 | `/project/plan` | 对齐后端 project_plan 实体 |
| 计划方案管理 | `/plan/list` | 独立菜单 |
| 实施管理 | `/implement/list` | 工序列表 + 实施日志 |
| 回款管理 | `/payment/list` | 合同管理（回款计划后端暂无） |

## 五、验收阶段（核心概念）

内部验收与客户验收**同时进行、不分先后**，均为验收阶段的一部分，分别记录、分别上传验收报告：

| 维度 | 内部验收 `project_internal_acceptance` | 客户验收 `project_acceptance` |
| --- | --- | --- |
| 负责人 | `acceptLeaderId/acceptLeaderName` | `acceptLeaderId/acceptLeaderName`、`acceptUnitLeader/acceptUnitPhone` |
| 日期 | `acceptDate`、`implementCompleteDate` | `acceptDate`、`implementCompleteDate` |
| 验收报告 | `reportFileId`（必传） | `completionReportFileId`(竣工报告，必传)、`acceptanceFormFileId`(验收单) |
| 结果/备注 | `result`、`remark` | `result`、`remark` |

详见 [验收阶段设计.md](验收阶段设计.md)。

## 六、已对接/待确认

- ✅ 接口全部对齐 apifox（127 个），mock 已删除（`mock/demo/project/projectDetail/plan/implement/payment.ts`）。
- ✅ 内部验收+客户验收并行、客户验收完成→进入质保。
- ⚠️ 待确认：`period.status` 字典值、验收报告 fileId 字段语义（存 id 还是 url）、主项目删除接口、回款计划接口。
