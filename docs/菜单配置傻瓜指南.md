# 首汇OA 菜单配置傻瓜指南（后台手动操作版）

## 2026-09-23 计划审批按钮编码更新

计划审批现在使用 `project:period:approve`，旧 `project:plan:audit` 不再控制新分期审批。给审批角色勾选现有“项目审批”编码即可；本轮未替任何角色改授权，也未删除旧菜单节点。当前本地登录权限已查到新码，admin可代办。提交仍限已接受经理/admin，撤回限原提交人/admin，无新路由；未开始/筹备中可编辑计划，待审批只读，通过后进入筹备中，不直接实施。

2026-09-23 本地合同查看授权完成：localhost:3101所连后端在项目管理下补建PC有效按钮“合同信息查看”（project:contract:view，ID2102567114563911682）。经用户指定，仅为市场人员market_staff增加该项（27→28），新按钮同步admin（150→151），原权限全部保留，未增加合同审批权限、未改单个用户或APP授权。市场人员已有合同隐藏页面及项目列表菜单；角色保存后回读确认均已授权，admin刷新后实际收到编码。市场人员重新登录后的驳回合同入口仍待账号验证；前端原规则驳回可显示、审批通过隐藏未改。模块业务/API规则无变动，未部署其他环境。

2026-09-22 通用审批菜单授权完成（覆盖此前待确认状态）：用户明确确认后，仅给 admin 增加审批中心、审批流程配置、通用审批申请三个 PC 菜单入口。保存后重新打开授权页确认三项均勾选，PC 147→150、App 仍16；刷新后导航生效，配置页面读取到13条既有模型，通用申请页面正常显示空列表。其他角色未改，未创建/发布模型或提交审批，真实审批闭环仍待验收。

2026-09-22 通用审批菜单已经建立：审批中心 → 审批流程配置、通用审批申请。页面代码及组件路径见[通用审批配置与接口](审批/通用审批配置与接口.md)。admin 新入口授权尚未保存，需用户明确确认后在角色管理仅勾选这三个 PC 菜单、保存、刷新登录再验收；不改变其他角色及移动端授权。菜单创建成功不表示账号已可见或真实审批已验收。

2026-09-22 市场报价列已接入：市场人员凭原新增/编辑报价权限填写成本价、报价，不需要定价权限。市场主管审批沿用“市场审批”plan:quotation:lock，且后台必须返回该报价canApprove；技术单沿用技术审批。没有新增菜单，未修改真实角色授权。已通过且已定价才显示报价授权，后端自动定价尚需对齐。

2026-09-21 项目详情报价入口：报价信息仅向真实所属市场部（用户确认即销售部）、技术部用户挂载；不按负责部门、销售角色或admin身份额外放行，URL tab=quotation及已打开页签不能绕过。优先复用登录部门名称，缺失才按所属部门ID查询queryTreeList，失败不加载报价。BACK动态路由已有项目详情/报价列表但缺少/plan/material-draft/editor时补共享隐藏路由，已有路由不重复注册；保留报价按钮权限、状态与后端鉴权，不修改真实菜单或角色授权。项目报价入口补齐名称、状态、version、adopted、priced、createBy及来源参数，返回原项目报价页签。专项路由/部门/SFC回归、定向ESLint及diff检查通过；permission.ts仍有原有未使用变量报错，旧报价锁定测试缺useSessionDraft桩未通过。浏览器账号/页面在检查期间已切换，未进行跨账号点击验收，未部署。

2026-09-20 报价草稿保存后留在只读详情：列表移除修改操作，详情按原edit权限及业务状态显示修改按钮，点击才解锁；已有edit URL也默认只读。保存明细成功后锁定并回查主单最新version及明细，URL更新为view，不返回列表。只读且待提交/驳回、具submit权限时可提交已保存内容；编辑未保存时隐藏并拦截提交。提交后按待审批状态锁定，原审批/市场定价权限保留。写入成功但回查失败时关闭写入口，刷新恢复最新版本，避免重复写入；不自动套新版本重试失败写入。沿用editBatch/submit及主单回查接口，无新菜单码/角色变更。状态模拟（新增/详情/旧edit链接、解锁保存、回查失败恢复、版本同步、提交锁定）、既有报价回归和ESLint通过，未真实账号浏览器保存验收。

2026-09-20 新规则覆盖此前“运维部普通成员可内验”：验收弹窗内部办理以登录角色编码operations_manager或admin及project:internalAccept判断；外验仍须本分期经理及project:accept。身份缓存只用于前端显示，提交由后端鉴权。无新菜单码、未在线更改用户或角色授权；角色修改后刷新登录身份或重新登录。

2026-09-20 验收权限说明：project:internalAccept须同时满足当前用户真实属于有效运维部（普通成员即可，不要求主管）；project:accept须同时满足当前分期已接受邀请的项目经理身份。PC/移动端弹窗均先查身份再开放对应编辑，不能仅凭角色名、负责部门或admin身份办理；未确定身份和已完成记录只读。未新增菜单码，未改实际用户/角色授权。

2026-09-18 报价角色最小授权：提交岗位按需勾选plan:quotation:submit和withdraw（撤回还会核验必须是原提交人）；技术部主管只勾plan:quotation:technicalApprove，进入只读技术审批后只能通过/驳回，不勾price或grant；市场部主管勾plan:quotation:price和plan:quotation:grant，且后端须确认canManage，前者在技术审批通过后维护比例、指导价，后者办理报价授权。不要再用旧lock作为当前审批入口。本次没有在线改两个主管角色；当前后端approve及待办仍只认管理员/市场部负责人，技术部主管权限需后端先同步。

2026-09-18 报价新流程：新增先add主单，新增/修改明细统一editBatch(candidateId/version/完整records)，已有名称只读（该接口不支持改名）。提交前隐藏指导比例/指导价。技术审批用technicalApprove菜单码和approve接口，市场定价要求canManage+price菜单+审批通过，成本只读。指导价=成本×(1+百分数比例/100)，四舍五入2位；修改比例重算，可手改指导价，已有指导价不覆盖。默认比例兼容明细guideMarkupRate，缺失不补0。grant仅canExport可选，canViewPrice/canEditPrice固定false，提示清除目标旧价格授权。submit/withdraw及版本校验保留。后端待对齐：Apifox approve仍限定市场负责人，需支持技术负责人及待办；明细schema未含guideMarkupRate，需补默认比例及百分比口径；成本写入仍需后端放开新增编辑者权限。未改真实角色或后端，未完成真实流程验收。

2026-09-18 报价列表移除 permissions 分期请求：加载、筛选、翻页直接调用 materialCandidate/list，不逐分期补权限；列表操作入口按菜单按钮、状态、采用/定价及version判断，不代表已获分期授权。提交/撤回/作废直接走专用接口并由后端校验，保留原版本和失败提示；详情/编辑器权限校验及导出exportCheck/exportData保留。未修改角色授权或菜单码，真实账号操作未验收。

2026-09-18 成本权限拆分（覆盖旧成本需价格授权的说明）：PC 新增/编辑报价时，成本 basePrice 随报价新增/编辑资格开放，不再要求 canEditPrice/canViewPrice；仍保留本人/管理资格和待审批、作废、已采用锁定规则。成本列与提价比例、终价分别判断，后两者权限不扩大。选料带入 costPrice、成本必填和提交 basePrice 保留。Apifox 当前 revise/addBatch 契约仍要求价格授权，后端需同步成本读写权限，否则可能隐藏成本或拒绝保存；本次不代表真实账号保存已通过。没有新增菜单码或修改角色授权。

2026-09-18 报价按钮菜单权限（覆盖此前不新增静态按钮码的说明）：报价管理下配置10项PC按钮，复用 add/edit/delete/lock，新增 submit/withdraw/technicalApprove/price/grant/export（均为 plan:quotation: 前缀）。delete改名作废报价，lock改名市场审批；technicalApprove仅预留，旧unlock保留但不作为当前流程入口。列表、编辑器、授权弹窗及合同内报价调整同时校验菜单权限、分期资格和业务状态。当前 localhost:3101 所连后端已回读确认全部按钮，admin角色全部授予并刷新确认实际权限；其他角色未改，其他环境未配置。权限回归、定向ESLint通过；未进行跨账号真实报价操作，后端接口菜单码强制鉴权未由本次前端改动实现。编码表与验证范围见《报价治理接口对接》；页面路由不变。

2026-09-17 报价治理更新（覆盖下文旧报价权限、价格联算和审批接口说明）：提交/审批/撤回/作废/定价接入专用接口；整单 version 校验；价格及导出按分期授权；审批通过且已定价才可受控导出；合同调整和报价审批待办同步。平铺列表与三个筛选保留。菜单及隐藏路由不变，未修改真实角色或用户授权。详见[报价治理接口对接](项目/报价治理接口对接.md)。本地页面和静态回归已验证，真实跨账号完整流程未验收。


2026-09-17 本地价格维护授权完成：经用户明确确认，本地 localhost:3101 所代理的局域网开发后端属于本次授权范围。已在该环境“物料基本维护”下创建 PC 按钮“价格维护”（mtl:goods:price、可见/可访问、有效），保存后重开核对标识；管理员 admin 角色勾选后保存，重开确认仍勾选，其他角色未改。刷新本地列表后按钮显示，点击可打开仅含成本价、基准单价、报价比例的独立弹窗，并加载物料名称及基准单位。未修改真实价格，价格保存及后端字段鉴权不在本次验证结论内。本次未访问或修改测试环境。此记录为本地配置的最新状态。

2026-09-17 价格维护权限后台配置完成：测试系统菜单管理已在“物料基本维护”下创建 PC 按钮“价格维护”，授权标识 mtl:goods:price，策略可见/可访问、状态有效；保存后重新打开节点核对标识。管理员（admin）角色 PC 授权已保存，重新打开授权页该项仍勾选，其他角色未调整。刷新测试物料列表后仍未出现价格维护按钮，页面入口尚未验证通过，需要核对已部署前端版本及重新登录后的权限加载；未操作价格数据。此记录覆盖此前浏览器超时、节点未配置的状态，不代表后端价格字段鉴权已完成。

2026-09-17 待配置：在“物料基本维护”（物料维护列表所在菜单）下新增按钮节点“价格维护”，类型=按钮，权限标识=`mtl:goods:price`，终端=PC，状态=启用。复用现有物料列表路由，无新页面路由。角色管理 → 授权 → PC，按需勾选此按钮；管理员角色应勾选，其他角色仅按指定范围授权。该权限独立于 `mtl:goods:edit`，仅有基本编辑权不得维护价格，只有价格权限仍需物料列表访问权。保存后重新登录验证。代码及Mock已接入，正式节点/角色尚未配置（浏览器连接超时）；仅隐藏按钮不构成后端字段权限，服务端仍需增加价格写入校验。

2026-09-17 验收/返工本地改造：两端验收文件改为acceptanceFormFileId，客户负责人输入与明确日期时间；提交验收核验本分期经理，取消admin办理豁免。返工支持process数组、多工序详情/日志、基础外协分页选择及人数工时校验、可选补料，完整方案直接submit，停用草稿调用。预计复验时间尚无字段，暂不开放；客户姓名持久化、跨施工轮只重验失败项、真实运维负责人能力回显仍待后端。本说明覆盖旧单工序、草稿提交及管理员兜底说明。菜单编码/路由不变，未在线修改授权。详见[验收返工改造后端对接清单](项目/验收返工改造后端对接清单.md)。

2026-09-16 待配置：在 `/project/list` 下新增 PC 按钮“合同信息”，权限编码 `project:contract:view`，有效、可见/可访问；只先给 admin 授权，其他角色须明确确认后再授予。它与隐藏页 `/project/contract` 是不同配置。合同审批角色进入独立合同页还需要此查看权限和隐藏页权限，审批权限本身不替代查看权限。保存后重新登录验证；本轮后台连接超时，未实际创建或授权。

延期审批待办：两端点击延期待办即可查看详情，不需要新增页面菜单。需要审批的角色应授权 `project:delay:approve` 按钮，并覆盖所用终端（两端共用可选 `ALL`）；审批前同步权限。没有该权限时只能查看。本次没有代为创建或修改正式授权。

> 适用：登录正式后端 → **系统管理 → 菜单管理** 页面，逐条手动录入。每配完一项点「保存」；全部配完后**重新登录**（或清权限缓存）生效。若菜单打开异常，先看文末「常见问题」。

---

## 〇、操作通用说明（先读这个）

**新增菜单的标准动作**：

1. 在左侧菜单树**点中父菜单**（如「物料管理」），再点右上角 **「新增菜单」**。
2. 弹出表单，按下面的「填表清单」填。
3. 保存。

**填表字段规则**（重点）：

| 表单字段       | 一级菜单              | 子菜单                               | 隐藏路由               | 按钮权限          |
| -------------- | --------------------- | ------------------------------------ | ---------------------- | ----------------- |
| 菜单类型       | 一级菜单              | 子菜单                               | 子菜单                 | 按钮权限          |
| 适用终端       | PC / 移动端 / 两端共用 | 不得超出父菜单终端范围               | 按实际入口选择         | 按按钮使用端选择  |
| 菜单名称       | 模块名                | 页面名                               | 页面名                 | 按钮名            |
| 路径(路由地址) | `/material`（带斜杠） | `/material/list`（**完整绝对路径**） | `/project/detail/:id`  | 空                |
| 组件路径       | `LAYOUT`              | `project/index`                      | `project/detail/index` | 空                |
| 移动端路径     | 按需填 `/pages/...`   | 按需填 `/pages/...`                  | 按需填 `/pages/...`    | 空                |
| 是否路由菜单   | **是**                | **是**                               | **是**                 | —                 |
| 是否隐藏       | 否                    | 否                                   | **是**                 | —                 |
| 权限标识       | 空                    | 空                                   | 空                     | `project:plan` 等 |
| 图标           | 有就填                | 有就填                               | 空                     | 空                |

**几个铁律**：

- ⚠️ 子菜单路径**填完整绝对路径**（`/material/list`），不要只填相对 `list`，否则路由丢失父前缀。
- ⚠️ **「是否路由菜单」开关必须打开**（`route=1`），否则页面打开 404（前端忽略路由构建）。
- ⚠️ 子菜单组件**必须是真实页面路径**（如 `project/index`），**不能是 `LAYOUT`**，否则打开是空白页（显示"查看组件引用是否正确"）。
- 隐藏路由 = 靠列表按钮跳转的页面（如新增项目、项目详情），不显示在侧边栏，但**必须配**，否则按钮跳转 404；领料申请和还料申请属于通用入口，不设为隐藏。
- `PC端` 只维护 PC 路由和组件，`移动端` 只维护 `/pages/...` 移动端路径，`两端共用` 同时维护两套路径。移动端子菜单不能挂在 PC 专用父菜单下。
- `移动端路径` 可填在一级菜单或需要独立 App 入口的子菜单上，必须与 App `pages.json` 完全一致，不带域名和参数；按钮权限不填。App 会递归读取已授权节点并按路径去重。
- 菜单保存后还要进入 **角色管理 → 授权**：PC 权限在“PC端权限”页签勾选，App/小程序权限在“移动端权限”页签勾选，两边互不覆盖。已定义数据规则的菜单节点右侧会显示“配置数据规则”按钮，点击按钮为当前角色、当前端选择规则；点击菜单名称不会再误打开规则弹窗。

---

## 一、物料管理 `/material`

### 1.1 一级菜单

| 字段     | 值          |
| -------- | ----------- |
| 菜单类型 | 一级菜单    |
| 菜单名称 | 物料管理    |
| 路径     | `/material` |
| 组件路径 | `LAYOUT`    |
| 图标     | `bx:bx-box` |
| 排序     | 2           |

### 1.2 子菜单：物料基本维护（物料库存列表）

选中「物料管理」父菜单 → 新增：

| 字段     | 值                     |
| -------- | ---------------------- |
| 菜单类型 | 子菜单                 |
| 菜单名称 | 物料基本维护           |
| 路径     | `/material/list`       |
| 组件路径 | `material/goods/index` |
| 图标     | `bx:bx-list`           |
| 排序     | 1                      |

**1.2.1 它的按钮权限**（再选中这个子菜单，逐个「新增」按钮类型）：

| 按钮名称 | 权限标识                |
| -------- | ----------------------- |
| 新增     | `mtl:goods:add`         |
| 批量删除 | `mtl:goods:deleteBatch` |

### 1.3 子菜单：库存管理

选中「物料管理」父菜单 → 新增：

| 字段     | 值                     |
| -------- | ---------------------- |
| 菜单类型 | 子菜单                 |
| 菜单名称 | 库存管理               |
| 路径     | `/material/stock`      |
| 组件路径 | `material/stock/index` |
| 图标     | `bx:bx-store`          |
| 排序     | 2                      |

### 1.4 子菜单：盘存记录

选中「物料管理」父菜单 → 新增：

| 字段     | 值                         |
| -------- | -------------------------- |
| 菜单类型 | 子菜单                     |
| 菜单名称 | 盘存记录                   |
| 路径     | `/material/stocktake`      |
| 组件路径 | `material/stocktake/index` |
| 图标     | `bx:bx-book-content`       |
| 排序     | 3                          |

### 1.5 子菜单：出入库管理

选中「物料管理」父菜单 → 新增：

| 字段     | 值                      |
| -------- | ----------------------- |
| 菜单类型 | 子菜单                  |
| 菜单名称 | 出入库管理              |
| 路径     | `/material/record`      |
| 组件路径 | `material/record/index` |
| 图标     | `bx:bx-book-open`       |
| 排序     | 4                       |

> 出入库管理页含「出入库申请 + 台账」，台账行操作有「详情」弹窗（展示该条出入库记录涉及的全部物料明细）。

### 1.6 子菜单：采购入库

选中「物料管理」父菜单 → 新增：

| 字段     | 值                        |
| -------- | ------------------------- |
| 菜单类型 | 子菜单                    |
| 菜单名称 | 采购入库                  |
| 路径     | `/material/purchase`      |
| 组件路径 | `material/purchase/index` |
| 图标     | `bx:bx-cart`              |
| 排序     | 7                         |

### 1.7 子菜单：物料申请（隐藏）

| 字段     | 值                             |
| -------- | ------------------------------ |
| 菜单类型 | 子菜单                         |
| 菜单名称 | 物料申请                       |
| 路径     | `/material/apply`              |
| 组件路径 | `material/apply/MaterialApply` |
| 是否隐藏 | **是**                         |
| 排序     | 3                              |

> 领料申请 `pick` / 还料申请 `return` 是“物料管理”下的普通可见子菜单（组件路径 `material/pick/index`、`material/return/index`），排序分别为 5、6，`是否隐藏` 均选“否”；移动端路径分别为 `/pages/ops/materialApply`、`/pages/ops/materialReturn`。角色授权时，PC 和移动端页签都要分别勾选“物料管理”父节点及所需申请节点。

---

## 二、项目管理 `/project`

> 正式后台中“项目详情”必须是本一级菜单下的隐藏子菜单，不能单独建成一级菜单。项目子菜单顺序统一为：项目列表 1、项目详情 2、新增项目 3、合同信息 4、编辑计划方案 5。

### 2.1 一级菜单

| 字段     | 值             |
| -------- | -------------- |
| 菜单类型 | 一级菜单       |
| 菜单名称 | 项目管理       |
| 路径     | `/project`     |
| 组件路径 | `LAYOUT`       |
| 图标     | `bx:bx-folder` |
| 排序     | 1              |

### 2.2 子菜单：项目管理（项目列表）

选中「项目管理」父菜单 → 新增：

| 字段     | 值              |
| -------- | --------------- |
| 菜单类型 | 子菜单          |
| 菜单名称 | 项目管理        |
| 路径     | `/project/list` |
| 组件路径 | `project/index` |
| 图标     | `bx:bx-folder`  |
| 排序     | 1               |

> 🔧 **如果你已配过这个菜单且打开是空白页**：找到该子菜单 → 点「编辑」→ 把 **组件路径改成 `project/index`**（当前配成了 `layouts/default/index` 导致空白），路径改成 `/project/list` → 保存。

**2.2.1 它的按钮权限（项目流转按钮，缺了会不显示）**：

新增权限：正式后台已于 2026-09-11 在 `/project/list` 下创建“创建项目（按钮）”，权限标识 `project:add`，PC 端、可见/可访问、有效，已回读确认。角色 PC 授权勾选此按钮及“新增项目”隐藏页，重新登录生效；本次只创建节点，没有改动角色授权。新建主项目与新增分期共用该按钮权限。

关闭权限：正式后台已于 2026-09-14 在 `/project/list` 下创建“关闭项目”按钮并通过编辑页回读确认，权限标识 `project:close`，适用终端 PC，可见/可访问、有效；已同步授予 `admin` 角色，重新登录后生效。其他角色后续只给允许直接结束项目分期的角色勾选。

管理员固定规则：以后正式后台新增菜单或按钮权限时，同时给角色编码 `admin` 勾选并保存；其他角色先不改，等明确后再配置。

| 按钮名称 | 权限标识                        |
| -------- | ------------------------------- |
| 合同签订 | `project:contract`              |
| 创建项目（按钮） | `project:add` |
| 关闭项目 | `project:close` |
| 合同审批 | `project:contract:approve`        |
| 分期计划审批 | `project:period:approve`     |
| 补料审批 | `project:materialApply:approve` |
| 确认到货 | `project:arrival:confirm`       |
| 实施完成 | `project:implement`             |
| 内部验收 | `project:internalAccept`        |
| 外部验收 | `project:accept`                |
| 申请返工 | `project:rework:apply`          |
| 返工审批 | `project:rework:approve`        |
| 质保     | `project:warranty`              |

2026-09-16：页面按钮统一叫“验收”，编码仍为 `project:acceptance:view`。运维部门主管用 `project:internalAccept` 提交内部结果；本项目经理用 `project:accept` 提交外部结果，用 `project:acceptance:submit` 申请内外任一失败项重新验收，用 `project:rework:apply` 申请施工返工；返工审批仍用 `project:rework:approve`。项目经理还须是当前分期已接受的经理成员。已有后台按钮只调整显示名为“验收”，不要改编码或重复建节点；本次未在线改名或勾选角色。移动端需在APP授权页单独勾选，不能只配PC。历史阶段继续允许查记录。

> 计划审批仅保留 `project:plan:audit`（计划审批）；旧 `project:plan:approve` 对应已废弃的 `/project/plan/approve`，后台“计划方案审批”旧按钮节点已于 2026-09-09 删除，不再重复配置。

> `project:plan:audit` 授权后，审批人可在计划审批弹窗或跳转后的只读计划详情页执行通过/驳回。

> `project:materialApply:approve` 授权后，审批人可在统一“补料管理”抽屉看到“待我审批 / 审批记录”，并按补料单单张或批量通过、驳回；未授权账号只显示“我的申请”。补料管理复用项目管理页面，不需要新增菜单或隐藏路由。

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。（2026-09-14 最新规则覆盖旧到货款限制；本次修改 PC，App 尚需同步验证。）

> `project:implement` 后台按钮名称统一为“实施完成”。计划审批通过后至完成实施前仅在项目列表显示，点击后按工序办理；项目详情不再显示该按钮，不新增路由或新的权限码。

> 2026-08-31 已在正式后台完成改名并回读确认：上级菜单“项目管理”、授权标识 `project:implement`、状态“有效”均保持不变，未重复新增按钮。

> 说明：项目生命周期 = 未开始（合同提交）→ 合同审批（先选结果，通过时指定项目经理）→ 筹备中（项目经理建计划）→ 待审批（计划审批）→ 实施中 → 全部工序完成 → 内部/外部并行验收 → 质保。内部验收待办给运维部门经理，外部验收待办给本项目经理；任一不通过可走“申请返工 → 返工审批 → 返工中 → 工序完成后自动验收”。验收和返工都复用项目详情验收标签，不新增菜单。

**项目经理角色授权（角色编码 `project_management`）**：

- 勾选主页/首页、项目管理/项目列表、项目详情、编辑计划方案。
- 勾选物料管理父节点及领料申请、还料申请。
- 勾选资源管理父节点及车辆管理、车辆详情。
- 勾选实施管理父节点及实施列表、查看日志、日志详情。
- 项目按钮勾选“实施完成”（`project:implement`）、“外部验收”（`project:accept`）和“申请返工”（`project:rework:apply`）；不要勾选内部验收、合同审批、计划审批、补料审批、返工审批及系统管理权限。

**运维部门经理角色**：勾选主页、项目详情及“内部验收”（`project:internalAccept`）。不要把“内部验收”授予普通项目成员；正式待办和数据范围还必须由后端按运维部门经理身份限制。

> 项目列表不再单独显示“合同审批”。合同通过前点击“合同信息”进入统一页面；页面只有在 `contractStatus=2` 且当前用户拥有 `project:contract:approve` 时显示通过/驳回区域。合同通过后列表入口隐藏，只从项目详情查看。

### 2.3 子菜单：新增项目（隐藏）

| 字段     | 值                           |
| -------- | ---------------------------- |
| 菜单类型 | 子菜单                       |
| 菜单名称 | 新增项目                     |
| 路径     | `/project/apply`             |
| 组件路径 | `project/apply/ProjectApply` |
| 是否隐藏 | **是**                       |
| 排序     | 3                            |

### 2.4 子菜单：合同信息（隐藏，本次新增）

| 字段     | 值                       |
| -------- | ------------------------ |
| 菜单类型 | 子菜单                   |
| 菜单名称 | 合同信息                 |
| 路径     | `/project/contract`      |
| 组件路径 | `project/contract/index` |
| 是否隐藏 | **是**                   |
| 排序     | 4                        |

> 这是隐藏路由，但后台仍必须配置；激活菜单填 `/project/list`。入口参数必须按用途区分：
>
> - 新增：`mode=create&periodId=...&projectId=...`（项目列表“合同签订”使用这一种）；
> - 查看：`mode=view&periodId=...&projectId=...`；
> - 编辑：`mode=edit&periodId=...&projectId=...`。
>
> 查看/编辑必须携带 `periodId`。原提交人可撤回待审批合同，并可修改待提交、驳回或已撤回合同后重新提交；审核通过合同锁定。撤回与重提按提交人身份控制，不再配置 `project:contract:edit`。

### 2.5 子菜单：编辑计划方案（隐藏）

| 字段     | 值                         |
| -------- | -------------------------- |
| 菜单类型 | 子菜单                     |
| 菜单名称 | 编辑计划方案               |
| 路径     | `/project/plan`            |
| 组件路径 | `project/plan/ProjectPlan` |
| 是否隐藏 | **是**                     |
| 排序     | 5                          |

### 2.6 子菜单：项目详情（隐藏）

| 字段     | 值                     |
| -------- | ---------------------- |
| 菜单类型 | 子菜单                 |
| 菜单名称 | 项目详情               |
| 路径     | `/project/detail/:id`  |
| 组件路径 | `project/detail/index` |
| 是否隐藏 | **是**                 |
| 排序     | 2                      |

---

## 三、资源管理 `/resource`

### 3.1 一级菜单

| 字段     | 值                  |
| -------- | ------------------- |
| 菜单类型 | 一级菜单            |
| 菜单名称 | 资源管理            |
| 路径     | `/resource`         |
| 组件路径 | `LAYOUT`            |
| 图标     | `bx:bx-folder-open` |
| 排序     | 4                   |

### 3.2 子菜单（选中「资源管理」父菜单逐个新增）

| 菜单名称     | 路径                           | 组件路径                        | 隐藏   | 图标                   | 排序 |
| ------------ | ------------------------------ | ------------------------------- | ------ | ---------------------- | ---- |
| 车辆管理     | `/resource/vehicle`            | `resource/vehicle/index`        | 否     | `bx:bx-car`            | 1    |
| 车辆详情     | `/resource/vehicle/detail/:id` | `resource/vehicle/detail/index` | **是** | —                      | 2    |
| 往来客户     | `/resource/customer`           | `resource/customer/index`       | 否     | `bx:bx-group`          | 3    |
| 供应商管理   | `/resource/supplier`           | `resource/supplier/index`       | 否     | `bx:bx-store`          | 4    |
| **外协单位** | `/resource/outsourcing`        | `resource/outsourcing/index`    | 否     | `bx:bx-building-house` | 5    |

> ⚠️ 外协单位路径注意拼写：`outsourcing`（别写成 `outsourcig`），否则 404。

---

> 待办只放在首页“我的待办”卡片中，不再创建“站内待办”一级菜单或“我的待办”子菜单。后台已有 `/todo`、`/todo/list` 时直接删除这两个菜单节点；合同、计划、补料和成员邀请仍按各自按钮权限办理。

---

## 四、计划方案管理 `/plan`

### 4.1 一级菜单

| 字段     | 值                          |
| -------- | --------------------------- |
| 菜单类型 | 一级菜单                    |
| 菜单名称 | 计划方案管理                |
| 路径     | `/plan`                     |
| 组件路径 | `LAYOUT`                    |
| 图标     | `ion:document-text-outline` |
| 排序     | 3                           |

### 4.2 子菜单

| 菜单名称           | 路径                          | 组件路径                     | 隐藏   | 排序 |
| ------------------ | ----------------------------- | ---------------------------- | ------ | ---- |
| 计划方案管理       | `/plan/list`                  | `plan/index`                 | 否     | 1    |
| 报价管理           | `/plan/material-draft`        | `plan/material-draft/index`  | 否     | 2    |
| 新增/修改/查看报价 | `/plan/material-draft/editor` | `plan/material-draft/editor` | **是** | 3    |
| 方案详情           | `/plan/detail/:id`            | `plan/detail/index`          | **是** | 4    |

“新增/修改/查看报价”隐藏页必须填写固定路径 `/plan/material-draft/editor`，不要填写旧的动态路径 `/plan/material-draft/:id`。新增时页面跳转为 `/plan/material-draft/editor?mode=create&periodId=项目分期ID&candidateName=报价单名称`，修改时传 `mode=edit&periodId=项目分期ID&candidateId=候选清单ID`，查看详情传 `mode=view` 并强制只读，确保后台动态路由能稳定匹配并精确回显一份报价。

当前菜单管理页面没有“当前激活菜单”输入项，不需要填写。前端识别到组件 `plan/material-draft/editor` 后，会自动让侧栏高亮 `/plan/material-draft` 的“报价管理”。

注意层级：“新增/修改/查看报价”与“报价管理”是同级菜单，都直接放在一级“计划方案管理”下面；报价按钮权限放在“报价管理”下面。不要把隐藏页放到“报价管理”里面，否则父子路径会再次拼接，新增跳转仍会找不到页面。

“报价管理”当前使用add、edit、submit、withdraw、technicalApprove、price、grant、export、delete九个按钮权限；旧lock/unlock保留兼容但不作为当前入口。技术部主管只勾technicalApprove；市场部主管勾price和grant，报价授权由市场部主管办理且后端还须返回canManage；提交岗位按需勾submit/withdraw，撤回仍必须是原提交人。已采用报价只在合同驳回或撤回后由可编辑合同人员解除采用。

---

## 五、实施管理 `/implement`

### 5.1 一级菜单

| 字段     | 值                   |
| -------- | -------------------- |
| 菜单类型 | 一级菜单             |
| 菜单名称 | 实施管理             |
| 路径     | `/implement`         |
| 组件路径 | `LAYOUT`             |
| 图标     | `ion:hammer-outline` |
| 排序     | 5                    |

### 5.2 子菜单

| 菜单名称 | 路径                               | 组件路径                 | 隐藏   | 排序 |
| -------- | ---------------------------------- | ------------------------ | ------ | ---- |
| 实施管理 | `/implement/list`                  | `implement/index`        | 否     | 1    |
| 查看日志 | `/implement/log/:id`               | `implement/log/index`    | **是** | 2    |
| 日志详情 | `/implement/log/:id/detail/:logId` | `implement/detail/index` | **是** | 3    |

这里 `/implement/log/:id` 的 `:id` 填项目分期 `periodId`。页面先展示该项目全部工序，再分别查看每道工序的日志；可选的 `processId` 查询参数只负责自动展开指定工序。日志详情中的 `logId` 才是实施日志 ID。

---

## 六、回款管理 `/payment`

### 6.1 一级菜单

| 字段     | 值                   |
| -------- | -------------------- |
| 菜单类型 | 一级菜单             |
| 菜单名称 | 回款管理             |
| 路径     | `/payment`           |
| 组件路径 | `LAYOUT`             |
| 图标     | `ion:wallet-outline` |
| 排序     | 6                    |

### 6.2 子菜单

| 菜单名称 | 路径                  | 组件路径               | 隐藏   | 排序 |
| -------- | --------------------- | ---------------------- | ------ | ---- |
| 回款管理 | `/payment/list`       | `payment/index`        | 否     | 1    |
| 回款记录 | `/payment/detail/:id` | `payment/detail/index` | **是** | 2    |

---

## 七、文件与报表占位、运维值班表

| 一级菜单 | 路径         | 子菜单                                                 |
| -------- | ------------ | ------------------------------------------------------ |
| 文件管理 | `/file`      | `/file/list` → 组件 `placeholder/index`                |
| 数据报表 | `/report`    | `/report/list` → 组件 `placeholder/index`              |
| 运维管理 | `/operation` | `/operation/list` → 组件 `operation/duty-roster/index` |

“运维管理”下的子菜单名称填写“运维值班表”，设置为显示菜单。当前不需要新增按钮权限；保存后给需要排班的角色勾选“运维管理/运维值班表”，重新登录验证。

---

## 八、常见问题（配完后排查）

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 打开某菜单是**空白页**，提示"查看组件引用是否正确" | 子菜单组件路径配成了 `LAYOUT` 或写错 | 编辑该子菜单，组件路径改成真实页面（如 `project/index`） |
| **页面 404，但菜单能显示在侧边栏** | 该菜单「是否路由菜单」开关没开（`route=0`） | 编辑菜单，把「是否路由菜单」打开 |
| 访问路径不对/404 | 子菜单路径只填了相对值（如 `list`） | 改成完整绝对路径（`/material/list`） |
| 列表页按钮不显示（如「合同签订」「计划审批」） | 按钮权限码没配，或角色没授权 | 按上面清单配按钮权限；再给角色授权 |
| 点「编辑/编辑计划方案/合同信息」跳 404 | 对应隐藏路由没配 | 补齐隐藏路由（apply/contract/plan/detail/:id 等） |
| 外协单位打开 404 | 后端菜单路径拼错 `outsourcig` | 改成 `outsourcing` |
| 配置完菜单不显示 | 权限缓存未刷新 | 重新登录，或清浏览器缓存 |
| **配置完（含角色授权后）菜单仍不出现** | **后端菜单权限缓存未刷新**（`getUserPermissionByToken` 返回旧菜单） | **让后端清 Redis 缓存或重启后端服务**，再重新登录 |

---

> 完整字段对照/组件映射规则见 [docs/菜单配置指南.md](菜单配置指南.md)。

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。（2026-09-14 最新规则覆盖旧到货款限制；本次修改 PC，App 尚需同步验证。）

### 2026-09-14 确认到货与到货款解耦

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。

列表不再逐项查询合同回款计划，改为对符合类型/阶段的当前页分期查询成员资格（去重、最多三项并发）。弹窗仍展示合同附件及可选到货款信息，提交前复查项目详情、项目类型字典和当前成员身份；只发送 `POST /project/period/arrivalStatus` 的 `{ periodId, arrivalStatus: 1 }`。不新增权限节点、不扩大其他角色授权。最新 Apifox 尚未明确类型、阶段、经理身份的服务端门禁；本次前端限制不能替代后端鉴权。本次未修改 App。


### 2026-09-15 报价审批统一入口

2026-09-18最新规则覆盖本节旧lock审批：待处理记录显示“技术审批”，技术部主管凭`plan:quotation:technicalApprove`在只读页办理通过/驳回且看不到比例、指导价；通过后市场部主管凭后端canManage和`plan:quotation:price`办理市场定价。报价授权由市场部主管凭canManage和`plan:quotation:grant`办理。当前后端approve及待办仍只认管理员/市场部负责人，需先同步后端。
> 2026-09-16：顶部头像 → 账户管理已补回固定隐藏路由 `/system/usersetting`，不用另配菜单；仍须登录。顶部“切换部门”已移除。用户管理里 `admin` 行的密码按钮置灰，个人自助修改密码保留。
2026-09-17 合同审批入口修正：待审批合同且具备 project:contract:approve 时，项目列表显示“合同审批”，不再要求额外的 project:contract:view；两种权限都有时仅显示审批按钮。普通合同信息仍要求查看权限。独立合同页仅 view 模式接受审批权限入口，详情回查非待审批且无查看权限则返回项目列表，edit/create 原门禁不变。未扩大角色授权，服务端数据鉴权不变。 本说明覆盖此前“审批也须额外查看权限”的描述；无新增接口或菜单编码。
