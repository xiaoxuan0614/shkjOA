# 项目模块接口对照文档

2026-09-24 合同新增编辑按需加载：取消初始报价/权限和全量用户查询，报价选择展开或修改调整时加载，人员下拉展开加载且成功复用；销售负责人优先详情/当前登录人回显，关联报价ID取materialCandidateId。编辑不再补查项目和附件列表，附件取合同组合详情；从查看切换编辑仅补字典。新增仍按periodId查项目基本信息，合同类型/回款节点字典保留；提交前原报价和业务校验不删。菜单权限不变。定向静态检查，未真实保存联调。

2026-09-24 合同只读请求精简：合同信息view及项目详情合同页签仅请求contract/queryById(periodId)，取消项目、报价/权限、用户、附件列表及字典预加载；复用组合详情，名称缺失不补查。新增编辑保留数据加载，查看切换编辑补加载；审批经理下拉展开才查询用户。菜单/API契约不变；静态检查，未浏览器Network联调。

2026-09-24 计划页面展示调整：共用用料表单位/数量列改为90/110px，输入控件宽度约束适用所有模式，防止跨列重叠；分期计划审批弹窗的审批记录/通过/驳回等动作整体移至底部右侧。计划查看和编辑页在页签上方显示当前计划审批状态、最近一轮结果及审批人/时间/意见、撤回原因，驳回意见红字；复用PeriodApprovalActions已有分期实体和history请求，不新增请求，加载/失败清除旧摘要，不将历史通过当作当前资格。接口、审批权限及提交载荷不变；两份菜单指南检查无需修改。分期审批回归通过，真实浏览器布局及业务提交未验收。

2026-09-23 查看返工/工序进度请求精简：ProcessCompletionDrawer打开与重新加载仅查询工序及字典，不调用member/list和projectPeriodDetai；分期状态、当前返工ID复用项目列表上下文。勾选仅选择候选工序，最终完成前仍刷新成员及分期状态并核验经理/现场负责人，防止取消预加载造成越权；服务端保留最终鉴权。接口和菜单配置不变。定向ESLint及SFC检查，未真实账号网络验收。

2026-09-23 返工工序轮次展示：ReworkDrawer列表摘要及详情、移动端ProjectReworkPanel详情/审批与日志入口名称，使用所属返工单reworkRound拼接“第N轮返工-原工序名称”；同单多工序逐项拼接，不用version、行号或项目当前轮次。缺失/异常轮次保留原名，已带相同前缀不重复拼接；仅展示，不写入提交载荷、不增加请求。Apifox已确认OaProjectRework字段，OaProjectProcess模型尚无轮次，因此独立工序/日志页暂未扩展。菜单权限不变，定向回归/SFC检查，未真实页面联调或打包。

## 2026-09-23 分期整体计划审批

| 接口 | 请求 |
| --- | --- |
| POST /project/period/approval/submit | JSON `{periodId}` |
| POST /project/period/approval/approve | JSON `{periodId, approvalId, approvalStatus: "1"或"0", approvalReason?}`；驳回原因必填 |
| POST /project/period/approval/withdraw | JSON `{periodId, approvalId, approvalReason?}`；返回原因字段为withdrawReason |
| GET /project/period/approval/history | `{periodId}`；result数组，无分页 |

- 新封装 PeriodApproval.api.ts 显式校验success===true且code=200，ID始终字符串；不传操作者、时间或项目主状态。
- 当前合并详情DTO未定义approvalStatus/currentApprovalId，改用 `/project/period/list?id=分期ID&pageNo=1&pageSize=2` 精确查询OaProjectPeriod，校验total=1及主键匹配。Apifox描述允许分期实体字段筛选；本地只读验证匹配返回。
- approvalId优先匹配当前实体轮次；历史仅用于核验及展示。待办actionParams.approvalId贯穿弹窗和详情，旧轮次不替换为新轮次执行。
- status接口不再用于进入/离开PENDING_APPROVAL，也不再用于合同审批后从NOT_STARTED推进PREPARING。通过/驳回/撤回均由专用接口事务流转。
- submittedSnapshot是JSON字符串，period/plans/processes均为数组；安全解析，只读显示，不回传保存。失败显示后端message并刷新，不自动重试写操作。
- 本轮仅PC改造；不处理V117，不代表真实跨角色提交审批已验收。

2026-09-23 PC计划编辑资格：项目列表编辑入口与ProjectPlan页面统一仅允许本分期已接受的项目经理或admin角色；经理通过member/list分期成员关系确认，不以全局经理角色或旧合同经理ID替代。列表复用当前页经理查询，页面在身份/分期变化时重载，查询失败不给编辑资格。保留PREPARING阶段、显式只读及上下文成功限制，保存/提交沿用editable门禁。接口写入载荷、计划审批权限不变；后端仍须执行身份鉴权，未验证真实账号保存。两份菜单指南已检查，无新增菜单或真实角色授权变更。

2026-09-22 PC验收驳回提示精简：内外验各自的最近驳回原因移至对应状态/结果标签旁红字展示，长文本自动换行；移除独立的最近驳回负责人、日期、原因区域，避免相同验收说明重复展示。仍使用已有 lastRejectedAcceptance.remark，已通过隐藏旧原因，不回填当前表单；接口、权限及提交不变。菜单指南已检查，无需调整。

2026-09-22 latestStatus外层资格适配（覆盖旧内层字段说明）：PC/移动端读取result.canSubmitReacceptance，PC读取result.canApplyRework；不再以internal/customer内层资格或历史本地权限兜底放行。类型按status筛选，FAILED免整改复验、PENDING_REACCEPTANCE普通复验，已通过项不重提；内外验lastRejectedAcceptance单独显示最近驳回负责人、日期、原因，不填入当前表单，PASSED隐藏。移动端仍不新增返工申请或文件详情。外层资格不替代服务端提交校验。菜单权限未变；回归及SFC检查通过，未真实账号提交或打包部署。

2026-09-22 客户验收角色按用户确认使用client_role字典，沿用GET /sys/dict/getDictItems/client_role，显示text、提交value至acceptUnitName。两端取消固定枚举。当前刷新Apifox的complete仍描述acceptUnitName为单位/部门自由文本，需后端同步字段语义；本次未增加字段或修改验收权限。

2026-09-22 复审入口更正：PC/移动端按latestStatus各类型canSubmitReacceptance===true显示申请复审；FAILED使用applyMode=WITHOUT_RECTIFICATION，PENDING_REACCEPTANCE使用NORMAL，类型仅取对应可申请项，已通过结果不重提。首次工序完成由后端自动启动验收，不额外调用apply；PC移除列表独立提交验收，移动端待验收按钮仅打开验收弹窗。历史无返工轮次PENDING_ACCEPT且NOT_APPLIED保留原经理/菜单资格下的兼容申请。FAILED/REACCEPTING补入验收入口/详情可见范围。移动端弹窗迁移latestStatus，使用外层状态说明，申请后重读最新状态、不伪造ACCEPTING。提交结果仍用complete，PC返工继续按canApplyRework；移动端沿用上一轮精简范围不新增返工申请。两种模式与资格矩阵、四个SFC、PC定向ESLint及diff检查通过；未真实账号端到端联调，未打包部署。

2026-09-22 PC返工详情补充：按最新要求移除“额外领料执行情况”区域、按钮、统计状态及getProjectReworkMaterials/loadMaterialMap请求；保留queryById返回的“返工额外领料计划”和工序行日志入口。覆盖此前按需加载执行汇总的说明。菜单、权限及接口定义不变。

2026-09-22 PC返工详情：移除“提交和审批记录”区域及解析代码；指定reworkId打开时仅查/project/rework/queryById，不预查返工列表。优先使用详情processes/materials/outsources数组（包括空数组），不让旧planJson覆盖真实工序ID。查看实施日志移至返工工序最后一列，固定右侧；仅审批通过且有真实工序ID显示，按该行processId跳转。额外领料执行汇总仍仅显式点击后按需加载，不影响首开单请求。新增/编辑/审批流程和权限不变；定向静态检查通过，未真实账号联调。

2026-09-22 PC验收最新状态接入（覆盖此前前端判断返工显隐及未迁移说明）：当前内外验改查 `GET /project/acceptance/latestStatus?periodId=...`，分别取 internal/customer.acceptance 填充详情，状态标签使用外层 status/statusText；不再查询全量验收列表推算有效来源。各类“申请返工”直接以对应 canApplyRework===true 显示，不叠加前端经理、菜单或返工状态判断；加载中、失败、字段缺失时不显示，切换上下文丢弃旧响应。历史列表仍按需分页查询；返工详情、原单ID/version仍由抽屉查询，提交后端最终校验。申请和返工变更后刷新最新状态，单类型刷新保留另一类型未提交内容。未修改菜单编码或角色授权，两份菜单指南已核对无需修改；未执行真实业务提交，移动端不在本轮范围。

## 2026-09-22 · 申请返工显示条件简化

当前内验或外验任一结果为未通过（FAILED），即满足“申请返工”的业务显示条件；保留已接受邀约的项目经理身份及 `project:rework:apply` 权限，不再按项目阶段、返工状态或另一类复验状态隐藏。打开和提交返工不再执行前端来源有效性、施工状态及返工互斥预检查，统一由 `POST /project/rework/submit` 校验并反馈原因；保留必填表单校验、原单复用及版本参数。此调整不包含 latestStatus 数据源迁移。PC 验收及返工契约回归通过；未代用户执行真实提交。两份菜单指南已核对，权限项未变，无需修改。

2026-09-22 返工申请入口修复：无需草稿保存，沿用submit一次提交完整方案；内部方法改名submitApplication。失败验收入口不再因返工列表阻止打开抽屉，已有来源申请复用原单：驳回/撤回及旧待提交记录由原申请人修改重提，待审批/已通过只查看；不新建重复单。入口资格变化明确提示，提交仍保留版本、身份、来源及有效返工互斥校验。经理资格不再依赖返工列表加载。最新Apifox确认submit完整方案协议。返工契约和PC验收回归通过，未代真实提交。菜单已检查无需修改。

2026-09-22 验收申请返工互斥交后端：普通验收及免整改复验入口移除blockingRework前端条件，保留项目经理/按钮权限、项目及验收状态、原因等校验；不改施工返工申请自身限制。仍调用acceptance/apply并显示后端业务错误、失败保留输入。最新Apifox仍明确拒绝与活动返工并行，因此前端改动不表示大连集运智能闸口分期能通过后端验收校验，未代提交业务。菜单已检查无变化。

2026-09-22 项目列表用户名筛选：Project.api.ts的projectList每次从当前登录store读取username，去首尾空白后附加至projectPeriodList请求，覆盖调用方同名参数；用户名缺失拒绝请求，不以空值查询全量。原搜索和分页保留，其他模块独立封装未改。已刷新Apifox：普通账号按未删除成员关系过滤，不限角色/邀请状态；有效负责部门账号范围放开仍保留其他条件，交由后端判断，前端无admin豁免。该参数后端仍为可选筛选，不等于服务端数据权限强制鉴权；后端应绑定认证用户防删改参数。用户名/账号切换/防覆盖/缺失拦截及列表筛选回归、ESLint通过，未跨账号真实列表验收。菜单文档已检查，无编码/角色/路由变更，不需调整；未打包部署。

2026-09-22 报价管理项目名称模糊查询修正：搜索表单改为keyword，quotationFilters保留非空keyword并去首尾空白，空串/空白/null/undefined不传；匹配项目与分期名称由服务端分页前完成。最新Apifox已核实materialCandidate/list支持keyword。旧projectName筛选兼容其他调用方，报价搜索不再使用。定向参数回归通过；菜单与权限不变。

2026-09-22 撤销报价不得低于成本价限制：输入、保存、重提共用校验仅保留必填、非负数及两位小数，允许报价低于成本或为0；不改后台接口。低价边界、驳回状态允许edit/submit的权限单测及原报价回归通过。当前浏览器王成旭驳回页无提交按钮，尚未核实其submit权限，不能将价格校验移除视为真实重提验收成功。结构/菜单已检查，无变化。

2026-09-22 市场直接报价前端校验：finalPrice不得小于basePrice，允许相等及0/0；使用Big精确比较，成本或报价变更时行内提示，保存和提交审批再次拦截。已保存值不自动改写，技术线路指导价流程不变。复用原editBatch/submit接口，无载荷变更；此为前端校验，不代表服务端已实施同规则。边界/小数/空值及SFC回归通过。结构和菜单文档已检查，无新增入口或权限。

2026-09-22 市场人员录入与审批线路解耦：真实市场部门人员查看/编辑草稿、驳回及待审批历史单时显示成本价、报价输入框，编辑仍受原按钮/状态限制，待审批只读；不因历史TECHNICAL线路隐藏报价，不改服务端approvalRoute。新选物料默认成本0，保存仍editBatch的basePrice/finalPrice。王销售新建页已目视确认两输入框可编辑、成本0，未代保存提交；旧单编辑需实际保存回读验收。回归/SFC检查通过，菜单权限未改。

2026-09-22 MARKET选料仅将新行basePrice初始化为0，不使用stock/material/list.costPrice；editBatch仍提交用户填写的basePrice，明细list回显不清零，不修改物料主数据。接口路径/字段无变化。

2026-09-22 已刷新Apifox：主单approvalRoute为TECHNICAL/MARKET，由服务端固定；编辑器已有单回查主单，新建查询部门树仅预览，不提交线路。editBatch市场单提交basePrice和finalPrice，不提交markupRate；最终报价必填且最多两位小数。permissions具体单携带periodId/candidateId，归一化保留canApprove供审批门禁。approve路径和载荷不变；当前文档两线路通过均status=1/priced=0，与市场单审批即定价需求不一致，待后端修正，前端未额外调用price或伪造priced。

2026-09-21 项目报价页签所属部门判断：优先复用登录departs与getIdentity.departmentIds；缺少所属部门名称时GET /sys/sysDepart/queryTreeList?ids=所属部门ID逗号列表，匹配市场部/技术部。使用所属部门，不使用departIds负责部门；无法确认时不挂载报价组件。报价列表仍限定periodId，详情跳转传递列表已有主单字段，无新增报价请求或提交协议变化。Apifox已刷新核对部门查询与materialCandidate/list；页签隐藏只是界面限制，报价接口仍需后端鉴权。

2026-09-21 PC验收调用修正：AcceptanceModal首开复用GET /project/project/projectPeriodDetail，传periodId并检查返回分期及status；申请成功后重新读取真实状态，不由前端设为ACCEPTING。POST /project/acceptance/complete仅CUSTOMER携带acceptUnitPhone，INTERNAL省略；其他字段及apply协议不变。REACCEPTING按现有status字段兼容，不新增参数。当前Apifox工具不可用，未重新核实运行后端契约及真实账号提交，不将静态/模拟回归视为后端验收。

2026-09-21 报价授权选人：GET /sys/user/list?pageNo=N&pageSize=100&status=1，读取result.records并完整分页，本地按姓名/账号搜索；兼容null空结果，遵守用户数据权限，不使用listAll。报价grant/grants协议保持不变。

2026-09-21 导出授权沿用 materialCandidate/grants 与 grant：新增/再次授权传canViewPrice=false、canEditPrice=false、canExport=true；删除传三个false。version始终取对应用户授权记录（含撤销记录），仅不存在时0，写前复核版本和管理资格，写后回查，不增加删除接口。

2026-09-21 projectPeriodList 的 keyword 按需发送：项目、计划、实施三个入口统一去首尾空格；未输入、清空、重置或仅空白时删除参数键，不发送 keyword=。

2026-09-21 项目、计划、实施列表 GET /project/project/projectPeriodList 的项目名称条件统一为 keyword；最新 Apifox 定义主项目名称和分期名称无分隔拼接后包含匹配。报价 materialCandidate/list 仍声明 projectName，采购 purchaseOrder/list 仍声明 periodName，出入库 stock/apply/list 仍声明 projectName，本轮未将这三处改为未声明参数，待后端确认。展示与保存字段不变。

2026-09-21 验收待办分流：PC TodoActionHost与移动端首页兼容todoType=PROJECT_ACCEPTANCE_FAILED及actionKey=PROJECT_ACCEPTANCE_FAILED_HANDLE，查看详情调用acceptance/queryById(id)，展示项目摘要、验收类型、实际负责人、日期和驳回原因，按钮为确认/申请复验。确认仅关闭弹窗，不调用待办完成接口。申请复验打开现有验收弹窗，重新查询最新验收及经理身份，来源ID与当前失败记录不一致或无资格时不自动打开申请；匹配时预选来源类型，填写原因后沿用apply WITHOUT_RECTIFICATION。PROJECT_ACCEPTANCE_PENDING/PROJECT_ACCEPTANCE_HANDLE直接打开验收弹窗，不跳项目详情整页；bizSubId是分期，bizId是验收记录，不混用。中文名称优先字典，补缺省名称。新增入口沿用原办理/复验权限，后端最终鉴权及关闭待办；未修改后台菜单角色。两端分派/ID/详情校验、SFC及既有申请/请求回归、PC定向ESLint通过；未真实账号办理或部署。

2026-09-21 列表项目名称统一（部分完成）：项目管理、计划方案、报价管理、实施管理均用projectDisplayName显示主项目名称-分期名称，缺一侧只显示已有名称，全空—；搜索标签项目名称、仅传projectName，移除分期名搜索，项目列表不再由该搜索框传keyword。采购订单展示也改组合名称，复用projectPeriodList完整分页补齐主项目/分期名称，不改表单保存字段。后端阻塞：purchaseOrder/list仅支持periodName搜索；stock/apply/list的projectName实际是保存的分期名称，缺主项目返回及查询协议。因此采购搜索和出入库记录暂保持原行为，不用前端当前页过滤或伪造查询参数；全OA统一尚未完成，需后端补主项目名字段与分页前查询。菜单/权限无变化，两份指南已检查。

2026-09-21 统一验收申请（覆盖此前period/status及逐项acceptance/submit说明）：PC列表提交验收、验收弹窗申请验收及移动端对应入口统一POST /project/acceptance/apply，JSON仅periodId、acceptTypes、reason、applyMode。普通申请NORMAL，失败免整改复验WITHOUT_RECTIFICATION；类型INTERNAL/CUSTOMER可多选，原因去首尾空白后1～400字。两个类型一次事务请求，失败保留输入，不再部分成功逐项重试、不传sourceAcceptanceId/reworkId。普通申请只选不存在或NOT_STARTED的类型，验收中可补申请另一类；复验只选失败类型。按返回数组的acceptType更新对应记录，保留未选项，外验成功后定位填写区；实际结果仍调用complete。后端最终核验本分期经理、工序完成、返工互斥及通知接收人，前端不新建返工记录。沿用project:acceptance:submit，无新菜单或角色授权。两端动作模拟及5个SFC、PC定向ESLint通过，真实账号端到端未验证，未部署。

2026-09-21 项目补料审批待办详情：PROJECT_ADD_MATERIAL_APPLY_APPROVAL按todoType/actionKey精确匹配，actionParams.applyId优先、bizId兜底为申请单ID，缺失提示错误；打开MaterialSupplementDrawer的targetOnly模式，queryById加载指定补料单并从响应periodId确定分期，自动展开物料记录，不用bizSubId猜分期、不走库存审批，也不依赖分期列表第一页命中。后端返回的目标详情可只读查看，审批仍受原权限与状态限制；禁用新增补料。普通项目补料管理入口保持原列表行为。派发/缺ID模拟和定向ESLint通过，未真实待办账号点击验收。菜单指南已检查，无新菜单码或角色变更。

2026-09-21 项目管理列表及筛选：主项目/分期两列合并为“项目名称”，以主项目名称-分期名称展示，缺失一侧仅展示另一侧，全空为—；仅展示拼接，不修改原数据。合并搜索框传keyword，新增projectManagerName姓名查询；status/arrivalStatus/contractStatus/projectType均为多选，API封装统一去空白/去重后英文逗号拼接，保留0/-1，清空省略，保留分页参数。已刷新核对projectPeriodList契约，keyword按主项目与分期无分隔拼接匹配，展示用连字符不作为后端搜索分隔协议；筛选均由后端分页前完成。专项回归和定向ESLint通过；浏览器被另一会话占用，未页面实测/真实查询验证。仅PC，未改新增编辑表单、权限或部署包；菜单文档已检查，无菜单/路由变化无需更新。

2026-09-21 PC内部验收填写资格修正（覆盖此前固定operations_manager角色判断）：保留project:internalAccept及当前记录IN_PROGRESS校验，admin可代办；非管理员以登录身份departIds负责部门ID精确匹配/sys/sysDepart/queryTreeList唯一未删除“运维部”，不按普通所属部门或主管角色名放行，部门status不参与判断。仅需要内验资格且有负责部门时查询部门树，失败只读并可重试；切换身份/租户重新校验，外验经理规则不变。已刷新核对Apifox，未修改菜单码、真实角色授权或提交载荷。本轮仅PC，移动端未修改；定向资格回归/SFC/ESLint通过，真实账号填写及提交尚未验收。

2026-09-20 重新申请验收提交去除返工前置查询：PC/移动端选择失败类型并填原因后，直接逐项POST /project/acceptance/submit（periodId、sourceAcceptanceId、reason），不再调用assertAcceptanceApplication读取成员/项目/验收/返工列表。保留已加载身份、按钮权限、失败状态、原因校验，最新状态/并发互斥交后端最终判断；不调用返工创建或提交接口。接口路径未改为period/status，因刷新后的Apifox尚无该接口复验参数；首次及施工返工完成后提交分支不变。动作回归（禁止返工前置查询）、两端SFC、PC ESLint通过，未真实账号联调。菜单权限无变化。

2026-09-20 失败后申请验收入口请求修正：PC openRecheck仅基于本次已加载身份及验收结果打开类型选择框，移除点击入口即调用loadManager产生的成员/返工列表请求。确认提交仍调用assertAcceptanceApplication复核并按所选类型提交acceptance/submit，不移除后端鉴权；首次/施工返工完成后的period/status分支未改。移动端失败后入口原本不预查，无需修改。动作及布局回归通过，尚未取得用户实际Network请求，不能确认其报告的问题已完全复现。菜单文档检查无权限变动。

2026-09-20 外验表单与提交修正：PC通过校验改为实际上传字段acceptanceFormFileId至少1且最多3个，不再误要求completionReportFileId；移动端已校验正确无需改。PC验收日期仅YYYY-MM-DD，与移动端一致，提交仍适配现有接口为所选日期00:00:00。注意当前后端要求结束时间不早于开始时间，当日开始晚于零点时仍可能拒绝，需后端按北京时间日期比较，不能宣称已端到端修复。两端新增可选客户负责人角色acceptUnitName（暂定字段，用户授权先写）：PROJECT_LEADER项目负责人、TECHNICAL_LEADER技术负责人、PURCHASE_LEADER采购负责人、OTHER其他；空字符串表示未选择/清空。仅CUSTOMER提交，已办和历史回显，不属于系统RBAC；需后端complete接收保存、list/queryById返回同名字段及edit保持一致，旧数据允许空。当前Apifox尚无该字段，未证明持久化。文件校验/提交载荷/日期控件/SFC定向回归、PC ESLint通过；未真实账号联调或打包部署。菜单及权限文档已检查，无新授权项。

2026-09-20 验收弹窗流程按钮补齐：PC弹窗底部统一查看详情、申请验收、申请返工，不再按失败类型重复入口；移动端新增同名固定底部入口。失败后申请验收可多选INTERNAL/CUSTOMER，已通过/办理中不可选，按所选类型逐笔调用acceptance/submit；部分成功保留成功结果，仅剩余项重试，外验全部申请成功后定位填写区，内验通知由后端事务创建。首次及施工返工完成后的PENDING_ACCEPT仍使用period/status，不传类型。返工不弹类型选择，自动关联最新失败记录作为必填sourceAcceptanceId（同时间内部优先），直接打开方案；另一失败项保留。两端申请前置校验支持未被替代的跨轮失败记录。身份及按钮权限保留，无角色授权改动；详情/外协/物料按原按需加载。定向动作、多选/部分失败、身份、请求、SFC回归及PC ESLint通过，PC/H5构建通过；未真实账号或浏览器联调，未打包部署。

2026-09-20 验收最新记录修正（覆盖此前按当前返工轮筛选的说明）：PC/移动端每次打开重新查询 acceptance/list，按分期及INTERNAL/CUSTOMER分别完整分页读取，按createTime（缺失时updateTime）倒序取未被sourceAcceptanceId替代的最新一条。旧项目列表轮次不再过滤失败或办理中记录，不跳过最新失败去取历史通过。完成提交使用该验收记录自身reworkId。排序契约尚未明确，不使用pageSize=1；历史详情仍按需加载。权限、菜单不变。定向回归通过，未真实账号联调、未打包部署。

2026-09-20 登录身份复用与验收查询调整：两端复用既有用户状态及其持久化机制，normalizeIdentity统一读取用户ID、所属部门ID、负责部门ID、服务端提供的角色ID及角色编码（不编造缺失ID）；PC退出清空角色缓存，移动端在登录/统一权限刷新时经getUserInfo补齐角色，失败清空角色资格。内验仅operations_manager/admin加原按钮权限，不再查userDepartList或部门树；外验仍为本分期已接受经理，页面成员查询按用户/登录会话/租户/分期缓存60秒并合并并发，失败不缓存，仅内验账号不查成员，业务提交的原鉴权保留。每次打开验收仍重新查验收列表，不缓存结果；PC移除reworkId查询过滤，无本轮记录时两端仅回显最新有效已通过记录且只读，不越过较新失败记录。未新增canHandle字段、未修改服务器通知/授权或申请接口；最新Apifox内验仍描述负责部门负责人/admin，前端依用户确认按角色收紧，真实主管配置需联调。定向身份/缓存/请求/布局回归与PC ESLint通过；真实登录刷新、跨账号办理及端到端返工验收未实测；未生成新的部署压缩包。

2026-09-20 验收弹窗恢复身份前置校验（覆盖此前仅按按钮权限开放编辑的说明）：PC/移动端首开查询当前分期成员，匹配当前用户、有效且已接受邀请、memberRole包含2才允许外验；内验有按钮权限时查询当前用户真实所属部门及部门树，匹配运维部，不按负责部门/主管角色/admin推断。身份未确定、查询失败不开放对应编辑；两项独立判定，双身份可分别办理。保留记录状态及菜单权限，已完成只读，后端提交鉴权不变。首开不预查返工/历史/物料，同次打开不因提交刷新验收记录重复查询身份；切换用户/项目不接收旧身份结果。Apifox已刷新核对member/list与部门树；userDepartList未收录，复用本地用户管理既有userId调用，真实环境仍待验证。身份、权限、请求、布局/SFC回归及PC定向ESLint通过；未做真实账号保存回显、未重新打包部署。

2026-09-18 外部验收负责人分开展示（PC/移动端）：办理表单“验收负责人”只读显示当前操作人；“客户验收负责人”手填并继续提交 acceptUnitLeader。已办摘要及历史记录的验收负责人只取 acceptLeaderName，不再用客户姓名兜底；外验另外显示客户验收负责人。Apifox 已刷新核对：acceptLeaderId/acceptLeaderName 由后端记录实际操作人，前端不新增提交字段或接口调用。权限、菜单和流程未改；菜单配置文档已检查，无需更新。SFC、定向回归及 PC ESLint 检查通过；未做真实账号保存回显或真机验证。

2026-09-18 PC 验收失败操作移至内外验卡片下方，按失败类型分别显示“申请重新验收 / 申请返工”，保留原项目经理、按钮权限、状态及在途返工校验，不修改已通过项。返工详情和审批确认框新增额外领料计划，只读展示 planJson.materials 的物料、plannedQty 和单位，与审批后执行统计分开；无计划时显示无需额外领料。点击审批时才读取 queryById 最新方案和校验审批资格，未增加验收首开请求或物料库预加载。Apifox 已核对，接口/载荷不变。布局、权限、请求和返工契约回归通过；未做真实账号联调。菜单配置文档已检查，无新增菜单或权限码；移动端本轮未修改（已有详情计划展示）。

2026-09-18 报价审批职责前端对齐：withdraw仍调用POST /project/materialCandidate/withdraw，但入口及提交前校验收紧为待审批原提交人；technicalApprove调用POST /project/materialCandidate/approve，仅提供通过/驳回，技术审批页隐藏markupRate/finalPrice；审批通过后，市场部主管通过canManage+plan:quotation:price进入POST /project/materialCandidate/price并提交全部明细id/markupRate/finalPrice；报价授权要求canManage+plan:quotation:grant，继续使用grants/grant接口。路径和载荷未改变。最新Apifox的approve描述及submit待办接收人仍是管理员/市场部负责人，与技术部主管审批要求冲突，需后端同步资格、待办接收人和鉴权；前端隐藏/显示不能替代服务端权限。

2026-09-18 按需加载二次修正（上一轮未完整落实）：历史验收改为详情打开后独立请求 acceptance/list，pageSize=10，按服务端分页，成功页按页码缓存，切页复用；不再直接把主区已加载数组当历史表。API 总数仍包含待办理记录，本页只展示已办/取消历史，空页允许继续翻页，未伪造服务端历史筛选字段。PC MaterialPlanTable 改为 materialsVisited 首次需要补料才挂载，关闭只隐藏以保留草稿；两端外协开关只在尚未加载第一页时请求，下一页由加载更多触发。移动端补料首次开关加载，重复选择复用。验收完成保留弹窗与另一侧未提交内容，只刷新完成类型并使历史缓存失效。已刷新 Apifox：仍缺当前有效记录/排序及可办理标志契约，因此主区为正确选择复验链末端仍可能翻页；PC 已有非空 currentReworkId 时限制该轮。此项未完成，不能把后台身份鉴权收紧视为查询字段已补齐。已有返工详情的额外领料统计和物料基础查询也已移到独立按钮，首次点击才加载并缓存；打开返工记录只查单据详情及其中审批快照。请求模拟验证历史首开/缓存/翻页/单类型刷新及补料挂载门禁，SFC、ESLint、PC/H5构建通过；无登录态真实 Network 验证。菜单指南已检查无变化。

2026-09-18 验收按需查询（覆盖同日提前查记录数量/资格的说明）：用户确认具体办理资格已由后端收紧。打开验收时两端只按 periodId、INTERNAL/CUSTOMER 查询 acceptance/list，每类型分页100，超过一页继续读取以正确选择复验链末端；取消弹窗项目详情、部门成员、项目成员和返工预查。PC 列表传入行上下文，移动端项目详情传入已有详情；旧上下文缺少 currentReworkId 时仅用验收记录最新创建轮次展示，complete 最终核验轮次/记录/身份。按钮按对应权限码和记录状态显示，不代表后端身份核验已通过；complete 直接提交，不再重复身份预查，后端拒绝保留表单。其他入口的原身份门禁不全局修改。查看详情固定保留，两页签空时显示空态，不为隐藏按钮提前查询；历史验收复用已加载记录，返工/审批列表首次切到该页签才挂载，单次打开切换复用。点击复验/返工申请时才执行原有申请资格/互斥检查，外协和物料仍在返工表单按原开关加载，不在验收打开时查询。真实 load 函数模拟确认两端各2次验收列表；布局/SFC、PC ESLint、PC/H5 构建通过，尚未登录账号验证 Network 和服务端拒绝链路。菜单指南已检查，无菜单/接口参数/后台授权变更。

2026-09-18 验收布局收敛：PC DetailAcceptance 主区仅展示当前内外验，非可编辑记录改为责任/负责人/日期/说明摘要，复验与申请返工保留当前卡片。底部“查看详情”打开右侧抽屉，按“历史验收记录 / 返工申请审批记录”分组，复用原返工详情办理组件；无某类记录隐藏对应页签，两类均空隐藏入口，返工查询失败保留详情入口供重试。历史验收 PC 每页10条，返工沿用分页。移动端 ProjectAcceptancePopup 同步只读摘要和右侧详情弹层；历史筛选沿用 isAcceptanceHistory，不把待填写草稿当历史。继续用 acceptance/list、rework/list，无新增 API/授权；只增加一次返工存在性查询，详细返工面板打开后加载。布局/SFC/业务回归、PC ESLint、PC/H5 构建通过（H5 原有图标警告）；本地浏览器停在登录页，未完成真实账号点击和视觉验收。两份菜单指南已检查，无菜单或权限变化。

2026-09-18 PC内部验收新增真实所属部门门禁（覆盖前述仅按钮判断）：operationsMembership读取现有userDepartList(userId)与queryTreeList，按部门ID匹配唯一未删除且未禁用的运维部，拒绝缺失/重名/失败；不按负责部门或主管角色，管理员不豁免。编辑/上传/提交统一要求有效用户、运维成员和project:internalAccept，上传/提交前重查，切换账号或重新加载先清空资格。陈雄真实所属工程部，浏览器调用新门禁返回false；未切换其账号完整UI验收。部门逻辑、PC权限矩阵、两端组件编译及ESLint通过。userDepartList为仓库既有接口且已实测，当前Apifox未收录此路径，需后端补文档；queryTreeList已核对最新契约，历史status=null按未禁用兼容。仅修PC，移动端仍需同步该部门门禁；未改真实角色/部门/后端或部署包。菜单文档已检查，编码与路由无变更。

2026-09-18 内验部门规则核对：最新complete契约要求有效运维部所属成员（sys_user_depart），不按负责部门depart_ids或operations_manager主管角色判断；同时需内部验收接口权限，无管理员代办。PC/移动端现有前端门禁仅依据project:internalAccept，不验证真实部门归属；最终资格必须由后端校验。修正PC“运维部门主管”残留提示为“运维部门成员”，同步两端旧主管限定回归断言。未修改真实部门、角色或后端代码；具体账号提交失败尚待现场定位。菜单指南已检查，权限码/路由未变，无需调整；部署包未更新。

2026-09-18 PC返工管理：预计再次验收日期移至申请信息内、返工原因上一行，增加必填标识、字段错误提示和提交前空值拦截；新申请及编辑重提共用校验，保留不得早于工序计划完成日期的规则。仍以expectedAcceptanceDate传YYYY-MM-DD；Apifox后端字段仍可空，本次为PC表单必填约束，移动端未改。空值/清空、顺序、日期范围、提交门禁及Vue编译和ESLint检查通过；未真实提交，未更新部署包。菜单/权限/路由已检查，无需调整。

2026-09-18 内部验收日期：按用户明确要求，PC仅选择年月日，提交acceptEndDate固定补为“YYYY-MM-DD 00:00:00”，旧日期值带时分秒时提交也归零；PC外部验收不变。移动端现有日期选择及零点提交已符合，无需改动。Apifox已刷新，complete仍规定不得早于验收开始时间，因此同日零点可能被后端拒绝，此风险已告知；不绕过后端校验。已检查菜单指南与路由，无变更。本次未重新生成部署包或完成真实提交验收。

2026-09-18 报价按钮菜单权限（覆盖此前不新增静态按钮码的说明）：报价管理下配置10项PC按钮，复用 add/edit/delete/lock，新增 submit/withdraw/technicalApprove/price/grant/export（均为 plan:quotation: 前缀）。delete改名作废报价，lock改名市场审批；technicalApprove仅预留，旧unlock保留但不作为当前流程入口。列表、编辑器、授权弹窗及合同内报价调整同时校验菜单权限、分期资格和业务状态。当前 localhost:3101 所连后端已回读确认全部按钮，admin角色全部授予并刷新确认实际权限；其他角色未改，其他环境未配置。权限回归、定向ESLint通过；未进行跨账号真实报价操作，后端接口菜单码强制鉴权未由本次前端改动实现。编码表与验证范围见《报价治理接口对接》；页面路由不变。

2026-09-17 报价治理更新（覆盖下文旧报价权限、价格联算和审批接口说明）：提交/审批/撤回/作废/定价接入专用接口；整单 version 校验；价格及导出按分期授权；审批通过且已定价才可受控导出；合同调整和报价审批待办同步。平铺列表与三个筛选保留。菜单及隐藏路由不变，未修改真实角色或用户授权。详见[报价治理接口对接](报价治理接口对接.md)。本地页面和静态回归已验证，真实跨账号完整流程未验收。


2026-09-17 实施日志媒体回显：PC 共享日志详情与移动端 implementLog 使用 logMediaUrl 仅在展示时规范 static/、sys/common/static/、shouhuiApi/sys/common/static/ 相对前缀，避免重复拼接；HTTP(S)/协议相对直链及本地临时预览不补前缀。上传返回和日志保存原路径保持不变。PC 从 photos 按扩展名区分照片、视频，视频使用原生控件点击播放、不自动播放、不预加载；独立实施详情与项目日志弹窗共用修复。已刷新核对 implementLog/queryById、上传契约；路径回归、两端 SFC 编译、PC 定向 ESLint 通过，真实文件预览/视频播放未实测。菜单两份指南已检查，无路由、权限或菜单变更。

2026-09-17 候选清单筛选：报价管理平铺列表新增主项目名称、审批状态、采用状态查询，quotationList 与 quotationPeriodGroupPage 封装均透传 projectName/status/adopted。名称直接传关键词并去首尾空白，不拼通配符；空值省略，status=0、adopted=0保留。状态仅-1/0/1/2/4，采用0/1，条件由后端分页前AND组合；不恢复分组页面，list total仍为候选单数。若后续使用分组页，组内list必须带相同筛选才能与candidateCount一致。物料维护与选料抽屉已直接传materialName关键词，无需改动。定向ESLint、diff及参数模拟检查通过；本地页面实际验证三条件查询（含两个0）success=true、重置清空条件并回第一页。未验证分组接口真实返回，不访问测试环境；菜单/角色权限无变化。

2026-09-17 接口刷新补充（覆盖下方同日旧限制）：complete 按 acceptType 提交内外验结果，外验 acceptUnitLeader 已支持客户姓名，两端移除旧覆盖提示。返工 expectedAcceptanceDate（可选日期）已接入提交、重提、清空及回显并校验工序结束日期；V102 数据库迁移未核实。PC 通过附件校验修正为 acceptanceFormFileId。新后端内验资格是有效运维部成员，不仅主管；前端尚缺真实成员资格返回，旧角色展示门禁未放宽，跨轮保留通过项亦待契约明确。回归与 PC/H5 构建通过，未完成本地账号闭环。菜单编码及路由未变，菜单指南检查无本次新增配置；详见 PC 仓库 docs/项目/验收返工改造后端对接清单.md 最新进展。

2026-09-17 验收/返工本地改造：两端验收文件改为acceptanceFormFileId，客户负责人输入与明确日期时间；提交验收核验本分期经理，取消admin办理豁免。返工支持process数组、多工序详情/日志、基础外协分页选择及人数工时校验、可选补料，完整方案直接submit，停用草稿调用。预计复验时间尚无字段，暂不开放；客户姓名持久化、跨施工轮只重验失败项、真实运维负责人能力回显仍待后端。本说明覆盖旧单工序、草稿提交及管理员兜底说明。菜单编码/路由不变，未在线修改授权。详见[验收返工改造后端对接清单](验收返工改造后端对接清单.md)。

2026-09-17 主项目名称可编辑：ProjectApply 按页面模式控制名称字段，仅“已有主项目新增分期”时只读；新建主项目及编辑时可输入，名称必填且不可全空白。编辑通过原 JSON `/project/project/editProjectPeriod` 的 projectName 保存，分期归属与原项目对接人编辑资格不变。定向 ESLint 和组件编译通过，未完成实际保存重开联调；菜单与权限无变化。

2026-09-17 PC 统一上传检查：新增 uploadDocument(file, biz?) 通用方法，统一公共上传及响应路径校验；uploadProjectDocument 保留分期校验并委托通用方法，新增项目复用通用方法。修复旧 Plan.api.ts 的 savePlan/editPlan multipart 包装为 JSON。业务附件和专用导入/解析接口的检查边界见 [统一上传检查](统一上传检查.md)。静态与模拟分支检查通过，未真实上传联调；菜单无变化。

2026-09-17 新增项目及分期附件：`addProjectPeriod` 改为 application/json；有附件先调用 `/sys/common/upload`（新增尚无分期 ID，biz=project），校验上传成功后将 message 路径写入顶层 attachmentFileId，不再传 data 包装、attachment 文件或 fileName。无附件直接新增，上传失败不调用新增接口。编辑仍沿用公共上传后 JSON 保存。

2026-09-16：刷新Apifox并核对 acceptance/complete，仍定义内部project:internalAccept、外部project:accept，本次无请求字段变化。前端额外要求内验账号角色operations_manager（admin原规则保留）；登录角色缺失不放行。当前Apifox未收录sys/user/getUserInfo，PC沿用现有store.getRoleList，移动端使用现有登录userInfo.roles[].value，未新增未文档化接口。需联调确认移动端主管角色返回及后端同样强制主管身份；前端限制不能替代后端授权。后台误授权限尚未撤销。

2026-09-16 报价分组：报价管理改为外层分期分组分页：`GET /project/materialCandidate/periodGroupPage`，按 periodId 唯一标识，展示主项目名称、分期名称、candidateCount，total 为分期数。默认折叠，展开才挂载 QuotationGroupTable，调用原 list 携带 periodId，组内独立分页；折叠卸载，重开重新读取。原报价操作及权限不变，提交/作废后刷新外层并收起。外层接口未定义搜索，移除原分期 ID 搜索，不传 groupByPeriod，不依赖 children。 本说明覆盖旧的平铺报价列表说明；菜单配置已检查，无菜单或授权变化。静态检查通过，浏览器文件选择窗口阻挡本次展开/翻页验证，尚未完成交互验收。

2026-09-16 外验资格：刷新并核对ProjectPeriodDetailResult，模型没有projectManagerUserId/projectManagerId；前端不再以缺失字段比对登录人。沿用GET /project/member/list（periodId必填、分页）核验有效已接受经理成员，complete前复查，project:accept仍必需。验收历史仅前端过滤当前进行中记录，无新增接口或数据删除。

2026-09-16 实施列表：`GET /project/project/projectPeriodList` 已支持 status 英文逗号多值，任一状态匹配，COUNT 与分页共用条件。PC 实施管理默认传 `IMPLEMENTING,DEBUGGING,DEBUG_COMPLETED,IMPLEMENT_COMPLETED,PENDING_ACCEPT,INTERNAL_ACCEPTING,ACCEPTING,REWORKING,WARRANTY,COMPLETED`，选择状态后只传范围内选中值，清空恢复默认。其余名称/类型/分页参数保留，不改变普通项目管理查询；CLOSED 的历史实施判断暂无契约，暂不纳入。

2026-09-16 验收：已刷新 Apifox 8679942 核对 complete/submit/list、返工list/add及相关模型。`acceptUnitLeader` 为姓名文本，由前端传当前提交人；complete必传当前类型最新进行中`acceptanceId`，日期传`acceptEndDate`。复验 `/project/acceptance/submit` 对内外类型通用，传`periodId/sourceAcceptanceId/reason`，取`result.id`继续完成；不再限制仅内验。两端申请前重读分期详情、成员、全部分页验收/返工记录核验当前轮及互斥；前端附件总数≤3，报告路径仍按现有逗号多路径协议。首次启动仍走period/status，通知投递及服务端并发限制未由前端替代，真实环境送达/保存需联调。

2026-09-15 SysTodo新增展示字段applyUserName（string|null，只读）：PC/移动端待办优先回显，空值回退已有createBy，两者均空显示—，无新增调用。契约说明STOCK出库审批/执行按bizId关联原申请，其他类型或来源删除时可为null。

报价明细定价（2026-09-15）：已刷新 Apifox，addBatch/editBatch 的行支持 basePrice/markupRate/finalPrice（非成本）；金额2位、比例4位，省略/null保留原值。前端采用小数比率0.2=20%，按底价计算终价并显式传入，后端当前不计算。审批仍使用完整明细editBatch→回读→主表edit(status=1)，不是事务；成本快照/字段权限与并发限制待后端补齐，详见 [后端待办](报价价格改造与后端待办.md)。

2026-09-14 返工衔接：已刷新 Apifox 8679942。返工 add/edit 传单个 process，其中 processName 为前端只读生成的“第N轮返工”；轮次通过详情 previousReworkId 链求得，接口未提供 roundNo。列表分页最大100，详情 historyJson 为提交/审批历史；submit/withdraw/approve 传最新 version，编辑仍检测打开版本避免覆盖。实施 signIn/signOutDraft/signOut 只传 periodId+新processId，由后端关联 reworkId；process/detail 的 onlyUnfinishedProcess=true 筛当前已批准未完成工序。返工用料 plannedQty 是额外领料量，日志 usedQty 是实际使用量，两者不互替。路径、权限码、后台迁移未改。


2026-09-14 报价列表请求收敛：列表初始化、查询和翻页仅请求 `GET /project/materialCandidate/list`，传 pageNo/pageSize 及可选 periodId；直接使用后端 total 与顺序，不枚举分期、不拉全量报价、不补查项目名称、不额外加载状态字典。按候选生命周期 -1/0/1/2/3/4 本地显示；作废记录按后端返回保留，不过滤当前页造成分页失真。最新契约未支持名称和状态查询，仅保留分期 ID 筛选；项目名称字段未返回时显示 —。新增选项目、编辑、审批、导出所需请求仍在操作时调用。菜单、路由及权限不变；移动端未发现独立报价管理聚合列表，本次未改合同选报价流程或待实施双单方案。

2026-09-13 两端操作资格：成员判断分页读取 member/list，以当前分期、当前用户、有效且inviteStatus=1、逗号分隔memberRole精确含2为项目经理。工序完成复查projectPeriodDetail、process/detail及成员，仅IN_PROGRESS调用process/status；未签退人员等由后端校验。移动端到货仅传periodId/arrivalStatus，验收complete必传当前记录acceptanceId，首次启动仍为period/status；重新内验用acceptance/submit的sourceAcceptanceId/reason，不恢复旧start。

项目状态查询（2026-09-13 最新扩展）：已刷新核对 `/project/project/projectPeriodList` 新增 arrivalStatus（0/1）及 contractStatus（字符串 -1/0/1/2/3），均单值，与其他查询条件 AND 组合，在 COUNT/LIMIT 前过滤。缺省/空白不筛选；合同 -1 匹配未建有效合同或状态 NULL/空白，不改变响应原状态；未到货 0 是有效值。新增 projectManagerName 列表返回，来自分期有效已接受成员、角色精确包含2；无经理为空，异常多条后端按创建时间和 ID 倒序取第一条。前端直接使用，不增加逐行请求。

项目详情阶段 Tab（2026-09-13）：刷新 Apifox 确认 `ProjectPeriodDetailResult` 有 `status` 及 `contractStatus`（空待提交、0驳回、1通过、2待审批、3撤回），但没有历史模块存在性/最高阶段字段。PC 使用这两个已有字段控制默认 Tab，不新增探测查询；已移除手动展开历史资料，深链只定位已开放 Tab，不以浏览器缓存代替后端历史事实。接口参数、路径和读写权限不变。

> 2026-09-13 验收报告多附件前端约定：每文件调用一次 `/sys/common/upload`，`biz=periodId`，将成功 `message` 追加到 `completionReportFileId`，按公共 JUpload 惯例使用英文逗号连接，最多三个；通过 `/project/acceptance/edit` 保存整个报告路径集合，完成验收也传该字符串。删除仅保存剩余路径，预览逐项使用 `/sys/common/static/`，不把整串作为文件 URL。Apifox 当前仍仅声明字符串，未明确多路径语义/字段长度；本约定不是后端已验证能力，保存与查询回读需联调确认。独立 `acceptanceFormFileId` 仍为单文件。

> 2026-09-12 已刷新 Apifox 核对：候选列表仍要求 periodId，合同组合提交仍采用原候选单，并未支持完整双单流程。新增需求与接口能力缺口见 [报价与合同报价双单改造方案](报价与合同报价双单改造方案.md)；其中建议字段和能力不是已发布接口，实施前须重新确认契约。

延期待办办理（2026-09-11，已刷新 Apifox）：`PROJECT_DELAY_APPROVAL` 的 `bizId` 作为延期申请 `id`，通过 `GET /project/delay/queryById` 查询；名称另按返回的 `periodId` 查询 `/project/project/projectPeriodDetail`。通过 `POST /project/delay/approve` JSON `{id}`，驳回 `POST /project/delay/reject` JSON `{id, approvalReason}`。两接口要求 `project:delay:approve` 且仅处理状态 `2`。状态为 `0=驳回/1=通过/2=待审批/3=撤回`，未知状态只读；日期字段 `originalEndTime/expectedEndTime` 为 yyyy-MM-dd，天数 `delayDays` 后端计算。审批人、审批时间、待办关闭及工序计划日期更新均由后端处理；不调用 `/project/delayApproval` CRUD。

2026-09-11：PC 已移除列表/详情“提交验收”以及 `/project/acceptance/submit` 调用。刷新 Apifox 确认该接口废弃；当前 `process/status` 文档仍称工序完成进入 `PENDING_ACCEPT` 并需人工提交，与用户确认的自动验收流程不一致，需后端核实自动推进与更新文档。前端只保留已有工序完成请求和验收结果办理，不额外代发分期状态变更。

验收弹窗与负责人（2026-09-10）：已刷新并读取 Apifox 分期详情、验收实体和 start/complete 最新契约。弹窗以 `GET /project/project/projectPeriodDetail?periodId=...` 获取最新状态；完成调用 `POST /project/acceptance/complete`，显式传 `periodId/acceptType/result/remark/acceptEndDate`、当前轮 `reworkId?`、报告引用及 `acceptUnitLeader=当前登录人姓名`、`acceptUnitPhone`，客户验收再传验收单引用，不再先 edit 后 complete。完成与资料同事务，独立“保存资料”仍用 edit。负责人显示优先 `acceptUnitLeader`，兼容历史 `acceptLeaderName`。最新 complete 将 `acceptUnitLeader` 定义为负责人姓名，实体模型仍保留旧“验收单位负责人”说明，以完成接口新定义为准。start 已废弃，不再在验收表单提供手动开始按钮；进入 ACCEPTING 后由后端自动启动。

验收日期回显（2026-09-10）：按用户确认，项目详情当前卡片及历史记录展示 `acceptEndDate`（格式化年月日），不读 `acceptDate` 兜底，不展示 `implementCompleteDate`。已刷新 Apifox；当前验收实体模型尚未补列 `acceptEndDate`，需后端补齐响应文档。此次仅调整展示，不变更资料保存及完成验收协议。

## 项目内实施详情复用（2026-09-10）

列表沿用 `/project/implementLog/list`，传分期 periodId 和当前 pageNo/pageSize，按 total 分页。详情弹窗不再直接使用列表行，改按日志 id 请求 `/project/implementLog/queryById`。使用 implementLocationName、internalParticipantIds、outsources、materials；负责人和名称补齐复用独立实施详情的工序/成员/外协查询，配置不作为本次参与记录。切换日志、关闭弹窗或切换分期后旧请求失效。

## 实施日志详情字段复核（2026-09-10）

- 刷新 Apifox 后按 `/project/implementLog/queryById?id=...` 读取 `implementLocationName/hours/signInTime/signOutTime/internalParticipantIds/outsources`；`hours` 空时仅前端计算并标注打卡时长，0 不视为空。
- `/project/process/detail` 的 `records.siteLeaderId` 与日志 `submitterId` 比较。当前契约无日志历史负责人标记，不按当前账号推断。
- `internalParticipantIds` 仅按日志 ID 集合展示，姓名通过 `/project/member/list?periodId=...` 补齐；外协按 `outsources[].outsourceMemberId` 匹配 `/project/memberOutsource/list` 的 `id` 获取单位名称，实际人数/工时/备注使用日志的 `headcount/workHours/remark`。两配置接口读齐分页，详情展示每页 10 条；无新增接口或写请求。

销售负责人字段复核（2026-09-09）：刷新 Apifox 后确认组合接口使用 `data.contract.salesUserId` 和 `data.contract.salesUserName`，不是 `saleName`。前端保存时始终成对提交两字段，不受修改差量过滤影响；其他合同字段仍按差量协议提交。

## 合同报价弹窗分页（2026-09-09）

已刷新 Apifox 并核对候选明细列表与批量修改：继续按 `candidateId + pageNo/pageSize` 读齐候选明细，前端用完整数组分 10/20/50/100 条展示及维护草稿。`materialCandidateItem/editBatch` 仍为全量同步，必须提交所有页的 `records`，不能只提交当前页（遗漏旧明细会被逻辑删除）。本次不改变后端字段或接口协议。

## 2026-09-09 最新契约补充

- 合同关联报价：`addWithPaymentRecords/editWithPaymentRecords` 的 multipart `data` JSON 顶层传 `materialCandidateId`（与 `contract/records` 同级）。只选当前分期已通过报价，可在合同入口调整原单；合同提交成功由后端事务改为 `3 已采用`。接口不保存合同关联新字段，页面按分期唯一已采用单回显。已采用仅合同驳回/撤回后可解锁为通过，不复制报价。计划首次为空且可编辑时自动带入采用报价，已有计划不覆盖；`candidateQty` 由后端处理，计划只传 `id?/materialId/plannedQty/unitId/remark`。

- 已刷新 Apifox 项目 8679942：候选主表六状态；使用 `materialCandidate/edit` 维护提交/审批状态，当前无独立报价审批接口。后端仍须强制校验审批权限及已锁定明细的可修改上下文；前端按钮不是安全边界。


> 依据 apifox 接口文档：https://s.apifox.cn/ffb89e1a-f7ea-4dd5-99ae-e8365679dc0c（「首汇OA」项目模块）
>
> 后端统一前缀 `/jeecgboot`（axios apiUrl）；所有实体均为**项目分期(periodId)**维度子表。
>
> 前端 API 定义：`src/views/project/Project.api.ts`、`src/views/project/detail/ProjectDetail.api.ts`、`src/views/payment/Payment.api.ts`。

## 一、接口清单（Apifox 127 个 + 当前页面批量接口）

### `/project/project`（12）

| 方法   | 路径                                   | 说明                                                                                      |
| ------ | -------------------------------------- | ----------------------------------------------------------------------------------------- |
| POST   | `/project/project/add`                 | 项目管理-添加                                                                             |
| POST   | `/project/project/addProjectPeriod`    | 新增主项目/已有主项目新增分期；JSON，附件先公共上传，顶层 attachmentFileId 保存返回路径 |
| DELETE | `/project/project/delete`              | 项目管理-删除                                                                             |
| DELETE | `/project/project/deleteBatch`         | 项目管理-批量删除                                                                         |
| DELETE | `/project/project/deleteProjectPeriod` | 项目管理新增页面-删除分期                                                                 |
| POST   | `/project/project/edit`                | 项目管理-编辑                                                                             |
| POST   | `/project/project/editProjectPeriod`   | 修改主项目及分期；multipart：必填 `data` JSON 字符串、可选新 `attachment` 文件            |
| GET    | `/project/project/list`                | 项目管理-分页列表查询                                                                     |
| GET    | `/project/project/periods`             | 项目-查询项目分期列表                                                                     |
| GET    | `/project/project/projectPeriodDetail` | 项目管理新增页面-详情                                                                     |
| GET    | `/project/project/projectPeriodList`   | 项目管理新增页面-分页列表                                                                 |
| GET    | `/project/project/queryById`           | 项目管理-详情                                                                             |

### `/project/period`（当前状态接口）

| 方法   | 路径                                     | 说明                                               |
| ------ | ---------------------------------------- | -------------------------------------------------- |
| POST   | `/project/period/add`                    | 项目分期-添加                                      |
| POST   | `/project/period/close`                  | 项目分期-项目关闭                                  |
| POST   | `/project/period/complete`               | 项目分期-项目完结                                  |
| POST   | `/project/period/status`                 | 项目分期通用状态流转（计划提交、计划审批、验收等） |
| DELETE | `/project/period/delete`                 | 项目分期-删除                                      |
| DELETE | `/project/period/deleteBatch`            | 项目分期-批量删除                                  |
| POST   | `/project/period/edit`                   | 项目分期-编辑                                      |
| POST   | `/project/period/implementComplete`      | 项目分期-实施完成                                  |
| GET    | `/project/period/list`                   | 项目分期-分页列表查询                              |
| GET    | `/project/period/queryById`              | 项目分期-详情                                      |
| GET    | `/project/period/searchByName`           | 项目分期名称模糊搜索                               |
| POST   | `/project/period/start`                  | 项目分期-开始实施                                  |
| POST   | `/project/period/warranty`               | 项目分期-进入质保                                  |

### `/project/plan`（6）

| 方法   | 路径                        | 说明                  |
| ------ | --------------------------- | --------------------- |
| POST   | `/project/plan/add`         | 计划方案-添加         |
| DELETE | `/project/plan/delete`      | 计划方案-删除         |
| DELETE | `/project/plan/deleteBatch` | 计划方案-批量删除     |
| POST   | `/project/plan/edit`        | 计划方案-编辑         |
| GET    | `/project/plan/list`        | 计划方案-分页列表查询 |
| GET    | `/project/plan/queryById`   | 计划方案-详情         |

### `/project/materialPlan`（8）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/project/materialPlan/add` | 项目用料计划-添加 |
| POST | `/project/materialPlan/addBatch` | 项目用料计划-批量新增 |
| DELETE | `/project/materialPlan/delete` | 项目用料计划-删除 |
| DELETE | `/project/materialPlan/deleteBatch` | 项目用料计划-批量删除 |
| POST | `/project/materialPlan/edit` | 项目用料计划-编辑 |
| POST | `/project/materialPlan/editBatch` | 按分期全量同步用料计划；明细传 `id（已有时）/materialId/plannedQty/unitId/remark`，单位名称由后端写入 |
| GET | `/project/materialPlan/list` | 项目用料计划-分页列表查询 |
| GET | `/project/materialPlan/queryById` | 项目用料计划-详情 |

用料计划选料继续调用 `GET /stock/material/list` 按 `pageNo/pageSize` 分页；前端保留已加入计划的物料行并标记“已选择”，不再在分页结果中剔除这些行，添加时仍按物料 ID 去重。本次不改变接口和提交字段。

### `/project/materialApply`（补料申请）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/project/materialApply/list` | 按必填 `periodId` 查询补料申请单；每条父单包含 `items`；通用数值审批状态为 `-1=待提交、0=驳回、1=审核通过、2=待审批、3=已撤回` |
| GET | `/project/materialApply/queryById` | 按申请单 `id` 查询父单及 `items` 物料明细 |
| POST | `/project/materialApply/addBatch` | 提交一张补料单，传 `{ apply: { periodId, applyReason, remark? }, items: [...] }`；审批状态和申请人由后端生成 |
| POST | `/project/materialApply/approve` | 审批一张补料申请单，传父单 `id+approved`；驳回时 `approvalReason` 必填 |
| POST | `/project/materialApply/approveBatch` | 批量审批多张补料申请单，传父单 `ids+approved`；驳回时 `approvalReason` 必填，批量处理保持事务一致性 |

### `/sys/todo`（统一待办中心）

| 方法 | 路径                | 说明                                                                                     |
| ---- | ------------------- | ---------------------------------------------------------------------------------------- |
| GET  | `/sys/todo/page`    | 当前账号活动待办分页；支持 `scope=ASSIGNED/CLAIMABLE` 与 `todoType`，最大 `pageSize=100` |
| GET  | `/sys/todo/summary` | 当前账号待办汇总；返回 `total` 和按类型统计 `byType`                                     |

待办接口只返回任务导航信息，不直接办理业务；首页另外读取 `todo_type` 数据字典回显类型标签文案和颜色，字典值必须与接口 `todoType` 完全一致。前端保留原始 `todoType/bizId/bizSubId/actionKey/actionParams`。`PROJECT_INTERNAL_ACCEPTANCE/PROJECT_EXTERNAL_ACCEPTANCE` 打开项目详情验收标签；建议 `bizId=验收记录ID`、`bizSubId=periodId`，`actionParams` 包含 `periodId/acceptanceId/acceptType/reworkId`。同一账号、同一项目分期的成员邀请仍归并为一条。

### `/project/member`（当前批量接口）

| 方法   | 路径                            | 说明                                                                        |
| ------ | ------------------------------- | --------------------------------------------------------------------------- |
| POST   | `/project/member/add`           | 项目成员-单条添加                                                           |
| POST   | `/project/member/addBatch`      | 项目成员-批量新增；同一用户一条明细，多角色在 `memberRole` 中以英文逗号连接 |
| DELETE | `/project/member/delete`        | 项目成员-删除（已废弃）                                                     |
| DELETE | `/project/member/deleteBatch`   | 项目成员-批量删除                                                           |
| POST   | `/project/member/edit`          | 项目成员-编辑（已废弃）                                                     |
| POST   | `/project/member/editBatch`     | 项目成员-全量同步                                                           |
| GET    | `/project/member/list`          | 项目成员-分页列表查询；邀请详情按 `periodId` 查询并筛选当前用户角色         |
| GET    | `/project/member/myInvitations` | 当前登录用户待处理邀请；全局自动弹窗暂不挂载                                |
| POST   | `/project/member/respond`       | 被邀请人按成员记录 `memberId` 同意或拒绝                                    |
| GET    | `/project/member/queryById`     | 项目成员-详情                                                               |

### `/project/process`（实施计划）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/project/process/add` | 项目工序-添加（已废弃） |
| POST | `/project/process/addBatch` | 首次批量新增实施计划；第一层传 `periodId/plannedAcceptanceDate/plannedAcceptanceRemark/records` |
| DELETE | `/project/process/delete` | 项目工序-删除 |
| DELETE | `/project/process/deleteBatch` | 项目工序-批量删除 |
| POST | `/project/process/edit` | 项目工序-编辑 |
| POST | `/project/process/editBatch` | 已有实施计划按分期全量同步；请求结构同 `addBatch`，明细有 `id` 更新、无 `id` 新增 |
| GET | `/project/process/list` | 项目工序-分页列表查询（Apifox 已标记废弃；实施管理首页不再使用） |
| GET | `/project/process/queryById` | 项目工序-详情 |
| GET | `/project/process/detail` | 按必填 `periodId` 查询整份实施计划的验收日期、备注及全部工序；编辑计划方案实施页、“确认工序实施进度”抽屉和实施管理项目详情共同使用，`processName` 按 `work_type` 回显 |
| POST | `/project/process/status` | 更新单道工序状态，传必填 `{ processId, status }`；状态枚举为 `NOT_STARTED/IN_PROGRESS/COMPLETED` |

前端提交实施计划前按 `processName` 阻止同一分期重复工序，并要求每条明细的 `processName/siteLeaderId/plannedStartTime/plannedEndTime` 必填；计划完成时间不得早于计划开始时间。

### `/project/location`（当前批量接口）

| 方法   | 路径                            | 说明                  |
| ------ | ------------------------------- | --------------------- |
| POST   | `/project/location/add`         | 实施位置-添加         |
| DELETE | `/project/location/delete`      | 实施位置-删除         |
| DELETE | `/project/location/deleteBatch` | 实施位置-批量删除     |
| POST   | `/project/location/edit`        | 实施位置-编辑         |
| POST   | `/project/location/editBatch`   | 位置信息“保存本页”一次全量同步：顶层 `periodId + records`，有 ID 更新、无 ID 新增、未传旧行删除、空数组清空；行内增删改仅维护草稿 |
| GET    | `/project/location/list`        | 实施位置-分页列表查询 |
| GET    | `/project/location/queryById`   | 实施位置-详情         |

### `/project/memberOutsource`（计划外协配置）

| 方法 | 路径                                 | 说明                       |
| ---- | ------------------------------------ | -------------------------- |
| GET  | `/project/memberOutsource/list`      | 按项目分期查询外协配置     |
| POST | `/project/memberOutsource/editBatch` | 按项目分期全量同步外协配置 |

人员配置的外协单位选择器另调用 `GET /project/outsourcingUnit/list?status=0` 加载启用单位；选择后写入 `unitId/unitName/unitType/contactPerson/contactPhone`。前端以 `unitId` 为唯一键排除其他行已选单位，并在提交 `editBatch` 前阻止同一分期重复选择同一外协单位。

接口没有独立“是否需要外协”字段，前端以列表是否存在记录作为开关回显；选择“否”并保存时调用 `editBatch` 传 `{ periodId, records: [] }`，按接口全量同步语义清空该分期外协配置。选择“是”时前端提交前校验 `records` 至少 1 条，并要求每条 `headcount >= 1`、`workHours >= 0`。

### `/project/implementLog`（6）

| 方法   | 路径                                | 说明                                                                         |
| ------ | ----------------------------------- | ---------------------------------------------------------------------------- |
| POST   | `/project/implementLog/add`         | 实施记录-添加                                                                |
| DELETE | `/project/implementLog/delete`      | 实施记录-删除                                                                |
| DELETE | `/project/implementLog/deleteBatch` | 实施记录-批量删除                                                            |
| POST   | `/project/implementLog/edit`        | 实施记录-编辑                                                                |
| GET    | `/project/implementLog/list`        | 实施记录-分页列表查询；项目详情页沿用既有 `processId` 条件分别加载各工序日志 |
| GET    | `/project/implementLog/queryById`   | 实施记录-详情；页面按路由 `logId` 查询                                       |

### `/project/implementLogPerson`（6）

| 方法   | 路径                                      | 说明                      |
| ------ | ----------------------------------------- | ------------------------- |
| POST   | `/project/implementLogPerson/add`         | 实施参与人员-添加         |
| DELETE | `/project/implementLogPerson/delete`      | 实施参与人员-删除         |
| DELETE | `/project/implementLogPerson/deleteBatch` | 实施参与人员-批量删除     |
| POST   | `/project/implementLogPerson/edit`        | 实施参与人员-编辑         |
| GET    | `/project/implementLogPerson/list`        | 实施参与人员-分页列表查询 |
| GET    | `/project/implementLogPerson/queryById`   | 实施参与人员-详情         |

### `/project/acceptance`（统一验收）

| 方法   | 路径                              | 说明                  |
| ------ | --------------------------------- | --------------------- |
| POST   | `/project/acceptance/edit`        | 保存内部/外部当前验收资料；不创建或完成验收 |
| GET    | `/project/acceptance/list`        | 按 `periodId/acceptType/reworkId` 查询内部或外部验收 |
| GET    | `/project/acceptance/queryById`   | 内部/外部验收共用详情 |

### 验收流程与返工（当前 Apifox）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/project/acceptance/submit` | 已废弃，PC 已移除调用，不再提供手工提交入口 |
| POST | `/project/acceptance/start` | 开始验收，传 `periodId/acceptType(INTERNAL|CUSTOMER)/acceptStartDate?/reworkId?`；内部与外部可并行 |
| POST | `/project/acceptance/complete` | 完成验收，传 `periodId/acceptType/result(PASSED|FAILED)/remark?/reworkId?`；失败时 `remark` 必填，两项都通过后自动进入 `WARRANTY` |
| GET | `/project/rework/list` | 按必填 `periodId` 查询返工记录及审批、执行状态 |
| GET | `/project/rework/queryById` | 查询返工详情；返回状态动作必需的最新 `version` |
| POST | `/project/rework/add` | 保存返工草稿；每张返工单只传一个 `process` 对象，`materials` 只记录本轮额外领料量 |
| POST | `/project/rework/edit` | 编辑草稿、驳回或撤回的返工；必传 `id + version`，version 取详情最新值 |
| POST | `/project/rework/submit` | 提交返工审批；前端传 `periodId + reworkId + version`。periodId 按用户反馈补传，2026-09-16 刷新后的 Apifox ActionRequest 尚未列出，待契约同步及联调 |
| POST | `/project/rework/withdraw` | 撤回待审批返工；必传 `reworkId + version` |
| POST | `/project/rework/approve` | 返工审批；必传 `reworkId + version + approvalResult`，驳回时 `approvalComment` 必填 |
| GET | `/project/rework/materials` | 按 `reworkId` 查询本轮额外领料计划、已出库、已消耗及 `availableApplyQty` |

返工单 `version` 是乐观锁。前端编辑打开时读取详情；提交、撤回、审批，以及保存后紧接提交审批时，均在状态动作前再次调用详情接口，不能直接使用列表或旧弹窗缓存值。过期 version 由后端拒绝，防止覆盖他人刚完成的编辑或审批。

返工保存主体：

```json
{
  "id": "编辑时传",
  "version": 3,
  "periodId": "分期ID",
  "sourceAcceptanceId": "失败验收ID",
  "reason": "返工原因",
  "description": "返工说明",
  "process": {
    "processName": "返工工序",
    "siteLeaderId": "负责人ID",
    "plannedStartTime": "2026-09-08",
    "plannedEndTime": "2026-09-09",
    "plannedHours": 8,
    "remark": ""
  },
  "materials": [
    { "materialId": "物料ID", "unitId": "单位ID", "plannedQty": 2, "remark": "本轮额外领料" }
  ]
}
```

当前 Apifox `OaProjectAcceptance` 已声明 `reworkId/acceptStatus`，但 `OaProjectInternalAcceptance` 尚未声明这两个字段。前端保留旧数据兼容，后端需按既定返工轮次契约补齐后再完成真实账号联调。

### `/project/file`（当前批量接口）

| 方法   | 路径                        | 说明                    |
| ------ | --------------------------- | ----------------------- |
| POST   | `/project/file/add`         | 项目文件-添加（已废弃） |
| POST   | `/project/file/addBatch`    | 项目文件-批量新增       |
| DELETE | `/project/file/delete`      | 项目文件-删除（已废弃） |
| DELETE | `/project/file/deleteBatch` | 项目文件-批量删除       |
| POST   | `/project/file/edit`        | 项目文件-编辑           |
| POST   | `/project/file/editBatch`   | 项目文件-全量同步       |
| GET    | `/project/file/list`        | 项目文件-分页列表查询   |
| GET    | `/project/file/queryById`   | 项目文件-详情           |

### `/project/dynamic`（6）

| 方法   | 路径                           | 说明                  |
| ------ | ------------------------------ | --------------------- |
| POST   | `/project/dynamic/add`         | 项目动态-添加         |
| DELETE | `/project/dynamic/delete`      | 项目动态-删除         |
| DELETE | `/project/dynamic/deleteBatch` | 项目动态-批量删除     |
| POST   | `/project/dynamic/edit`        | 项目动态-编辑         |
| GET    | `/project/dynamic/list`        | 项目动态-分页列表查询 |
| GET    | `/project/dynamic/queryById`   | 项目动态-详情         |

### `/project/attendance`（6）

| 方法   | 路径                              | 说明                  |
| ------ | --------------------------------- | --------------------- |
| POST   | `/project/attendance/add`         | 施工打卡-添加         |
| DELETE | `/project/attendance/delete`      | 施工打卡-删除         |
| DELETE | `/project/attendance/deleteBatch` | 施工打卡-批量删除     |
| POST   | `/project/attendance/edit`        | 施工打卡-编辑         |
| GET    | `/project/attendance/list`        | 施工打卡-分页列表查询 |
| GET    | `/project/attendance/queryById`   | 施工打卡-详情         |

### `/project/delay`（8）

`OaProjectDelay.status` 使用统一五态：`-1=待提交`、`0=驳回`、`1=审核通过`、`2=待审批`、`3=已撤回`。2026-09-03 刷新的 Apifox 已明确新增固定为 `2`、仅 `2` 可通过或驳回，但 schema 枚举仍缺 `-1`，后端与 Apifox需补齐待提交态。

| 方法   | 路径                         | 说明                  |
| ------ | ---------------------------- | --------------------- |
| POST   | `/project/delay/add`         | 工期延期-添加         |
| POST   | `/project/delay/approve`     | 工期延期-审批通过     |
| DELETE | `/project/delay/delete`      | 工期延期-删除         |
| DELETE | `/project/delay/deleteBatch` | 工期延期-批量删除     |
| POST   | `/project/delay/edit`        | 工期延期-编辑         |
| GET    | `/project/delay/list`        | 工期延期-分页列表查询 |
| GET    | `/project/delay/queryById`   | 工期延期-详情         |
| POST   | `/project/delay/reject`      | 工期延期-审批驳回     |

### `/project/delayApproval`（6）

| 方法   | 路径                                 | 说明                      |
| ------ | ------------------------------------ | ------------------------- |
| POST   | `/project/delayApproval/add`         | 工期延期审批-添加         |
| DELETE | `/project/delayApproval/delete`      | 工期延期审批-删除         |
| DELETE | `/project/delayApproval/deleteBatch` | 工期延期审批-批量删除     |
| POST   | `/project/delayApproval/edit`        | 工期延期审批-编辑         |
| GET    | `/project/delayApproval/list`        | 工期延期审批-分页列表查询 |
| GET    | `/project/delayApproval/queryById`   | 工期延期审批-详情         |

### `/project/contract`（当前合同页使用组合新增、详情、编辑与审批）

| 方法   | 路径                                       | 说明                                                       |
| ------ | ------------------------------------------ | ---------------------------------------------------------- |
| POST   | `/project/contract/add`                    | 合同管理-添加                                              |
| POST   | `/project/contract/addWithPaymentRecords`  | 合同、合同文件元数据与回款计划事务新增                     |
| POST   | `/project/contract/editWithPaymentRecords` | 按 `periodId` 差量修改合同、替换可选文件并全量同步回款计划 |
| DELETE | `/project/contract/delete`                 | 合同管理-删除                                              |
| DELETE | `/project/contract/deleteBatch`            | 合同管理-批量删除                                          |
| POST   | `/project/contract/edit`                   | 合同管理-编辑（已废弃，前端无调用）                        |
| GET    | `/project/contract/list`                   | 合同管理-分页列表查询                                      |
| GET    | `/project/contract/queryById`              | 合同管理-详情                                              |
| POST   | `/project/contract/status`                 | 合同审批状态变更                                           |

### `/project/paymentRecord`（合同页当前使用 4 个）

| 方法   | 路径                                 | 说明                           |
| ------ | ------------------------------------ | ------------------------------ |
| GET    | `/project/paymentRecord/list`        | 按 `periodId` 分页查询回款计划 |
| POST   | `/project/paymentRecord/addBatch`    | 项目回款计划-批量新增          |
| POST   | `/project/paymentRecord/editBatch`   | 项目回款计划-全量同步          |
| DELETE | `/project/paymentRecord/deleteBatch` | 项目回款计划-批量删除          |

**合同信息页调用约定**：

- 页面将后端 `contractAmount` 字段显示为“项目金额”，并将 `contractAmount`、`warrantyPeriod` 设为必填；`warrantyPeriod` 只接受大于等于 0 的整数，提交字段名不变。
- 新增 `mode=create`：按 `periodId` 调用项目详情用于顶部基本信息回显；不调用合同列表、合同详情或合同明细列表。
- 查看/编辑：合同详情接口仅接受必填 `periodId`，返回 `contract/contractFile/materialFile/records`；同时调用 `/project/file/list` 读取 `CONTRACT_ATTACHMENT` 统一附件，原两个文件位仅作历史兼容。合同信息页不再额外调用回款计划列表，也不得传合同 `id`。
- 合同只读详情使用 `contract_type` 映射 `contractType`、使用 `payback_node` 映射回款计划的 `paymentNode`；`projectManagerUserName` 为空时按 `projectManagerUserId` 从 `/sys/user/list` 补齐。附件字段存在路径时通过静态文件访问地址提供预览。
- 合同附件每次只选一个并立即调用 `/sys/common/upload`，以 `biz=periodId` 上传；成功响应的 `message` 原始路径用于预览和后续关联，数量必须为 1～3 个。合同组合请求只提交合同与回款 `data`，不再传 `attachment/materialAttachment`；组合保存成功后调用 `/project/file/addBatch` 写入 `CONTRACT_ATTACHMENT`，编辑删除调用 `/project/file/deleteBatch`。
- 组合新增的 `records` 元素使用 `paymentNode/ratio/plannedAmount`，不带 `periodId/id/contractId`，至少提交一条；后端任一步骤失败时整体回滚。
- 已有合同重提调用 `/project/contract/editWithPaymentRecords`：`data.contract.periodId` 必传，其他合同字段与详情原值比较后只传变化项；`records` 为全量列表，带 `id` 更新、不带 `id` 新增，未提交的旧记录逻辑删除。接口成功后合同状态固定为 `2`。
- 新增合同在组合请求的 `contract.status` 中直接传 `2`（待审批）。统一合同信息页内审批：驳回传 `{ periodId, status: '0', approvalReason }`；通过时当前仍需调用合同编辑接口保存项目经理，再传 `{ periodId, status: '1' }` 并推进分期到 `PREPARING`；原提交人撤回传 `{ periodId, status: '3' }`。
- 项目列表通过 `contractStatus` 控制统一入口：合同已存在且未通过时显示“合同信息”，通过后隐藏；合同信息页仅在状态 `2` 且拥有 `project:contract:approve` 时显示审批区域。
- 项目列表对接人列优先读取 `projectLiaisonUserName`；姓名快照为空时使用 `projectLiaisonUserId` 从 `/sys/user/list` 补齐，仍无法识别的行按 `periodId` 调 `GET /project/project/projectPeriodDetail` 补查对接人字段。
- 新增项目与已有主项目新增分期统一调用 `POST /project/project/addProjectPeriod`，编辑调用 `POST /project/project/editProjectPeriod`；均传 `multipart/form-data`，其中 `data` 是合法 JSON 字符串，`attachment` 是单个可选文件。前端不预调用 `/sys/common/upload`，不在 `data` 中传 `attachmentFileId`；编辑未传新附件时保留旧文件。
- 当前 `defHttp` 对非 GET 请求会将 `params` 移入 body，项目 multipart 请求需从 `params` 传入完整 FormData；若直接设置 `data=FormData`，其 `Object.keys(data)` 判断会误判为空并丢失 multipart parts。
- 前端通用数值审批规则为 `-1=待提交`、`0=驳回`、`1=审核通过`、`2=待审批`、`3=已撤回`，其他异常值按待提交兜底；Apifox 中仍使用旧说明的模型字段需要后端同步契约。
- 原提交人可撤回待审批合同，并可修改待提交、驳回或已撤回合同；驳回只读页与编辑页展示 `approvalReason`，重新提交后状态改为 `2` 并清空旧驳回原因。
- `mock/demo/project.ts` 与 `mock/demo/contract.ts` 已删除，这些接口均需真实后端。

## 二、实体字段（后端返回）

### `acceptance`

| 字段                     | 说明(依据接口返回) |
| ------------------------ | ------------------ |
| `id`                     |                    |
| `periodId`               |                    |
| `acceptDate`             |                    |
| `implementCompleteDate`  |                    |
| `acceptUnitLeader`       |                    |
| `acceptUnitPhone`        |                    |
| `acceptLeaderId`         |                    |
| `acceptLeaderName`       |                    |
| `completionReportFileId` |                    |
| `acceptanceFormFileId`   |                    |
| `result`                 |                    |
| `remark`                 |                    |
| `createBy`               |                    |
| `createTime`             |                    |
| `updateBy`               |                    |
| `updateTime`             |                    |
| `delFlag`                |                    |

### `attendance`

| 字段             | 说明(依据接口返回) |
| ---------------- | ------------------ |
| `id`             |                    |
| `periodId`       |                    |
| `processId`      |                    |
| `userId`         |                    |
| `userName`       |                    |
| `sourceType`     |                    |
| `relatedNo`      |                    |
| `attendanceDate` |                    |
| `signInTime`     |                    |
| `signOutTime`    |                    |
| `workHours`      |                    |
| `workContent`    |                    |
| `locationName`   |                    |
| `longitude`      |                    |
| `latitude`       |                    |
| `photoFileId`    |                    |
| `status`         |                    |
| `remark`         |                    |
| `createBy`       |                    |
| `createTime`     |                    |
| `updateBy`       |                    |
| `updateTime`     |                    |
| `delFlag`        |                    |

### `contract`

| 字段                  | 说明(依据接口返回)                                                           |
| --------------------- | ---------------------------------------------------------------------------- |
| `id`                  |                                                                              |
| `contractNo`          |                                                                              |
| `projectId`           |                                                                              |
| `periodId`            |                                                                              |
| `contractName`        |                                                                              |
| `contractType`        |                                                                              |
| `contractSignedDate`  |                                                                              |
| `contractAmount`      |                                                                              |
| `plannedDeliveryDate` | 计划交付日期；合同新增、修改重新提交时前端必填，历史缺失记录不得直接审批通过 |
| `actualDeliveryDate`  |                                                                              |
| `warrantyPeriod`      |                                                                              |
| `warrantyEndDate`     |                                                                              |
| `receivedAmount`      |                                                                              |
| `contractFileId`      |                                                                              |
| `status`              |                                                                              |
| `remark`              |                                                                              |
| `createBy`            |                                                                              |
| `createTime`          |                                                                              |
| `updateBy`            |                                                                              |
| `updateTime`          |                                                                              |
| `delFlag`             |                                                                              |

### `contractItem`

| 字段            | 说明(依据接口返回) |
| --------------- | ------------------ |
| `id`            |                    |
| `contractId`    |                    |
| `itemName`      |                    |
| `specification` |                    |
| `quantity`      |                    |
| `unit`          |                    |
| `unitPrice`     |                    |
| `amount`        |                    |
| `remark`        |                    |
| `createBy`      |                    |
| `createTime`    |                    |
| `updateBy`      |                    |
| `updateTime`    |                    |
| `delFlag`       |                    |

### `paymentRecord`

| 字段            | 说明                                                             |
| --------------- | ---------------------------------------------------------------- |
| `id`            | 回款计划 ID（编辑全量同步时使用）                                |
| `periodId`      | 项目分期 ID（批量请求仅放在外层）                                |
| `paymentNode`   | 回款节点                                                         |
| `ratio`         | 回款比例                                                         |
| `plannedAmount` | 计划回款金额                                                     |
| `actualAmount`  | 实际回款金额                                                     |
| `plannedDate`   | 计划回款日期，`yyyy-MM-dd`                                       |
| `paymentDate`   | 实际回款日期，`yyyy-MM-dd`                                       |
| `invoiceStatus` | 开票状态                                                         |
| `payStatus`     | 回款状态                                                         |
| `payMethod`     | 回款方式                                                         |
| `rollbackTime`  | 回款周期（新增必填且为大于等于 0 的整数；旧记录缺失时按 7 回显） |
| `remark`        | 备注                                                             |

### `delay`

| 字段              | 说明(依据接口返回)         |
| ----------------- | -------------------------- |
| `id`              |                            |
| `projectId`       |                            |
| `periodId`        |                            |
| `processId`       |                            |
| `applyNo`         |                            |
| `delayReason`     |                            |
| `originalEndTime` |                            |
| `expectedEndTime` |                            |
| `delayDays`       |                            |
| `status`          | 延期审批：`0=驳回/1=通过/2=待审批/3=撤回` |
| `applyUserId`     |                            |
| `applyUserName`   |                            |
| `remark`          |                            |
| `createBy`        |                            |
| `createTime`      |                            |
| `updateBy`        |                            |
| `updateTime`      |                            |
| `delFlag`         |                            |

### `delayApproval`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `delayId`          |                    |
| `nodeName`         |                    |
| `approvalUserId`   |                    |
| `approvalUserName` |                    |
| `approvalResult`   |                    |
| `approvalComment`  |                    |
| `approvalTime`     |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `dynamic`

| 字段            | 说明(依据接口返回) |
| --------------- | ------------------ |
| `id`            |                    |
| `projectId`     |                    |
| `periodId`      |                    |
| `actionType`    |                    |
| `actionContent` |                    |
| `operatorId`    |                    |
| `operatorName`  |                    |
| `operateTime`   |                    |
| `createBy`      |                    |
| `createTime`    |                    |
| `updateBy`      |                    |
| `updateTime`    |                    |
| `delFlag`       |                    |

### `file`

| 字段         | 说明(依据接口返回) |
| ------------ | ------------------ |
| `id`         |                    |
| `periodId`   |                    |
| `fileType`   |                    |
| `fileId`     |                    |
| `fileName`   |                    |
| `remark`     |                    |
| `createBy`   |                    |
| `createTime` |                    |
| `updateBy`   |                    |
| `updateTime` |                    |
| `delFlag`    |                    |

### `implementLog`

| 字段                | 说明(依据接口返回) |
| ------------------- | ------------------ |
| `id`                |                    |
| `periodId`          |                    |
| `processId`         |                    |
| `logDate`           |                    |
| `submitterId`       |                    |
| `submitterName`     |                    |
| `workContent`       |                    |
| `locationId`        |                    |
| `hours`             |                    |
| `vehicleNo`         |                    |
| `mileage`           |                    |
| `signInTime`        |                    |
| `signOutTime`       |                    |
| `outsourcingUnit`   |                    |
| `outsourcingPeople` |                    |
| `outsourcingHours`  |                    |
| `photos`            |                    |
| `remark`            |                    |
| `createBy`          |                    |
| `createTime`        |                    |
| `updateBy`          |                    |
| `updateTime`        |                    |
| `delFlag`           |                    |

### `implementLogPerson`

| 字段          | 说明(依据接口返回) |
| ------------- | ------------------ |
| `id`          |                    |
| `logId`       |                    |
| `userId`      |                    |
| `userName`    |                    |
| `signInTime`  |                    |
| `signOutTime` |                    |
| `workContent` |                    |
| `score`       |                    |
| `createBy`    |                    |
| `createTime`  |                    |
| `updateBy`    |                    |
| `updateTime`  |                    |
| `delFlag`     |                    |

### `location`

| 字段           | 说明(依据接口返回) |
| -------------- | ------------------ |
| `id`           |                    |
| `periodId`     |                    |
| `locationName` |                    |
| `longitude`    |                    |
| `latitude`     |                    |
| `description`  |                    |
| `createBy`     |                    |
| `createTime`   |                    |
| `updateBy`     |                    |
| `updateTime`   |                    |
| `delFlag`      |                    |

### `materialPlan`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `periodId`         |                    |
| `materialId`       |                    |
| `materialCategory` |                    |
| `materialName`     |                    |
| `brand`            |                    |
| `model`            |                    |
| `unit`             |                    |
| `supplierId`       |                    |
| `supplierName`     |                    |
| `purchaseQty`      |                    |
| `plannedQty`       |                    |
| `actualQty`        |                    |
| `purchaseStatus`   |                    |
| `expressNo`        |                    |
| `remark`           |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `member`

| 字段              | 说明(依据接口返回) |
| ----------------- | ------------------ |
| `id`              |                    |
| `periodId`        |                    |
| `userId`          |                    |
| `userName`        |                    |
| `memberRole`      |                    |
| `outsourcingFlag` |                    |
| `outsourcingUnit` |                    |
| `remark`          |                    |
| `createBy`        |                    |
| `createTime`      |                    |
| `updateBy`        |                    |
| `updateTime`      |                    |
| `delFlag`         |                    |

### `period`

> 注：下表为分期实体的旧有字段清单；项目管理合并新增/编辑/详情接口已使用 `projectLiaisonUserId/projectLiaisonUserName` 表示“项目对接人”，前端不再将 `projectLeaderId/projectLeaderName` 当作对接人。

| 字段                      | 说明(依据接口返回) |
| ------------------------- | ------------------ |
| `id`                      |                    |
| `projectId`               |                    |
| `periodNo`                |                    |
| `periodName`              |                    |
| `periodSort`              |                    |
| `projectType`             |                    |
| `projectLeaderId`         |                    |
| `projectLeaderName`       |                    |
| `projectAddress`          |                    |
| `projectRequirement`      |                    |
| `involvedProducts`        |                    |
| `businessAttribute`       |                    |
| `startDate`               |                    |
| `status`                  |                    |
| `totalProgress`           |                    |
| `attachmentFileId`        |                    |
| `remark`                  |                    |
| `createBy`                |                    |
| `createTime`              |                    |
| `updateBy`                |                    |
| `updateTime`              |                    |
| `delFlag`                 |                    |
| `projectLeaderDepartId`   |                    |
| `projectLeaderDepartName` |                    |

### `plan`

| 字段                | 说明(依据接口返回) |
| ------------------- | ------------------ |
| `id`                |                    |
| `periodId`          |                    |
| `planName`          |                    |
| `planType`          |                    |
| `planFileId`        |                    |
| `planStartTime`     |                    |
| `planEndTime`       |                    |
| `plannedTotalHours` |                    |
| `status`            |                    |
| `remark`            |                    |
| `createBy`          |                    |
| `createTime`        |                    |
| `updateBy`          |                    |
| `updateTime`        |                    |
| `delFlag`           |                    |

### `process`

| 字段               | 说明(依据接口返回) |
| ------------------ | ------------------ |
| `id`               |                    |
| `periodId`         |                    |
| `processName`      |                    |
| `siteLeaderId`     |                    |
| `siteLeaderName`   |                    |
| `plannedStartTime` |                    |
| `plannedEndTime`   |                    |
| `plannedHours`     |                    |
| `actualStartTime`  |                    |
| `actualEndTime`    |                    |
| `status`           |                    |
| `sortNo`           |                    |
| `remark`           |                    |
| `createBy`         |                    |
| `createTime`       |                    |
| `updateBy`         |                    |
| `updateTime`       |                    |
| `delFlag`          |                    |

### `project`

| 字段          | 说明(依据接口返回) |
| ------------- | ------------------ |
| `id`          |                    |
| `projectNo`   |                    |
| `projectName` |                    |
| `customerId`  |                    |
| `remark`      |                    |
| `createBy`    |                    |
| `createTime`  |                    |
| `updateBy`    |                    |
| `updateTime`  |                    |
| `delFlag`     |                    |

## 三、前端页面 ↔ 后端接口 对照

| 前端页面 | 后端接口 | 说明 |
| --- | --- | --- |
| 项目管理列表 [project/index.vue](src/views/project/index.vue) | `GET /project/project/projectPeriodList` | 主项目+分期合并行；列表不展示接口返回的 `projectNo`，搜索 projectName/periodName/customerName/projectType/status |
| 项目补料统一抽屉 [MaterialSupplementDrawer.vue](src/views/project/components/MaterialSupplementDrawer.vue) | 当前计划 `GET /project/materialPlan/list`；补料父单 `GET /project/materialApply/list`；展开明细 `GET /project/materialApply/queryById`；提交 `POST /project/materialApply/addBatch`；单张审批 `POST /project/materialApply/approve`；多张审批 `POST /project/materialApply/approveBatch` | `IMPLEMENTING/DEBUGGING`（兼容历史 `DEBUG_COMPLETED`）时在“更多”只显示“补料管理”；同一抽屉按权限展示我的申请、待我审批和审批记录，待办入口定位父单 `bizId`。审批按钮受 `project:materialApply:approve` 控制，状态按统一 `approval_status` 判断；列表接口当前只有 `periodId`，页面本人过滤不能替代后端数据范围 |
| 新增/编辑项目 [ProjectApply.vue](src/views/project/apply/ProjectApply.vue) | 新建主项目+首期或已有主项目新增期均调 `POST /project/project/addProjectPeriod`；编辑调 `POST /project/project/editProjectPeriod`；两者均 JSON 提交；附件先公共上传，路径传 attachmentFileId；回显 `GET /project/project/projectPeriodDetail`(periodId) | `parentProjectId` 为空时新建主项目；项目对接人字段 `projectLiaisonUserId/projectLiaisonUserName`，新增默认当前用户；创建后仅该对接人可修改 |
| 删除分期 | `DELETE /project/project/deleteProjectPeriod`(periodId) | 后端仅允许删「期」 |
| 状态流转（列表/详情/合同审批） | `POST /project/period/status`（`{ periodId, status }`） | 合同审批通过并指定项目经理后进入 `PREPARING`；“更多 → 关闭项目”在 `project:close` 权限下提交 `CLOSED`，后端同步终止未完成项目领料；合同仅提交不推进项目 |
| 详情-基本信息 [DetailBasic.vue](src/views/project/detail/components/DetailBasic.vue) | `GET /project/project/projectPeriodDetail` | 主项目+分期合并字段 |
| 详情-合同信息 [DetailContract.vue](src/views/project/detail/components/DetailContract.vue) | `GET /project/contract/queryById`（必填 `periodId`）+ `GET /project/file/list` | 展示合同、回款与 `CONTRACT_ATTACHMENT` 统一附件并逐个站内预览；无统一记录时兼容原 `contractFile/materialFile` |
| 详情-项目成员 [DetailMember.vue](src/views/project/detail/components/DetailMember.vue) | 参与人员 `GET /project/member/list`（`periodId`）；邀请用户 `GET /sys/user/list`、角色字典 `member_role`、提交 `POST /project/member/addBatch`；外协人员 `GET /project/memberOutsource/list`（`periodId`） | 两类数据分区展示；参与人员按用户合并单角色多行或逗号分隔角色，可手动刷新并继续邀请。邀请过滤角色 `0/1/2`、跳过已有成员角色组合，成功后重查参与人员；外协展示 `unitName/unitType/headcount/workHours/contactPerson/contactPhone`，接口失败互不阻断 |
| 详情-实施位置 [DetailPosition.vue](src/views/project/detail/components/DetailPosition.vue) | `GET /project/location/list`；增 `POST /project/location/add`；改 `POST /project/location/edit`；删 `DELETE /project/location/delete` | project_location 实体 |
| 详情-实施记录 [DetailImplement.vue](src/views/project/detail/components/DetailImplement.vue) | `GET /project/implementLog/list`(periodId)；详情 `GET /project/implementLog/queryById` | project_implement_log 实体 |
| 详情-验收记录 [DetailAcceptance.vue](src/views/project/detail/components/DetailAcceptance.vue) | 提交 `POST /project/acceptance/submit`；资料统一 `GET/POST /project/acceptance/list|edit`；开始/完成 `POST /project/acceptance/start|complete`；均以 `acceptType=INTERNAL/CUSTOMER` 区分；返工 `GET/POST /project/rework/*` | 工序全完成后端自动进入 `PENDING_ACCEPT`；提交后进入 `ACCEPTING` 并生成内部/外部待办。内部只对运维部门经理开放，外部只对本项目经理开放，两者均须上传 `completionReportFileId`；两项通过自动质保，失败可返工 |
| 返工领料 [material/pick/index.vue](src/views/material/pick/index.vue) | `GET /project/rework/materials?reworkId=...`；提交 `POST /stock/apply/add|edit` 携带 `periodId + reworkId` | 只展示本轮额外领料计划中 `availableApplyQty>0` 的物料，不混入首次计划 |
| 详情-项目文件 [DetailFile.vue](src/views/project/detail/components/DetailFile.vue) | 文件先调 `POST /sys/common/upload`，multipart 传 `file` 与 `biz=periodId`；列表 `GET /project/file/list`；新增 `POST /project/file/addBatch`，顶层传 `periodId`、明细传 `records`；删除 `DELETE /project/file/deleteBatch?ids=...` | 所有详情访问者均显示上传入口；上传响应 `message` 写入 `fileId`，按钮提供加载防重，新增成功后刷新列表；详情删除仅项目对接人或项目经理可见 |
| 编辑计划-方案文件 [PlanFileMgmt.vue](src/views/project/plan/PlanFileMgmt.vue) | 列表 `GET /project/plan/list`（前端传 `periodId`，当前 Apifox 尚未声明该参数）；保存本页：新增记录 multipart `POST /project/plan/addBatch`（`data.periodId + records[{plan, attachmentIndex}]` 与 `attachments`）；已有修改 `POST /project/plan/edit`；删除 `DELETE /project/plan/deleteBatch` | 父上下文成功且首次进入页签后才查询；请求序号阻止旧响应覆盖，列表失败前禁止新增、修改、删除与保存。后端需补齐 `periodId` 契约并保证按分期过滤；列表仅取 `project_plan` 并排除 `CONTRACT_MATERIAL_DRAFT`；序号按当前行位置从 1 连续显示；移除行先暂存，保存本页最后批量删除；新增/修改均为 multipart，全部完成回查后只读；部分失败保留未完成草稿，整页不保证事务原子性；提交审批前至少已有一条已保存文件且不存在未保存行 |
| 详情-用料统计 [DetailMaterialAccount.vue](src/views/project/detail/components/DetailMaterialAccount.vue) | `GET /project/materialAccount/page`（`periodId` 必填，`keyword/pageNo/pageSize` 可选） | 项目物料总账服务端分页；主表展示计划/出库/消耗/待处置/成本，展开查看申请、归还、库存和金额。原 `DetailMaterial.vue + /project/materialPlan/list` 仅保留给补料计划预览 |
| 详情-项目动态(右栏) | `GET /project/dynamic/list`（`periodId`） | 按接口倒序展示 `actionContent/operatorName/operateTime`，右栏限制高度并独立滚动 |
| 计划页基本信息抽屉 [ProjectBasicDrawer.vue](src/views/project/components/ProjectBasicDrawer.vue) | 计划页直接复用父级已读取的 `GET /project/project/projectPeriodDetail` 结果；其他入口仍可按 `periodId` 查询 | “编辑/查看计划方案”页面标题旁的详情按钮打开；展示主项目、分期与对接、实施基本信息，不重复查分期详情，不改变路由 |
| 编辑计划方案 [ProjectPlan.vue](src/views/project/plan/ProjectPlan.vue) | 父上下文：并行 `GET /project/project/projectPeriodDetail` + `GET /project/contract/queryById`；实施读取 `GET /project/process/detail`；方案文件保存本页：新增 `POST /project/plan/addBatch`、修改 `POST /project/plan/edit`、删除 `DELETE /project/plan/deleteBatch`；用料 `POST /project/materialPlan/editBatch`；外协 `POST /project/memberOutsource/editBatch`；工序首次 `POST /project/process/addBatch`、已有记录 `POST /project/process/editBatch`；位置 `POST /project/location/editBatch`；成员 `GET /project/member/list`、系统用户 `GET /sys/user/list`、多角色邀请 `POST /project/member/addBatch`、删除 `DELETE /project/member/deleteBatch`；提交审批 `POST /project/period/status`；物料及单位 `GET /stock/material/list` | 父上下文两项均成功后才挂载六个页签；同路由切换 `periodId` 使旧响应失效。全量同步页在首次查询成功前禁写，保存后回查失败不报“已保存”。用料明细传 `id（已有时）/materialId/plannedQty/unitId/remark`。提交与审批通过前都逐一检查六个页签已访问、业务主数据加载成功、必填校验通过且无保存中或未保存数据；状态请求中整页禁写 |
| 首页待办中心 [TodoCenterCard.vue](src/views/todo/components/TodoCenterCard.vue) | 列表 `GET /sys/todo/page`；汇总 `GET /sys/todo/summary`；类型字典 `/sys/dict/getDictItems/todo_type`；办理继续调用来源业务接口 | 不配置独立待办页面。类型标签按 `todo_type` 回显；内部/外部验收待办按 `bizSubId/actionParams.periodId` 打开项目详情验收标签，其他类型仍按原始业务字段分派。`/sys/todo/*` 不直接办理业务 |
| 编辑计划方案-回款计划 [PlanPayment.vue](src/views/project/plan/PlanPayment.vue) | 复用父页面合同及 `records`，另调 `GET /project/file/list` 读取统一合同附件；字典 `contract_type/payback_node`；姓名快照缺失时按需查询用户与成员 | 只读展示当前分期合同及 1～3 个合同附件，逐个站内预览；原两个文件位作为历史兜底 |
| 计划审批详情 [PlanAuditModal.vue](src/views/project/components/PlanAuditModal.vue) / [ProjectPlan.vue](src/views/project/plan/ProjectPlan.vue) | 查看详情跳转 `/project/plan?mode=view&audit=1&periodId=...`，六类详情复用计划页现有查询；审批流转 `POST /project/period/status` | 只读页同时要求生命周期 `PENDING_APPROVAL`、分期 `approvalStatus=2` 与 `project:plan:audit` 才显示办理按钮；点击通过前仍须逐页签完成加载与校验，通过传 `IMPLEMENTING`，驳回传 `PREPARING`，完成后返回列表并刷新待办。当前接口契约仅有 `periodId/status`，无法持久化驳回原因，页面明示警告该限制，需后端扩展字段或提供专用计划审批接口 |
| 实施完成 [ProcessCompletionDrawer.vue](src/views/project/components/ProcessCompletionDrawer.vue) | 工序 `GET /project/process/detail?periodId=...`；完成 `POST /project/process/status`；日志 `GET /project/implementLog/list?processId=...` | `IMPLEMENTING/DEBUGGING`（兼容 `DEBUG_COMPLETED`）仅在项目列表显示，项目详情不再挂载该抽屉；单选未开始/进行中工序，二次确认后只提交 `{ processId, status: 'COMPLETED' }`，最后一道工序完成后的项目状态由后端推进；抽屉详情跳转当前项目分期实施日志页并自动展开所选工序 |
| 确认到货 [ArrivalConfirmModal.vue](src/views/project/components/ArrivalConfirmModal.vue) | 合同详情 `GET /project/contract/queryById?periodId=...`、附件 `GET /project/file/list`；确认 `POST /project/period/arrivalStatus` | 弹窗读取“到货款”计算预计日期并展示全部统一合同附件；确认仍只传 `{ periodId, arrivalStatus: 1 }` |
| 合同信息/审批 [project/contract/index.vue](src/views/project/contract/index.vue) | 文件 `POST /sys/common/upload` + `GET /project/file/list` + `POST /project/file/addBatch` + `DELETE /project/file/deleteBatch`；合同 `queryById/addWithPaymentRecords/editWithPaymentRecords/status` | 合同附件逐个上传、立即预览、1～3 个，路径以 `CONTRACT_ATTACHMENT` 关联；合同字段差量提交、回款全量同步，历史两个文件位兼容回显 |
| 外协维护 [views/resource/outsourcing](src/views/resource/outsourcing/) | `GET /project/outsourcing/list` + `POST add/edit` + `DELETE delete/deleteBatch` | ⚠️ 接口为推断(仿 /project/supplier)，后端待建/确认；人员配置页「外协单位」下拉数据源 |
| 计划方案管理 [views/plan](src/views/plan/) | 入口 `GET /project/project/projectPeriodList`；方案 `GET /project/plan/list`；用料 `GET /project/materialPlan/list`；位置 `GET /project/location/list` | 独立菜单；入口列表不展示接口返回的 `projectNo` 列 |
| 报价管理 [material-draft](src/views/plan/material-draft/) | 分期 `GET /project/project/projectPeriodList`；候选主表 `GET /project/materialCandidate/list`、`POST /project/materialCandidate/add\|edit`；候选明细 `GET /project/materialCandidateItem/list`、`POST /project/materialCandidateItem/addBatch\|editBatch`；物料及单位 `GET /stock/material/list`；计划保存 `POST /project/materialPlan/editBatch` | 报价表格不展示 `periodId` 列，但搜索、路由和业务请求继续使用该字段定位分期。候选主表列表必须传 `periodId`，页面按分期聚合；新增主表只传 `periodId/candidateName`，兼容返回对象、直接 ID 和按唯一名称反查三种取 ID 方式，再以 `candidateId` 新增明细；修改按 `candidateId` 全量同步。新增时先完成主表和明细保存，再切换到带 `candidateId` 的编辑 URL；页面按完整 URL 重建时携带并回显本次保存快照，使旧查询失效，候选列表短暂未返回主表时也不退出。普通修改保存优先使用 `addBatch/editBatch` 返回的明细数组刷新页面；未返回数组时按 `candidateId` 重新查询，查询暂无数据或失败时保留本次提交快照。明细传 `materialId/quantity/unitId/remark`。状态直接读取 `project_material_candidate_status` 的 `-1/0/1/2/3/4`；删除转作废状态 `4`。隐藏页支持新增、修改、只读查看。导出和 `.xlsx` 解析均在前端完成；首次空计划自动读取当前 `periodId` 唯一状态 `3` 的已采用报价及全部明细，去掉候选明细 `id` 后作为计划草稿（已有计划不覆盖），最终仍由 `/project/materialPlan/editBatch` 保存；Apifox 无专用导入/导出接口 |
| 实施管理 [views/implement](src/views/implement/) | 项目分期 `GET /project/project/projectPeriodList`；项目详情 `GET /project/project/projectPeriodDetail`；全部工序 `GET /project/process/detail`；实施日志 `GET /project/implementLog/list`（`processId`）；日志详情 `GET /project/implementLog/queryById` | 独立菜单首页每个项目分期一行且不展示接口返回的 `projectNo`；详情以 `periodId` 按工序分组并懒加载分页日志，工序名称按 `work_type`、工序状态按 `project_process_status` 回显 |
| 回款管理 [views/payment](src/views/payment/) | 合同 `GET /project/contract/list` + `POST add` + `GET queryById`（`POST edit` 已废弃）；回款计划 `GET /project/paymentRecord/list` + `POST addBatch/editBatch` | 回款计划按 `periodId` 维护，批量请求使用 `{ periodId, records }` |

## 四、对接要点 / 待确认

1. **状态码**：`project_period_status` 仅表示项目生命周期；计划审批结果另读 `approvalStatus` 五态，统一字典编码为 `approval_status`（`-1/0/1/2/3`）。当前前端通过 `POST /project/period/status` 传 `{ periodId, status }`，计划提交、审批通过和驳回分别传生命周期 `PENDING_APPROVAL`、`IMPLEMENTING`、`PREPARING`。2026-09-03 的 Apifox `ProjectPeriodDetailResult` 尚未声明 `approvalStatus`，需后端和文档补齐该返回字段。
2. **项目文件路径**：项目基本信息附件由 `addProjectPeriod/editProjectPeriod` 组合提交；合同双文件由 `addWithPaymentRecords/editWithPaymentRecords` 组合提交；方案、详情和验收等仍使用统一上传的页面，仅在成功响应有效时读取路径。公共上传要求 `success=true`、`code` 为 `0` 或 `200`（兼容对应字符串），从非空 `message` 读取文件路径。2026-09-13 浏览器实际响应为 `code=0`，不可只接受 `200`；相对路径通过 `/sys/common/static/` 读取预览，业务字段仍保存上传返回路径。
3. **删除主项目**：后端仅提供删除分期，主项目删除/批量删除未接入。
4. **回款计划**：合同信息页从合同组合详情的 `records` 读取；新增/修改均随合同组合请求提交全量列表，页面节点、金额、回款周期分别映射为 `paymentNode/plannedAmount/rollbackTime`。
5. **standalone 计划/实施/回款模块**：列表已对齐后端，详情页(方案详情/日志详情/合同回款)为设计稿结构，后续按后端实体重构。


### 计划撤回接口待补充（2026-09-09）

当前 Apifox 无计划撤回接口；`POST /project/period/status` 仅声明 `periodId/status`，未定义撤回语义。前端暂不将回退 `PREPARING` 当作撤回。后端需明确：仅当前计划提交人可撤回待审批计划；原子校验未被审批、关闭对应待办、记录撤回人及时间、将审批状态设为已撤回并恢复筹备阶段编辑能力。需返回或暴露提交人及可撤回状态，支持前端显示入口；已审批或并发审批成功后拒绝撤回。具体路径、参数及状态协议以后端补充的 Apifox 为准。


### 项目审批待办入口（2026-09-09）

`PROJECT_PERIOD_APPROVAL`（todoType 或 actionKey）优先映射现有计划审批弹窗，与合同审批分开；分期 ID 优先读取 actionParams，缺失时使用该类型的 bizId。该类型接收权限为 `project:period:approve`；本次只接通办理入口，沿用原计划审批业务接口，不新增权限或接口。


### 计划审批弹窗详情回显（2026-09-09）

计划审批弹窗每次打开按 `periodId` 调用 `/project/project/projectPeriodDetail`，项目名称、分期名称和生命周期状态均从真实详情回显，不再读取待办 title/summary 或假定状态；加载失败提供重试且禁用查看/审批，关闭或切换时旧响应失效。 后端尚未返回 approvalStatus 时，仅生命周期为 PENDING_APPROVAL 才推导待审批，其他状态不开放审批。


工序日志列表（2026-09-11）：按“实施日期、提交人、签到时间、签退时间、工时、实施位置、实施内容”排列，保留详情操作。实施日期取 signInTime 的日期部分，不用 logDate；实施位置取 implementLocationName；工时使用共享展示函数，保留 0，缺失时按打卡时长计算并注明来源。实施内容前 15 个字符后加 ...，悬浮查看全文；移除照片数量列，详情仍可查看照片。接口参数及分页规则不变。


项目实施日志顶部（2026-09-11）：增加销售负责人、项目经理，按当前 periodId 查询 `/project/contract/queryById`，分别读取合同的 salesUserName、projectManagerUserName；复用合同详情封装解包 contract。加载失败单独提示并可重试，不阻断工序日志；姓名缺失显示 —。

## 2026-09-14 两端到货款门禁

最新 Apifox `OaPaymentRecord.paymentNode` 明确 `2=到货款`；合同详情返回全部有效 `records`。PC 项目分页响应尚无到货款存在标志，当前仅对有权限且符合阶段的当前页分期查 `/project/contract/queryById`，同页去重、最多3并发；没有权限/已到货/阶段不符不补查。App 复用详情原合同查询。两端统一存在性判断，未查明不开放按钮，提交前复查合同和分期。确认仍只提交 periodId/arrivalStatus=1，计划回款日期由后端计算。无节点不推进或修改到货状态。

### 2026-09-14 确认到货与到货款解耦

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。

列表不再逐项查询合同回款计划，改为对符合类型/阶段的当前页分期查询成员资格（去重、最多三项并发）。弹窗仍展示合同附件及可选到货款信息，提交前复查项目详情、项目类型字典和当前成员身份；只发送 `POST /project/period/arrivalStatus` 的 `{ periodId, arrivalStatus: 1 }`。不新增权限节点、不扩大其他角色授权。最新 Apifox 尚未明确类型、阶段、经理身份的服务端门禁；本次前端限制不能替代后端鉴权。本次未修改 App。


### 2026-09-15 合同签订新增改为 JSON 与多附件路径

- `POST /project/contract/addWithPaymentRecords` 按最新 Apifox 改为 `application/json`，直接提交 `{contract, contractFile, records, materialCandidateId?}`，不再包 multipart `data`，不传文件二进制。
- 保留现有 1～3 个合同附件交互：选文件逐个调用 `/sys/common/upload`，校验成功响应后取 `message` 原始路径，立即显示文件名并可预览/移除；上传中禁止提交。按用户确认的多文件规则，将当前文件列表路径用英文逗号拼入 `contract.contractFileId`，原文件名同序放入 `contractFile.fileName`。
- 新增由合同组合接口负责文件记录关联，不再随后调用 `/project/file/addBatch`，避免重复关联。合同页、项目详情合同标签、确认到货弹窗按逗号拆分路径逐个预览，兼容历史项目文件记录。
- `editWithPaymentRecords` 最新文档明确仍为 multipart，本次保留原编辑请求及附件同步流程。新增多文件拼接按用户要求实现；当前 Apifox 仅单路径示例，后端对逗号多路径的保存、拆分及原名映射需联调确认。未新增菜单、按钮或角色权限。


### 2026-09-15 用料移除提示与方案附件选填

- 用料计划的合同物料操作列保留“移除”按钮并禁用，悬浮提示“合同物料，不可移除”；保护逻辑与其他物料移除行为不变。
- 方案名称、类型仍必填，附件改为选填，无文件也可保存方案记录；提交审批仍要求至少一条已保存方案记录，不要求每条上传附件。
- 对齐当日 Apifox：批量新增 `/project/plan/addBatch` 使用 JSON `{periodId,records:[{plan}]}`，无附件省略 `planFileId`；有新文件在保存时先调用 `/sys/common/upload`，将 message 路径写入 `plan.planFileId`，不传文件名、attachmentIndex 或 attachments。已成功上传路径保留在草稿中，保存失败重试不重复上传。新记录状态 -1；编辑省略 status，由服务端处理原状态，不再回传 DRAFT 等英文枚举。
- 单条编辑仍按当前接口使用 multipart，可选 attachment，不上传时由后端保留原文件。菜单配置已检查，未改变菜单、按钮权限或角色授权。


### 2026-09-15 四个接口统一预上传与 JSON 保存

- 刷新 Apifox 确认 `/project/project/editProjectPeriod`、`/project/contract/editWithPaymentRecords`、`/project/plan/add`、`/project/plan/edit` 均仅接收 application/json。文件先 `/sys/common/upload`，业务请求使用成功响应 message 原始路径，不再传 multipart data/attachment、文件名或元数据对象。
- 编辑分期传顶层 `attachmentFileId`；合同编辑传 `contract.contractFileId`（当前多附件按英文逗号拼接）；方案单条新增/编辑传顶层 `planFileId`。无新文件不触发公共上传；方案上传成功路径保留在草稿，保存失败可复用。
- 合同编辑取消后置 `/project/file/addBatch` 和 `/project/file/deleteBatch` 关联流程，由合同接口事务保存。合同回显优先使用当前合同路径，兼容历史统一附件记录；项目详情和确认到货同样优先当前路径，避免旧附件记录遮蔽替换结果。
- 编辑路径省略、null、空字符串均保留原文件，不能借空值删除。方案移除旧文件后保存须选择替换文件；无历史附件的方案仍允许不上传。方案普通保存不回传历史审批结果，状态交服务端按最新规则处理。
- 本轮未更改新增分期接口，合同新增仍按其独立契约提交。已检查菜单配置，无新增节点、权限或角色授权变化。静态检查与构建不能替代真实上传→保存→重开预览联调。


### 2026-09-15 报价审批统一入口

报价管理待处理记录仅显示“报价审批”入口，点击进入 `/plan/material-draft/editor?mode=view&audit=1&periodId=...&candidateId=...` 的只读报价详情；详情顶部显示“通过/驳回”，分别二次确认后提交。仅待审批且拥有 plan:quotation:lock 时可办理，提交前重新查询报价状态；成功返回列表。普通查看详情不显示审批按钮。沿用 `plan:quotation:lock` 和现有候选清单状态接口，1通过、0驳回；不新增权限或扩大授权。正式菜单原“锁定”节点应改名“报价审批”，权限码及角色授权保留。本轮浏览器连接连续超时，正式菜单名称尚未修改，不能视为已同步。


### 2026-09-15 方案文件整页选填

所有合同类型均允许不添加方案记录、不上传方案附件。保存本页取消“至少添加一个方案文件”拦截；空列表不调用新增接口，删除全部已有记录时仍提交待删除 IDs 并回查。已添加行继续校验名称、类型，未保存修改和加载失败仍阻止审批提交。菜单及 API 参数无变化。


### 2026-09-16 报价审批状态与采用标记分离

最新 Apifox 候选清单 `status` 仅 -1草稿/0驳回/1通过/2待处理/4已作废，采用单独使用 `adopted=0/1`，不再读写 status=3。报价列表分列显示审批状态、采用状态，详情增加采用标签；通过报价不论是否采用均可脱敏导出。

合同候选仅选择 status=1，已采用回显和计划合同物料带入均用 adopted=1。合同取消/更换采用时，通过候选 edit 仅提交 id/periodId/candidateName/adopted=0，保留审批通过状态；已采用报价不可直接调整，仍须合同驳回/撤回后按原权限取消采用。合同保存由后端原事务设置 adopted=1，并校验同分期唯一采用；前端不新增采用操作入口。合同自身 status=3 仍代表撤回，不受此次报价字段调整影响。

接口路径、菜单与权限码不变，菜单配置已检查无需调整。前端静态/构建校验不代表真实合同保存、取消采用和计划回显联调通过。
### 2026-09-16 合同信息前端入口门禁

新增前端权限码 `project:contract:view`，用于独立合同查看/编辑入口；不更改合同查询、保存或审批 API 参数。前端隐藏按钮和页面加载拦截不能替代后端合同数据权限。正式菜单节点及角色关联未因 Mock 更新自动生效，本轮后台连接超时，尚未配置或完成真实账号验收。
2026-09-17 报价管理恢复按报价单分页：主表直接使用 GET `/project/materialCandidate/list`，默认仅传 pageNo/pageSize 查询全部项目，不再请求 periodGroupPage 或渲染分期展开表。项目名称、分期名称直接取接口，缺失显示 —；rowKey=id、total为报价条数。原查看/修改/提交/审批/作废/导出与权限不变。菜单两份指南已检查，无路由或权限变化。
2026-09-17 项目详情新增“报价信息”页签，位于基本信息之后，各阶段均可查看；`DetailQuotation.vue` 按当前分期 periodId 调用 `/project/materialCandidate/list`，只读分页展示报价名称、最后更新人、审批及采用状态。页签默认懒加载，切换分期重新挂载，缺失 periodId 不发全量请求，失败可重试。未新增编辑/审批入口或菜单权限。
2026-09-17 合同审批入口修正：待审批合同且具备 project:contract:approve 时，项目列表显示“合同审批”，不再要求额外的 project:contract:view；两种权限都有时仅显示审批按钮。普通合同信息仍要求查看权限。独立合同页仅 view 模式接受审批权限入口，详情回查非待审批且无查看权限则返回项目列表，edit/create 原门禁不变。未扩大角色授权，服务端数据鉴权不变。 本说明覆盖此前“审批也须额外查看权限”的描述；无新增接口或菜单编码。
