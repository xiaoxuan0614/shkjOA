# 菜单配置指南（后端「菜单管理」）

> 本工程为 JeecgBoot Vue3 前端，权限模式为 **BACK（后端返回菜单）**。
> **正式环境的侧边栏菜单、路由全部由后端 `sys_permission` 菜单表驱动**，前端 `src/router/routes/modules/*.ts` 只是 mock/兜底。
> 新功能上线，必须在后台「系统管理 → 菜单管理」里配好菜单，前端才能访问。

> 💡 **需要逐条手把手操作？** 请直接看 [菜单配置傻瓜指南.md](菜单配置傻瓜指南.md)（每个菜单填什么值都写好了）。

---

## 一、配置前必读：component 路径规则

后端菜单里的 `组件路径(component)` 会映射到前端 `src/views/` 目录下的文件（`src/utils/dynamicPages.ts` 的 `import.meta.glob` 注册，支持 `.vue` / `.tsx`）。

| 菜单里的 component 写法 | 对应的前端文件 |
| --- | --- |
| `project/index` | `src/views/project/index.vue` |
| `project/apply/ProjectApply` | `src/views/project/apply/ProjectApply.vue` |
| `project/contract/index` | `src/views/project/contract/index.vue` |
| `project/plan/ProjectPlan` | `src/views/project/plan/ProjectPlan.vue` |
| `resource/outsourcing/index` | `src/views/resource/outsourcing/index.vue` |
| `todo/index` | `src/views/todo/index.vue` |

**规则**：不带 `.vue` 后缀；目录层级用 `/` 分隔；`xxx/index` 命中 `src/views/xxx/index.vue`。
**约定**：路径统一**不带前导斜杠**（`resource/outsourcing/index` 而非 `/resource/outsourcing/index`）。

---

## 二、全量菜单清单（按模块）

> 「是否隐藏」= 侧边栏不显示，仅由列表页按钮跳转访问（隐藏路由也**必须配**，否则按钮跳转 404）。

### 1. 首页
| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 首页 | `/dashboard`（一级，LAYOUT） | `dashboard/Analysis` | 否 |

### 2. 物料管理 `/material`（一级：LAYOUT，图标 `bx:bx-box`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 物料基本维护 | `/material/list` | `material/goods/index` | 否 |
| 库存管理 | `/material/stock` | `material/stock/index` | 否 |
| 盘存记录 | `/material/stocktake` | `material/stocktake/index` | 否 |
| 出入库管理 | `/material/record` | `material/record/index` | 否 |
| 领料申请 | `/material/pick` | `material/pick/index` | **是** |
| 还料申请 | `/material/return` | `material/return/index` | **是** |
| 采购入库 | `/material/purchase` | `material/purchase/index` | 否 |
| 物料申请 | `/material/apply` | `material/apply/MaterialApply` | **是** |

### 3. 项目管理 `/project`（一级：LAYOUT，图标 `bx:bx-folder`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 项目管理 | `/project/list` | `project/index` | 否 |
| 项目详情 | `/project/detail/:id` | `project/detail/index` | **是** |
| 新增项目 | `/project/apply` | `project/apply/ProjectApply` | **是** |
| **合同信息** | `/project/contract` | `project/contract/index` | **是** |
| 编辑计划方案 | `/project/plan` | `project/plan/ProjectPlan` | **是** |

> `project/contract`（合同信息页面）为本次新增：列表「合同签订」按钮跳转进入，隐藏。

### 4. 资源管理 `/resource`（一级：LAYOUT，图标 `bx:bx-folder-open`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 车辆管理 | `/resource/vehicle` | `resource/vehicle/index` | 否 |
| 车辆详情 | `/resource/vehicle/detail/:id` | `resource/vehicle/detail/index` | **是** |
| 往来客户 | `/resource/customer` | `resource/customer/index` | 否 |
| 供应商管理 | `/resource/supplier` | `resource/supplier/index` | 否 |
| **外协单位** | `/resource/outsourcing` | `resource/outsourcing/index` | 否 |

> ⚠️ 外协单位路径注意拼写：**`outsourcing`**（别写成 `outsourcig`），否则页面 404。

### 5. 站内待办 `/todo`（一级：LAYOUT，图标 `bx:bx-bell`）——本次新增

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 邀约待办 | `/todo/list` | `todo/index` | 否 |

### 6. 计划方案管理 `/plan`（一级：LAYOUT，图标 `bx:bx-file`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 计划方案管理 | `/plan/list` | `plan/index` | 否 |
| 方案详情 | `/plan/detail/:id` | `plan/detail/index` | **是** |

### 7. 实施管理 `/implement`（一级：LAYOUT，图标 `bx:bx-wrench`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 实施管理 | `/implement/list` | `implement/index` | 否 |
| 查看日志 | `/implement/log/:id` | `implement/log/index` | **是** |
| 日志详情 | `/implement/log/:id/detail/:logId` | `implement/detail/index` | **是** |

### 8. 回款管理 `/payment`（一级：LAYOUT，图标 `bx:bx-money`）

| 菜单名称 | 路由地址 | 组件路径 | 隐藏 |
| --- | --- | --- | --- |
| 回款管理 | `/payment/list` | `payment/index` | 否 |
| 回款记录 | `/payment/detail/:id` | `payment/detail/index` | **是** |

### 9. 占位模块（可选）

| 一级菜单 | 路由地址 | 子菜单 |
| --- | --- | --- |
| 文件管理 | `/file` | `/file/list` → `placeholder/index` |
| 数据报表 | `/report` | `/report/list` → `placeholder/index` |
| 运维管理 | `/operation` | `/operation/list` → `placeholder/index` |

---

## 三、按钮权限清单（操作按钮显隐）

在「菜单管理」里为对应**子菜单**添加「按钮」类型的权限项，再在「角色管理 → 授权」勾选，前端 `v-auth` / 流转按钮 `auth` 依据权限码放行。

### 3.1 项目管理 `/project/list`

| 按钮名称 | 权限标识 | 作用 |
| --- | --- | --- |
| 合同签订 | `project:contract` | 未开始项目的「合同签订」按钮（跳转合同信息页） |
| 计划审批 | `project:plan:audit` | 待立项项目的「计划审批」按钮（通过/驳回） |
| 开始/完成实施 | `project:implement` | 实施中/调试完成 的流转按钮 |
| 内部验收 | `project:internalAccept` | 验收阶段的内部验收按钮 |
| 客户验收 | `project:accept` | 验收阶段的客户验收/进入质保按钮 |
| 质保 | `project:warranty` | 质保中的项目完结按钮 |

> 角色授权建议：发起人（销售/项目经理）至少给 `project:contract`；审批角色再给 `project:plan:audit`、`project:accept` 等。

### 3.2 合同信息页 `/project/contract`（只读，编辑权限单独放）

| 按钮名称 | 权限标识 | 作用 |
| --- | --- | --- |
| 编辑合同 | `project:contract:edit` | 合同提交后只读页面上的「编辑」按钮（按人放开） |

### 3.3 物料管理 `/material/list`

| 按钮名称 | 权限标识 |
| --- | --- |
| 新增 | `mtl:goods:add` |
| 编辑 | `mtl:goods:edit` |
| 删除 | `mtl:goods:delete` |
| 批量删除 | `mtl:goods:deleteBatch` |
| 出入库 | `mtl:goods:io` |
| 导出/导入 | `mtl:materiallist:exportXls` / `mtl:materiallist:importExcel` 等 |

---

## 四、配置步骤（后台操作）

1. 登录系统，进入 **系统管理 → 菜单管理**。
2. 点「新增」创建**一级菜单**：菜单类型=`一级菜单`，路由地址=`/xxx`，组件路径=`LAYOUT`，保存。
3. 选中该一级菜单，点「新增」创建**子菜单**：菜单类型=`子菜单`，路由地址=**完整绝对路径**（如 `/project/list`），组件路径=`project/index`（不带前导斜杠、不带 .vue），保存。
4. 隐藏路由同样配置，勾选「是否隐藏」。
5. 按钮权限：选中子菜单 → 新增「按钮」类型 → 权限标识填上表编码。
6. 角色管理 → 授权 → 给角色勾选对应按钮权限。
7. **重新登录**（或清权限缓存）生效；若后端菜单有缓存，需后端清 Redis / 重启。

> 若「菜单管理」里 `菜单类型` 支持 `按钮`，为操作按钮加权限标识配合前端 `v-auth` 使用。

---

## 五、菜单与前端路由文件对应关系

| 前端路由文件 | 一级 path | 说明 |
| --- | --- | --- |
| `src/router/routes/modules/material.ts` | `/material` | 物料管理 |
| `src/router/routes/modules/project.ts` | `/project` | 项目管理 |
| `src/router/routes/modules/resource.ts` | `/resource` | 资源管理（车辆/客户/供应商/外协） |
| （mock 内联）`todoRoute` | `/todo` | 站内待办 |

> 前端路由文件仅用于 **mock 模式**（`pnpm mock`）和开发兜底。正式环境（`pnpm dev` 代理真后端）以菜单管理配置为准。

---

## 六、常见排查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| 打开某菜单是**空白页**，提示"查看组件引用是否正确" | 子菜单组件路径配成了 `LAYOUT` 或写错 | 组件路径改成真实页面（如 `project/index`） |
| **页面 404，但菜单能显示** | 「是否路由菜单」没开（`route=0`） | 打开「是否路由菜单」 |
| 访问路径不对/404 | 子菜单路径只填了相对值 | 改成完整绝对路径（`/project/list`） |
| 列表按钮不显示（合同签订/计划审批等） | 按钮权限码没配 / 角色没授权 | 按第三节配按钮权限 + 角色授权 |
| 点「编辑/新增/合同信息」跳 404 | 对应隐藏路由没配 | 补齐隐藏路由 |
| 外协单位 404 | 后端菜单路径拼错 `outsourcig` | 改成 `outsourcing` |
| 配置完菜单不显示 | 权限缓存未刷新 / 后端菜单缓存 | 重新登录；后端清 Redis/重启 |
