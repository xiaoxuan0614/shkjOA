# 物料管理模块 - 逻辑总结

2026-09-24 物料名称点击详情（覆盖上一条Tooltip方案）：取消参数及选料名称Tooltip，参数列仍10字摘要；物料基本维护和共用选料抽屉点击名称打开MaterialBasicInfoModal，只读显示编码、名称、类别、品牌、型号、完整参数、库存（基准单位）。点击时才GET stock/material/queryById，项目总账行取materialId；不逐行查询、不新增权限、不写业务。类别/品牌字典名称优先，保留零库存与参数换行；加载、失败重试、关闭/切换丢弃过期响应。字段/组件及零数量选料回归检查通过，未真实页面点击验收。已检查两份菜单配置文档，无权限/路由变化无需修改。

2026-09-24 共用选料参数列及Tooltip：MaterialSelectDrawer型号后增加“参数”列，并与物料基本维护列表共用materialSpecification.ts；前10个Unicode字符加...，空值—，鼠标悬浮/键盘聚焦显示完整原文并保留换行，超长内容可滚动，纯文本渲染。抽屉增加水平滚动，不改变选择/零数量禁用/提交行为。stock/material/list已声明specificationParams；project/materialAccount/page实体尚未声明该字段，项目用料模式缺字段时显示—，未逐行补查。文本/Tooltip/零数量回归及ESLint通过，未真实页面悬浮验收。两份菜单配置文档已检查，接口路径和权限不变无需改菜单。

2026-09-24 物料基本维护列表：在型号后新增“参数”列，读取specificationParams；最多显示前10个Unicode字符，超出追加...，空值显示—。移除列表基准单价列，不改变原始参数、编辑表单或独立价格维护。刷新Apifox确认列表返回字段，文本边界及列配置回归、ESLint通过；菜单配置两份指南已检查，无权限/路由变化无需修改，未部署。

2026-09-23 物料规格参数：PC物料基本维护新增/编辑/只读详情共用表单增加specificationParams（规格参数），整行多行文本、选填。对齐最新Apifox StockMaterial，UTF-8最多65535字节，使用TextEncoder校验；新增/编辑沿用stock/material/add与edit，详情queryById回显，清空提交空字符串。中文/emoji边界、多行及新增/编辑/清空载荷回归和ESLint通过；未写入真实物料验证持久化。菜单两份指南已检查，沿用现有新增/编辑权限，无需改菜单；不扩展列表列、Excel模板或价格维护。

2026-09-23 普通项目领料“添加可申请物料”：PC MaterialSelectDrawer、移动端ops/materialApply保留materialAccount/page返回的可申请数量为0的物料，不再在分页结果映射后过滤；零数量行展示但禁用选择并提示暂无可申请量。全选仅作用于正可申请量记录，移动端全选状态及确认校验同步，非项目用料不受此数量门禁影响。原数量上限与提交校验保留；返工专用额外领料来源和草稿恢复规则不变。两端SFC、零数量单选/混合全选/取消全选及PC ESLint通过，未真实浏览器/真机或提交联调。接口/菜单/权限未变，菜单文档已检查无需调整；未重新打包部署。

2026-09-21 PC入库执行待办：STOCK_IN_EXECUTE与STOCK_OUT_EXECUTE统一接入既有StockExecuteModal；actionParams.applyId查询指定申请，核对返回ID和IN/OUT方向。执行只使用原库管权限、审批通过及未执行完状态，不查询经理审批资格；审批待办保持原规则。成功沿用待办刷新。未修改真实审批/库存数据或角色权限；分派和拒绝分支模拟通过，真实账号入库待联调。

2026-09-21 采购编辑：项目下拉回显主项目-分期；采购详情缺名称时复用同分期列表行，缺项则加载项目选项，不串用其他分期名称。先完成表单重置再回填，选中值统一字符串。保存接口及权限不变。

2026-09-21 列表项目名称统一（部分完成）：项目管理、计划方案、报价管理、实施管理均用projectDisplayName显示主项目名称-分期名称，缺一侧只显示已有名称，全空—；搜索标签项目名称、仅传projectName，移除分期名搜索，项目列表不再由该搜索框传keyword。采购订单展示也改组合名称，复用projectPeriodList完整分页补齐主项目/分期名称，不改表单保存字段。后端阻塞：purchaseOrder/list仅支持periodName搜索；stock/apply/list的projectName实际是保存的分期名称，缺主项目返回及查询协议。因此采购搜索和出入库记录暂保持原行为，不用前端当前页过滤或伪造查询参数；全OA统一尚未完成，需后端补主项目名字段与分页前查询。菜单/权限无变化，两份指南已检查。

2026-09-21 采购订单项目选择：新增/编辑共用表单改为“项目名称”，显示主项目名称-分期项目名称；原独立主项目只读框隐藏但保留保存字段。首次展开按数字pageNo/pageSize=100分页调用/project/project/projectPeriodList，无额外状态筛选，读取当前账号可见全部项目后本地按完整名称忽略大小写、首尾空白包含匹配。仅本次弹窗缓存，重复展开不重复查询，重开重新加载；加载提示、失败重新展开重试、过期响应忽略。编辑当前关联项合并保留，清空同步清除主项目关联，提交projectId/periodId/projectName协议不变。采购列表筛选及其他模块共享预加载未改。Apifox已刷新核对分页接口；分页/搜索/取消/失败与SFC回归、定向ESLint通过，未真实采购账号浏览器保存联调。菜单两份指南已检查，无路由或权限变化。

2026-09-21 领料/还料会话草稿：按用户、租户、新增/申请ID及返工ID隔离，只恢复填写项；新增领料重新检查可选项目、项目可申请物料/劳保类别/返工额度，还料重新取总账再恢复数量和差异原因。编辑单基准变化不恢复草稿。提交成功或显式取消清草稿并关闭表单页签；旧物料申请也接入草稿。库存及可用额度不作为草稿权威来源。

2026-09-20 添加物料共用选择抽屉：切换分类回到第1页；请求前将分页归一为正整数，非法值回退1/10，表格无分页事件不覆盖原分页。已刷新Apifox，GET /stock/material/list实际分页字段为pageNo/pageSize，保持字段名不改为page/size，参数对象使用number。菜单和权限无变化。

2026-09-17 候选清单筛选：报价管理平铺列表新增主项目名称、审批状态、采用状态查询，quotationList 与 quotationPeriodGroupPage 封装均透传 projectName/status/adopted。名称直接传关键词并去首尾空白，不拼通配符；空值省略，status=0、adopted=0保留。状态仅-1/0/1/2/4，采用0/1，条件由后端分页前AND组合；不恢复分组页面，list total仍为候选单数。若后续使用分组页，组内list必须带相同筛选才能与candidateCount一致。物料维护与选料抽屉已直接传materialName关键词，无需改动。定向ESLint、diff及参数模拟检查通过；本地页面实际验证三条件查询（含两个0）success=true、重置清空条件并回第一页。未验证分组接口真实返回，不访问测试环境；菜单/角色权限无变化。

2026-09-17 本地价格维护授权完成：经用户明确确认，本地 localhost:3101 所代理的局域网开发后端属于本次授权范围。已在该环境“物料基本维护”下创建 PC 按钮“价格维护”（mtl:goods:price、可见/可访问、有效），保存后重开核对标识；管理员 admin 角色勾选后保存，重开确认仍勾选，其他角色未改。刷新本地列表后按钮显示，点击可打开仅含成本价、基准单价、报价比例的独立弹窗，并加载物料名称及基准单位。未修改真实价格，价格保存及后端字段鉴权不在本次验证结论内。本次未访问或修改测试环境。此记录为本地配置的最新状态。

2026-09-17 价格维护权限后台配置完成：测试系统菜单管理已在“物料基本维护”下创建 PC 按钮“价格维护”，授权标识 mtl:goods:price，策略可见/可访问、状态有效；保存后重新打开节点核对标识。管理员（admin）角色 PC 授权已保存，重新打开授权页该项仍勾选，其他角色未调整。刷新测试物料列表后仍未出现价格维护按钮，页面入口尚未验证通过，需要核对已部署前端版本及重新登录后的权限加载；未操作价格数据。此记录覆盖此前浏览器超时、节点未配置的状态，不代表后端价格字段鉴权已完成。

2026-09-17 物料价格独立维护：物料基本维护新增“价格维护”按钮及 GoodsPriceModal，权限 mtl:goods:price；打开读取 queryById，三个字段 costPrice（成本价）、unitPrice（基准单价）、guideMarkupRate（报价比例）通过 /stock/material/edit 仅携带 id 与价格字段保存。基本新增/编辑移除价格表单字段，API 层剔除三项，取消原 unitPrice 默认 0 回传；普通编辑不覆盖价格。报价比例原值保存，不自动计算，不限制100上限，最多4位小数；价格最多2位小数，空值省略、0有效。按钮、打开及提交重复验权。成本价不加入列表，基准单价列表保持。

Mock 已同步；正式菜单节点及管理员角色授权已完成并重开回读，见本页最新配置记录。后端当前仍共用 edit 接口，尚无价格独立权限契约；需服务端对三字段增加 mtl:goods:price 校验并确认省略字段保留/价格部分更新行为，防止绕过 UI 或经导入修改价格。guideMarkupRate 的迁移/部署状态及真实保存重开尚未验证。无权/有权请求分支模拟、SFC、ESLint及diff检查通过。

2026-09-16 入口收敛：所有物料审批在首页待办办理，出入库管理移除行审批及批量审批入口。仅保留查看、库管执行和申请人操作；项目经理审批资格不变，覆盖下方历史入口描述。

2026-09-16 最新审批规则：项目领料与 IN+RETURN+PROJECT 还料均由本分期已接受的项目经理审批；库管仅执行已通过的待执行/部分执行单。stockAccess 的成员预加载与审批判断统一，普通库管无项目经理关系不可审批项目还料。其他入库审批保持原逻辑。经理本人申请自动通过由后端处理，尚未做真实账号联调。

2026-09-15 物料维护增加独立成本价 costPrice（基准单位），可新增/编辑/查看，历史空值显示—；不替代 unitPrice，不参与库存金额计算。报价页当前仅引用为只读参考，报价自身成本快照及物料默认比例待后端扩展。

2026-09-13 两端同步：还料仅传必要头/明细，审批与执行状态交后端，不从编辑页强写待入库。移动端补当前返工专属领料额度和reworkId，不能混用首次计划。项目成员申请、经理审批、经理本人自动通过是用户最新目标；现后端文档仍写项目领料全员免审，不能认定新审批链已生效。

## 出入库用途分流与前端权限（2026-09-10）

项目领料 OUT+PROJECT 新增及重提由后端免审批放行，主单/明细 status=1、executeStatus=PENDING；前端显示“免审批·待出库/部分出库/已出库”，不提供库管确认或驳回。维修/劳保领料由申请部门的有效负责人审批；前端要求 `mtl:apply:approve`、身份为上级、负责部门包含申请 deptId。单张及批量执行要求 `mtl:goods:io`、status=1、执行状态为 PENDING/PARTIAL_EXECUTED。实际出库身份与部门审批范围由后端最终校验，提交和审批都不扣库存。入库保持原审批流程。

单张审批、批量审批、执行弹窗和 STOCK_OUT_APPROVAL/STOCK_OUT_EXECUTE 待办使用共同门禁；待办按 actionParams.applyId 取最新详情。统一审批请求只传 applyId、approvalResult、approvalComment，不传已不在当前契约内的 approvalUserId。

契约边界：当前后端禁止已通过申请撤回/编辑/删除；旧 status=2 项目申请不自动迁移，当前前端按新要求不提供项目审批按钮，需后端确认历史数据处置，不能直接前端改状态。本次未执行库存变更、历史数据迁移或正式角色授权，部门领导角色尚待明确。


## 2026-09-09 领料类型范围

- 项目用料：仅项目可申请清单，沿用 `availableApplyQty` 上限。
- 维修：手填必填维修单号，可选全物料，前端字段暂按 `repairOrderNo`。
- 劳保用品领取：只选择 `material_category` 字典“劳保用品”对应编码，隐藏类别切换和新增物料入口；字典缺失或重名时阻止加载，不退回全部物料。
- 最新 Apifox 的 StockApply 尚未声明维修单号字段，后端保存/回显需补齐；劳保提交范围也需后端强制校验。此次修改为 PC，未完成移动端同步或真实账号验收。


> 日期：2026-08-16 本模块 = `src/views/material/*`，含 物料基本维护 / 库存管理 / 盘存记录 / 出入库管理（申请+审批+执行+台账）/ 采购入库 / 领料还料申请 / 物料申请。后端走 `/shouhuiApi` 下的 `/stock/*`（物料库存）与 `/project/*`（采购订单/供应商/项目分期）。

---

## 一、菜单结构（正式环境由后端菜单表配置）

| 菜单           | 路径                  | 页面                                            |
| -------------- | --------------------- | ----------------------------------------------- |
| 物料基本维护   | `/material/list`      | `material/goods/index`                          |
| 库存管理       | `/material/stock`     | `material/stock/index`                          |
| 盘存记录       | `/material/stocktake` | `material/stocktake/index`                      |
| 出入库管理     | `/material/record`    | `material/record/index`（申请 + 台账）          |
| 采购入库       | `/material/purchase`  | `material/purchase/index`                       |
| 物料申请(隐藏) | `/material/apply`     | `material/apply/MaterialApply`                  |
| 领料申请       | `/material/pick`      | `material/pick/index`（通用可见申请入口）       |
| 还料申请       | `/material/return`    | `material/return/index`（通用可见申请入口）     |

> 领料/还料从侧边菜单进入时面包屑归属“物料管理”，提交或取消后返回首页；从“出入库管理”跳入时携带来源，仍返回 `/material/record`。普通申请人无需获得库管列表权限。

---

## 二、物料基本维护（goods）

- 物料 CRUD + **单位子表**：第一行即基准单位（换算系数固定 1，不可移动/删除），副单位填换算系数；**单位下拉统一走数据字典 `inv_unit`**（`material.util::loadUnitOptions`），其他行已选单位不再显示，所有单位用完后禁用添加，提交前再次校验单位唯一和换算系数必填。
- 列表“品牌”使用 `material_brand` 字典将接口 `brand` 编码转为名称，字典未命中时才回退显示原值；新增、编辑和搜索使用同一字典。
- **物料编码后端生成**：新增时隐藏，创建后只读展示（编号显示规则）。其它业务记录统一按 `materialId` 从物料主表解析 `materialCode`，业务快照仅作兜底；物料变更后清理映射缓存，避免继续显示旧编码。
- 新增/编辑：`unitPrice`（基准单价）、`stockQty`（初始库存）**未填默认 0，始终上送**；编辑不传 `version`。
- 行操作含手动出入库（`IOModal` → `/stock/ioRecord/manualIn|manualOut`）。
- 导入/导出走 `JImportModal`（下载模板在弹窗内）。

## 三、库存管理（stock）

- 库存总览（按基准单位显示库存数量/金额）。
- 行操作：**出入库**（手动入库/出库，生成台账）、**盘存**（`POST /stock/ioRecord/takeStock`，按实际盘点数量留痕）。

## 四、盘存记录（stocktake）

- 固定按 `sourceType=stocktake` 过滤 `/stock/ioRecord/list`。
- 差异 = 盘点后 − 盘点前（正=盘盈绿 / 负=盘亏红），物料编码/名称按 `materialId` 从刷新后的物料主表映射富化。

## 五、出入库管理（record）

两条 tab：**出入库申请** + **台账**。

- 出入库申请列表不展示 `projectNo` 对应的“关联单号”列，仅保留项目名称；搜索表单仍可按关联单号筛选，申请详情和提交字段也保持不变。

### 5.1 申请创建

- 三种入口：领料申请（`/material/pick`）、还料申请（`/material/return`）、物料申请（`/material/apply`）。
- 领料申请必选领料类型：项目用料（`PROJECT`）、维修用料（`MAINTENANCE`）、劳保用品领取（`LABOR_PROTECTION`）。三类均提交 `applyType=OUT`、`bizType=PICK` 和对应 `usageType`。
- 项目用料通过“项目名称”选择分期：页面进入读取 `/project/member/participatedProjects`，传 `periodStatus=IMPLEMENTING,DEBUGGING,DEBUG_COMPLETED,IMPLEMENT_COMPLETED,PENDING_ACCEPT,INTERNAL_ACCEPTING,ACCEPTING,REWORKING`，只展示当前用户已接受邀请且处于计划审批完成后至验收完成、尚未进入质保的参与项目（`DEBUG_COMPLETED` 兼容历史数据），候选只按主项目名称或分期名称在接口返回结果中模糊搜索，下拉左侧显示主项目、右侧显示分期，不显示编号或 ID；选择值及去重键统一为 `periodId`。切换或清空项目时清空原物料明细。编辑时回填主项目/分期名称，不在可领料范围的原项目仅用于回显，需重新选择后才能提交；返工项目保持锁定。选中后按 `periodId` 分页读取 `/project/materialAccount/page`，抽屉以 `keyword` 搜索编码、名称、品牌或型号，不读取 `/project/materialPlan/list`，也不为项目候选加载 `/stock/material/list`。候选和明细显示 `availableApplyQty` 当前可申请数量，数量为 0 时不进入候选列表，申请数量不得超过该值，单位使用 `baseUnitName`；全选只处理当前页可申请且未加入明细的物料。物料名称与编码同行展示，明细可逐条删除。新增和重提均提交有效分期 `periodId`；`projectNo/projectName` 由后端派生且不进入提交体。
- 维修用料和劳保用品领取不显示、不提交项目字段，选料范围为 `/stock/material/list` 全部物料。切换类型或分期会清空当前明细，避免来源混用。
- 还料页固定为项目类型：进入页面调用 `/project/member/participatedProjects`，仅展示当前登录用户已确认参与的项目分期；选择后按 `periodId` 分页调用 `/project/materialAccount/page`，过滤 `remainingReturnQty>0` 的待还物料。该值按基准单位显示为系统应还数量并默认回填本次还料数量，允许在 `0～系统应还数量` 内修改；提交数量大于 0 且与系统应还不一致时，逐行差异原因必填并提交到明细 `remark`。
- 项目还料新增/重提统一调用 `/stock/apply/add|edit`，固定提交 `applyType=IN`、`bizType=RETURN`、`usageType=PROJECT`、`periodId` 和数量大于 0 的 `itemList`；`projectNo/projectName/returnUser/unitPrice` 由后端按分期和当前用户派生。前端不再调用未在当前 Apifox 声明的 `/stock/apply/returnList`，也不再使用本地还料示例兜底。
- 领料申请头不传 `deptId/deptName/projectName/projectNo/applyUserName`；领料 `itemList` 每项只传 `materialId/unitQty/unitName`。页面显示所需的名称、编码、类别、品牌和型号不回传给领料接口。
- 旧“物料申请”兼容入口同样用显式白名单组装 `/stock/apply/add` 请求，不回传申请人、部门或项目展示字段，`itemList` 仅含 `materialId/unitQty/unitName`；还料按独立契约保留差异原因等专用明细。
- 非项目领料明细的“库存”列展示 `currentStockQty + baseUnitName`（如 `1个`）；项目领料用“当前可申请数量”替代库存列。
- 共用选料抽屉和领料明细的“品牌”都按 `material_brand` 字典回显名称，筛选使用名称、提交保留原始品牌值，未命中字典时显示原值。
- 物料申请、领料和采购共用的选料抽屉接收当前明细物料 ID；项目领料再次打开时保留该记录并标记“已添加”，其他来源继续排除已加入物料。提交时仍逐行校验物料唯一，以及数量、单位等业务必填项。

### 5.2 审批（整单审批）

- 申请主表与明细 `status` 统一为 `-1=待提交`、`0=驳回`、`1=审核通过`、`2=待审批`、`3=已撤回`；列表与详情均使用公共五态转换，不读取状态字典。
- **只有「待审批(2)」显示「审批」按钮**。
- 审批弹窗：申请明细**只读展示**（整单审批，无逐条概念）；审批结果单选 **整单通过 / 整单驳回**，**驳回必填审批备注**（驳回原因）。
- 提交：通过 `approveApply({applyId, approvalUserId, approvalResult:'AGREE', approvalComment})`、驳回 `rejectApply({...REJECT, approvalComment})`，**不带明细 items**。这里的 `AGREE/REJECT` 是 Apifox 明确规定的审批动作参数，不是申请状态码。

### 5.3 执行出入库

- 「审核通过(1)」→ 出库(领料) / 入库(还料/采购)，明细也仅执行 `status=1` 的记录；统一五态不再包含“部分通过”。
- **出库/入库按钮展示条件一致**，均按执行状态区分：仅「待执行(PENDING)/部分执行(PARTIAL_EXECUTED)」(`executeStatus`) 显示对应按钮，执行完不再显示。
- 单张申请执行弹窗按 `GET /stock/apply/items` 返回的 `pages/current/total` 逐页加载全部明细并按 `id` 去重，只保留 `status=1` 的已审批项；界面以 30/50 条本地分页展示，支持搜索、待执行/已完成筛选、仅看已选、当前页选择、跨页全选筛选结果及批量填入可执行上限。出库明细每种物料使用两行：第一行展示选择、物料、申请量、库存、已出和待出，第二行集中填写本次出库数量与备注；申请量取 `applyQty`，为空回退 `unitQty`。跨页操作保留同一行对象，换页或筛选不清空已选数量和备注。
- 每行剩余可执行量取 `approvedQty（回退 applyQty/unitQty）- executedQty`。出库展示所选单位实时库存 `currentUnitStockQty`，本次出库数不得超过剩余量或实时库存；没有库存字段时仍由后端做最终库存校验。
- 执行弹窗调用 `POST /stock/apply/execute`，请求为 `{applyId, items:[{itemId, executeQty, remark?, dispositionType?, dispositionRemark?}]}`，不重复传 `itemIds`；只有 `IN + RETURN + PROJECT` 的项目还料逐项提交 `RESTOCK/SCRAP/LOST`，报废和遗失原因必填。**执行人由后端从登录 token 取**，明细记录 `executeUserId/executeUserName`。
- 明细「执行人」列展示执行出入库的经办人（后端 `executeUserName`）。

### 5.4 申请人动作

- **待提交/已撤回/已驳回 → 可「修改」** 跳回申请页（`?applyId=`）改数量后重新提交，重新提交固定传 `status=2`。
- 待审批可「撤回」（`2→3`）；**已驳回不可删除**；删除仅限待提交或已撤回。

### 5.5 台账

- 出入库记录（`/stock/ioRecord/list`），行操作「详情」弹窗展示该条记录涉及的全部物料详情（物料主表 + 单位换算 + 本次变动）。
- 台账来源/类型标签走字典 `stock_io_source_type` / `stock_io_type`。
- 项目详情“用料统计”不读取计划表，按分期 `periodId` 调用 `/project/materialAccount/page`，以服务端搜索和 20/50/100 分页展示计划、申请占用、出库、施工消耗、归还处置、库存和成本的聚合结果；数量统一使用总账返回的 `baseUnitName`。

## 六、采购入库（purchase）— 状态机

| 状态     | 行操作                                       | 目标                    |
| -------- | -------------------------------------------- | ----------------------- |
| 1 待采购 | 修改 / **确认采购** / 关闭                   | →2                      |
| 2 采购中 | **采购完成** / 关闭                          | →3                      |
| 3 已到货 | **开始入库** / 关闭                          | →4                      |
| 4 入库中 | **入库**（逐物料实际入库数量）/ **入库完成** | 入库后仍 4；入库完成 →5 |
| 5 已完成 | —                                            |                         |
| 0 关闭   | —                                            |                         |

- 状态切换走 **`POST /project/purchaseOrder/status`（`{orderId, status}`）**，不再用 editOrder 改状态。
- **列表筛选**：搜索表单含 采购单号(orderNo 输入)/供应商(supplierName)/分期项目(periodName)/状态(status 字典)。供应商与分期下拉同弹窗一致——主页面渲染预载第 1 页、输入后服务端模糊查询（`/project/supplier/list`、`/project/period/searchByName`），查询参数按实体字段名上送。
- **入库**：`StockInModal` 逐物料填 `inboundQty`，调 **`POST /project/purchaseOrder/inbound`**（`{orderId, items:[{itemId, inboundQty}]}`），库存/台账按实际入库数量计算；入库完成由后端校验。
- **关闭**：仅 1/2/3 可关闭（开始入库前），`changeStatus({orderId, status:'0', closeReason})`。
- 状态标签走字典 `purchase_order_status`（1待采购/2采购中/3已到货/4入库中/5已完成/0关闭）。

## 七、跨页面的下拉预加载

- 弹窗/抽屉下拉数据（供应商/项目分期等）在**主页面渲染时预载第 1 页 10 条**并缓存（`material.options.ts` 的 `ensureSupplierOptions`/`ensurePeriodRecords`），打开弹窗直接读缓存，输入模糊查询走服务端。
- 字典类下拉（单位/状态/类别/品牌）统一走 `material.util::loadDictMap`（模块级缓存），改后台字典重新登录生效。

## 八、关键约定（全局）

1. **所有状态/枚举/下拉标签走后端 `sys_dict` 数据字典**，前端不写死（mock 字典已全部移除）。
2. **编号显示规则**：新增时隐藏编号（后端生成），创建后只读展示。
3. **操作人身份**：`getCurrentUser()` 取当前用户；审批提交带 `approvalUserId`；列表/详情按 id 解析姓名（后端姓名优先）。
4. **审批人展示**：直接用接口 `approvalUserName` 字段。
5. **执行人** = 执行出入库的经办人（`executeUserName`）。
6. **单位**统一走 `inv_unit` 字典；**主项目名称** = 项目主项目名（`projectName`），分期名仅作下拉 label。
7. **全局禁用浏览器自动填充**（`autocomplete="off"`，`src/utils/autocompleteOff.ts`）。


### 2026-09-14 项目还料参与范围

- PC 还料查询 `/project/member/participatedProjects` 时传 `periodStatus=IMPLEMENTING,DEBUGGING,DEBUG_COMPLETED,IMPLEMENT_COMPLETED,PENDING_ACCEPT,INTERNAL_ACCEPTING,ACCEPTING,REWORKING,WARRANTY,COMPLETED,CLOSED`，不传 `onlyUnfinishedProcess`。正常阶段从计划审批通过后开始，完结/关闭保留剩余物料处理入口；关闭本身不能证明历史计划通过，最终提交资格仍需服务端校验。
- 接口按当前用户已确认参与过滤，前端按状态复核并按 periodId 去重；支持主项目名称/分期名称搜索。还料按整个分期总账，不按原领料人或经理角色过滤。历史回显不能绕过当前参与项目范围重新提交。
- 选择后才分页读取整个分期物料总账，避免列表逐项目查询；没有 remainingReturnQty 的项目仍可选择并显示“该项目当前没有待还物料”，不能提交空单。完结/关闭项目也只能提交实际存在的待还数量。
- 此次只修改 PC 项目选择和校验，沿用现有 IN+RETURN+PROJECT 提交及库管办理流程。最新 Apifox 未明确关闭项目还料提交门禁、在途还料占用的完整规则，未宣称后端并发防超还或关闭项目提交已经实测通过；不在前端自行扣减总账。


### 2026-09-15 项目领料默认申请数量

领料申请选择项目用料后，新加入的物料默认申请数量等于当前可申请数量 availableApplyQty（基准单位），允许调小，现有上限校验保留。返工项目用料复用同一回填逻辑。编辑已有申请保留原申请数量，维修/劳保仍默认 1。接口参数与菜单权限不变。


### 2026-09-16 物料成本价展示调整

物料管理列表移除成本价列；新增/编辑共用表单中成本价移至基准单价之后。字段、校验及保存请求不变。菜单、权限和接口契约均不变。


### 2026-09-16 入库审批待办接入

STOCK_IN_APPROVAL 进入与出库相同的申请审批弹窗，标题按 IN/OUT 区分。详情、只读物料明细、整单通过/驳回和驳回原因沿用现有组件；项目入库统一校验本分期已接受经理，与最新审批契约一致。成功刷新待办，不新增审批接口。菜单配置已检查，不新增权限或路由。
2026-09-18 价格维护补传物料编码：GoodsPriceModal 从物料详情读取并保留 materialCode，保存 /stock/material/edit 时白名单提交 id、materialCode 和三项价格字段；缺失编码禁止保存并提示完善物料。独立价格权限及普通编辑字段隔离不变。Apifox 已刷新，定向 ESLint/diff检查通过，未修改真实价格，实际保存回查待验证。
2026-09-18 价格维护输入：报价比例输入框尾部显示 %，仅展示单位，保存仍为原值不除以100。成本价、基准单价、报价比例统一拦截字母/中文/正负号/科学计数法输入和非法粘贴，允许非负小数；保留价格2位、比例4位小数及提交校验。ESLint及输入/粘贴模拟测试通过，未浏览器实测。菜单、权限与API请求不变。 菜单两份指南及物料接口文档已检查，无需调整。
2026-09-18 价格维护补传 materialName：使用详情返回的原名称，与 id、materialCode、三项价格字段一同提交 /stock/material/edit；API白名单保留名称并拦截空名称。已刷新核对Apifox，ESLint/diff检查通过，未真实保存。权限与菜单不变。
2026-09-18 价格维护切换专用 POST /stock/material/updatePrice：仅传 id 和非空 costPrice/unitPrice/guideMarkupRate，0保留，至少一项；不再要求或提交 materialCode/materialName/单位列表，普通 edit 不变。按 success 判定业务结果，前端价格权限保留。最新Apifox明确后端仅沿用登录鉴权，独立角色权限仍需后端落实。ESLint、请求白名单/0/全空/失败模拟检查通过，未真实保存联调；菜单两份指南已检查无需调整。此条覆盖此前价格调用edit及补名称编码的说明。
