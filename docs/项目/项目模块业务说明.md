# 项目模块业务说明

2026-09-24 恢复报价详情/编辑页顶部“操作记录”按钮：不再被simpleLoad简化加载模式隐藏；沿用既有记录弹窗，仅点击打开时加载，保留分页。未保存的新单不显示。接口、菜单及权限规则不变，ESLint与差异检查通过，未做浏览器联调。

2026-09-24 报价明细型号与选料列表一致：最多两行，超出省略号，Tooltip展示完整型号；仅quotation模式，查看/编辑共用，不修改原值或提交数据。菜单、接口规则无需调整。

2026-09-24 报价明细物料名称可点击：MaterialPlanTable报价模式复用MaterialBasicInfoModal，明确传materialId（不以报价明细id代替），缺物料ID禁用；只读/编辑报价均可查看，点击后才查询，不预加载、不修改报价数据。菜单及API规则不变。

2026-09-24 报价选料型号最多显示两行，超出省略号，Tooltip保留完整型号；仅quotationLayout生效，原始数据与选择提交不变。菜单/API检查无变化。

2026-09-24 报价添加物料布局：共用选料抽屉新增quotationLayout，仅报价启用；取消类别/品牌独立列，品牌按既有字典拼在物料名称后括号内，无品牌不显示括号。报价抽屉宽min(1440px,96vw)，名称列340px可换行，横向滚动阈值900px供窄屏兜底；保留分类树、搜索、分页和选择逻辑。接口、菜单与权限不变；未浏览器目视验证。

2026-09-24 合同新增编辑按需加载：取消初始报价/权限和全量用户查询，报价选择展开或修改调整时加载，人员下拉展开加载且成功复用；销售负责人优先详情/当前登录人回显，关联报价ID取materialCandidateId。编辑不再补查项目和附件列表，附件取合同组合详情；从查看切换编辑仅补字典。新增仍按periodId查项目基本信息，合同类型/回款节点字典保留；提交前原报价和业务校验不删。菜单权限不变。定向静态检查，未真实保存联调。

2026-09-24 合同只读请求精简：合同信息view及项目详情合同页签仅请求contract/queryById(periodId)，取消项目、报价/权限、用户、附件列表及字典预加载；复用组合详情，名称缺失不补查。新增编辑保留数据加载，查看切换编辑补加载；审批经理下拉展开才查询用户。菜单/API契约不变；静态检查，未浏览器Network联调。

2026-09-24 分期计划审批提示位置：旧计划无审批轮次/轮次已变化提示由PeriodApprovalActions发出，在PlanAuditModal正文提示区显示；底部只留操作按钮。其他调用保持原提示位置。加载时清除提示，重开重置，不改变审批资格、接口或动作。菜单/API文档检查无需调整。

2026-09-24 计划页面展示调整：共用用料表单位/数量列改为90/110px，输入控件宽度约束适用所有模式，防止跨列重叠；分期计划审批弹窗的审批记录/通过/驳回等动作整体移至底部右侧。计划查看和编辑页在页签上方显示当前计划审批状态、最近一轮结果及审批人/时间/意见、撤回原因，驳回意见红字；复用PeriodApprovalActions已有分期实体和history请求，不新增请求，加载/失败清除旧摘要，不将历史通过当作当前资格。接口、审批权限及提交载荷不变；两份菜单指南检查无需修改。分期审批回归通过，真实浏览器布局及业务提交未验收。

2026-09-23 查看返工/工序进度请求精简：ProcessCompletionDrawer打开与重新加载仅查询工序及字典，不调用member/list和projectPeriodDetai；分期状态、当前返工ID复用项目列表上下文。勾选仅选择候选工序，最终完成前仍刷新成员及分期状态并核验经理/现场负责人，防止取消预加载造成越权；服务端保留最终鉴权。接口和菜单配置不变。定向ESLint及SFC检查，未真实账号网络验收。

2026-09-23 返工记录列表：验收详情页签统一改名“返工记录”，移除DetailReworks的a-card重复标题头，刷新保留为表格工具按钮。列表新增明确“审批”入口，仅project:rework:approve且approvalStatus=2显示，点击打开原单详情办理，保留查看详情入口；不按admin身份绕过授权，不更改后端鉴权或角色配置。组件编译与定向ESLint检查，管理员真实登录权限下发尚未验证。菜单和API定义已检查无需变更。

2026-09-23 PC返工入口合并：移除ReworkDrawer重复返工管理列表及嵌套详情抽屉；查看详情中的返工申请与审批记录保留为列表入口，点击查看/办理直接打开单据详情；申请返工直接表单，原单复用检查保留。修改、重提、撤回、审批、额外领料与工序日志入口保留。成功操作关闭并通知父级刷新，不再刷新废弃列表；取消新建关闭，取消已有单修改返回详情。移动端不在本次范围。定向ESLint、两端组件及轮次回归通过，未真实账号操作验证。接口与菜单权限未变，菜单指南检查无需调整。

2026-09-23 返工工序轮次展示：ReworkDrawer列表摘要及详情、移动端ProjectReworkPanel详情/审批与日志入口名称，使用所属返工单reworkRound拼接“第N轮返工-原工序名称”；同单多工序逐项拼接，不用version、行号或项目当前轮次。缺失/异常轮次保留原名，已带相同前缀不重复拼接；仅展示，不写入提交载荷、不增加请求。Apifox已确认OaProjectRework字段，OaProjectProcess模型尚无轮次，因此独立工序/日志页暂未扩展。菜单权限不变，定向回归/SFC检查，未真实页面联调或打包。

## 2026-09-23 分期整体计划审批（覆盖旧状态流转说明）

- 审批对象为整个分期，方案文件、用料、人员、实施工序、位置仍按原页面保存，提交前校验加载状态及未保存改动。
- 未开始→提交→待审批；通过→筹备中；驳回/撤回→未开始。审批状态独立为-1/0/1/2/3，不用历史第一条推断当前状态。
- 已接受项目经理或admin可提交；project:period:approve或admin可审批；本轮提交人或admin可撤回。后端再次鉴权，前端不扩大真实角色授权。
- 未开始/筹备中可编辑；待审批必须先撤回；已通过的筹备期受控资料修改后由后端作废原审批，前端回查当前阶段。实施后不修改原计划。
- 列表弹窗、计划详情共用 PeriodApprovalActions。审批记录按轮次展示提交/审批/撤回人及时间、原因、只读JSON快照。成功刷新页面与待办；业务失败保留输入，仅刷新门禁，不重放请求。
- 旧待审批且没有历史轮次：经理/admin从查看计划页重新提交。旧待办携带过期approvalId时禁止操作新轮次，须重新进入最新待办。
- 合同审批仍走原合同接口，但前端不再调用period/status将未开始推进筹备中；后续施工流程本轮未新增或改变。

2026-09-23 项目列表操作宽度收紧（覆盖前述4个阈值）：可见操作≤3个全部显示，>3个显示前2个加更多，行内最多3个按钮位置。权限、顺序、确认逻辑不变；0—7按钮专项回归及ESLint通过。文档已同步，菜单/API无变化，未重新打包。

2026-09-23 项目列表操作折叠试用：仅本页合并原行内与更多操作，先按auth/ifShow过滤，实际可见操作≤4个全部行内且无更多，>4个显示前3个，其余入更多。保留原顺序、disabled、回调及二次确认，未改公共TableAction，不扩展其他列表。0—7按钮、权限/隐藏、顺序/回调及SFC回归通过；ESLint排除现有index命名规则后通过。浏览器跳转登录页，未登录态目视验收。已检查菜单及模块API文档，权限码/业务接口/路由不变无需修改，未打包部署。

2026-09-23 项目列表甲方名称：单元格保持单行省略，鼠标悬浮或键盘聚焦通过Tooltip查看完整customerName，空值显示—且无提示。保持列宽和原始数据，仅展示调整。SFC编译通过；ESLint仅报原有index组件命名规则，临时关闭该规则后检查通过，未修改规则配置。未浏览器悬浮实测；已检查菜单和模块API文档，接口/路由/权限无变化无需修改，未打包部署。

2026-09-23 PC计划编辑资格：项目列表编辑入口与ProjectPlan页面统一仅允许本分期已接受的项目经理或admin角色；经理通过member/list分期成员关系确认，不以全局经理角色或旧合同经理ID替代。列表复用当前页经理查询，页面在身份/分期变化时重载，查询失败不给编辑资格。保留PREPARING阶段、显式只读及上下文成功限制，保存/提交沿用editable门禁。接口写入载荷、计划审批权限不变；后端仍须执行身份鉴权，未验证真实账号保存。两份菜单指南已检查，无新增菜单或真实角色授权变更。

2026-09-22 PC验收驳回提示精简：内外验各自的最近驳回原因移至对应状态/结果标签旁红字展示，长文本自动换行；移除独立的最近驳回负责人、日期、原因区域，避免相同验收说明重复展示。仍使用已有 lastRejectedAcceptance.remark，已通过隐藏旧原因，不回填当前表单；接口、权限及提交不变。菜单指南已检查，无需调整。

2026-09-22 latestStatus外层资格适配（覆盖旧内层字段说明）：PC/移动端读取result.canSubmitReacceptance，PC读取result.canApplyRework；不再以internal/customer内层资格或历史本地权限兜底放行。类型按status筛选，FAILED免整改复验、PENDING_REACCEPTANCE普通复验，已通过项不重提；内外验lastRejectedAcceptance单独显示最近驳回负责人、日期、原因，不填入当前表单，PASSED隐藏。移动端仍不新增返工申请或文件详情。外层资格不替代服务端提交校验。菜单权限未变；回归及SFC检查通过，未真实账号提交或打包部署。

2026-09-22 客户验收角色选项由client_role字典维护，PC/移动端显示字典名称、acceptUnitName提交字典值，未知历史值原样回显，不改变操作权限。

2026-09-22 复审入口更正：PC/移动端按latestStatus各类型canSubmitReacceptance===true显示申请复审；FAILED使用applyMode=WITHOUT_RECTIFICATION，PENDING_REACCEPTANCE使用NORMAL，类型仅取对应可申请项，已通过结果不重提。首次工序完成由后端自动启动验收，不额外调用apply；PC移除列表独立提交验收，移动端待验收按钮仅打开验收弹窗。历史无返工轮次PENDING_ACCEPT且NOT_APPLIED保留原经理/菜单资格下的兼容申请。FAILED/REACCEPTING补入验收入口/详情可见范围。移动端弹窗迁移latestStatus，使用外层状态说明，申请后重读最新状态、不伪造ACCEPTING。提交结果仍用complete，PC返工继续按canApplyRework；移动端沿用上一轮精简范围不新增返工申请。两种模式与资格矩阵、四个SFC、PC定向ESLint及diff检查通过；未真实账号端到端联调，未打包部署。

2026-09-22 PC返工详情补充：按最新要求移除“额外领料执行情况”区域、按钮、统计状态及getProjectReworkMaterials/loadMaterialMap请求；保留queryById返回的“返工额外领料计划”和工序行日志入口。覆盖此前按需加载执行汇总的说明。菜单、权限及接口定义不变。

2026-09-22 PC返工详情：移除“提交和审批记录”区域及解析代码；指定reworkId打开时仅查/project/rework/queryById，不预查返工列表。优先使用详情processes/materials/outsources数组（包括空数组），不让旧planJson覆盖真实工序ID。查看实施日志移至返工工序最后一列，固定右侧；仅审批通过且有真实工序ID显示，按该行processId跳转。额外领料执行汇总仍仅显式点击后按需加载，不影响首开单请求。新增/编辑/审批流程和权限不变；定向静态检查通过，未真实账号联调。

2026-09-22 PC验收最新状态接入（覆盖此前前端判断返工显隐及未迁移说明）：当前内外验改查 `GET /project/acceptance/latestStatus?periodId=...`，分别取 internal/customer.acceptance 填充详情，状态标签使用外层 status/statusText；不再查询全量验收列表推算有效来源。各类“申请返工”直接以对应 canApplyRework===true 显示，不叠加前端经理、菜单或返工状态判断；加载中、失败、字段缺失时不显示，切换上下文丢弃旧响应。历史列表仍按需分页查询；返工详情、原单ID/version仍由抽屉查询，提交后端最终校验。申请和返工变更后刷新最新状态，单类型刷新保留另一类型未提交内容。未修改菜单编码或角色授权，两份菜单指南已核对无需修改；未执行真实业务提交，移动端不在本轮范围。

## 2026-09-22 · 申请返工显示条件简化

当前内验或外验任一结果为未通过（FAILED），即满足“申请返工”的业务显示条件；保留已接受邀约的项目经理身份及 `project:rework:apply` 权限，不再按项目阶段、返工状态或另一类复验状态隐藏。打开和提交返工不再执行前端来源有效性、施工状态及返工互斥预检查，统一由 `POST /project/rework/submit` 校验并反馈原因；保留必填表单校验、原单复用及版本参数。此调整不包含 latestStatus 数据源迁移。PC 验收及返工契约回归通过；未代用户执行真实提交。两份菜单指南已核对，权限项未变，无需修改。

2026-09-22 返工申请入口修复：无需草稿保存，沿用submit一次提交完整方案；内部方法改名submitApplication。失败验收入口不再因返工列表阻止打开抽屉，已有来源申请复用原单：驳回/撤回及旧待提交记录由原申请人修改重提，待审批/已通过只查看；不新建重复单。入口资格变化明确提示，提交仍保留版本、身份、来源及有效返工互斥校验。经理资格不再依赖返工列表加载。最新Apifox确认submit完整方案协议。返工契约和PC验收回归通过，未代真实提交。菜单已检查无需修改。

2026-09-22 验收申请返工互斥交后端：普通验收及免整改复验入口移除blockingRework前端条件，保留项目经理/按钮权限、项目及验收状态、原因等校验；不改施工返工申请自身限制。仍调用acceptance/apply并显示后端业务错误、失败保留输入。最新Apifox仍明确拒绝与活动返工并行，因此前端改动不表示大连集运智能闸口分期能通过后端验收校验，未代提交业务。菜单已检查无变化。

2026-09-22 项目列表用户名筛选：Project.api.ts的projectList每次从当前登录store读取username，去首尾空白后附加至projectPeriodList请求，覆盖调用方同名参数；用户名缺失拒绝请求，不以空值查询全量。原搜索和分页保留，其他模块独立封装未改。已刷新Apifox：普通账号按未删除成员关系过滤，不限角色/邀请状态；有效负责部门账号范围放开仍保留其他条件，交由后端判断，前端无admin豁免。该参数后端仍为可选筛选，不等于服务端数据权限强制鉴权；后端应绑定认证用户防删改参数。用户名/账号切换/防覆盖/缺失拦截及列表筛选回归、ESLint通过，未跨账号真实列表验收。菜单文档已检查，无编码/角色/路由变更，不需调整；未打包部署。

2026-09-22 报价管理项目名称模糊查询修正：搜索表单改为keyword，quotationFilters保留非空keyword并去首尾空白，空串/空白/null/undefined不传；匹配项目与分期名称由服务端分页前完成。最新Apifox已核实materialCandidate/list支持keyword。旧projectName筛选兼容其他调用方，报价搜索不再使用。定向参数回归通过；菜单与权限不变。

2026-09-22 撤销报价不得低于成本价限制：输入、保存、重提共用校验仅保留必填、非负数及两位小数，允许报价低于成本或为0；不改后台接口。低价边界、驳回状态允许edit/submit的权限单测及原报价回归通过。当前浏览器王成旭驳回页无提交按钮，尚未核实其submit权限，不能将价格校验移除视为真实重提验收成功。结构/菜单已检查，无变化。

2026-09-22 市场直接报价前端校验：finalPrice不得小于basePrice，允许相等及0/0；使用Big精确比较，成本或报价变更时行内提示，保存和提交审批再次拦截。已保存值不自动改写，技术线路指导价流程不变。复用原editBatch/submit接口，无载荷变更；此为前端校验，不代表服务端已实施同规则。边界/小数/空值及SFC回归通过。结构和菜单文档已检查，无新增入口或权限。

2026-09-22 市场直接报价新选物料的finalPrice默认0.00，沿用两位小数输入；仅初始化新行，不覆盖已保存报价或恢复草稿，不为技术线路自动填指导价。专项回归和SFC编译通过；结构、菜单/API无变化。

2026-09-22 新增报价弹窗仅保留项目分期选择，移除报价单名称输入框。沿用原项目名自动生成名称并带入编辑页；详情页及接口必传名称协议不变，版本号未实施。SFC回归通过。结构、菜单及API文档已检查，无变化。

2026-09-22 报价编辑器排版统一：MaterialPlanTable始终启用combinedMaterialIdentity，新增、编辑、查看均使用物料名称（品牌）及下一行编码，复用220px名称列及1100px横向宽度，不再随viewOnly切回编码/名称/品牌分列的1800px布局。仅改变展示，不改可编辑条件、接口或权限。王销售新建页热更新后已确认合并表头、价格输入仍可编辑；报价回归及SFC编译通过。菜单和API文档已检查，无需修改。

2026-09-22 市场人员录入与审批线路解耦：真实市场部门人员查看/编辑草稿、驳回及待审批历史单时显示成本价、报价输入框，编辑仍受原按钮/状态限制，待审批只读；不因历史TECHNICAL线路隐藏报价，不改服务端approvalRoute。新选物料默认成本0，保存仍editBatch的basePrice/finalPrice。王销售新建页已目视确认两输入框可编辑、成本0，未代保存提交；旧单编辑需实际保存回读验收。回归/SFC检查通过，菜单权限未改。

2026-09-22 市场报价新选物料的成本价默认0，由人员填写；不自动带出物料库成本。已保存报价重新打开或恢复草稿不得清零，技术线路仍带物料成本。

2026-09-22 市场直接报价：以服务端固定approvalRoute=MARKET识别，发起人填写成本价与最终报价（两位小数），不填指导比例；保存只读，草稿/驳回可解锁修改，待审批可按原提交人规则撤回。市场主管只读审批；通过后锁定。目标为通过即定价并授权导出，但最新后端文档仍为priced=0，前端明确提示未定价，不模拟成功、不自动串调price。技术线路及历史TECHNICAL单保持原流程。真实跨账号验收待进行。

2026-09-21 项目详情报价入口：报价信息仅向真实所属市场部（用户确认即销售部）、技术部用户挂载；不按负责部门、销售角色或admin身份额外放行，URL tab=quotation及已打开页签不能绕过。优先复用登录部门名称，缺失才按所属部门ID查询queryTreeList，失败不加载报价。BACK动态路由已有项目详情/报价列表但缺少/plan/material-draft/editor时补共享隐藏路由，已有路由不重复注册；保留报价按钮权限、状态与后端鉴权，不修改真实菜单或角色授权。项目报价入口补齐名称、状态、version、adopted、priced、createBy及来源参数，返回原项目报价页签。专项路由/部门/SFC回归、定向ESLint及diff检查通过；permission.ts仍有原有未使用变量报错，旧报价锁定测试缺useSessionDraft桩未通过。浏览器账号/页面在检查期间已切换，未进行跨账号点击验收，未部署。

2026-09-21 PC项目列表：质保期WARRANTY不再显示操作栏“验收”按钮；其他状态规则及项目详情历史验收入口保持不变。仅显示条件调整，无接口、菜单权限变化。

2026-09-21 验收详情抽屉列宽优化：抽屉最大1440px且不超过96vw；历史表固定布局，横向滚动宽度按列宽总和1420px计算，避免长附件名挤压类型/负责人/日期。表头及短字段不拆成竖排，长文本省略，附件按钮限制在列内并保留完整文件名title及点击预览。仅PC展示调整，未改接口/权限/业务。SFC布局回归、ESLint通过，尚未浏览器视觉实测。

2026-09-21 PC验收：最终规则为仅运维主管及管理员代办，普通运维成员不开放，叠加按钮权限及有效记录状态。弹窗每次打开查询真实分期详情，待办不传假定ACCEPTING；apply成功保留表单刷新项目状态，complete成功关闭、失败保留。内部不传联系电话。项目列表、验收标签及办理/复验/返工判断兼容REACCEPTING。定向回归及Vite构建通过，Apifox当前不可用，真实账号和运行后端状态待联调；无部署/移动端/角色授权修改。

2026-09-21 待验收待办主管办理修正：PC内验填写资格优先读取登录roleCodes中的operations_manager/admin；无指定角色时按responsibleDepartmentIds（departIds负责部门）匹配唯一有效运维部，不再用普通所属部门放行。保留project:internalAccept、当前记录IN_PROGRESS、页面原状态校验；外验仍仅本分期经理。待办入口无需额外project:acceptance:view权限。移动端已识别operations_manager/admin，本轮未改。后端complete仍按负责部门/admin最终鉴权，角色本身不替代后端配置。主管/管理员/负责部门/普通成员否定回归及定向ESLint通过；未真实主管账号提交。

2026-09-21 导出授权弹窗操作布局：选择用户后点击下拉右侧“授权”，底部不显示按钮，右上角关闭；下方标签撤销交互不变。

2026-09-21 导出授权上方直接展示可搜索姓名/账号的用户下拉，下方“已授权用户”使用标签及×撤销，代替表格。用户分页完整加载，受当前登录账号数据权限约束，不显示冻结用户；失败可重试。

2026-09-21 本分期报价导出授权仅包含用户选择与已授权用户列表；点击授权导出后刷新列表，删除需确认且仅撤销报价权限、不删除用户。不授予查看/修改价格权限，原导出不含成本价。仅显示有效导出授权，内部保留撤销记录用于版本校验。

2026-09-21 确认定价后页面价格锁定，再次进入或刷新均只读；原有定价资格用户点击“解锁”仅开放指导比例和指导价，再次确认成功后锁定。失败保留修改供重试，不开放成本、数量、单位或物料编辑。

2026-09-21 报价授权仅在审批通过且确认定价成功后显示；仍限有报价管理资格及授权按钮权限的用户，未定价不挂载授权弹窗。

2026-09-21 报价表响应式修正：名称列固定220px，长名称换行，备注自适应剩余空间；数字控件取消全局最小宽度影响，保持在各自单元格内，窄屏横向滚动，不改变价格和编辑资格。

2026-09-21 报价详情指导比例输入和只读展示保留两位小数及百分号；单位、数量、成本价、指导比例、指导价使用紧凑定宽，名称列分配剩余空间，减少横向拉伸。

2026-09-21 项目、计划、实施列表的名称查询仅在有效输入时发送 keyword；清空或只输入空格时不携带该条件，恢复无名称限制的查询。

2026-09-21 项目名称搜索修正：项目管理、计划方案、实施管理使用 keyword，检索主项目与分期拼接名称，不再限定仅主项目名；展示仍为主项目-分期。报价、采购、出入库统一 keyword 尚待接口支持确认。

2026-09-21 报价详情的物料组合列显示“物料名称（品牌）”，品牌为空仅显示名称，物料编码保留第二行，不再单列品牌；其他清单及导出字段不变。

2026-09-21 报价成本价只读展示：使用与数量一致的禁用数字输入框，不再退化成纯文本；是否可编辑仍由原成本价权限控制，不增加价格写权限。

2026-09-21 验收待办分流：PC TodoActionHost与移动端首页兼容todoType=PROJECT_ACCEPTANCE_FAILED及actionKey=PROJECT_ACCEPTANCE_FAILED_HANDLE，查看详情调用acceptance/queryById(id)，展示项目摘要、验收类型、实际负责人、日期和驳回原因，按钮为确认/申请复验。确认仅关闭弹窗，不调用待办完成接口。申请复验打开现有验收弹窗，重新查询最新验收及经理身份，来源ID与当前失败记录不一致或无资格时不自动打开申请；匹配时预选来源类型，填写原因后沿用apply WITHOUT_RECTIFICATION。PROJECT_ACCEPTANCE_PENDING/PROJECT_ACCEPTANCE_HANDLE直接打开验收弹窗，不跳项目详情整页；bizSubId是分期，bizId是验收记录，不混用。中文名称优先字典，补缺省名称。新增入口沿用原办理/复验权限，后端最终鉴权及关闭待办；未修改后台菜单角色。两端分派/ID/详情校验、SFC及既有申请/请求回归、PC定向ESLint通过；未真实账号办理或部署。

2026-09-21 列表项目名称统一（部分完成）：项目管理、计划方案、报价管理、实施管理均用projectDisplayName显示主项目名称-分期名称，缺一侧只显示已有名称，全空—；搜索标签项目名称、仅传projectName，移除分期名搜索，项目列表不再由该搜索框传keyword。采购订单展示也改组合名称，复用projectPeriodList完整分页补齐主项目/分期名称，不改表单保存字段。后端阻塞：purchaseOrder/list仅支持periodName搜索；stock/apply/list的projectName实际是保存的分期名称，缺主项目返回及查询协议。因此采购搜索和出入库记录暂保持原行为，不用前端当前页过滤或伪造查询参数；全OA统一尚未完成，需后端补主项目名字段与分页前查询。菜单/权限无变化，两份指南已检查。

2026-09-21 统一验收申请（覆盖此前period/status及逐项acceptance/submit说明）：PC列表提交验收、验收弹窗申请验收及移动端对应入口统一POST /project/acceptance/apply，JSON仅periodId、acceptTypes、reason、applyMode。普通申请NORMAL，失败免整改复验WITHOUT_RECTIFICATION；类型INTERNAL/CUSTOMER可多选，原因去首尾空白后1～400字。两个类型一次事务请求，失败保留输入，不再部分成功逐项重试、不传sourceAcceptanceId/reworkId。普通申请只选不存在或NOT_STARTED的类型，验收中可补申请另一类；复验只选失败类型。按返回数组的acceptType更新对应记录，保留未选项，外验成功后定位填写区；实际结果仍调用complete。后端最终核验本分期经理、工序完成、返工互斥及通知接收人，前端不新建返工记录。沿用project:acceptance:submit，无新菜单或角色授权。两端动作模拟及5个SFC、PC定向ESLint通过，真实账号端到端未验证，未部署。

2026-09-21 项目补料审批待办详情：PROJECT_ADD_MATERIAL_APPLY_APPROVAL按todoType/actionKey精确匹配，actionParams.applyId优先、bizId兜底为申请单ID，缺失提示错误；打开MaterialSupplementDrawer的targetOnly模式，queryById加载指定补料单并从响应periodId确定分期，自动展开物料记录，不用bizSubId猜分期、不走库存审批，也不依赖分期列表第一页命中。后端返回的目标详情可只读查看，审批仍受原权限与状态限制；禁用新增补料。普通项目补料管理入口保持原列表行为。派发/缺ID模拟和定向ESLint通过，未真实待办账号点击验收。菜单指南已检查，无新菜单码或角色变更。

2026-09-21 项目管理列表及筛选：主项目/分期两列合并为“项目名称”，以主项目名称-分期名称展示，缺失一侧仅展示另一侧，全空为—；仅展示拼接，不修改原数据。合并搜索框传keyword，新增projectManagerName姓名查询；status/arrivalStatus/contractStatus/projectType均为多选，API封装统一去空白/去重后英文逗号拼接，保留0/-1，清空省略，保留分页参数。已刷新核对projectPeriodList契约，keyword按主项目与分期无分隔拼接匹配，展示用连字符不作为后端搜索分隔协议；筛选均由后端分页前完成。专项回归和定向ESLint通过；浏览器被另一会话占用，未页面实测/真实查询验证。仅PC，未改新增编辑表单、权限或部署包；菜单文档已检查，无菜单/路由变化无需更新。

2026-09-21 会话草稿：合同仅恢复编辑字段、附件引用、关联报价及付款输入，服务端原始合同基准变化则不恢复；报价仅编辑模式恢复输入，需最新version一致且仍有编辑资格，审批/定价不使用草稿。新增项目成功/取消后再次激活初始化空表单。计划通过独立路由上下文及KeepAlive保留未保存内容，尚无刷新后的session草稿。普通切页不清草稿，成功清理；返回合同保留草稿。

2026-09-21 PC内部验收填写资格修正（覆盖此前固定operations_manager角色判断）：保留project:internalAccept及当前记录IN_PROGRESS校验，admin可代办；非管理员以登录身份departIds负责部门ID精确匹配/sys/sysDepart/queryTreeList唯一未删除“运维部”，不按普通所属部门或主管角色名放行，部门status不参与判断。仅需要内验资格且有负责部门时查询部门树，失败只读并可重试；切换身份/租户重新校验，外验经理规则不变。已刷新核对Apifox，未修改菜单码、真实角色授权或提交载荷。本轮仅PC，移动端未修改；定向资格回归/SFC/ESLint通过，真实账号填写及提交尚未验收。

2026-09-20 PC新增项目会话草稿：ProjectApply在页签离开、组件销毁/停用及pagehide时保存sessionStorage，输入/变更延迟250ms补存；初始化下拉和默认值完成后恢复表单，多选数组还原、地图经纬度同步。按账号/租户/创建模式/父项目隔离，token变化时仅清除此类草稿，旧组件禁止跨会话回写。成功保存或显式取消清除，保存失败保留；编辑已有项目不启用草稿。未上传附件仅内存保存，页签切换可恢复；整页刷新后只提示重新选择，不伪造文件。缓存不可写时提示。草稿工具/隔离/清除/附件/SFC回归及ESLint通过，未真实浏览器切页/刷新联调。未改业务API、移动端或菜单权限；菜单及模块API文档已检查无契约变化。

2026-09-20 重新申请验收提交去除返工前置查询：PC/移动端选择失败类型并填原因后，直接逐项POST /project/acceptance/submit（periodId、sourceAcceptanceId、reason），不再调用assertAcceptanceApplication读取成员/项目/验收/返工列表。保留已加载身份、按钮权限、失败状态、原因校验，最新状态/并发互斥交后端最终判断；不调用返工创建或提交接口。接口路径未改为period/status，因刷新后的Apifox尚无该接口复验参数；首次及施工返工完成后提交分支不变。动作回归（禁止返工前置查询）、两端SFC、PC ESLint通过，未真实账号联调。菜单权限无变化。

2026-09-20 失败后申请验收入口请求修正：PC openRecheck仅基于本次已加载身份及验收结果打开类型选择框，移除点击入口即调用loadManager产生的成员/返工列表请求。确认提交仍调用assertAcceptanceApplication复核并按所选类型提交acceptance/submit，不移除后端鉴权；首次/施工返工完成后的period/status分支未改。移动端失败后入口原本不预查，无需修改。动作及布局回归通过，尚未取得用户实际Network请求，不能确认其报告的问题已完全复现。菜单文档检查无权限变动。

2026-09-20 外验表单与提交修正：PC通过校验改为实际上传字段acceptanceFormFileId至少1且最多3个，不再误要求completionReportFileId；移动端已校验正确无需改。PC验收日期仅YYYY-MM-DD，与移动端一致，提交仍适配现有接口为所选日期00:00:00。注意当前后端要求结束时间不早于开始时间，当日开始晚于零点时仍可能拒绝，需后端按北京时间日期比较，不能宣称已端到端修复。两端新增可选客户负责人角色acceptUnitName（暂定字段，用户授权先写）：PROJECT_LEADER项目负责人、TECHNICAL_LEADER技术负责人、PURCHASE_LEADER采购负责人、OTHER其他；空字符串表示未选择/清空。仅CUSTOMER提交，已办和历史回显，不属于系统RBAC；需后端complete接收保存、list/queryById返回同名字段及edit保持一致，旧数据允许空。当前Apifox尚无该字段，未证明持久化。文件校验/提交载荷/日期控件/SFC定向回归、PC ESLint通过；未真实账号联调或打包部署。菜单及权限文档已检查，无新授权项。

2026-09-20 验收弹窗流程按钮补齐：PC弹窗底部统一查看详情、申请验收、申请返工，不再按失败类型重复入口；移动端新增同名固定底部入口。失败后申请验收可多选INTERNAL/CUSTOMER，已通过/办理中不可选，按所选类型逐笔调用acceptance/submit；部分成功保留成功结果，仅剩余项重试，外验全部申请成功后定位填写区，内验通知由后端事务创建。首次及施工返工完成后的PENDING_ACCEPT仍使用period/status，不传类型。返工不弹类型选择，自动关联最新失败记录作为必填sourceAcceptanceId（同时间内部优先），直接打开方案；另一失败项保留。两端申请前置校验支持未被替代的跨轮失败记录。身份及按钮权限保留，无角色授权改动；详情/外协/物料按原按需加载。定向动作、多选/部分失败、身份、请求、SFC回归及PC ESLint通过，PC/H5构建通过；未真实账号或浏览器联调，未打包部署。

2026-09-20 验收最新记录修正（覆盖此前按当前返工轮筛选的说明）：PC/移动端每次打开重新查询 acceptance/list，按分期及INTERNAL/CUSTOMER分别完整分页读取，按createTime（缺失时updateTime）倒序取未被sourceAcceptanceId替代的最新一条。旧项目列表轮次不再过滤失败或办理中记录，不跳过最新失败去取历史通过。完成提交使用该验收记录自身reworkId。排序契约尚未明确，不使用pageSize=1；历史详情仍按需加载。权限、菜单不变。定向回归通过，未真实账号联调、未打包部署。

2026-09-20 登录身份复用与验收查询调整：两端复用既有用户状态及其持久化机制，normalizeIdentity统一读取用户ID、所属部门ID、负责部门ID、服务端提供的角色ID及角色编码（不编造缺失ID）；PC退出清空角色缓存，移动端在登录/统一权限刷新时经getUserInfo补齐角色，失败清空角色资格。内验仅operations_manager/admin加原按钮权限，不再查userDepartList或部门树；外验仍为本分期已接受经理，页面成员查询按用户/登录会话/租户/分期缓存60秒并合并并发，失败不缓存，仅内验账号不查成员，业务提交的原鉴权保留。每次打开验收仍重新查验收列表，不缓存结果；PC移除reworkId查询过滤，无本轮记录时两端仅回显最新有效已通过记录且只读，不越过较新失败记录。未新增canHandle字段、未修改服务器通知/授权或申请接口；最新Apifox内验仍描述负责部门负责人/admin，前端依用户确认按角色收紧，真实主管配置需联调。定向身份/缓存/请求/布局回归与PC ESLint通过；真实登录刷新、跨账号办理及端到端返工验收未实测；未生成新的部署压缩包。

2026-09-20 报价只读详情表合并物料名称与编码：列名为“物料名称”，名称在上、编码在下且使用12px次级文字；普通报价详情、技术审批、市场定价和合同关联报价只读详情统一使用。报价新增、修改及合同调整编辑态仍分列显示，不改变提交字段、接口、菜单或权限。

2026-09-20 验收弹窗恢复身份前置校验（覆盖此前仅按按钮权限开放编辑的说明）：PC/移动端首开查询当前分期成员，匹配当前用户、有效且已接受邀请、memberRole包含2才允许外验；内验有按钮权限时查询当前用户真实所属部门及部门树，匹配运维部，不按负责部门/主管角色/admin推断。身份未确定、查询失败不开放对应编辑；两项独立判定，双身份可分别办理。保留记录状态及菜单权限，已完成只读，后端提交鉴权不变。首开不预查返工/历史/物料，同次打开不因提交刷新验收记录重复查询身份；切换用户/项目不接收旧身份结果。Apifox已刷新核对member/list与部门树；userDepartList未收录，复用本地用户管理既有userId调用，真实环境仍待验证。身份、权限、请求、布局/SFC回归及PC定向ESLint通过；未做真实账号保存回显、未重新打包部署。

2026-09-18 补齐 PC 验收弹窗底部操作：查看详情、申请验收、失败项申请重新验收和申请返工，与结果提交按钮统一进入弹窗 footer 右侧。弹窗正文隐藏对应重复入口；非弹窗项目详情布局保持不变。无可提交结果时底部仍可显示查看详情；多失败项以内部/外部标签区分。复用既有权限、状态、点击处理和按需请求，上传/提交中禁用操作。布局/SFC、动作分派、权限与请求回归通过；菜单和模块 API 文档已检查，无契约变化，不修改。未做浏览器实测。

2026-09-18 PC 验收弹窗的结果提交按钮移至弹窗 footer 右下角，不再放在各验收卡片内部；只展示可办理类型，分别标注“提交内部验收 / 提交外部验收”。复用原提交方法、权限和校验，上传/提交中禁用并显示加载状态。项目详情非弹窗入口保留原布局。未改接口、移动端、菜单或权限配置；模块 API 和菜单文档已检查无需修改。SFC、布局/权限回归及定向 ESLint 通过，未做浏览器实测。

2026-09-18 外部验收负责人分开展示（PC/移动端）：办理表单“验收负责人”只读显示当前操作人；“客户验收负责人”手填并继续提交 acceptUnitLeader。已办摘要及历史记录的验收负责人只取 acceptLeaderName，不再用客户姓名兜底；外验另外显示客户验收负责人。Apifox 已刷新核对：acceptLeaderId/acceptLeaderName 由后端记录实际操作人，前端不新增提交字段或接口调用。权限、菜单和流程未改；菜单配置文档已检查，无需更新。SFC、定向回归及 PC ESLint 检查通过；未做真实账号保存回显或真机验证。

2026-09-18 PC 验收失败操作移至内外验卡片下方，按失败类型分别显示“申请重新验收 / 申请返工”，保留原项目经理、按钮权限、状态及在途返工校验，不修改已通过项。返工详情和审批确认框新增额外领料计划，只读展示 planJson.materials 的物料、plannedQty 和单位，与审批后执行统计分开；无计划时显示无需额外领料。点击审批时才读取 queryById 最新方案和校验审批资格，未增加验收首开请求或物料库预加载。Apifox 已核对，接口/载荷不变。布局、权限、请求和返工契约回归通过；未做真实账号联调。菜单配置文档已检查，无新增菜单或权限码；移动端本轮未修改（已有详情计划展示）。

2026-09-18 报价角色分工：待审批报价只允许原提交人在审批前撤回；技术部主管通过technicalApprove进入只读技术审批，只可通过/驳回且不展示提价比例、指导价；技术审批通过后，市场部主管须同时满足后端canManage及plan:quotation:price，方可从“市场定价”维护比例和指导价并确认定价；报价授权由满足canManage及plan:quotation:grant的市场部主管办理。普通详情不展示市场价格。最新Apifox的approve和待办仍限定管理员/市场部负责人，后端尚需改为技术部主管，正式角色授权本次未修改。

2026-09-18 报价编辑页成本价与数量输入框统一列宽和最多2位小数精度；成本价0继续视为有效值，保存字段仍为basePrice，未调整报价状态、权限或菜单。

2026-09-18 按需加载二次修正（上一轮未完整落实）：历史验收改为详情打开后独立请求 acceptance/list，pageSize=10，按服务端分页，成功页按页码缓存，切页复用；不再直接把主区已加载数组当历史表。API 总数仍包含待办理记录，本页只展示已办/取消历史，空页允许继续翻页，未伪造服务端历史筛选字段。PC MaterialPlanTable 改为 materialsVisited 首次需要补料才挂载，关闭只隐藏以保留草稿；两端外协开关只在尚未加载第一页时请求，下一页由加载更多触发。移动端补料首次开关加载，重复选择复用。验收完成保留弹窗与另一侧未提交内容，只刷新完成类型并使历史缓存失效。已刷新 Apifox：仍缺当前有效记录/排序及可办理标志契约，因此主区为正确选择复验链末端仍可能翻页；PC 已有非空 currentReworkId 时限制该轮。此项未完成，不能把后台身份鉴权收紧视为查询字段已补齐。已有返工详情的额外领料统计和物料基础查询也已移到独立按钮，首次点击才加载并缓存；打开返工记录只查单据详情及其中审批快照。请求模拟验证历史首开/缓存/翻页/单类型刷新及补料挂载门禁，SFC、ESLint、PC/H5构建通过；无登录态真实 Network 验证。菜单指南已检查无变化。

2026-09-18 验收按需查询（覆盖同日提前查记录数量/资格的说明）：用户确认具体办理资格已由后端收紧。打开验收时两端只按 periodId、INTERNAL/CUSTOMER 查询 acceptance/list，每类型分页100，超过一页继续读取以正确选择复验链末端；取消弹窗项目详情、部门成员、项目成员和返工预查。PC 列表传入行上下文，移动端项目详情传入已有详情；旧上下文缺少 currentReworkId 时仅用验收记录最新创建轮次展示，complete 最终核验轮次/记录/身份。按钮按对应权限码和记录状态显示，不代表后端身份核验已通过；complete 直接提交，不再重复身份预查，后端拒绝保留表单。其他入口的原身份门禁不全局修改。查看详情固定保留，两页签空时显示空态，不为隐藏按钮提前查询；历史验收复用已加载记录，返工/审批列表首次切到该页签才挂载，单次打开切换复用。点击复验/返工申请时才执行原有申请资格/互斥检查，外协和物料仍在返工表单按原开关加载，不在验收打开时查询。真实 load 函数模拟确认两端各2次验收列表；布局/SFC、PC ESLint、PC/H5 构建通过，尚未登录账号验证 Network 和服务端拒绝链路。菜单指南已检查，无菜单/接口参数/后台授权变更。

2026-09-18 验收布局收敛：PC DetailAcceptance 主区仅展示当前内外验，非可编辑记录改为责任/负责人/日期/说明摘要，复验与申请返工保留当前卡片。底部“查看详情”打开右侧抽屉，按“历史验收记录 / 返工申请审批记录”分组，复用原返工详情办理组件；无某类记录隐藏对应页签，两类均空隐藏入口，返工查询失败保留详情入口供重试。历史验收 PC 每页10条，返工沿用分页。移动端 ProjectAcceptancePopup 同步只读摘要和右侧详情弹层；历史筛选沿用 isAcceptanceHistory，不把待填写草稿当历史。继续用 acceptance/list、rework/list，无新增 API/授权；只增加一次返工存在性查询，详细返工面板打开后加载。布局/SFC/业务回归、PC ESLint、PC/H5 构建通过（H5 原有图标警告）；本地浏览器停在登录页，未完成真实账号点击和视觉验收。两份菜单指南已检查，无菜单或权限变化。

2026-09-18 PC内部验收新增真实所属部门门禁（覆盖前述仅按钮判断）：operationsMembership读取现有userDepartList(userId)与queryTreeList，按部门ID匹配唯一未删除且未禁用的运维部，拒绝缺失/重名/失败；不按负责部门或主管角色，管理员不豁免。编辑/上传/提交统一要求有效用户、运维成员和project:internalAccept，上传/提交前重查，切换账号或重新加载先清空资格。陈雄真实所属工程部，浏览器调用新门禁返回false；未切换其账号完整UI验收。部门逻辑、PC权限矩阵、两端组件编译及ESLint通过。userDepartList为仓库既有接口且已实测，当前Apifox未收录此路径，需后端补文档；queryTreeList已核对最新契约，历史status=null按未禁用兼容。仅修PC，移动端仍需同步该部门门禁；未改真实角色/部门/后端或部署包。菜单文档已检查，编码与路由无变更。

2026-09-18 内验部门规则核对：最新complete契约要求有效运维部所属成员（sys_user_depart），不按负责部门depart_ids或operations_manager主管角色判断；同时需内部验收接口权限，无管理员代办。PC/移动端现有前端门禁仅依据project:internalAccept，不验证真实部门归属；最终资格必须由后端校验。修正PC“运维部门主管”残留提示为“运维部门成员”，同步两端旧主管限定回归断言。未修改真实部门、角色或后端代码；具体账号提交失败尚待现场定位。菜单指南已检查，权限码/路由未变，无需调整；部署包未更新。

2026-09-18 PC返工外协填写改为紧凑表格：外协单位、人数、工时、操作四列，必填列标识、底部添加按钮、逐行移除及空表提示；已选单位从其他行下拉选项过滤，稳定行标识避免删除时输入串行。保留人数/工时/重复单位提交校验，内部行标识不传后端。Vue编译及校验函数测试、ESLint通过；未浏览器实测。已检查模块API文档及菜单指南，接口、权限和路由无变化，无需修改；移动端及既有部署包未改。

2026-09-18 PC返工管理：预计再次验收日期移至申请信息内、返工原因上一行，增加必填标识、字段错误提示和提交前空值拦截；新申请及编辑重提共用校验，保留不得早于工序计划完成日期的规则。仍以expectedAcceptanceDate传YYYY-MM-DD；Apifox后端字段仍可空，本次为PC表单必填约束，移动端未改。空值/清空、顺序、日期范围、提交门禁及Vue编译和ESLint检查通过；未真实提交，未更新部署包。菜单/权限/路由已检查，无需调整。

2026-09-18 内部验收日期：按用户明确要求，PC仅选择年月日，提交acceptEndDate固定补为“YYYY-MM-DD 00:00:00”，旧日期值带时分秒时提交也归零；PC外部验收不变。移动端现有日期选择及零点提交已符合，无需改动。Apifox已刷新，complete仍规定不得早于验收开始时间，因此同日零点可能被后端拒绝，此风险已告知；不绕过后端校验。已检查菜单指南与路由，无变更。本次未重新生成部署包或完成真实提交验收。

2026-09-18 报价按钮菜单权限（覆盖此前不新增静态按钮码的说明）：报价管理下配置10项PC按钮，复用 add/edit/delete/lock，新增 submit/withdraw/technicalApprove/price/grant/export（均为 plan:quotation: 前缀）。delete改名作废报价，lock改名市场审批；technicalApprove仅预留，旧unlock保留但不作为当前流程入口。列表、编辑器、授权弹窗及合同内报价调整同时校验菜单权限、分期资格和业务状态。当前 localhost:3101 所连后端已回读确认全部按钮，admin角色全部授予并刷新确认实际权限；其他角色未改，其他环境未配置。权限回归、定向ESLint通过；未进行跨账号真实报价操作，后端接口菜单码强制鉴权未由本次前端改动实现。编码表与验证范围见《报价治理接口对接》；页面路由不变。

2026-09-17 报价治理更新（覆盖下文旧报价权限、价格联算和审批接口说明）：提交/审批/撤回/作废/定价接入专用接口；整单 version 校验；价格及导出按分期授权；审批通过且已定价才可受控导出；合同调整和报价审批待办同步。平铺列表与三个筛选保留。菜单及隐藏路由不变，未修改真实角色或用户授权。详见[报价治理接口对接](报价治理接口对接.md)。本地页面和静态回归已验证，真实跨账号完整流程未验收。


2026-09-17 内部验收权限：整个运维部按后台已授 project:internalAccept 办理，PC/移动端取消主管角色硬编码。当前轮次和待填写状态仍校验；外部验收保持项目经理及 project:accept 双重校验。后端仍为最终授权边界，本次无角色授权修改。

2026-09-17 实施日志媒体回显：PC 共享日志详情与移动端 implementLog 使用 logMediaUrl 仅在展示时规范 static/、sys/common/static/、shouhuiApi/sys/common/static/ 相对前缀，避免重复拼接；HTTP(S)/协议相对直链及本地临时预览不补前缀。上传返回和日志保存原路径保持不变。PC 从 photos 按扩展名区分照片、视频，视频使用原生控件点击播放、不自动播放、不预加载；独立实施详情与项目日志弹窗共用修复。已刷新核对 implementLog/queryById、上传契约；路径回归、两端 SFC 编译、PC 定向 ESLint 通过，真实文件预览/视频播放未实测。菜单两份指南已检查，无路由、权限或菜单变更。

2026-09-17 候选清单筛选：报价管理平铺列表新增主项目名称、审批状态、采用状态查询，quotationList 与 quotationPeriodGroupPage 封装均透传 projectName/status/adopted。名称直接传关键词并去首尾空白，不拼通配符；空值省略，status=0、adopted=0保留。状态仅-1/0/1/2/4，采用0/1，条件由后端分页前AND组合；不恢复分组页面，list total仍为候选单数。若后续使用分组页，组内list必须带相同筛选才能与candidateCount一致。物料维护与选料抽屉已直接传materialName关键词，无需改动。定向ESLint、diff及参数模拟检查通过；本地页面实际验证三条件查询（含两个0）success=true、重置清空条件并回第一页。未验证分组接口真实返回，不访问测试环境；菜单/角色权限无变化。

2026-09-17 接口刷新补充（覆盖下方同日旧限制）：complete 按 acceptType 提交内外验结果，外验 acceptUnitLeader 已支持客户姓名，两端移除旧覆盖提示。返工 expectedAcceptanceDate（可选日期）已接入提交、重提、清空及回显并校验工序结束日期；V102 数据库迁移未核实。PC 通过附件校验修正为 acceptanceFormFileId。新后端内验资格是有效运维部成员，不仅主管；前端尚缺真实成员资格返回，旧角色展示门禁未放宽，跨轮保留通过项亦待契约明确。回归与 PC/H5 构建通过，未完成本地账号闭环。菜单编码及路由未变，菜单指南检查无本次新增配置；详见 PC 仓库 docs/项目/验收返工改造后端对接清单.md 最新进展。

2026-09-17 验收/返工本地改造：两端验收文件改为acceptanceFormFileId，客户负责人输入与明确日期时间；提交验收核验本分期经理，取消admin办理豁免。返工支持process数组、多工序详情/日志、基础外协分页选择及人数工时校验、可选补料，完整方案直接submit，停用草稿调用。预计复验时间尚无字段，暂不开放；客户姓名持久化、跨施工轮只重验失败项、真实运维负责人能力回显仍待后端。本说明覆盖旧单工序、草稿提交及管理员兜底说明。菜单编码/路由不变，未在线修改授权。详见[验收返工改造后端对接清单](验收返工改造后端对接清单.md)。

2026-09-17 主项目名称可编辑：ProjectApply 按页面模式控制名称字段，仅“已有主项目新增分期”时只读；新建主项目及编辑时可输入，名称必填且不可全空白。编辑通过原 JSON `/project/project/editProjectPeriod` 的 projectName 保存，分期归属与原项目对接人编辑资格不变。定向 ESLint 和组件编译通过，未完成实际保存重开联调；菜单与权限无变化。

2026-09-17 PC 统一上传检查：新增 uploadDocument(file, biz?) 通用方法，统一公共上传及响应路径校验；uploadProjectDocument 保留分期校验并委托通用方法，新增项目复用通用方法。修复旧 Plan.api.ts 的 savePlan/editPlan multipart 包装为 JSON。业务附件和专用导入/解析接口的检查边界见 [统一上传检查](统一上传检查.md)。静态与模拟分支检查通过，未真实上传联调；菜单无变化。

2026-09-17 新增项目及分期附件：`addProjectPeriod` 改为 application/json；有附件先调用 `/sys/common/upload`（新增尚无分期 ID，biz=project），校验上传成功后将 message 路径写入顶层 attachmentFileId，不再传 data 包装、attachment 文件或 fileName。无附件直接新增，上传失败不调用新增接口。编辑仍沿用公共上传后 JSON 保存。

2026-09-16 返工提交补参：保存后提交和已有返工单提交均携带当前项目分期 periodId、返工单 reworkId、详情最新 version。草稿保存、审批规则及权限不变；新增 periodId 按用户反馈补齐，尚未完成后端联调。

2026-09-16 内验资格加固（覆盖下文仅看按钮权限的旧说明）：PC与移动端内部验收同时要求正式角色编码 operations_manager 和 project:internalAccept；工程实施人员即使误勾该权限也不可办理。内置admin原兼容规则不变，外验仍为本分期已接受经理+project:accept。办理标签、编辑、上传及提交复用同一门禁；不按姓名硬编码。PC读取登录角色列表，移动端读取登录userInfo.roles[].value，角色缺失时拒绝内验。两端误授权回归及SFC编译、PC定向ESLint通过；浏览器连接中断，尚未完成陈雄/tian账号实测，后台角色误授未修改。

2026-09-16 报价分组：报价管理改为外层分期分组分页：`GET /project/materialCandidate/periodGroupPage`，按 periodId 唯一标识，展示主项目名称、分期名称、candidateCount，total 为分期数。默认折叠，展开才挂载 QuotationGroupTable，调用原 list 携带 periodId，组内独立分页；折叠卸载，重开重新读取。原报价操作及权限不变，提交/作废后刷新外层并收起。外层接口未定义搜索，移除原分期 ID 搜索，不传 groupByPeriod，不依赖 children。 本说明覆盖旧的平铺报价列表说明；菜单配置已检查，无菜单或授权变化。静态检查通过，浏览器文件选择窗口阻挡本次展开/翻页验证，尚未完成交互验收。

2026-09-16 验收资格补充：外验由当前分期已接受经理成员且具有project:accept的账号办理，不依赖详情未返回的经理ID，提交前复查；内验授权独立。两端历史验收区排除当前未提交/进行中记录，仅展示完成、取消或旧版已保存结论，无历史整区隐藏，不删除任何后端记录。

2026-09-16 实施管理范围：PC 首页仅显示实施中及后续正常阶段，包含调试、历史实施完成、待验收、验收中、返工中、质保中、完结；未开始、筹备、计划待审批不显示。状态筛选仅提供该范围内字典选项，清空/重置仍保持阶段范围，名称与类型查询继续组合生效。由服务端分页前过滤，保留准确总数。CLOSED 可能是实施前关闭，暂无历史阶段筛选契约，暂不纳入；原工序日志详情访问及权限不改。

验收（2026-09-16）：两端入口由“返工验收”改为“验收”。内验由运维部门主管、外验由本项目经理提交，负责人为实际登录提交人。日期/结果必填，失败填原因，通过传报告；含验收单合计每类最多3个附件。项目经理可对内外任一失败类型申请重新验收或施工返工，另一类通过不挡申请；免整改复验只重开对应失败类型，历史和另一类结果保留。两端补身份/来源/施工轮次及复验返工互斥检查；审批、单工序返工实施、质保按原流程。通知由后端投递，前端不新增消息写入。完整规则及验证边界见 [验收阶段设计](验收阶段设计.md)。

首页待办申请人（2026-09-15）：PC/移动端优先显示applyUserName，空值回退createBy，两者均空显示—，不补查用户。最新契约仅注明STOCK出库审批/执行待办关联原申请人，项目待办是否返回姓名仍取决于后端。

报价价格（2026-09-15）：创建人维护独立底价，审批人只调比例/终价；成本仅显示物料基准单位当前参考值，非报价快照。通过/采用才可导出且不含成本/底价/比例。详见 [报价价格改造与后端待办](报价价格改造与后端待办.md)，旧“成本×系数”方案不作为本轮公式。

计划提交（2026-09-15）：取消至少一条已保存方案文件限制，空方案文件列表不阻止提交/审批。各页签随上下文就绪自动加载，不要求用户逐个访问；仍检查就绪、加载失败、保存中、未保存内容及用料/外协/实施计划业务规则。文件页已新增或编辑的草稿仍需保存，不自动丢弃。

附件预览安全边界（2026-09-14）：PC 共用预览仅向配置文件服务/API 的域名与路径前缀发送认证头，禁止重定向；超过 20 MB 或 30 秒的预览明确失败，关闭/切换取消旧读取。移动端 H5 同步禁止重定向，不改变附件上传、关联或保存接口。服务端不得将受保护附件下载重定向至第三方；原生端仍需专项验证。

2026-09-14 报价列表请求收敛：列表初始化、查询和翻页仅请求 `GET /project/materialCandidate/list`，传 pageNo/pageSize 及可选 periodId；直接使用后端 total 与顺序，不枚举分期、不拉全量报价、不补查项目名称、不额外加载状态字典。按候选生命周期 -1/0/1/2/3/4 本地显示；作废记录按后端返回保留，不过滤当前页造成分页失真。最新契约未支持名称和状态查询，仅保留分期 ID 筛选；项目名称字段未返回时显示 —。新增选项目、编辑、审批、导出所需请求仍在操作时调用。菜单、路由及权限不变；移动端未发现独立报价管理聚合列表，本次未改合同选报价流程或待实施双单方案。

两端资格同步（2026-09-13）：项目详情邀请仅本分期有效已接受、角色含2的项目负责人；工序完成仅当前轮已开工工序，由具备实施权限的项目负责人或对应现场负责人操作，提交前复查。不自动给普通参与人或全局管理员代入项目经理身份。移动端到货范围已同步待计划审批/实施/调试，回款节点读字典；验收嵌入详情，按最新记录ID一次保存结果与附件，保留历史和重新内验。无后台角色授权写入。

项目列表状态筛选（2026-09-13）：主项目、分期、客户、项目类型、项目状态、到货状态和合同状态直接展示，不需展开；各状态单选、可清空，交后端组合查询及分页。合同待提交显式传 -1，未到货传 0，不与清空混用。列表项目经理直接显示接口 projectManagerName（当前分期有效、已接受项目经理成员姓名），缺失显示 —，不使用对接人或逐条合同请求兜底。

## 项目详情按阶段开放（2026-09-13）

- 新增且合同未提交：仅基本信息。
- 合同状态 0/1/2/3 说明进入合同流程，显示合同；驳回和撤回不隐藏合同。
- 合同通过或筹备/计划待审批：增加项目成员、实施位置、项目文件。
- 实施/调试/实施完成：增加实施记录、用料统计。
- 待验收、验收、返工、质保、完结：开放全部八个 Tab。CLOSED 或未知状态不能直接推断走完全部阶段。
- 当前页面已经开放的阶段入口在刷新退回时保留；按用户要求移除“查看历史资料”和全部展开提示，不再手动展开未开放模块。`?tab=...` 仅定位已开放 Tab，未开放或非法值回到基本信息。
- 跨会话历史模块仍需后端提供已开放模块标记，当前不额外探测，不改变业务权限。
- 分期详情加载完成前不挂载业务 Tab；切换分期清除上个项目数据，旧响应不回写。阶段刷新不强制跳回基本信息，保留当前办理页。

项目详情展示（2026-09-13）：PC 详情顶部和基本信息不再展示项目编号；统一显示“主项目名称-分期项目名称”。基本信息仅保留合并的“项目名称”，不重复列出主项目与分期名称；缺失名称不产生多余连字符，长名称换行。仅调整展示，不修改项目数据或编辑接口。

> 待实施新需求（2026-09-12）：[报价与合同报价双单改造方案](报价与合同报价双单改造方案.md)。签约前报价经成本与系数审批后锁定，签约后生成独立合同报价；替代旧需求中修改同一候选单的方向。下文仍记录当前实现，尚未切换新流程。

延期审批（2026-09-11）：PC 和移动端首页待办详情统一展示主项目－分期名称、申请单号、申请人/时间、原计划完成日期、申请延期至、延期天数、原因及审批结果，不展示附件。原计划日期为后端申请快照，延期天数用后端返回值。待审批且具备 `project:delay:approve` 才能操作，通过需确认，驳回需填写原因并确认；再次查询状态后提交，成功刷新待办。通过由后端更新对应工序计划结束日期，不是前端修改整个项目周期。

项目新增入口权限（2026-09-11）：列表新增按钮、新增类型选择框入口及确认跳转统一校验 `project:add`。无权限不显示按钮，也不能经列表处理函数打开新增表单；覆盖新建主项目和已有主项目新增分期。角色还需具有 `/project/apply` 隐藏页权限。本次不扩展为独立表单路由及后端新增接口的鉴权改造，服务端仍须独立校验写权限。

验收办理（2026-09-10）：项目列表“验收中”直接显示“验收”按钮，无办理权限置灰。弹窗只显示当前账号可办理的内验/外验内容（外验仍校验本项目经理），两类都具备时同时显示；复用详情表单，无需再次开始验收。负责人自动取实际提交人，不允许手填；完成后重新打开或查看历史时回显已保存姓名，不随查看人变化。资料和结果由完成接口原子保存，成功后关闭弹窗并刷新列表。菜单、路由与授权范围不变。

验收日期展示（2026-09-10）：项目详情内部/外部验收卡片及历史表统一只读展示 `acceptEndDate`，不再显示实施完成日期；缺失显示“—”，不以旧 `acceptDate` 或实施日期替代。

## 项目内实施记录（2026-09-10）

实际用料表仅展示物料名称、品牌、规格型号、实际用量和单位，不展示备注列；共享详情的两个入口一致。

实施记录提供分页、加载及错误重试，显示签到/签退时间、实施位置、内容、工时与行车信息。工时保留零值，缺失时按打卡时间计算并注明来源。项目内详情弹窗与独立实施详情使用同一组件，每次打开重新查询日志，按本次日志显示内部参与人员、实际外协、照片及实际用料；旧请求不能覆盖新的日志。

## 实施记录详情（2026-09-10）

- 去掉实施日期；位置与实施内容上下整行显示。工时优先接口值，缺失时按有效签到/签退时长展示并注明来源，不更改后端工时。
- 比较日志提交人和对应工序现场负责人，仅负责人日志显示参与人员区（参与人员信息、外协参与记录）。只展示本日志参与 ID 和实际外协清单，配置仅补充名称；非现场负责人日志不显示此区，行车和实际用料原有展示保留。
- 当前接口无历史负责人快照，判断依据现工序负责人；工序加载失败明确提示，人员名称查询失败保留实际记录并提示。

销售负责人提交规则（2026-09-09）：合同保存时从所选用户解析姓名，始终携带销售负责人 ID 和姓名；用户未切换负责人也不能省略姓名，缺失时提示重新选择。适用于新增、编辑和审批前保存项目经理。

## 合同报价调整分页（2026-09-09）

- 编辑表单移除关联报价的独立“查看详情”，直接点击“修改调整”；已采用报价的解锁限制与只读合同详情保持原规则。
- 调整弹窗明细按 10/20/50/100 条分页，默认 10 条；跨页保留数量、单位和备注修改，保存校验全部页，错误跳至对应页，移除末页最后一行回到有效页。

## 2026-09-09 合同与报价联动

- 合同关联报价：`addWithPaymentRecords/editWithPaymentRecords` 的 multipart `data` JSON 顶层传 `materialCandidateId`（与 `contract/records` 同级）。只选当前分期已通过报价，可在合同入口调整原单；合同提交成功由后端事务改为 `3 已采用`。接口不保存合同关联新字段，页面按分期唯一已采用单回显。已采用仅合同驳回/撤回后可解锁为通过，不复制报价。计划首次为空且可编辑时自动带入采用报价，已有计划不覆盖；`candidateQty` 由后端处理，计划只传 `id?/materialId/plannedQty/unitId/remark`。


> 更新：2026-09-08（验收返工闭环）
>
> 接口文档：https://s.apifox.cn/ffb89e1a-f7ea-4dd5-99ae-e8365679dc0c

## 一、模块定位

项目模块覆盖：**项目管理**（主项目+分期）、**计划方案**、**实施管理**（工序/实施记录/参与人员/施工打卡/工期延期）、**项目成员/实施位置/项目文件/项目动态/用料计划**、**验收管理**（内部验收 + 外部验收并行）、**返工申请/审批/执行**、**合同管理/回款**。

计划方案、合同、项目用料申请、项目延期以及出入库申请/明细的审批状态统一使用 `approval_status`：`-1=待提交`、`0=驳回`、`1=审核通过`、`2=待审批`、`3=已撤回`，由 `src/utils/approvalStatus.ts` 负责页面判断和缓存失败兜底。项目生命周期、工序状态、出入库执行状态和 `AGREE/REJECT` 审批动作参数不属于该字段。

后端以「**项目(project) — 分期(period)**」两级结构组织：主项目下挂多个分期，**所有子表（计划/成员/位置/实施/验收/文件/用料/动态）都挂在分期(periodId)上**。

## 二、角色 × 页面

合同审批人统一授予 `project:contract:approve`，用于待审批合同的审批区域及通过/驳回操作，与合同状态接口权限一致；旧 `project:contract:audit` 不再用于前端判断。

新增/编辑项目的地址使用按需选点弹窗：默认展示地址，点击「选择位置」或在输入框按回车后加载地图；确定回填地址和经纬度，取消不更改表单，关闭后卸载地图。

选点弹窗全屏时地图随内容区填满剩余空间，保留底部地址和坐标；窗口尺寸变化同步调整画布，退出全屏恢复原高度，不重新定位或重建地图。

| 角色         | 页面                                 | 职责                                   |
| ------------ | ------------------------------------ | -------------------------------------- |
| 项目对接人   | 项目管理/项目详情                    | 创建项目；创建后从详情修改项目基本信息 |
| 合同审批人   | 项目管理                             | 审批合同；通过时指定项目经理           |
| 项目经理     | 编辑计划方案                         | 编写、保存并提交后续项目计划           |
| 项目实施人员 | 项目管理-补料管理                    | 查看本人申请并在同一抽屉发起补料       |
| 补料审批人   | 项目管理-补料管理                    | 查看待审/审批记录，按单或批量审批      |
| 实施人员     | 项目详情-实施记录/实施位置/成员/文件 | 查看/维护实施维度数据                  |
| 运维部门经理 | 项目详情-验收记录                    | 接收内部验收待办，上传报告并确认       |
| 项目经理     | 项目详情-验收记录                    | 接收本项目外部验收待办，上传报告并确认 |
| 项目经理/成员 | 项目详情-验收记录                   | 查看验收、针对失败验收发起返工          |
| 返工审批人   | 项目详情-验收记录/首页待办           | 审批返工方案                             |
| 财务         | 回款管理(合同)                       | 合同信息、已回款金额                   |

用料计划的“选择物料”抽屉默认按全部分类分页展示物料库，保留搜索和分类筛选；已加入当前计划（含尚未保存的行）的物料继续显示为“已选择”，禁止重复添加。新选物料可取消，跨页选择继续保留；从计划移除后重新打开抽屉即可再次选择。该显示模式仅用于计划用料，其他业务选料行为保持原有规则。

## 三、项目生命周期（状态流转）

```
未开始 ──合同提交──> 合同待审批（2） ──审批通过（1）+指定项目经理──> 筹备中 ──计划提交审批──> 待审批
待审批 ──审批通过──> 实施中 ──全部工序完成──> 验收中（自动生成验收记录）
验收中：运维部门经理办理内部验收，项目经理办理外部验收；两项都通过后端自动进入质保
任一验收不通过 ──返工申请/审批──> 返工中 ──统一返工工序完成──> 验收中
质保中 ──质保结束──> 完结
任意允许关闭的状态 ──关闭审批──> 关闭（例外终态）
```

- 合同提交后，项目列表的同一“合同信息”入口进入只读详情：审批权限人可通过/驳回，原提交人可撤回；驳回详情展示原因，驳回或撤回后由原提交人修改并重新提交；通过时指定项目经理，项目进入筹备中，列表入口随即隐藏，后续从项目详情查看。
- 合同待审批只读详情将合同类型和回款项编码转换为字典名称；销售负责人优先显示合同姓名快照，快照为空时按负责人 ID 从系统用户补齐，并兼容历史姓名字段、嵌套用户对象和创建账号；项目经理姓名同样支持按用户 ID 补齐。已上传的 1～3 个统一合同附件可逐个在站内弹窗查看。
- 合同信息页将 `contractAmount` 以“项目金额”展示，计划交付日期、项目金额和质保期均为必填；质保期使用月数并只允许填写大于等于 0 的整数。历史待审批合同缺少计划交付日期时不能直接通过，应驳回补充后重新提交。
- 已有合同修改并重新提交时调用 `editWithPaymentRecords`，项目分期 ID 必传；合同字段仅提交变化项，回款计划全量同步，修改成功后转为待审批。合同审批通过前写入项目经理也使用该组合接口并携带当前完整回款计划，再调用合同状态接口审批通过；前端不再调用已停用的 `/project/contract/edit`。回款管理列表的“编辑”统一进入正式合同编辑页。
- 合同回款计划增加“回款周期”，表头提示其含义为“阶段任务完成后回款的周期”；回款项、比例和周期均为必填，新行不预填比例，回款周期默认 7 天。比例和周期使用数字输入；其他行已选择的回款项不会再出现在当前下拉中，全部节点使用后禁用“添加款项”，提交前再次校验重复；比例必须大于 0、不超过 100% 且合计为 100%，`rollbackTime` 必须是大于等于 0 的整数并随合同组合接口保存，旧记录没有该字段时仍按 7 天显示。
- 仅项目处于筹备中（`PREPARING`）时，项目列表显示“编辑计划方案”；提交计划审批后立即隐藏，审批通过进入实施中后也不再显示。
- 计划审批通过后至完成实施之前（`IMPLEMENTING/DEBUGGING`，兼容历史 `DEBUG_COMPLETED`），仅项目列表显示受 `project:implement` 控制的“实施完成”，项目详情不再提供该按钮。列表点击后打开“确认工序实施进度”抽屉并通过 `GET /project/process/detail?periodId=...` 展示全部工序；未开始、进行中的工序均可单选完成，已完成工序禁选但可查看。确认前二次提示，提交 `POST /project/process/status` 的 `{ processId, status: 'COMPLETED' }`；最后一道工序完成时项目状态由后端自动推进，前端不再直接提交调试完成或实施完成状态。
- 工序抽屉展示工序名称、状态、计划周期（计划结束日减开始日加一）、实际开始时间、已进行自然日、现场负责人；“详情”进入当前项目分期的实施日志页并自动展开所选工序。接口 `processName` 按 `work_type` 转换名称，抽屉选择提示、二次确认和工序日志分组使用同一中文名称，未命中才回退原值。工序状态统一使用 `project_process_status`：`NOT_STARTED/IN_PROGRESS/COMPLETED`。
- PC 新增项目先选择“新建主项目”或“已有主项目下新增分期”；分期模式先锁定所属主项目，再填写分期信息，提交继续以 `parentProjectId` 区分两种模式。
PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。（2026-09-14 最新规则覆盖旧到货款限制；本次修改 PC，App 尚需同步验证。）
- 项目列表的对接人姓名优先使用列表姓名快照或用户 ID 反查；仍缺失时按当前分期补查项目详情，避免列表留空。
- 新增项目、已有主项目新增分期及编辑项目的附件改为随项目保存接口组合提交：multipart 的 `data` 为业务 JSON 字符串，`attachment` 为单个可选文件，不预调用公共上传接口，也不在 JSON 中传 `attachmentFileId`。编辑不选新附件时保留旧文件，选择后由后端替换；允许 Word、PPT、Excel、PDF 和图片，页面移除表单上方重复的“基本信息”标题。
- “编辑/查看计划方案”页面标题旁显示“详情”，在当前计划页打开项目基本信息抽屉；抽屉展示主项目、分期与对接、实施信息。
- 计划页标签由“方案文件管理”改为“方案文件”，按项目分期 ID 读取 `/project/plan/list`，仅展示计划方案记录，不显示合同附件或 `CONTRACT_MATERIAL_DRAFT` 合同用料草稿。序号按当前表格行位置从 1 连续显示，新增、删除和重载不会显示内部行键。顶部“保存本页”统一保存本页方案：新增记录及附件通过 multipart `/project/plan/addBatch` 提交，已有修改调用 `/project/plan/edit`，移除记录在最后调用 `/project/plan/deleteBatch`。每个新增记录用 `attachmentIndex` 对应附件数组；先校验全部必填项，全部成功并回查后整页锁定，点击“修改”解锁。不同请求并非整页事务，中途失败停止后续操作并保留未完成草稿。
- 项目基本信息附件仍使用 `addProjectPeriod/editProjectPeriod` 的 `attachment` part 组合提交。合同页将原合同文件和物料清单合并为“合同附件”：每次选择一个文件即调用 `/sys/common/upload`（`biz=periodId`），成功后立即用返回路径预览；最少 1 个、最多 3 个。合同及回款组合保存后以 `fileType=CONTRACT_ATTACHMENT` 调用 `/project/file/addBatch` 关联路径，编辑移除的已保存记录通过 `/project/file/deleteBatch` 删除。
- 合同附件上传中禁止提交，上传失败不加入附件列表；合同已保存但项目文件关联失败时停留当前页面提示重试，避免重复新增合同。查看页、项目详情、计划回款和确认到货都从 `/project/file/list` 筛选统一附件并逐个预览；没有统一记录时兼容原 `contractFile/materialFile` 历史路径。
- 人员配置列表调用 `/project/member/list`，实时读取 `member_role` 字典并将 `memberRole` 回显为名称。邀请弹窗过滤角色 `0/1/2`，成员通过 `/sys/user/list` 汇总全部分页并在打开弹窗时刷新；项目角色支持多选，点击“提交邀请”立即调用 `/project/member/addBatch`，同一用户只放一条 `records` 明细，多个角色编码在该明细的 `memberRole` 中以英文逗号连接。列表以成员记录为粒度，同一人员的多个角色合并在一行展示；已存在的用户角色组合不重复提交。成员删除立即调用 `/project/member/deleteBatch`，不等待外协配置保存。
- 计划页六个标签不再重复显示固定项目基本信息条；父页面先并行加载当前分期详情与合同组合详情，两项都成功后才挂载页签，任一失败都阻断子页查询和写入并提供重试，避免空上下文触发全量覆盖。同路由切换 `periodId` 时会重置页签且忽略旧响应。项目基本信息仍从页面标题旁“详情”按钮打开抽屉查看，抽屉复用父页已加载的分期详情，不再重复请求。
- 编辑计划方案不提供“保存全部”；“提交审批”只通过 `/project/period/status` 把当前分期生命周期更新为 `PENDING_APPROVAL`，不会代替用户保存任何页签数据。提交前必须逐一查看六个页签，所有页签业务主数据已加载，必填数据校验通过，且无失败、保存中或未保存内容，方案文件至少已有一条已保存记录；否则自动定位第一个阻塞页签。审核人点击“通过”也执行同一门禁，状态请求开始后整页禁写。编辑还要求当前账号是合同指定项目经理且分期为 `PREPARING`。计划审批页同时按生命周期 `PENDING_APPROVAL`、分期 `approvalStatus=2` 和权限判断是否可办理；切换期仅在接口缺审批字段时按该生命周期推导为 `2`。当前 `/project/period/status` 不接收驳回原因，`/project/plan/list` 的 Apifox 参数表也未声明前端必需的 `periodId`，两项都需后端补齐。
- 每次切换到“人员配置”或“实施计划”时，按当前 `periodId` 重新调用 `GET /project/member/list`；首次进入由子页初始化加载，再次进入分别只刷新参与人员列表或现场负责人选项，不重新加载外协、工序或验收日期，保留未保存草稿。
- 用料计划和实施计划顶部按钮在“保存本页/修改”之间切换：已有记录默认只读，空列表直接编辑；初始查询未完成或失败时禁止全量保存，成功保存后刷新并锁定。用料计划首次为空且可编辑时自动带入当前分期唯一状态 `3` 的已采用报价，已有计划不覆盖；仍支持 `.xlsx` 追加导入，自动带入及文件导入只修改页面草稿，点击保存本页后才提交。`/project/materialPlan/editBatch` 的每条明细提交 `id（已有记录时）/materialId/plannedQty/unitId/remark`，单位名称快照由后端生成；计划列表与 `/stock/material/list` 主数据并行加载后统一回显，后续刷新复用物料缓存，不逐条查询物料详情。共享选料抽屉仅在真正打开时初始化，并用请求序号防止旧响应覆盖。实施计划通过 `/project/process/detail` 一次读取计划验收信息和工序明细，不再调用已废弃的 `/project/process/list`。人员配置将成员、外协、外协单位和角色字典合并为一次并行初始化，各接口首次只调用一次并阻止旧响应回写；全部加载成功前不得调用外协全量保存。成员邀请、删除仍在操作时立即调用成员接口，不随外协批量保存。“是否需要外协”默认否，选是才显示列表，保存否通过空 `records` 清空已有外协。选择需要外协后至少维护 1 条配置，每条人数至少为 1 人且只能输入整数，工时必须为大于等于 0 的有效数字；新增行人数和工时保持空值，必须由用户填写。同一外协单位在当前分期只能选择一次。位置信息改为顶部“保存本页/修改”：新增、修改和删除先更新页面草稿，保存时校验全部位置并通过 `/project/location/editBatch` 一次提交当前分期完整列表（空数组清空）。初始查询读齐全部分页前禁止写入；保存成功并回查后整页只读。未保存的删除也计入脏状态，阻止直接提交审批。
- 报价管理使用项目用料候选清单主子表独立维护报价物料，不再与计划方案“用料计划”共用数据。列表展示主项目、分期、报价单名称、最后更新人和报价状态，不展示分期 ID；同一分期可创建多份名称不重复的报价。新增通过 `/plan/material-draft/editor?mode=create&periodId=...&candidateName=...`，首次保存主表只提交 `periodId+candidateName`，取得 `candidateId` 后批量新增物料；主表和明细全部保存完成后才切换到 `mode=edit`，并把本次保存快照交给重建后的编辑页回显，使旧查询失效，避免完整 URL 变化时先查到空数据。候选列表短暂未返回新主表时使用本次主表快照继续展示；普通修改保存优先用批量接口返回的已保存明细刷新，未返回数组时再按 `candidateId` 查询，查询暂无数据或失败时回显本次提交快照。修改通过 `mode=edit&periodId=...&candidateId=...` 精确回显；查看详情使用 `mode=view` 并强制只读。列表导出读取候选明细并由前端生成 `.xlsx`。状态直接按 `project_material_candidate_status` 回显：`-1` 草稿、`0` 驳回可编辑和作废，`2` 待处理经审批变为 `1` 通过（锁定）或 `0` 驳回；通过可在合同入口调整，合同提交由后端改为 `3` 已采用，作废使用状态 `4`；已采用只有合同驳回或撤回时可解锁，不复制新报价。新增响应未直接返回 ID 时按分期和唯一名称反查，已有同名草稿则作为未完成保存继续维护。项目管理、计划方案管理、实施管理和出入库申请列表均不展示项目编号，详情、表单、项目选择器和列表搜索仍可使用编号定位；报价搜索、路由和接口继续使用 `periodId` 精确定位分期。
- 全局项目邀请登录弹窗暂不挂载；不再保留独立站内待办页面，首页“我的待办”调用 `GET /sys/todo/page`，汇总调用 `GET /sys/todo/summary`。同一账号、同一项目分期的成员邀请在待处理列表中归并为一条。点击“查看详情”后按 `periodId` 调用 `GET /project/member/list`，筛选当前登录用户的成员记录并按 `member_role` 字典显示其全部项目角色；随后可调用 `POST /project/member/respond` 同意或拒绝，办理成功后首页待办重新加载。
- 编辑计划方案的“回款计划”直接复用父页面合同组合详情中的合同和 `records`，不再重复查询合同、回款列表或项目分期；合同类型与回款节点分别匹配 `contract_type/payback_node` 字典名称。销售负责人优先读取合同或项目姓名快照，仅在快照缺失时按负责人 ID、合同创建账号 `createBy` 和当前分期项目成员补齐。合同文件提供站内弹窗预览，不显示链接或下载；回款明细不显示计划日期和操作列。
- 项目列表复用同一个计划方案入口：筹备中显示“编辑计划方案”，提交后处于 `PENDING_APPROVAL` 时切换为“查看计划方案”，并通过 `mode=view&periodId=...` 进入六页签只读详情；不额外增加独立查看按钮，查看不依赖计划审批权限。
- 计划审批弹窗每次打开按 `periodId` 调用 `/project/project/projectPeriodDetail`，项目名称、分期名称和生命周期状态均从真实详情回显，不再读取待办 title/summary 或假定状态；加载失败提供重试且禁用查看/审批，关闭或切换时旧响应失效。
- 待审批项目的“计划审批”弹窗底部为“查看详情/驳回/通过”，不显示关闭按钮。查看详情跳转 `/project/plan?mode=view&audit=1&periodId=...`，强制只读查看方案文件、用料计划、人员配置、实施计划、位置信息和回款计划；当前分期 `approvalStatus=2` 且账号拥有 `project:plan:audit` 权限时，计划详情页顶部继续显示“驳回/通过”，办理完成后返回项目列表并刷新待办。
- 计划审批通过后，实施中和调试阶段的项目在项目列表“更多”中只显示“补料管理”。统一抽屉包含“我的申请 / 待我审批 / 审批记录”，并在抽屉内直接新增申请：普通账号仅保留本人 `applyUserId` 对应记录；拥有 `project:materialApply:approve` 的账号才显示审批标签、审批历史及通过/驳回按钮。补料继续使用与计划用料一致的库内物料选择、单位和数量控件，补料原因整单只填一次；“查看用料清单”仍在当前抽屉上方弹窗预览。提交 `/project/materialApply/addBatch` 时，父单 `apply` 保存 `periodId/applyReason`，物料写入 `items`。`/project/materialApply/list` 每条记录对应一张申请，展开且列表未带明细时调用 `/project/materialApply/queryById`；审批记录由状态 `0=驳回/1=审核通过` 的申请单组成。单张审批调用 `/project/materialApply/approve`，多张调用 `/project/materialApply/approveBatch`，审批不下沉到物料行，通过后补充计划由后端处理。首页补料待办打开同一抽屉的“待我审批”并定位 `bizId`。当前列表接口没有申请人/审批范围参数，前端本人过滤仅控制页面展示，服务端仍须补充可靠的数据权限。
- 项目详情右栏按时间倒序展示项目动态，每条显示动态内容、操作人 `operatorName` 和操作时间；列表在桌面端按视口高度、窄屏按固定高度独立滚动，避免动态过多拉长整个详情页。
- 项目详情移除顶部合同快捷按钮和“计划方案”标签，新增只读“合同信息”标签；按当前 `periodId` 一次读取合同、两个附件和回款计划。顶部、基本信息、合同信息和项目成员统一转换字典编码：`project_type`、`project_business_attr`、`project_products`、`contract_type`、`payback_node`、`approval_status`、`member_role` 均显示名称；业务属性、涉及产品和多角色值支持逗号分隔逐项回显。最后一个标签改为“用料统计”，按当前分期分页读取 `/project/materialAccount/page`，主表展示计划、出库、消耗、待处置与实际成本，展开行查看申请占用、归还处置、库存和金额；不再把 `/project/materialPlan/list` 的计划明细当成项目总账。
- 项目详情“项目成员”标签将参与人员与外协人员分成两个区块。参与人员按 `/project/member/list` 加载，同一人员无论后端返回单角色多行还是逗号分隔多角色都合并为一行，仅显示序号、成员名称、成员角色和邀请状态；参与人员区支持手动刷新和继续邀请，邀请时从 `/sys/user/list` 选人、从实时 `member_role` 中多选角色，过滤系统角色 `0/1/2` 并跳过已有成员角色组合后调用 `/project/member/addBatch`，成功后只刷新参与人员列表。外协按 `/project/memberOutsource/list` 独立只读加载，显示外协单位、单位类型、人数、工时、联系人和联系电话。两块互不阻断，分别展示数量、空状态和失败重试。
- 项目详情“项目文件”标签对所有详情访问者显示“上传文件”。文件仅允许 PDF、Word、Excel 和 PPT，先以 `biz=periodId` 调用 `/sys/common/upload`，再把成功响应 `message` 作为 `fileId`，通过 `/project/file/addBatch` 的顶层 `periodId + records` 新增文件记录；成功后立即刷新文件列表。删除权限仍只属于当前项目对接人或项目经理。
- “实施计划”根据全部工序的最早计划开始日期和最晚计划完成日期计算跨度总工时：自然日差 × `8h`；每条工序的计划工时也按本行起止日期自动计算。工序名称按 `work_type` 字典值排除其他行已选工序，全部字典工序均已添加后禁用添加，并在保存前再次阻止重复；每条工序名称、现场负责人、计划开始时间和计划完成时间必填，完成时间不得早于开始时间。现场负责人仅可选已接受邀请的项目成员并按用户 ID 去重，计划验收日期必填。工序列表会过滤接口或父组件回填中的空记录，日期汇总和保存校验均兼容空数据，避免读取空工序的 `startTime/endTime`。“实施位置”地图确认后回填位置名称、经纬度和描述，并关闭选点弹窗；位置列加宽展示完整地址。计划页查询成功不弹 Toast，各页签保存、审批和状态流转均只保留一条页面业务结果提示。
- 当前页面统一通过项目分期状态接口推进，前端按钮按当前状态和权限码显示，后端负责校验不可跳步。
- 项目列表“更多”中的“关闭项目”使用 `project:close` 控制，只在非 `CLOSED/COMPLETED` 分期展示；确认文案明确关闭会终止该分期未完成的项目领料，提交 `POST /project/period/status` 的 `{ periodId, status: 'CLOSED' }` 后刷新列表和首页待办。前端在点击时再次检查权限和分期 ID，但服务端仍必须校验 `project:close`。本次仅支持关闭，当前暂不提供重新开启；同时没有关闭申请、销售/总经理多级审批或对应待办接口，因此该入口是直接关闭，不等同于完整关闭审批流程。
- **验收阶段 = 内部验收 + 外部验收并行**：全部工序完成后进入待验收，开始验收后进入验收中，生成两条责任人待办；运维部门经理上传并确认内部验收，本项目经理上传并确认外部验收。任一项不通过可申请返工，两项都通过才由后端自动进入质保。
- **返工统一为一个工序**：每张返工单只保存一个 `process` 对象；返工用料 `materials.plannedQty` 只表示本轮额外领料量，现场余料不重复申请。返工审批通过后通过 `reworkId` 关联新工序、额外领料、实施日志和复验记录。
- **返工 version 不能缓存复用**：编辑、提交、撤回和审批均传 `GET /project/rework/queryById` 返回的最新 `version`；页面在每个状态动作前重新读取详情，保存并提交时也在保存后重读详情再提交。
- 状态码以 `project_period_status` 字典和后端接口为准。

## 四、页面清单

项目列表验收入口（2026-09-13）：仅 `ACCEPTING` 且具备 `project:acceptance:view` 时显示“验收”；无入口权限直接隐藏。有入口但无本项目内/外验办理资格仍禁用，弹窗重新核验，原内外验职责不变。本次不调整“开始验收”、详情和待办权限。

验收报告多附件（2026-09-13）：内外验报告各最多三个文件，每次选择一个、上传一次，支持常见图片、Word、PDF、Excel，成功追加并保存关联；满额禁止新增，逐项删除后可再上传，已完成及历史记录逐项只读预览。独立验收单仍为单文件。DOC 可上传，但浏览器不能直接解析时沿用预览器说明。逗号分隔存储沿用公共组件惯例，验收后端持久化尚待联调。

验收附件上传成功判断兼容公共上传的 `success=true、code=0/200`，成功后立即回显文件并保存验收附件关联，预览使用 `/sys/common/static/` 读取返回路径对应的文件。旧版仅接受 `200` 的校验会误拦实际 `code=0` 的成功上传（2026-09-13 修复）；修复前未关联的文件需重新上传。

| 页面               | 路由                          | 说明                                                                                          |
| ------------------ | ----------------------------- | --------------------------------------------------------------------------------------------- |
| 项目管理列表       | `/project/list`               | 主项目+分期合并列表，行操作=状态流转+编辑                                                     |
| 新增/编辑项目      | `/project/apply`              | 新建主项目+首期 / 已有主项目下新增期 / 编辑                                                   |
| 项目详情(8-tab)    | `/project/detail/:periodId`   | 基本信息/合同信息/成员/位置/实施记录/验收记录/文件/用料统计                                   |
| 编辑计划方案       | `/project/plan`               | 对齐后端 project_plan 实体                                                                    |
| 合同信息（隐藏）   | `/project/contract`           | `create` 读项目基本信息但不读合同/回款；`view/edit` 必须按 `periodId` 查该分期唯一合同        |
| 计划方案管理       | `/plan/list`                  | 独立菜单                                                                                      |
| 报价管理           | `/plan/material-draft`        | 报价列表；支持新增、查看、导出、修改、锁定、指定权限解锁及草稿删除                            |
| 新增/修改/查看报价 | `/plan/material-draft/editor` | 固定隐藏页；新增传 `periodId/candidateName`，修改/查看传 `periodId/candidateId`，查看强制只读 |
| 实施管理           | `/implement/list`             | 项目分期列表 + 按工序分组的实施日志                                                           |
| 回款管理           | `/payment/list`               | 合同管理与项目回款计划                                                                        |

## 五、验收阶段（核心概念）

内部验收与外部验收**同时进行、不分先后**，均为同一轮验收的一部分，统一记录在 `project_acceptance`，分别上传验收报告：

| 维度      | 内部验收 `acceptType=INTERNAL`  | 外部验收 `acceptType=CUSTOMER`                                        |
| --------- | -------------------------------- | --------------------------------------------------------------------- |
| 办理责任  | 运维部门经理                     | 本项目经理                                                            |
| 负责人    | `acceptLeaderId/acceptLeaderName` | `acceptLeaderId/acceptLeaderName`、`acceptUnitLeader/acceptUnitPhone` |
| 验收报告  | `completionReportFileId`（必传）  | `completionReportFileId`（必传）、`acceptanceFormFileId`（验收单）    |
| 结果/备注 | `result`、`remark`                | `result`、`remark`                                                    |

详见 [验收阶段设计.md](验收阶段设计.md)。

当前轮任一验收结果为 `FAILED` 时，拥有 `project:rework:apply` 的人员可发起返工。返工原因必填，统一工序的名称、负责人、计划起止日期和预计工时必填；额外领料计划可为空。返工申请使用统一审批状态，审批通过进入 `REWORKING`，返工工序完成后自动进入 `ACCEPTING`，验收记录关联当前 `reworkId`。

## 六、已对接/待确认

- ✅ 项目模块接口已按实际页面持续对齐；合同详情使用 `/project/contract/queryById` 一次返回合同、文件与回款计划；新增/修改分别使用 `/project/contract/addWithPaymentRecords|editWithPaymentRecords` multipart 事务接口。
- ✅ 项目与合同业务 Mock `mock/demo/project.ts`、`mock/demo/contract.ts` 已删除；`mock/sys/menu.ts` 与字典等共享 Mock 保留。
- ✅ 内部验收与外部验收统一使用 `/project/acceptance/*`，两项均通过后自动进入质保；失败验收可进入单工序返工闭环。
- ⚠️ 两类验收待办的接收人、关闭规则和数据范围尚未写入当前 Apifox；后端需实现内部指派运维部门经理、外部指派本项目经理并补齐契约。
- ⚠️ 待确认：`period.status` 字典值、主项目删除接口、回款流水最终接口。项目文件字段统一存 `/sys/common/upload` 响应 `message` 路径。


工序日志列表（2026-09-11）：按“实施日期、提交人、签到时间、签退时间、工时、实施位置、实施内容”排列，保留详情操作。实施日期取 signInTime 的日期部分，不用 logDate；实施位置取 implementLocationName；工时使用共享展示函数，保留 0，缺失时按打卡时长计算并注明来源。实施内容前 15 个字符后加 ...，悬浮查看全文；移除照片数量列，详情仍可查看照片。接口参数及分页规则不变。


项目实施日志顶部（2026-09-11）：增加销售负责人、项目经理，按当前 periodId 查询 `/project/contract/queryById`，分别读取合同的 salesUserName、projectManagerUserName；复用合同详情封装解包 contract。加载失败单独提示并可重试，不阻断工序日志；姓名缺失显示 —。


项目管理查询（2026-09-11）：项目类型为可选筛选项，支持清空，不再执行必填校验；新增、编辑项目的项目类型仍必填。


验收入口修正（2026-09-11）：全部工序完成后进入 `PENDING_ACCEPT`，列表和详情提供“开始验收”，调用 `POST /project/period/status`（status=PENDING_ACCEPT）启动内部及外部验收并进入 `ACCEPTING`，不调用废弃 submit/start。内置管理员登录账号 `admin` 可进入两类验收办理；普通用户继续按验收权限和本项目经理身份判断。列表、弹窗、详情共用 `useAcceptanceAccess`；后端授权与工序/轮次/结果校验仍生效。本次未修改全局角色授权。


项目计划合同物料保护（2026-09-12）：每次打开可编辑用料计划均加载本分期已采用报价，按物料 ID 识别合同物料；合同物料禁止移除、锁定合同单位，计划数量不得小于合同数量。已有计划缺失的合同物料补回草稿，已有数量不覆盖；Excel 导入及保存前再次校验完整性、数量和单位。额外追加物料仍可删除，报价/补料/返工模式不受影响。合同来源加载失败阻止保存，接口 payload 不增加保护字段。


合同关联报价（2026-09-13）：关联报价单为选填，未选时不校验报价状态、不提交 materialCandidateId；已选时仍核验当前分期报价状态。尚未采用的选择可清空，已采用报价仍按原解锁规则处理，不传字段不代表解除采用。


合同报价选择（2026-09-13）：提供清空图标和取消选择按钮，按报价单名称忽略大小写、去除搜索首尾空格进行包含匹配；查询数据刷新不再自动覆盖用户选择。已采用报价在合同允许解锁时先解除采用再取消或更换，失败保留选择；其他状态提示驳回/撤回后操作。取消选择不删除报价单。


项目列表（2026-09-14）：项目经理直接使用 projectPeriodList 返回的 projectManagerName，空白显示 `-`。移除 afterFetch 为缺失项目对接人逐条调用 projectPeriodDetail 的补查；对接人沿用列表姓名及已有用户名称映射，缺失显示占位。验收/计划审批弹窗打开时用于状态校验的详情查询保留。


出入库用料类型与审批（2026-09-14）：列表、申请表和明细统一使用“用料类型”；显示值为项目/维修/劳保，接口值不变。项目待审批单按当前分期已接受项目经理身份展示审批，列表、单张、批量及待办共用身份预加载，取消项目单一律免审标签。实际经理审批和经理本人申请免审由后端判断；库管仍仅凭 mtl:goods:io 执行通过单。维修/劳保仍按 mtl:apply:approve 和申请部门负责人校验。申请部门实时补查当前账号部门；编辑他人申请不套用当前账号部门。还料后端仅定义 IN+RETURN+PROJECT，三类名称统一但维修/劳保暂禁选，待补齐其可还物料来源和数量规则。

## 2026-09-14 确认到货规则更新（覆盖旧阶段说明）

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。（2026-09-14 最新规则覆盖旧到货款限制；本次修改 PC，App 尚需同步验证。）

### 2026-09-14 确认到货与到货款解耦

PC 确认到货：仅当前分期已接受的项目经理且拥有 `project:arrival:confirm`，在 `IMPLEMENTING/DEBUGGING/DEBUG_COMPLETED`、尚未到货且项目类型为“硬件/软硬一体”时显示。计划待审批、实施完成及后续阶段隐藏；纯软件隐藏按钮，到货状态显示“—”。无论合同有无到货款均需确认，有到货款时由后端计算计划回款日期。

列表不再逐项查询合同回款计划，改为对符合类型/阶段的当前页分期查询成员资格（去重、最多三项并发）。弹窗仍展示合同附件及可选到货款信息，提交前复查项目详情、项目类型字典和当前成员身份；只发送 `POST /project/period/arrivalStatus` 的 `{ periodId, arrivalStatus: 1 }`。不新增权限节点、不扩大其他角色授权。最新 Apifox 尚未明确类型、阶段、经理身份的服务端门禁；本次前端限制不能替代后端鉴权。本次未修改 App。


### 2026-09-15 合同签订新增改为 JSON 与多附件路径

- `POST /project/contract/addWithPaymentRecords` 按最新 Apifox 改为 `application/json`，直接提交 `{contract, contractFile, records, materialCandidateId?}`，不再包 multipart `data`，不传文件二进制。
- 保留现有 1～3 个合同附件交互：选文件逐个调用 `/sys/common/upload`，校验成功响应后取 `message` 原始路径，立即显示文件名并可预览/移除；上传中禁止提交。按用户确认的多文件规则，将当前文件列表路径用英文逗号拼入 `contract.contractFileId`，原文件名同序放入 `contractFile.fileName`。
- 新增由合同组合接口负责文件记录关联，不再随后调用 `/project/file/addBatch`，避免重复关联。合同页、项目详情合同标签、确认到货弹窗按逗号拆分路径逐个预览，兼容历史项目文件记录。
- `editWithPaymentRecords` 最新文档明确仍为 multipart，本次保留原编辑请求及附件同步流程。新增多文件拼接按用户要求实现；当前 Apifox 仅单路径示例，后端对逗号多路径的保存、拆分及原名映射需联调确认。未新增菜单、按钮或角色权限。


### 2026-09-15 用料移除提示与方案附件选填

- 用料计划的合同物料操作列保留“移除”按钮并禁用，悬浮提示“合同物料，不可移除”；保护逻辑与其他物料移除行为不变。
- 方案名称、类型仍必填，附件改为选填，无文件也可保存方案记录；提交审批仍要求至少一条已保存方案记录，不要求每条上传附件。
- 对齐当日 Apifox：批量新增 `/project/plan/addBatch` 使用 JSON `{periodId,records:[{plan}]}`，无附件省略 `planFileId`；有新文件在保存时先调用 `/sys/common/upload`，将 message 路径写入 `plan.planFileId`，不传文件名、attachmentIndex 或 attachments。已成功上传路径保留在草稿中，保存失败重试不重复上传。新记录状态 -1；编辑省略 status，由服务端处理原状态，不再回传 DRAFT 等英文枚举。
- 单条编辑仍按当前接口使用 multipart，可选 attachment，不上传时由后端保留原文件。菜单配置已检查，未改变菜单、按钮权限或角色授权。


### 2026-09-15 四个接口统一预上传与 JSON 保存

- 刷新 Apifox 确认 `/project/project/editProjectPeriod`、`/project/contract/editWithPaymentRecords`、`/project/plan/add`、`/project/plan/edit` 均仅接收 application/json。文件先 `/sys/common/upload`，业务请求使用成功响应 message 原始路径，不再传 multipart data/attachment、文件名或元数据对象。
- 编辑分期传顶层 `attachmentFileId`；合同编辑传 `contract.contractFileId`（当前多附件按英文逗号拼接）；方案单条新增/编辑传顶层 `planFileId`。无新文件不触发公共上传；方案上传成功路径保留在草稿，保存失败可复用。
- 合同编辑取消后置 `/project/file/addBatch` 和 `/project/file/deleteBatch` 关联流程，由合同接口事务保存。合同回显优先使用当前合同路径，兼容历史统一附件记录；项目详情和确认到货同样优先当前路径，避免旧附件记录遮蔽替换结果。
- 编辑路径省略、null、空字符串均保留原文件，不能借空值删除。方案移除旧文件后保存须选择替换文件；无历史附件的方案仍允许不上传。方案普通保存不回传历史审批结果，状态交服务端按最新规则处理。
- 本轮未更改新增分期接口，合同新增仍按其独立契约提交。已检查菜单配置，无新增节点、权限或角色授权变化。静态检查与构建不能替代真实上传→保存→重开预览联调。


### 2026-09-15 报价审批统一入口

报价管理待处理记录仅显示“报价审批”入口，点击进入 `/plan/material-draft/editor?mode=view&audit=1&periodId=...&candidateId=...` 的只读报价详情；详情顶部显示“通过/驳回”，分别二次确认后提交。仅待审批且拥有 plan:quotation:lock 时可办理，提交前重新查询报价状态；成功返回列表。普通查看详情不显示审批按钮。沿用 `plan:quotation:lock` 和现有候选清单状态接口，1通过、0驳回；不新增权限或扩大授权。正式菜单原“锁定”节点应改名“报价审批”，权限码及角色授权保留。本轮浏览器连接连续超时，正式菜单名称尚未修改，不能视为已同步。


### 2026-09-15 方案文件整页选填

所有合同类型均允许不添加方案记录、不上传方案附件。保存本页取消“至少添加一个方案文件”拦截；空列表不调用新增接口，删除全部已有记录时仍提交待删除 IDs 并回查。已添加行继续校验名称、类型，未保存修改和加载失败仍阻止审批提交。菜单及 API 参数无变化。


### 2026-09-16 报价审批状态与采用标记分离

最新 Apifox 候选清单 `status` 仅 -1草稿/0驳回/1通过/2待处理/4已作废，采用单独使用 `adopted=0/1`，不再读写 status=3。报价列表分列显示审批状态、采用状态，详情增加采用标签；通过报价不论是否采用均可脱敏导出。

合同候选仅选择 status=1，已采用回显和计划合同物料带入均用 adopted=1。合同取消/更换采用时，通过候选 edit 仅提交 id/periodId/candidateName/adopted=0，保留审批通过状态；已采用报价不可直接调整，仍须合同驳回/撤回后按原权限取消采用。合同保存由后端原事务设置 adopted=1，并校验同分期唯一采用；前端不新增采用操作入口。合同自身 status=3 仍代表撤回，不受此次报价字段调整影响。

接口路径、菜单与权限码不变，菜单配置已检查无需调整。前端静态/构建校验不代表真实合同保存、取消采用和计划回显联调通过。
### 2026-09-16 合同信息独立入口权限

项目列表“合同信息”仅对拥有 `project:contract:view` 且已有合同、合同未审批通过的用户展示；点击事件重复校验。独立合同页 view/edit 在业务加载前校验该权限，拒绝时返回项目列表；create 保留 `project:contract`，审批和原提交人编辑资格继续独立校验。未调整项目详情合同标签。正式 PC 按钮及 admin 授权仍待配置，其他角色不自动授权。
2026-09-17 报价管理恢复按报价单分页：主表直接使用 GET `/project/materialCandidate/list`，默认仅传 pageNo/pageSize 查询全部项目，不再请求 periodGroupPage 或渲染分期展开表。项目名称、分期名称直接取接口，缺失显示 —；rowKey=id、total为报价条数。原查看/修改/提交/审批/作废/导出与权限不变。菜单两份指南已检查，无路由或权限变化。
2026-09-17 项目详情新增“报价信息”页签，位于基本信息之后，各阶段均可查看；`DetailQuotation.vue` 按当前分期 periodId 调用 `/project/materialCandidate/list`，只读分页展示报价名称、最后更新人、审批及采用状态。页签默认懒加载，切换分期重新挂载，缺失 periodId 不发全量请求，失败可重试。未新增编辑/审批入口或菜单权限。
2026-09-17 项目详情报价信息补充“查看详情”操作，跳转现有 `/plan/material-draft/editor?mode=view&periodId=当前分期&candidateId=报价ID`，不传审批模式，不新增编辑能力。复用原隐藏路由及权限；菜单指南已检查无需修改。组件编译、ESLint与跳转参数测试通过，未浏览器实测。 API文档已检查，本次无接口变更。
2026-09-17 合同审批入口修正：待审批合同且具备 project:contract:approve 时，项目列表显示“合同审批”，不再要求额外的 project:contract:view；两种权限都有时仅显示审批按钮。普通合同信息仍要求查看权限。独立合同页仅 view 模式接受审批权限入口，详情回查非待审批且无查看权限则返回项目列表，edit/create 原门禁不变。未扩大角色授权，服务端数据鉴权不变。 本说明覆盖此前“审批也须额外查看权限”的描述；无新增接口或菜单编码。
